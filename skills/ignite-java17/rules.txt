# Add architectural rules here (one per line)
# Strict Rules for Apache Ignite on Java 17
Use Apache Ignite version 2.18.0.
Always include both ignite-core and ignite-spring Maven dependencies when setting up Ignite with Spring XML.
Always add the mandatory --add-opens flags for java.base (lang, reflect, io, util, concurrent, nio, net, concurrent.locks) to JVM execution configuration.
Enable peer class loading in Spring XML configuration if dynamic tasks/classes are run.
Use Java 17 source and target compatibility in the Maven Compiler Plugin.
Cache operations like getOrCreateCache must be called on an Ignite instance (returned by Ignition.start), never on the Ignition utility class.
Do not pass a Spring ApplicationContext object to Ignition.start().
The cache name argument to ignite.getOrCreateCache(...) or ignite.cache(...) must be a String literal (e.g. "my-cache") - never an Ignite instance, IgniteConfiguration, or ApplicationContext object. Passing the wrong type here ("incompatible types: Ignite cannot be converted to String") has caused repeated compile failures - double check the cache name argument's type before returning generated code.
IgniteCache is in the top-level org.apache.ignite package - import org.apache.ignite.IgniteCache;, NOT org.apache.ignite.cache.IgniteCache. Confirmed directly against the real ignite-core-2.18.0.jar contents: org.apache.ignite.cache holds configuration/enum types (CacheMode, CacheAtomicityMode, CacheConfiguration) but not IgniteCache itself, which is a plausible but wrong place to guess it lives. The wrong import compiles-fails with "cannot find symbol: class IgniteCache, location: package org.apache.ignite.cache" and was observed to repeat unchanged across every retry attempt in a real run - this is a fact to know outright, not something to work out from the compiler error.
Give every variable holding the started Ignite instance (from Ignition.start(...), or from context.getBean("igniteNode") when using the Spring-managed IgniteSpringBean approach - see below, never context.getBean(Ignite.class) by type, which throws NoSuchBeanDefinitionException since the bean is only registered under its id) a name and keep it consistently in scope for the rest of the method - a variable referenced before or without being assigned (e.g. "cannot find symbol: variable igniteInstance") is a common self-inflicted regression when fixing an unrelated compile error.
To start Ignite using Spring XML, use EXACTLY ONE of these two mechanisms - NEVER both together in the same app, since combining them starts two separate Ignite nodes under the same instance name and crashes with "Ignite instance with this name has already been started: <name>":
1) Direct Ignition, NO IgniteSpringBean anywhere in the XML: the XML defines only a plain org.apache.ignite.configuration.IgniteConfiguration bean (no IgniteSpringBean wrapping it), and the Java code calls: Ignite ignite = Ignition.start("applicationContext.xml");
2) Spring-managed IgniteSpringBean, NO Ignition.start() call anywhere in the Java code: the XML defines an org.apache.ignite.IgniteSpringBean (which auto-starts the node the instant the Spring context loads, before your code even runs), and the Java code loads that context and retrieves the ALREADY-STARTED instance from it - it must never call Ignition.start() at all:
   import org.springframework.context.ConfigurableApplicationContext;
   import org.springframework.context.support.ClassPathXmlApplicationContext;
   ...
   try (ConfigurableApplicationContext context = new ClassPathXmlApplicationContext("applicationContext.xml")) {
       Ignite ignite = (Ignite) context.getBean("igniteNode");
       // use ignite here - it is already started, do not call Ignition.start() again
       // context.close() (implicit here via try-with-resources) is what stops the
       // Ignite node under this approach - see the resource-lifecycle rule below.
   }
   Declare the variable as ConfigurableApplicationContext (or ClassPathXmlApplicationContext
   directly), NEVER the bare ApplicationContext interface - ApplicationContext does not
   extend Closeable/AutoCloseable, so `try (ApplicationContext context = ...)` FAILS TO
   COMPILE ("incompatible types: try-with-resources not applicable to variable type
   'ApplicationContext'"). Confirmed live, 2026-08-07: this skill's own earlier example used
   the bare ApplicationContext type, so a model that hit that compile error correctly
   dropped the try-with-resources to keep it building - but then never closed the context by
   any other means either, leaving the IgniteSpringBean-managed node's background threads
   running forever after main() finishes (see the resource-lifecycle rule below for why that
   hangs the whole process, not just leaks memory).
Confirmed live, repeatedly: mixing the two (an XML that defines IgniteSpringBean, AND a separate Ignition.start("applicationContext.xml") call in the code) makes Ignition.start() parse that SAME XML as a second, independent Spring context internally, auto-starting the IgniteSpringBean a second time under the identical igniteInstanceName - guaranteed "already been started" at runtime regardless of how correct the rest of the code is. The model has been observed to correctly self-diagnose this exact cause from the stack trace every time, but then flip-flop between an INCOMPLETE option-2 fix (referencing a `context` variable that was never actually constructed via ClassPathXmlApplicationContext, or missing that class's import - a NEW compile error) and reverting straight back to the exact Ignition.start() call that caused the original crash - never landing on one COMPLETE, correct option. If IgniteSpringBean is present in the XML at all, option 2 (with the full context-construction code shown above, not just the getBean call in isolation) is the one to use.

When running Ignite with Java 11 or later, pass the following JVM flags: --add-exports=java.base/jdk.internal.misc=ALL-UNNAMED --add-exports=java.base/sun.nio.ch=ALL-UNNAMED --add-exports=java.management/com.sun.jmx.mbeanserver=ALL-UNNAMED --add-exports=jdk.internal.jvmstat/sun.jvmstat.monitor=ALL-UNNAMED --add-exports=java.base/sun.reflect.generics.reflectiveObjects=ALL-UNNAMED --add-opens=jdk.management/com.sun.management.internal=ALL-UNNAMED --add-opens=java.base/java.nio=ALL-UNNAMED --illegal-access=permit --add-opens=java.base/jdk.internal.access=ALL-UNNAMED --add-opens=java.base/jdk.internal.misc=ALL-UNNAMED --add-opens=java.base/sun.nio.ch=ALL-UNNAMED --add-opens=java.base/sun.util.calendar=ALL-UNNAMED --add-opens=java.management/com.sun.jmx.mbeanserver=ALL-UNNAMED --add-opens=jdk.internal.jvmstat/sun.jvmstat.monitor=ALL-UNNAMED --add-opens=java.base/sun.reflect.generics.reflectiveObjects=ALL-UNNAMED --add-opens=jdk.management/com.sun.management.internal=ALL-UNNAMED --add-opens=java.base/java.io=ALL-UNNAMED --add-opens=java.base/java.nio=ALL-UNNAMED --add-opens=java.base/java.net=ALL-UNNAMED --add-opens=java.base/java.util=ALL-UNNAMED --add-opens=java.base/java.util.concurrent=ALL-UNNAMED --add-opens=java.base/java.util.concurrent.locks=ALL-UNNAMED --add-opens=java.base/java.util.concurrent.atomic=ALL-UNNAMED --add-opens=java.base/java.lang=ALL-UNNAMED --add-opens=java.base/java.lang.invoke=ALL-UNNAMED --add-opens=java.base/java.math=ALL-UNNAMED --add-opens=java.sql/java.sql=ALL-UNNAMED --add-opens=java.base/java.lang.reflect=ALL-UNNAMED --add-opens=java.base/java.time=ALL-UNNAMED --add-opens=java.base/java.text=ALL-UNNAMED --add-opens=java.management/sun.management=ALL-UNNAMED --add-opens=java.desktop/java.awt.font=ALL-UNNAMED
Use Maven dependency <groupId>org.apache.ignite</groupId> <artifactId>ignite-core</artifactId> <version>2.18.0</version>
Use Maven dependency <groupId>org.apache.ignite</groupId> <artifactId>ignite-spring</artifactId> <version>2.18.0</version>
Set the JAVA_HOME environment variable to point to the Java installation directory when running Ignite with Java 11 or later
Set the IGNITE_HOME environment variable to point to the installation folder and ensure there is no trailing / in the path when using binary distribution
Any output a goal explicitly requires to be observable (e.g. a "[RESULT] ..." marker string proving the app worked) MUST use System.out.println(...), never an SLF4J/logger call - confirmed live for a different exec:java-based skill: Maven's `-q` flag silently suppresses SLF4J logger output entirely when the app class runs inside Maven's own JVM (the exec:java goal, not a separate spawned process), even though the exact same logger call prints fine without `-q`. System.out.println is unaffected by `-q` either way and is the only reliable choice here.
Apache Ignite needs its --add-opens flags applied at JVM STARTUP, which the exec:java goal cannot do (it runs inside Maven's own already-started JVM, and exec-maven-plugin's "java" goal has no "jvmArguments" parameter at all - confirmed by reading the actual mojo descriptor). Use exec:exec instead: <executable>java</executable> with every --add-opens flag as its own <argument>, then <argument>-classpath</argument> and the bare <classpath/> element, then the main class as the final argument - never <mainClass>+<jvmArguments>. Run via `mvn -q compile exec:exec -Dexec.mainClass=com.example.App`, not exec:java. See examples/pom.xml for the complete, verified-working shape.
The final <argument> in exec:exec's <arguments> list (the main class to run) must be ${exec.mainClass}, not a hardcoded literal class name - confirmed live: a literal value ignores `-Dexec.mainClass=...` on the command line and always runs that class regardless. Set a sensible default via an <exec.mainClass> property, so `mvn exec:exec` with no override still works, while every class in a multi-entrypoint project stays independently runnable via its own -Dexec.mainClass override.
If a goal writes an object into an Ignite cache in one method and reads it back in another (or in a separate step later in the same run), reuse the SAME Ignite instance for both operations - do NOT call Ignition.start() again for the read, and do NOT wrap each call in its own try-with-resources block. Ignition.start() creates a brand-new node, and try-with-resources auto-closes (stops) that node the instant its own method returns - a later Ignition.start() call in a different method creates an entirely separate node with no knowledge of what the first one cached, so the read always comes back null even though the write appeared to succeed and logged no error. Confirmed live: a `storeInIgniteCache(person)` method (`try (Ignite ignite = Ignition.start()) { ... }`) followed by a separate `readAndLogFromCache()` method with its own `try (Ignite ignite = Ignition.start())` produced "No Person found in cache" every time, with both methods individually correct in isolation. Start the Ignite node ONCE (typically in main(), the same way the embedded Qpid broker is started once), and pass that single Ignite/IgniteCache reference into whichever methods need to put or get from it. But starting it once is only half of correct lifecycle management - it must also be explicitly closed exactly once when the application is done, e.g. `try (Ignite ignite = Ignition.start(...)) { ... }` wrapping the ENTIRE main() body (not per-operation - that's the mistake above), or an explicit `ignite.close()` call in the same finally block that shuts down the Qpid broker. Confirmed live via a minimal, isolated reproduction: an Ignite node started with `Ignite ignite = Ignition.start()` and never closed leaves background threads running that prevent the JVM from exiting naturally - the process hangs indefinitely even after every line of application code, including a Qpid broker's own `systemLauncher.shutdown()`, has already completed successfully and all expected output (including a required "[RESULT]" marker) has already printed. This is easy to miss because the hang produces no error of its own - it just times out, and the timeout is easy to misdiagnose as "shutdown is slow" rather than "nothing ever asked Ignite to stop."
This same "started but never closed" hang applies EQUALLY to Method B (Spring-managed IgniteSpringBean, see the two-mechanisms rule above) - the closing call there is NOT ignite.close() (calling close() directly on a bean whose lifecycle Spring owns is not the intended pattern for this approach), it's closing the Spring ApplicationContext itself: try (ConfigurableApplicationContext context = new ClassPathXmlApplicationContext(...)) { ... } wrapping the ENTIRE main() body, or an explicit context.close() call in the same finally block that shuts down the Qpid broker - context.close() is what triggers Spring's own bean-destruction lifecycle, which stops the IgniteSpringBean-managed node as part of it. Confirmed live, 2026-08-07: a real generated app declared the context as the bare ApplicationContext interface (which has no close() method at all, so this mistake is easy to fall into even when TRYING to close it) and never closed it by any means - the app printed every expected output including the "[RESULT]" marker, then hung identically to the Method-A case above, for the identical underlying reason (Ignite's own background threads never got asked to stop).
The "reuse the same instance across methods, don't start it twice" rule above applies to the ApplicationContext object itself under Method B, not just to the Ignite instance retrieved from it - if the XML defines an IgniteSpringBean, calling `new ClassPathXmlApplicationContext(...)` ANYWHERE ELSE in the app (even in a helper method that never touches Ignite directly, e.g. one that only needs a different bean like a JMS ConnectionFactory also defined in the same XML) re-parses that same XML as a second, independent Spring context and auto-starts the IgniteSpringBean a second time under the identical instance name - guaranteed "Ignite instance with this name has already been started", the exact same crash as calling Ignition.start() after IgniteSpringBean, just triggered without Ignition.start() being involved at all. Confirmed live, 2026-08-07: a `main()` correctly constructed the context ONCE and passed the resulting `Ignite` instance into a `sendPersonMessage(Ignite ignite)` helper - but that helper method ALSO called `new ClassPathXmlApplicationContext("applicationContext.xml")` itself, purely to reach the XML's OTHER bean (`qpidConnectionFactory`), unaware that doing so re-triggers the SAME auto-start behavior. Construct the ApplicationContext exactly ONCE (in main()), and pass that SAME context object - not just the Ignite instance - into every helper method that needs ANY bean from that XML, Ignite-related or not.