# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
bin/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Pyinstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translation files
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# PEP 582; incoming replacement for pipenv and poetry
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyder-py3

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype
.pytype/

# Cython debug symbols
cython_debug/

# Kriya Memory and cached indexes - anchored to repo root (leading slash) so
# this doesn't also match kriya/memory/, the real source package (vector
# store, embedding client) - an unanchored "memory/" matches a directory of
# that name at ANY depth, which silently kept kriya/memory/ out of git
# entirely, undetected until a genuinely fresh checkout (CI) failed with
# ModuleNotFoundError: No module named 'kriya.memory'.
/memory/
.system_generated/
*.log

# Kriya's own runtime working directory when dogfooding kriya generate/
# analyze/prompt against this repo itself - checkpoints, the sandboxed
# worktree, and prompt generate's auto-saved .kriya/last_prompt.md. Found
# missing while adding last_prompt.md: checkpoint.py/worktree.py already
# wrote here with no corresponding .gitignore entry, so any real dogfooding
# run showed these as untracked files.
/.kriya/

# SQLite databases and temp files
*.db
*.db-wal
*.db-shm
/logs/

# Scratch generation folders
scratch_generate/

# Eval harness batch output (workspaces, logs, traces.db, summaries) - scratch,
# regenerated by spikes/eval_harness/run_harness.py on every run.
/spikes/eval_harness/runs/

# Per-stage eval harness output (e.g. architect_eval.py) - same reasoning, scratch
# regenerated on every run.
/spikes/eval_harness/stage_runs/

# Fix-alignment spike output (JSON results per batch) - same reasoning, scratch
# regenerated on every run.
/spikes/fix_alignment/runs/

# Wire-format-roundtrip POC output (generated Protocol.java/ProtocolParser.java
# per trial, plus the Maven build it was compiled/run in) - same reasoning,
# scratch regenerated on every run, kept locally (not tempfile-deleted) so a
# specific trial's generated code can actually be inspected afterward.
/spikes/protocol_bug_pocs/01_wire_format_roundtrip/runs/

# Incremental-composition POC output (prompt/response/extracted files/verdict
# per trial per step, plus a run.log) - same reasoning as the wire-format POC
# above, kept locally so a specific trial can be inspected after the fact.
/spikes/protocol_bug_pocs/05_incremental_composition/runs/
