# Kriya Production-Readiness Log

Running, dated log of the incremental production-hardening effort on Kriya - what's been done, what broke, what was found and fixed via real live-model testing, and the current honest readiness assessment. Mirrored from this session's working memory; kept updated going forward as part of the repo rather than only externally.

Kriya (this repo) is mid-way through an incremental production-readiness pass, done a step at a time per the user's preference ("let's decide as we go" rather than one big upfront plan — see [[feedback-incremental-scoping]]). Guided by [[project-kriya-vision]].

**Done so far** (as of 2026-07-29):
- Fixed `kriya/cli.py`: missing `List`/`Dict` typing imports (was crashing the CLI on every Python version except 3.14 due to PEP 649 lazy-annotation change), wired up previously-dead `AppConfig.logging` config, fixed silent-success exit codes in `plugins`/`tools_list`, added `tests/test_cli_smoke.py`.
- Fixed `kriya/mcp/server.py`: `mcp` SDK 2.0.0 renamed `FastMCP` → `MCPServer` (moved to `mcp.server.mcpserver`); updated import + bumped `pyproject.toml`'s `mcp` floor to `>=2.0.0`.
- Added a pip-compile-generated, fully pinned `requirements.txt` lock file (replacing a stale hand-maintained duplicate that had drifted from `pyproject.toml`), `pip-tools` as a dev extra, and README instructions for regenerating it. Verified reproducible from a totally fresh venv.
- Added `.github/workflows/ci.yml`: a `test` job matrixed across Python 3.10-3.14 (declared-range install) and a `lock-file` job (pinned-lock install + `pip check` + tests). **Not yet exercised for real — this repo hasn't been pushed to the `origin` remote, so the workflow has never actually run on GitHub.** Verified the equivalent commands locally instead (3.11/3.12/3.13 pass; 3.10 unavailable locally).
- Process-level execution hardening (`kriya/tools/sandbox.py`, new `AutonomyConfig.sandbox_*` fields, default on): env-allowlist restriction + CPU/memory resource limits applied to `PolymorphicValidator` (quality-gate compile/test execution) and `ShellTool`; `ShellTool` now requires interactive confirmation. Verified for real (not just mocked) — secret env vars confirmed stripped from actual subprocess output, and a runaway CPU-bound process confirmed killed by RLIMIT_CPU in ~1s. Explicitly does NOT provide filesystem/network isolation (documented in README) — full containerization deliberately out of scope, flagged as a separate future item if wanted.
- Broad-except audit: reviewed all 130 `except Exception` blocks in `kriya/`/`plugins/` via a forked sub-agent categorization pass, fixed 44 (added logging to ~33 fully-silent swallows, bumped 8 under-logged ones from debug to warning, restructured one in `analyzer/graph.py` so an existing caller-side error handler actually fires). Found and fixed one real bug in the process: `kriya/cli.py`'s RAG-query fallback in `generate` referenced a `logger` that didn't exist in that module, so a RAG failure raised `NameError` and aborted the whole `generate` run instead of degrading gracefully.
- Full test suite is green (124/124) as of the last session, verified on Python 3.12 too.

- Docs reconciliation: audited `docs/design.md`/`docs/user_guide.md` section-by-section against the real code (forked sub-agent comparison pass) and corrected everything that didn't match - no sqlite-vec/tree-sitter/GBNF (none of these are actually used), `analyze` flags, flat `llm`/list-based `llm_chain` config shape (not the fictional `llm.profiles`), fictional fix-pipeline three-tier classification and repair-loop bounds, wrong `risk_threshold_lines` default (500 not 100), no linter pre-pass in `review`, no domain allowlist on `learn`, no CPU-core detection in `doctor`. CLAUDE.md's docs-directory caveat note updated to reflect the docs are now reconciled (as of 2026-07-29) rather than permanently distrusted.

- Merged a divergent `origin/main` commit (2026-07-29) that had been pushed directly to GitHub outside this branch/session — it independently redid overlapping work from a stale pre-reconciliation baseline (a competing Docker-container-based sandbox under the *same* `AutonomyConfig.sandbox_execution` flag name as our process-level resource-limit sandbox, a `requirements.txt` regression pinning `mcp==0.1.0` which is the exact version our earlier fix moved away from, and an independent docs rewrite). Resolved by keeping our audited/verified versions throughout (confirmed via `AskUserQuestion`), dropped the Docker sandbox code and a redundant duplicate `.github/workflows/test.yml`. `production-readiness-hardening` branch merged into `main` cleanly (was already an ancestor — no-op). Full suite green (143/143) after merge, pushed to `origin/main` at `e12bbda`.

- Added ruff (2026-07-29): `[tool.ruff]` in `pyproject.toml`, rule set `E`/`F`/`I`/`B` (deliberately excluded `UP`/pyupgrade — `List`/`Optional` vs `list`/`X|None` modernization is pure style churn, not bug-catching, so it was scoped out of this pass). Fixed all 188 findings that surfaced (147 safe auto-fixes: import sorting, redundant open-mode args, f-string cleanup; ~41 manual: `raise ... from e` exception chaining, ambiguous `l` variable renames, unused loop vars, unused locals/imports, `zip(..., strict=True)`, import-order-vs-logger-init cleanup). Added narrow per-file-ignores for 3 genuinely-intentional patterns rather than forcing artificial changes: `B017` in `tests/*` (asserting "some error occurs" is valid test style), `B024` on `BaseAgent` and `B027` on `BasePlugin` (deliberate template-method base classes with non-abstract hook methods). Added a standalone `lint` job to `.github/workflows/ci.yml` (not matrixed across Python versions — ruff itself doesn't need it). Regenerated `requirements.txt` lock file via pip-compile to include ruff. Full suite green (143/143) after.

- 2026-07-30 session: fixed a KV-cache-hostile prompt ordering in `DeveloperAgent` (stable context now precedes the volatile, per-retry-growing task/error block), a reasoning-model markdown-fence-extraction bug (`_strip_markdown_fences` now finds a fence anywhere in the response, not just at the start), and a src-layout test-import-resolution bug in `PolymorphicValidator.run_tests` (deterministically appends workspace-root + `src/` to `sys.path` instead of relying on pytest's fragile rootdir-walk). Also did an end-to-end real-world validation pass: generated and actually *ran* (not just compiled) a Spring XML + embedded Apache Ignite 2.18 + embedded Apache Qpid AMQP messaging app, which surfaced and fixed 4 real runtime bugs in the `qpid` skill invisible to compile-only checking (wrong `modelVersion`, missing `virtualhostaliases`, missing `nodeAutoCreationPolicies`, missing `DeliveryMode.NON_PERSISTENT` for a Memory-store broker) — all verified against the actual Apache `qpid-broker-j`/`qpid-jms` source and test suites, not guessed. Tagged and pushed `0.3` on `origin/main` with a full changelog message covering everything since `0.2`. The working generated app itself lives in a separate local repo at `~/WorkingDirectory/AI/ClaudeCode/kriya-ignite-qpid-app` (no remote).

- 2026-07-30 session (continued): implemented both items 1-2 below in full.
  - **Runtime Verification Gate**: `RunVerifierAgent` (`kriya/agents/agent.py`) with `judge()` (decides should_run/run_command/success_criteria) and `grade()` (LLM-judges captured output against the goal); `PolymorphicValidator.run_app()` (`kriya/tools/validate.py`) executes via the existing sandboxed subprocess machinery; wired into `workflow.py`'s retry loop as a new tier right after targeted-test success (before the pre-apply approval gate and before worktree cleanup, so files are guaranteed present) - deliberately NOT placed alongside the existing full-regression-check tier, which runs later in a spot that turned out to already be timing-fragile (worktree gets `git clean -fd`'d before it runs) - flagged as a separate latent bug, not fixed (out of scope for this pass). New `AutonomyConfig` fields `run_verification_enabled` (default `true`) and `run_verification_timeout_seconds` (default `90`). Confirmation for judgment-inferred (not goal-explicit) runs routed through the existing `autonomy.mode` human-in-the-loop gate, asked once per generation run via a `run_verification_confirmed`/`run_verification_declined` pair of loop-scoped flags. 12 new unit tests (agent judge/grade, validator run_app success/nonzero-exit/timeout, two full-workflow wiring tests for the goal-explicit and declined-confirmation paths). **Verified against a real local LLM end-to-end** in a fresh project (`~/WorkingDirectory/AI/ClaudeCode/kriya-run-verification-test`, committed) — a goal with no explicit run command correctly triggered the judgment-inferred path, the confirmation gate fired and was auto-approved via `-y`, the app actually ran, and the grader correctly validated real captured output against the goal with cited evidence, first attempt.
  - **Lesson-promotion to shared skills**: `kriya skills promote SOURCE TARGET (--rule "..." | --all)` in `kriya/cli.py`, promoting an already-locally-approved rule from a source skill's `rules.txt` into Kriya's shared/global skill library (always the install-dir `skills/`, via a new `_get_global_skills_dir()` helper - not whatever project-local skills dir is active). Deliberately has no `--yes` bypass, even under `generate -y` - the only Kriya autonomy gate that's unconditionally interactive, since it permanently changes shared knowledge every future project inherits. Also generalized the existing lesson-extraction step's hardcoded "compilation/test" wording (`workflow.py`) so it labels a resolved Runtime Verification failure correctly - the staging mechanism itself already fired generically for any resolved error type, no new plumbing needed there. 5 new CLI tests, plus one real (unmocked) end-to-end sanity check using disposable throwaway skills, cleaned up after.
  - Full suite green (165/165), ruff clean, throughout.
  - Fixed the full-regression-check timing bug (commit `79ff270`) before pushing, per explicit user request: the check reused a `validator` instance still pointed at the worktree, which the "Clean up worktree sandbox" step already `git checkout -f HEAD` + `git clean -fd`'d back to pre-change state - so it silently tested stale content and could false-pass regardless of what was actually applied. Confirmed the bug was real by temporarily reverting the fix and observing the new regression test fail exactly as predicted (against a deliberately-wrong committed file + untouched test, in a real git repo) before re-applying the fix and confirming it passes. Fix: construct a fresh `PolymorphicValidator` pointed at `workspace_path` (which always has the just-applied content by that point) instead of reusing the worktree-pointed one.
  - All four commits (`7d64827`, `75d4af0`, `79ff270` on top of `0.3`, plus whatever tag/push follows) - check `git log`/`git tag` for current push/tag state before assuming what's on `origin`.
  - Full suite green (166/166) after the timing fix, ruff clean.

- 2026-07-30 session (continued further): implemented skill-content trust & coverage (the item flagged just above as "not yet scoped"), scoped in detail with the user first. Core design: verification is anchored to an objective signal (a passing Runtime Verification Gate run, or an explicit human `kriya skills promote`) rather than LLM self-assessed confidence, which is unreliable - the model that wrote `qpid`'s wrong `modelVersion: "9.2"` earlier this session presumably didn't flag itself as unsure either.
  - `Skill` gained `verified`/`verification_gap_acknowledged` fields (skill.yaml-persisted) + `source_path`; `SkillEngine.mark_verified()`/`mark_gap_acknowledged()` writer methods.
  - New `SkillGapAgent` (`kriya/agents/agent.py`): `extract_skill_update()` turns supplied reference material into rules/examples, cross-checking against a skill's existing rules and routing contradictions to a separate `conflicts` list instead of silently accumulating them (this is Gap 4 from the "gaps after this ships" list, folded into scope on user's call - see below).
  - Wired into `workflow.py`'s skill-matching stage (before Planner/Architect run): any relevant-but-unverified-and-unacknowledged skill triggers a new `skill_gap_callback` (once per skill per gap, not per retry); a goal-mentioned technology with no matching skill at all is also detected (via `extract_library_versions` cross-referenced against skill tags) and can bootstrap a brand-new skill (Gap 1, folded in). Supplied answer is fetched (URL, honoring `autonomy.egress_policy` - refused under `local_only`) or read (file/pasted text), extracted, and written **straight into the skill's live files**, not staged - the user actively supplying it in response to Kriya's own question was judged a strong enough intent signal (explicit user decision during scoping). Folded into the *current* run's context immediately, not just future ones. A passing Runtime Verification run marks every contributing skill verified automatically.
  - `git_commit_if_tracked()` (`kriya/skills/skill.py`): best-effort commit scoped to the changed skill file, wraps every skill-write path (`mark_verified`, extraction writes, conflict staging, `create_skill_skeleton`, `kriya skills promote`) - gives skill content a structured undo/audit trail via ordinary `git log`/`git revert` (Gap 5, folded in). `kriya skills promote` now also marks its target skill verified.
  - CLI: `on_skill_gap` callback wired into all three `generate` call sites, `-y` auto-skips it exactly like every other Kriya confirmation gate. Deliberately NOT wired into `fix` (narrower, often-piped diagnostic flow - an interactive prompt there risks hanging a pipeline that didn't pass `-y`).
  - Real bug found and fixed via live-LLM testing (not just reasoned about): the extraction prompt told the model not to put a contradicting candidate in both `rules` and `conflicts`, but a real run showed a non-reasoning model (qwen3-coder:30b) doing exactly that in the same JSON response. Fixed by enforcing mutual exclusivity in code after parsing rather than trusting prompt adherence - confirmed the fix mattered by temporarily reverting it and watching the new regression test fail exactly as predicted, then restoring it.
  - **Verified against a real local LLM end-to-end**, twice (project: `~/WorkingDirectory/AI/ClaudeCode/kriya-skillgap-demo`, committed, git history shows the per-write commits from `git_commit_if_tracked` in action): a hand-authored skill with a deliberately wrong fact (constant = 999) triggered the gap prompt; supplying a file with the correct fact (42) got extracted and the SAME run's generated code used the corrected value; a second, cleaner run (after the mutual-exclusivity fix) confirmed the conflict path specifically - the wrong existing rule stayed authoritative for generation (999), the new conflicting claim (42) was correctly staged for human review rather than silently trusted or silently duplicated, and `Quality Gates: PASSED`.
  - **Found but explicitly did NOT fix** (out of scope for this pass, flagged to the user): a real, unrelated bug where the Developer Agent sometimes returns an absolute filepath instead of a relative one, which `os.path.join(worktree_path, absolute_filepath)` silently mishandles (Python discards the first argument when the second is absolute) - writes land directly in the real workspace instead of the sandboxed worktree, and later `shutil.copy2` sees source==dest and raises `SameFileError`. Surfaced by coincidence because a test project directory name happened to contain the substring "test", which also tripped `extract_target_test`'s naive `"test" in f.lower()` substring check against the (absolute-path-containing) written-files list. Two related but distinct latent issues worth their own pass: (a) Developer Agent output isn't normalized to relative paths before use, (b) `extract_target_test`/similar "test"-detection does substring matching against full paths rather than basenames.
  - Full suite green (176/176), ruff clean. Three new commits (`3bbaab7`, `b3ce947`, `901afe3`) on top of `901afe3`'s predecessor `79ff270`.
  - Fixed the absolute-path bug found above (commit `1277d56`) before pushing, per explicit user request, same pattern as the earlier regression-check timing fix: `normalize_written_filepath()` (`kriya/workflow/workflow.py`) rewrites an absolute Developer Agent filepath to be relative to `workspace_path`, applied once right after generation returns so every downstream use (diffing, worktree writes, `extract_target_test`) sees a normalized path; rejects (skips, logs) anything that would still resolve outside the workspace root after normalization rather than ever writing there. Confirmed real by reverting and watching the new test fail with the exact `SameFileError` from the original run, then restored and confirmed it passes. Full suite green (181/181), ruff clean.
  - All pushed to `origin/main` - check `git log origin/main` for the current tip rather than assuming from this note.
  - Of the "gaps after this ships" list from the prior scoping conversation: **done** (folded in): #1 (no-skill-exists detection), #4 (contradiction-checking), #5 (structured undo). #3 (staleness/re-verification) done as its own pass shortly after, see below. **Still deferred**, each needs its own design pass: #2 (skill-to-skill conflict/overlap detection - what even counts as "overlapping"?), #6 (deeper extraction-trust mitigation beyond "nothing is verified until a real pass proves it" - would need a `rules.txt` schema change beyond flat text).

- 2026-07-30 session (continued further still): implemented Gap 3 (staleness/re-verification of already-`verified` skills). Scoped via `AskUserQuestion` first, deliberately conservative: no automatic staleness detection (no expiry timer, no version-drift trigger) - just makes the existing one-way `verified` flag *reversible and provenanced*, so a human who notices a verified skill is now wrong (or was promoted/verified against a version that's since changed) has a real command to correct the record instead of hand-editing `skill.yaml`.
  - `Skill` gained `verified_at` (ISO date) and `verified_context` (free text, e.g. `"widgetlib 2.0.0"` or `"promoted from 'auto-myrepo'"`) fields, persisted in `skill.yaml`.
  - `SkillEngine` writer refactored from a singular `_set_skill_yaml_field` into a multi-field `_set_skill_yaml_fields` (single commit per logical change instead of one commit per field); added `mark_unverified()`.
  - New `_skill_verification_context(skill, goal)` helper in `workflow.py`: reuses `extract_library_versions()` against the goal text, cross-referenced against the skill's name/tags, to compute a concrete version string for `verified_context` when the Runtime Verification Gate marks a skill verified - falls back to `"version unspecified"` if the goal didn't name one. `kriya skills promote` now records `verified_context=f"promoted from '{source_skill}'"`.
  - New CLI command `kriya skills unverify <name>` - resets `verified`/`verification_gap_acknowledged`, no-ops cleanly if already unverified, fails clearly on an unknown skill name.
  - `kriya skills list`/`show` now display verification provenance, e.g. `[VERIFIED - widgetlib 2.0.0, on 2026-01-01]`.
  - ~18 new tests, all using an isolated `tmp_path`-based `_make_local_project()` helper rather than the shared/global skills dir - deliberate, see incident below.
  - **Incident during manual smoke-testing**: ran `kriya skills list/show/unverify` by hand against the REAL Kriya-By-ClaudeCode repo's `skills/` directory to sanity-check CLI output, forgetting that `mark_verified`/`mark_unverified`/`create_skill_skeleton` all auto-commit via `git_commit_if_tracked` - produced 3 unwanted commits on `main` plus an uncommitted deletion from a manual `rm -rf` cleanup attempt, *while genuine uncommitted Gap 3 code was also sitting in the working tree at the same time*. Recovered with `git reset 1277d56` (mixed mode, not `--hard`) - moves HEAD/index back without touching the working tree - which cleanly dropped the 3 stray commits while leaving all in-progress file edits exactly as they were. Verified clean state after. Lesson for future smoke-tests: use a disposable tmp project dir for any command that can write/commit to a skills folder, never the real repo's own `skills/`.
  - Fixed one ruff `E741` (ambiguous variable name `l`) in a new test.
  - Full suite green (186/186), ruff clean. Committed as `c1cda30` ("feat: record verification provenance and add manual re-verification") on top of `1277d56`. Pushed to `origin/main`.

- 2026-07-30 (continued yet again): re-reconciled `README.md`/`docs/user_guide.md`/`docs/design.md` against code - they'd drifted since the 2026-07-29 reconciliation because Runtime Verification Gate and the whole skill-content-trust/verification subsystem (§2.3.1/§2.3.2/§2.7 in design.md) shipped afterward with no doc updates. Added: Runtime Verification Gate config/CLI/design coverage, `skills show`/`promote`/`unverify` command docs, the two-objective-signals-only verification lifecycle explanation, `SkillGapAgent`/gap-detection design writeup, and (found missing even from the prior reconciliation pass) a `design.md` §4.1 bullet for the process-level sandbox execution hardening that was already documented in README/user_guide but never made it into design.md. Committed as `eebc0c9` and pushed to `origin/main`.

- 2026-07-30 (Gap 2): implemented skill-to-skill conflict/overlap detection. Scoped via `AskUserQuestion` first: (1) trigger only when 2+ skills are co-active in a real `generate` run (a standalone library-wide `skills audit` command was explicitly deferred, not built), (2) on detection, pause and ask a human to resolve it (not warn-only, not silent staging), (3) detection method is a full LLM pairwise rule comparison (the cheaper heuristic-prefilter option was explicitly rejected), (4) a resolved pair is remembered and never re-asked.
  - New `SkillGapAgent.check_skill_conflicts(skill_a_name, skill_a_rules, skill_b_name, skill_b_rules)` (`kriya/agents/agent.py`) - same LLM-call shape as `extract_skill_update`, with the same defensive pattern as its earlier mutual-exclusivity fix: a returned "conflict" is only trusted if `rule_a`/`rule_b` are exact, verbatim matches against the real rule lists, so a hallucinated/paraphrased conflict can never silently exclude real rule content.
  - New conflict-resolution registry (`kriya/skills/skill.py`): `.skill_conflicts.json` at the skills-dir root (not inside any skill folder, so `discover_and_load` never mistakes it for one), storing `{skill_a, rule_a, skill_b, rule_b, resolution, resolved_at}` records; `find_conflict_resolution`/`record_conflict_resolution` are order-independent (looking up (B,ruleB,A,ruleA) correctly flips prefer_a\/prefer_b). Every write goes through `git_commit_if_tracked`, same audit trail as the rest of the skill-trust subsystem.
  - Wired into `workflow.py` right after the existing skill-gap-detection block, once `active_skills` is finalized: for every pair (sorted alphabetically for a stable orientation), a registry hit is applied silently; a miss invokes a new `skill_conflict_callback` once. Applying "prefer_a"/"prefer_b" surgically strips the losing rule's exact `"- {rule}\n"` line back out of the already-built `convention_prompt` string, rather than restructuring the existing single-pass context builder. A callback returning nothing usable (declined, `-y` auto-skip, or a callback exception) proceeds without excluding either rule for that run only and does NOT persist a non-decision to the registry - only an explicit human choice (including explicitly choosing "both are fine") gets remembered.
  - CLI: new `on_skill_conflict` in `generate`, wired into all three `run_generation_workflow` call sites exactly like `on_skill_gap`; skipped under `-y`.
  - 12 new tests (agent method, registry round-trip/order-independence/distinct-pairs, 4 full-workflow integration tests using fictional `brokeralpha`/`brokerbeta` skill names - deliberately NOT `qpid`/`activemq-artemis`, since `SkillEngine` loads the real repo's own global `skills/` dir by default and using the real names collided with it in a first draft of the tests, caught before commit).
  - **Verified against a real local LLM end-to-end, twice** (project: `~/WorkingDirectory/AI/ClaudeCode/kriya-skillconflict-demo`, committed): two fictional skills with genuinely conflicting, differently-worded rules (same "shared message bus config file," different port numbers) correctly triggered the LLM-detected conflict; the interactively-chosen resolution (`prefer_a`) was persisted with a `git_commit_if_tracked` audit commit, and the *losing* rule's port number never appeared anywhere in generated code across either run, while the winning rule's port was used consistently. A second run under `-y` reused the remembered resolution silently - no re-prompt, no duplicate registry entry, no duplicate commit.
  - Full suite green (198/198), ruff clean. Committed as `86958e9`, docs reconciled and committed as `faf1a88` (§2.3.3 in design.md, §4.4 in user_guide.md, README feature bullet) - both pushed to `origin/main`.
  - User then said "Remember to update the docs when needed" - saved as a standing preference, see [[feedback-docs-reconciliation]]: update README/user_guide/design.md as part of shipping a feature going forward, don't wait to be asked each time.

- 2026-07-30 session (Live Lookup, developed on `feature/live-lookup`, merged to `main`): user asked how Kriya's Runtime Verification + skill-gap behavior compares to Claude Code's own agentic behavior; identified the two biggest gaps as (a) continuous single-agent loop vs. Kriya's fixed multi-agent pipeline, and (b) live web lookup vs. Kriya's local-only default. User agreed to scope "live lookup" first, explicitly setting aside "continuous agent" as its own future exploratory effort given local-model tool-use reliability risk.
  - **Hard design constraint from the user**: proprietary project content must NEVER leave the machine via an outbound search query - this drove the whole design. Search queries are built *exclusively* from bare technology-name strings already produced by the existing, bounded `extract_library_versions()` regex extractor - never goal text, design text, or code. Enforced structurally (the only input `_resolve_via_web_lookup()` accepts), not a prompt instruction.
  - New `autonomy.web_lookup_enabled` (default `false`) + `search.base_url`/`search.top_k` (default `3`) config - both switches required, off unless a project explicitly opts in.
  - Two trigger points: Stage 1.2 (goal-text-derived gaps, tried before the existing human-ask), and new Stage 2B (design-derived gaps found after the Architect runs - catches technologies a vague goal never named but the design ends up using).
  - `kriya/tools/search.py::search_web()` - SearXNG-compatible `/search?format=json` client.
  - Found-but-unfetched results always require one batch human confirmation (`web_lookup_callback`) before use - "nothing trusted until proven" principle preserved.
  - **Real end-to-end testing against a real local SearXNG instance (Docker) surfaced two real bugs, both fixed and re-verified live, not just reasoned about**:
    1. **Landing-page problem**: the single top search result for a well-known library (`qpid.apache.org/documentation.html`, `python-httpx.org`'s homepage) is often marketing/index content with nothing extractable - confirmed live twice. Fix: try up to `search.top_k` ranked candidates per term, not just the first, stopping at the first one that yields anything usable (`_extract_first_usable()`). User's framing: "if it not getting valid information no point in having live lookup" - saved as [[feedback-live-lookup-quality-bar]].
    2. **"Resolved" bug** (found via the user directly questioning the design, not by testing): a term was marked "resolved" as soon as live lookup found *any* search result and the batch was accepted - regardless of whether extraction later actually succeeded. A real run proved this: `qpid` gap found and fetched real pages, extraction correctly declined all of them, skill left permanently unverified with no human ever asked. Fixed by running extraction immediately and only excluding a term from the human-ask fallback if extraction actually yielded something; extended the same fallback to Stage 2B, which originally had no human-ask fallback at all by design (deliberately changed after discussion - silently generating against zero grounding was judged worse than one extra prompt).
  - **Final real-world proof** (what the user explicitly required before agreeing to merge): a real gap live lookup couldn't resolve (tried, fetched real pages, correctly declined) correctly fell back to asking; a human-supplied real fact was correctly extracted, written to the skill with a git-audited commit, folded into the same run, passed Quality Gates, and the generated script was run **independently, outside Kriya** and printed the correct fact.
  - **Incident, cleanly resolved**: a manual live test used the real "qpid" library name as a search term, which matched and modified this repo's actual `skills/qpid/rules.txt` (global skill, loaded alongside the scratch test project) with a git commit (`fc14ca4`) - same class of incident as the earlier Gap 3 smoke-test mistake. Cleaned up via `git reset` (mixed) to drop the stray commit, then `git checkout --` to discard the near-duplicate rule content, preserving genuine in-progress work throughout.
  - 21 new tests total across the two commits. Full suite green (210/210), ruff clean throughout. Two commits (`8cb7f58` feature, `abccce9` bug fixes) on `feature/live-lookup`. After the `requests` autonomous-success proof below, merged via `--no-ff` merge commit `9230ce7` and pushed to `origin/main`. `feature/live-lookup` branch left on `origin`, not deleted (not explicitly asked).
  - New standing preference saved: [[feedback-docs-reconciliation]] (update docs as part of shipping, don't wait to be asked) was reinforced again here - docs were updated in both commits without a separate request.
  - **Follow-up real test, after the user asked directly "does live lookup have value in production?"**: honest answer at the time was "yes as a safety net, but every real attempt so far (qpid x3, httpx x1) failed to autonomously extract anything - success only ever came from a human manually supplying content after the fallback." User asked for one more real test specifically against a library with readthedocs-style API reference docs (the hypothesis being that landing/marketing pages, not the mechanism, were the reason for repeated failures). Tested against `requests==2.31.0` (readthedocs.io, real Quickstart page with real code snippets in the top-3 search results) - **fully autonomous success this time**: 15 rules + 5 examples extracted with zero human involvement, git-audited commits, folded into the same run, Quality Gates passed, and the generated script was run independently in a fresh venv against the real GitHub API and printed the correct `200`. This confirms the landing-page hypothesis and meaningfully upgrades live lookup's real-world value assessment from "safety net only" to "genuinely autonomous for libraries with structured/readthedocs-style docs, safety net for everything else."

- 2026-07-30 session (Gap 6, committed straight to `main` like Gaps 1-5 - no branch, lower risk than live lookup since it's pure local file I/O, no new network egress): scoped via `AskUserQuestion` first. User's answers: (1) parallel lightweight tracking file (not a rules.txt format migration) - same pattern as Gap 2's `.skill_conflicts.json`, (2) Runtime-Verification-pass marking scoped to rules present in the run's active context (a pre-retry-loop snapshot), not a fresh re-read at verification time, (3) unverified rules get a distinct, differently-labeled section in generation prompts (not just tracking/display), (4) no retroactive backfill - only rules extracted from this point forward get tracked; existing rules.txt content (including the real qpid/ignite/artemis/spring-boot skills) stays implicitly trusted.
  - New `rule_provenance.json` per skill (`kriya/skills/skill.py`: `load_rule_provenance`/`record_rule_provenance`/`mark_rules_verified`) - keyed on exact rule text, no `rules.txt` format change.
  - `_write_skill_extraction()` now takes a `source` string (e.g. `"live_lookup:<url>"`, `"human_url:<url>"`, `"human_text"`) and records provenance for every newly-written rule.
  - New `_split_rules_by_verification()` in `workflow.py`, applied everywhere skill content reaches a prompt (the main skill-matching loop, and both "just added" extraction-folding blocks in Stage 1.2/2B) - splits into a `Rules:` block and a separate `Unverified Rules (... use with appropriate caution)` block.
  - **Found and fixed a real correctness bug while implementing** (not from live testing this time - reasoned through it while building): the rule-verification snapshot (`active_skill_rules_snapshot`, taken via `se.get_skill(name).rules` before the Developer retry loop) would have silently missed any rule just extracted in Stage 1.2 for an *already-existing* skill, because `_write_skill_extraction` appends to the rules.txt file on disk without refreshing `SkillEngine`'s in-memory cache for skills that weren't freshly bootstrapped. Fixed by calling `se.discover_and_load()` immediately before taking the snapshot.
  - `kriya skills show` now flags unverified rules inline (`[unverified]`), same provenance lookup.
  - 10 new tests (skill.py round-trip/overwrite/no-op-on-untracked-rule, a full workflow integration test covering extraction → prompt labeling → post-Runtime-Verification-pass promotion, a CLI display test).
  - **Verified against a real local LLM twice**: first attempt tried to get a full live extraction-through-verification loop working, but the real `SkillGapAgent` (unchanged, pre-existing code) conservatively declined plain single-sentence "facts" as not rule-shaped enough on two different phrasings - a real, consistent finding, not a Gap 6 bug (matches the same conservative behavior already proven with qpid/httpx in the live-lookup work). Pivoted to directly verifying the part Gap 6 actually changed: pre-seeded a skill with one trusted and one provenance-tracked-unverified rule, ran `kriya generate` for real, and confirmed via the actual persisted trace DB (`logs/traces.db`) that the real prompt sent to the model contained exactly the intended `Rules:`/`Unverified Rules:` split - and the real model correctly used content from both sections without confusion (used the verified rule's `[WIDGET]` print-prefix convention AND the unverified rule's specific `WIDGET_CONSTANT = 999` value), generated code that passed Quality Gates, and was independently run and printed correctly.
  - **Found and later fixed** (same session, immediately after): a real, unrelated Runtime Verification bug hit during live testing - see the dedicated entry below.
  - Full suite green (216/216), ruff clean. Committed as `619dda4` on top of `9230ce7`, pushed to `origin/main`.
  - Also tagged `0.5` at `9230ce7` (pre-Gap-6 checkpoint, everything since `0.4`: Gap 2 conflict detection + Live Lookup) and pushed to `origin` before starting Gap 6 work, per explicit user request ("tag the repo in remote before we commit any changes").

- 2026-07-30 session (Runtime Verification "python not found" bug, investigated and fixed immediately after Gap 6, committed separately as `79136ee`): user asked to investigate the "Process exited with code None" bug flagged at the end of the Gap 6 report.
  - **Root cause found, correcting an earlier mischaracterization**: the Gap 6 report said the error message looked masked/unhelpful - that was actually a mistake from grepping without enough context lines. The full raised error always included the real cause: `RunVerifierAgent.judge()` inferred `["python", "main.py"]` as the run command; this dev machine (Homebrew Python, no activated venv when invoking `.venv/bin/kriya` by full path) has no bare `python` on PATH, only `python3` - confirmed directly (`which python` → not found). `subprocess.run(["python", ...])` raised `FileNotFoundError` immediately (sub-10ms, explaining the suspiciously fast failure), which `PolymorphicValidator.run_app`'s catch-all correctly captured into `output`, and `workflow.py`'s `raise ValueError(...)` correctly included that `output` - just not visible in the first, too-narrow grep.
  - Fix: new pure helper `_resolve_run_command()` (`kriya/workflow/workflow.py`) substitutes `sys.executable` (guaranteed to exist, matches Kriya's own venv) whenever the judge's inferred command is a bare `"python"` not resolvable via `shutil.which`. Wired in right before `validator.run_app()` is called.
  - Verified real via the established revert/confirm-fail/restore pattern: reverted the fix, watched the new test fail with the exact original `FileNotFoundError` message, restored, confirmed it passes.
  - **Verified live end-to-end a second way too**: reran the exact same real scratch scenario that originally failed (`kriya-gap6-demo`, `qwen3-coder:30b`, `run_verification_enabled: true`) - the model again inferred `python`, Kriya correctly logged the substitution and used its own venv interpreter, and this time the run actually executed and passed Runtime Verification for real (`[WIDGET] 42 is the answer to the Ultimate Question of Life, the Universe, and Everything`).
  - 5 new tests. Full suite green (221/221), ruff clean. Committed as `79136ee` on top of `619dda4`, pushed to `origin/main`.

- 2026-07-30 session (backlog item 8, per-role LLM model selection, committed `7ba336b` on `main`, not yet pushed): user's stated motivation was "may decrease testing time and increase accuracy/efficiency" - real per-call model tiering rather than one shared model for every agent. Scoped via `AskUserQuestion` first (fully granular per-role config, full independent config block per role like `llm_chain` entries, every role gets its own escalation chain) - user overrode my "simpler" recommended defaults on all three, wanting the more granular/powerful version. User also asked for a specific real model mapping and my opinion on it: Devstral Small 2 24B for Plan+Inspect (Planner+Reviewer), qwen3-coder:7b for fast utility calls (RunVerifier+SkillGapAgent), qwen3-coder:30b for Developer+Architect. Also asked "does langchain help here?" - answered no, given LangChain's fallback abstraction would mean re-wrapping `LLMClient.complete()`'s already-hand-tuned local-model-specific behavior (reasoning `<think>`-tag stripping, JSON-mode/reasoning-model interaction, egress validation) for little gain on a problem this small.
  - `qwen3-coder:7b` doesn't actually exist in Ollama's registry (only 30B) - substituted `qwen2.5-coder:7b` (confirmed real, pulled) with the user's explicit approval after flagging it.
  - New `agent_llms` config section (`kriya/config/config.py::AgentRolesConfig`): `AgentModelConfig` (optional `llm` override + its own `llm_chain`) per role - planner/architect/reviewer/run_verifier/skill_gap. **Developer is deliberately excluded** - stays on the pre-existing top-level `llm`/`llm_chain`, escalated by the existing quality-gate retry loop (a different failure signal entirely). Every field defaults unset = zero behavior change for any project that doesn't touch it.
  - `LLMClient.complete()` gained `temperature_override`/`max_tokens_override`/`reasoning_override` - a real, unrelated pre-existing gap surfaced along the way: `FallbackModelConfig`'s `temperature`/`max_tokens` fields were dead code for Developer's own existing escalation chain (only `model`/`base_url`/`api_key`/`reasoning`-via-chain-lookup ever actually applied) - now fixed for both the old chain and the new per-role one.
  - New shared `call_with_escalation()` helper (`kriya/agents/agent.py`): tries a role's own model then its chain in order. Failure signal differs by role per the user's answer: free-text roles (Planner/Architect/Reviewer) escalate only on a hard call failure (never on content, so a short-but-correct response is never wrongly retried); JSON-mode roles (RunVerifier, SkillGapAgent) also escalate on unparseable JSON. RunVerifier's judge/grade lost their old external `model_override` params - no longer coupled to whatever model Developer's own retry loop happens to be on (a deliberate decoupling; the old coupling was an accident of RunVerifier never having had its own config before, not intentional design).
  - **Found and fixed a real, unrelated pre-existing test bug while updating the suite**: `test_auto_skills_generation_and_injection`'s hand-written LLM mock was missing `**kwargs`, which happened to mask that the test never disabled Runtime Verification (unlike the rest of the suite) - the mock's TypeError on unexpected kwargs was silently absorbed into Developer's own retry-failure handling, coincidentally producing a passing test for the wrong reason. Fixed both (added `**kwargs`, added `run_verification_enabled = False`).
  - **Verified end-to-end against three real local models simultaneously** in a fresh scratch project: confirmed via the real trace log that Planner and Reviewer used `devstral-small-2:24b`, RunVerifier used `qwen2.5-coder:7b`, Architect and Developer correctly defaulted to the primary `qwen3-coder:30b`, Developer's own pre-existing escalation chain fired independently and correctly (escalated to `deepseek-r1:32b` after an initial failure, unaffected by this change), and the final generated script was independently run outside Kriya and printed the correct output.
  - 17 new tests. Full suite green (230/230), ruff clean. Committed as `7ba336b` on top of `79136ee`.
  - **User asked to measure real speed before pushing - result was the opposite of the motivating hypothesis.** Timed, same-goal, same-machine comparison: baseline (everything on `qwen3-coder:30b`) took 33.1s; the 3-tier config (Devstral 24B / qwen2.5-coder:7b / qwen3-coder:30b) took 124.4s - **~3.8x SLOWER**, not faster. Root cause: Kriya never explicitly loads/unloads models - it just sends a `model` name per request, and Ollama decides server-side whether to reload; on this machine only one model of this size class stays resident, so every role-to-role model CHANGE (not same-model-to-same-model, which is free - confirmed Architect→Developer on the same model took ~2s with zero penalty) pays a full 19-43s reload cost that dwarfs any inference-speed gain from the smaller models.
  - User's proposed fix ("check before removing a model if the next phase needs the same one") turned out to already be true by construction, not something needing new code - Kriya has no hook into Ollama's load/evict decision at all, and same-model-consecutive-calls already incur zero cost today. Explained this rather than building anything speculative.
  - Fix instead: **documentation**, per explicit instruction ("keep them all matching before pushing... even for our development let's have one model qwen3-coder:30b") - changed the example configs in README/user_guide/design.md from the distinct-3-model example to matching models everywhere, added an explicit warning with the measured numbers, reframed distinct-model tiering as something to adopt only after confirming (`ollama ps`) the hardware can hold every configured model resident simultaneously. Committed as `8318a84`.
  - Both commits (`7ba336b`, `8318a84`) pushed to `origin/main`.

- 2026-07-30 session (backlog item 5, stage-level checkpoint/resume for `generate`/`fix`, not yet committed): scoped via `AskUserQuestion` first - explicit `--resume`/`--resume-id` flag only (no auto-detection from goal text), stage-level checkpoint granularity (post-Plan, post-Design, post-Developer-passed-Quality-Gates), strict drift handling (any workspace/config/goal difference refuses resume and starts fresh with a warning), and - diverging from my recommended "generate only for this pass" - the user chose to cover **both** `generate` and `fix` together in one pass.
  - New `kriya/workflow/checkpoint.py`: `save_checkpoint`/`load_checkpoint`/`delete_checkpoint`/`find_latest_checkpoint`/`list_checkpoints`, `compute_workspace_fingerprint()` (git HEAD SHA + clean/dirty, `None` for non-git workspaces - resume is refused outright in that case, not silently allowed via a trivial `None == None` match, which was a real bug caught before committing), `compute_config_fingerprint()` (SHA-256 of full resolved `AppConfig.model_dump()`). Checkpoints live at `<workspace>/.kriya/checkpoints/<run_id>.json`.
  - `fix` needed zero pipeline-specific work beyond adding its own `--resume`/`--resume-id` CLI flags - it already calls the exact same `run_generation_workflow()` as `generate` (goal fixed to `"Fix compilation/test failure"`, `error_context` set to the error log), so the shared implementation covers both automatically, exactly as the user's "both together" choice anticipated.
  - Wired into `kriya/workflow/workflow.py`: `plan`/`design`-stage checkpoints substitute saved text for the Planner/Architect calls (Stage 2A/2B still run normally against restored `design` text but are naturally near-no-ops on replay, since any skills they'd bootstrap already exist on disk from the interrupted first attempt). `developer_success`-stage checkpoint is only honored on the retry loop's first iteration - substitutes saved `final_files` for the Developer Agent call and skips the completeness check + compile/test/Runtime-Verification gates entirely, writing saved content into a freshly created worktree and falling through into the unchanged human-approval/apply/regression-suite code path. A checkpoint is deleted on any normal terminal return (full success, or explicit human rejection at the approval gate); if Quality Gates exhaust all retries without passing, the last checkpoint is deliberately left on disk for a future `--resume-id` attempt to skip re-planning.
  - **Found and fixed two real bugs while implementing, both caught before committing (not from live testing)**: (1) a local `run_id` variable already existed for trace-log purposes (`TraceLogger.log_run`), silently colliding with the new checkpoint `run_id` - renamed the pre-existing one to `trace_id`. (2) writing the checkpoint file itself dirties the git working tree (untracked file under `.kriya/`), which would make every resume attempt see the workspace as "changed since checkpoint" and permanently refuse to resume, discovered via a failing new test - fixed via `git status --porcelain -- . ':!.kriya'` (pathspec exclusion) so the checkpoint directory itself is invisible to the drift check.
  - 11 new tests (checkpoint save/load/delete round-trip, fingerprint stability/sensitivity, `find_latest_checkpoint` ordering, three resume-skip integration tests confirming the exact LLM-call count drops for plan/design/developer_success resumes, a goal-drift-refuses-resume test, a checkpoint-deleted-on-success test).
  - **Verified against real local LLMs live, partially**: ran a real `generate` against `qwen3-coder:30b`, confirmed real `plan`- and `design`-stage checkpoint files were written mid-run with correct content, killed the process (`SIGTERM`, confirmed dead), reran the identical command with `--resume` and confirmed via the real log output that both "Resuming checkpoint ... using saved Plan/Design, skipping Planner/Architect Agent call" fired correctly - i.e. the two most valuable resume points (skip re-planning, skip re-designing) are confirmed working against a real model, not just mocks. Did **not** get a live confirmation of the `developer_success`-stage resume specifically - two separate live attempts to reach that stage instead hit real, unrelated LLM-quality flakiness (the model's own generated unicode-reversal test assertions were wrong; a botched package `__init__.py` structure) that kept triggering the *existing* retry/escalation loop instead of ever reaching a clean Quality-Gates-passed state to checkpoint from, and chasing it further was low marginal value given the `developer_success` resume path is already covered end-to-end (real file I/O, only the LLM call itself mocked) by a dedicated unit test.
  - Full suite green (241/241). Updated README/design.md §2.9 (new)/user_guide.md with the `--resume`/`--resume-id` behavior, both commands.
  - **Found, discussed with user, explicitly left unaddressed (not a Kriya defect)**: live testing surfaced the Developer Agent hand-computing a wrong expected value in its own generated pytest test for reversing a string with an accented character (`"naïve"` → asserted `"evían"`, actually `"evïan"`) - a real, repeatable LLM limitation (can't execute code to check its own arithmetic on tricky unicode cases), not a bug in Kriya. Confirmed with the user it doesn't affect correctness or interact badly with resume: the retry loop is already hard-bounded (`max(4, 1+len(llm_chain))`), fails safe (`quality_gates_passed: False`, broken files never copied out of the sandbox worktree into the real workspace), and resume doesn't change this behavior at all - it only skips Plan/Design before Developer hits the same flakiness again. User's explicit criterion: leave it if it doesn't impact correctness/generate/fix/resume behavior, else fix - verdict was "doesn't impact," so left as-is. A real, well-scoped-if-ever-picked-up future idea (not started): detect Quality Gates failing on the *same test, same reason* across multiple retries/escalations as a signal the generated *test's* expected value is wrong (not the implementation), and surface that for human review instead of exhausting the retry budget unproductively - a new feature, not a bug fix.
  - Committed as `cfafa7b` on top of `8318a84` and pushed to `origin/main`, after the real-world Ignite+Qpid gate below and the user's explicit go-ahead.

- 2026-07-31 session (item 5 real-world gate, before checkin): user required a real end-to-end test against the same Ignite+Qpid messaging app built earlier this session (`~/WorkingDirectory/AI/ClaudeCode/kriya-ignite-qpid-app`, commit `9e4b870`) - exercising skill-gap/reference-providing, resume, AND ending in a genuinely successful run - as the explicit gate before committing item 5. Built a fresh scratch project (`kriya-resume-ignite-demo`) plus a bash orchestration script that watches the log for known prompt text and answers interactively via a FIFO (skill-gap → supply a local reference file; skill-conflict → "both"; knowledge-risk/approval-family → "y"), so the full interactive flow (not `-y` auto-skip) could be exercised unattended.
  - **Incident, found and fully cleaned up**: the demo's `kriya.yaml` initially pointed `paths.skills` at this repo's own GLOBAL `skills/` dir (to reuse the real, not-yet-verified `qpid`/`ignite-java17` skills) - this correctly loaded skill content read-only, but `record_conflict_resolution()` (the skill-conflict registry writer) always targets `self.kernel.config.paths.skills` directly rather than any per-skill `source_path`, so 6 stray "Kriya: record skill-conflict resolution" commits landed on `main` in the real Kriya-By-ClaudeCode repo. Caught immediately, cleaned up via `git reset <last-real-commit>` (mixed, not `--hard` - preserves the uncommitted item-5 working tree exactly as it was) plus deleting the stray `skills/.skill_conflicts.json`, verified clean after. Root cause: `SkillEngine(skills_dir, load_global=True)` (default) ALWAYS loads the install-dir's own `skills/` as a base layer regardless of `paths.skills`, and per-skill discovery correctly resolves each skill's own `source_path` to whichever dir last discovered it (local overrides global by name) - but the conflict registry doesn't have a "per-skill" home, so it always writes to the literal configured `paths.skills`. **Fix, and the correct pattern for any future test that needs real qpid/ignite-style skill names**: copy the needed skill folders into a local project-owned `./skills/` dir and point `paths.skills` there (not at the global install dir) - this is a DIFFERENT root cause than the earlier Gap-3/live-lookup incidents (accidental collision via matching names), worth remembering as a second distinct way this class of mistake can happen.
  - **Resume mechanics: further confirmed correct, including two new real scenarios not previously exercised live**: (1) a checkpoint that was retained after a run fully exhausted its 4-attempt retry budget without ever passing Quality Gates was successfully used by a later `--resume` to skip Plan/Design and retry Developer fresh - the exact "leave it on disk for a future attempt" design intent, now proven live, not just unit-tested. (2) Strict drift detection correctly REFUSED a stale checkpoint (with a clear warning) the moment the workspace's git HEAD changed between save and resume (from committing an unrelated skill-rule fix in the same repo) - real confirmation the "strict" policy the user chose is not just theoretical, it fires on genuine incidental drift, not just goal-text changes.
  - **App build/run: not achieved after ~4 hours across two full fresh attempts (8 total Quality-Gate attempts).** Consistently got closer each retry but a NEW small defect appeared each cycle even as the prior one got fixed: malformed `exec-maven-plugin` `<arguments>`/`<classpath/>` block (reproduced identically across 3 separate attempts, including after adding an explicit preventive rule to the local `ignite-java17` skill - the rule did not reliably prevent it, a real instruction-following limitation, not a rule-content problem) → missing `TimeUnit`/`JmsConnectionFactory` imports → a duplicate `App` class accidentally left in `BrokerServer.java` → a self-added `AppTest.java` missing its Mockito/JUnit dependencies. Every single failure was caught correctly (compile check, targeted test, or Runtime Verification) and zero broken code was ever applied to the real workspace across all 8 attempts - the safety guarantees held perfectly throughout; this was purely a local-model capability/reliability ceiling on a nontrivial multi-file Java/Maven/Spring/Qpid/Ignite build, same class of thing as the earlier unicode-test finding, not a Kriya defect.
  - **User's explicit decision when asked how to proceed**: accept resume/checkpoint as proven from the extensive evidence already gathered; defer getting this specific Ignite+Qpid app to a genuinely clean build to a separate future session, rather than blocking item 5's checkin on it.
  - Skill-gap/reference-providing confirmed working correctly within this same real, complex build: 13 rules + 4 examples extracted from a supplied local reference file into the local `qpid`/`ignite-java17` skills (git-audited, isolated to the demo project); re-asked correctly on the next resume (Stage 1.2 isn't checkpointed, by design) and correctly found "nothing new" the second time (working dedup). Skill-conflict detection correctly fired for `ignite-java17` vs `qpid` rule pairs and correctly resolved them as "not actually conflicting."
  - No changes to committable code from this session beyond what item 5 already had - the incident cleanup only affected the main repo's git history (reverted) and the demo scratch project (disposable, not part of this repo).

**Remaining backlog, prioritized:**
1. A standalone library-wide `kriya skills audit` command (deferred during Gap 2 scoping, re-confirmed deferred 2026-07-30 after discussing its value-add directly with the user). Real, narrower value-add identified: the per-run conflict check (Gap 2) only ever fires when two skills are actually co-activated in a live `generate` run - a conflict introduced purely via `kriya skills promote` (team A promotes a rule into skill-x, team B separately promotes a contradicting rule into skill-y) can sit undetected indefinitely if that combination never comes up in a real run, since promotion never triggers the pairwise check. Everything else an audit would catch, the per-run check already catches reactively, just later - and O(N²) full-library comparison would spend real LLM calls flagging plenty of pairs that will never actually be used together in practice. Worth building if/when actively growing the shared library via `promote` across multiple projects; not before then. When scoped, three design questions were queued but not yet answered (comparison method: full pairwise vs. prefiltered; output mode: report-only vs. interactive-resolve; whether to skip pairs already in `.skill_conflicts.json`) - re-ask via `AskUserQuestion` when this is picked up.
2. Rule-level verification is still coarse in one respect (flagged as a known gap in Gap 6's own design.md section): a passing run marks every rule in the active-context snapshot verified, not just the ones the generated code demonstrably used - attributing specific generated code back to specific rules would be a substantially harder follow-up.
3. mypy/type-checking — not yet added; deferred pending a concrete pain point (dynamic JSON-heavy agent-output contracts make broad mypy adoption a heavier lift than ruff was).
4. Full containerized isolation (Docker) for quality-gate execution — explicitly deferred during the sandboxing pass; would need its own planning round (per-stack images, network policy for e.g. Maven needing network to fetch deps).
5. **Targeted single-file retry** (new, highest-confidence candidate as of 2026-07-31): on a Quality Gates retry, the Developer loop currently regenerates the entire file set every attempt, not just the file(s) implicated by the error. A real live A/B test (see the error-triggered-live-lookup entry above) directly demonstrated that a focused single-file "fix just this" task succeeds where the same bug survived 2+ consecutive full-file-set regenerations in the real Ignite+Qpid run - strongly suggesting multi-file-response consistency/attention, not missing information, is the actual driver of the "whack-a-mole" pattern (fix A, break B) seen repeatedly this session. Not yet scoped - open questions when picked up: how to reliably map a compile/test/RV error back to the specific implicated file(s) (compiler errors usually name a file path directly; runtime errors may not); whether to always narrow-retry or only after the first full-set attempt; how this interacts with `_fill_missing_content`'s existing per-file-if-missing behavior.

**Other gaps surfaced but not yet backlog-ordered** (from a 2026-07-29 functionality/speed audit, still open as of 2026-07-30):
- ~~No resume/checkpoint for an interrupted `generate` run~~ **Done 2026-07-30** - see item 5 entry above (`kriya/workflow/checkpoint.py`, covers both `generate` and `fix`, not yet committed).
- No REPL/multi-turn session - all CLI subcommands are one-shot invocations, each building its own `Kernel`.
- No mid-task web/HTTP tool for the Developer/Reviewer agent to look something up during generation (`learn` is a separate offline pre-step only) - **partially addressed** by the skill-gap feature above, which does give agents a supervised, human-gated path to pull in reference material mid-generation, though only at the skill-matching stage, not from inside Developer/Reviewer's own reasoning.
- Brute-force vector search in `kriya/memory/vector.py` (full-table scan + NumPy cosine similarity, no ANN index/caching) - fine today, will degrade as an index grows past a few thousand chunks.
- ~~Single global LLM model config - no per-role model selection~~ **Done 2026-07-30** - see `agent_llms` entry above (commit `7ba336b`, not yet pushed).

- 2026-07-31 session (real bug fix, surfaced by the Ignite+Qpid real-world gate above, not a scoped backlog item): user was directly frustrated by wasted time from "malformed JSON response" failures during that test and asked why not fix it. Root-caused to two compounding bugs in the reasoning-model path, both fixed together:
  1. `kriya/core/llm.py::LLMClient.complete()` unconditionally set `response_format=None` for any reasoning model even under `json_mode=True` - confirmed via a direct live probe against `deepseek-r1:32b` through Ollama that this exclusion wasn't actually necessary (Ollama keeps reasoning/thinking output in a separate response field and only json_object-constrains the actual "content" field). Now applies `response_format` unconditionally, with a one-time retry without it if a given backend/model combination rejects the combination (defensive, in case some other backend genuinely can't handle it) - split the request logic into a new `LLMClient._request_once()` helper to support the retry cleanly.
  2. Even with `response_format` set, a reasoning model can still wrap the JSON in prose (not a hard token-level grammar constraint on every backend) - and `DeveloperAgent.run_generation`'s first (file-list) call only ever did a bare `json.loads()`, so any prose prefix immediately forced the much slower single-stage fallback call, and if THAT also came back prose-wrapped, the whole attempt failed and burned a full quality-gate retry. This is what was actually "killing time." Extracted the bracket-span JSON recovery that already existed (but was previously only used in the slow fallback path) into a shared `DeveloperAgent._extract_json_value()` and applied it to the file-list call too, so a prose-wrapped response is recovered directly - confirmed against the exact live-captured failure text from the Ignite+Qpid session.
  - **Verified against 4 real local models** (`qwen3-coder:30b`, `devstral-small-2:24b`, `deepseek-r1:32b`, `qwen3:8b` - the last two both reasoning-capable): all four produced a correct fix for the same real compile-error-repair scenario. `qwen3:8b`'s file-list call under reasoning mode degenerated into repeated garbage tokens (`</think></think>...`) - a real, different failure mode from deepseek-r1's prose-wrapping - `_extract_json_value` correctly identified it as unrecoverable, logged it, and cleanly fell through to the single-stage fallback (which then succeeded), rather than crashing or looping.
  - 15 new tests (`test_llm_extra.py`, `test_agents.py`). Full suite green (253/253). Committed as `e64f8a4` on top of `cfafa7b`, pushed to `origin/main`.
  - Real, meaningful lever for the "whack-a-mole" retry-reliability gap flagged just above: this doesn't fix a model regenerating working code into broken code, but it does mean fewer retries get wasted entirely on unrecoverable JSON parsing rather than actual code content - a smaller, more surgical fix than the deferred "detect repeated failure -> external lookup" idea, found and fixed opportunistically rather than as a scoped backlog pass.

- 2026-07-31 session (error-triggered live lookup in the Developer retry loop, not yet committed): user's stated motivation was reducing retries/time and getting "proper fixes" for errors, following directly from the Ignite+Qpid whack-a-mole findings. Scoped via `AskUserQuestion`, all "Recommended": (1) trigger only on a REPEATED failure (same signature across 2 consecutive attempts), not every failure, (2) query safety via regex-extracted Maven/Gradle-style `groupId:artifactId` coordinates found in the error text only - never raw error/stack-trace text, (3) scoped to compile + runtime-verification failures only, not test-assertion failures, (4) found content folds into that retry's prompt only (ephemeral) with no human confirmation and no durable skill write - the retry loop is already unattended.
  - New `extract_error_search_terms()` (regex-only, `kriya/workflow/workflow.py`) and `_augment_error_with_live_lookup()` (reuses the existing, already-live-verified `_resolve_via_web_lookup()` - deliberately skips the SkillGapAgent extraction call other lookup paths use, to avoid adding another slow LLM round-trip to an already-failing retry loop). Wired into the Quality Gates `except` block: tracks `last_failure_signature` (fail_type + sorted extracted terms, or raw text if no terms) across the while loop, comparing each new failure against the previous one.
  - 15 new tests (term extraction, augmentation function, two full-workflow integration tests - one confirming the third attempt's prompt contains the found reference material and search fires exactly once, one confirming `web_lookup_enabled=False` never searches even on an identical repeat). Full suite green (262/262).
  - **Real live end-to-end test, with an honest, non-confirming result**: stood up a real local SearXNG (Docker) with JSON format enabled, confirmed real search + real fetch work correctly (top result for `org.codehaus.mojo:exec-maven-plugin documentation` was the official MojoHaus plugin docs). Ran a rigorous A/B test using the EXACT broken `pom.xml` and EXACT error text from this morning's Ignite+Qpid failures (reproduced byte-identical via real `mvn`): asked `qwen3-coder:30b` to fix just that one file, once with only the raw error (control), once with the same error plus the real fetched reference material (treatment) - both fixes were then objectively verified with a REAL `mvn compile exec:java` run, not eyeballed. **Both control and treatment succeeded.** This means the exec-plugin bug, in isolation, was never actually a knowledge gap for this model - it fixes it fine either way. The real reason it survived 2+ consecutive attempts this morning was almost certainly that each retry regenerates ALL 6 files at once, and multi-file-response consistency/attention (not missing information) is the actual bottleneck - live lookup fixes "doesn't know X," not "knows X but loses track of it while juggling five other files."
  - **User's explicit decision when shown this finding**: ship live-lookup as-is (it's real, tested, and still has genuine value for actual knowledge gaps - e.g. the earlier qpid/httpx skill-gap cases are a much better fit than this one was), and scope a new, likely higher-impact idea separately: on retry, narrow WHAT gets regenerated to just the file(s) actually implicated by the error, instead of the whole file set every time. Not yet scoped or built - next candidate backlog item.
  - Committed as `791b605` on top of `e64f8a4` and pushed to `origin/main`, after the real live A/B test above and the user's explicit go-ahead.

- 2026-07-31 session (item 5's backlog follow-up, targeted single-file retry, not yet committed): user's stated motivation, directly following the live-lookup A/B test finding - decrease retries, get proper fixes, decrease generation time. Scoped through real back-and-forth rather than a single `AskUserQuestion` pass - the user twice pushed back on my first-draft options ("needs discussion, not a simple statement" on file identification; a genuinely different design than either of my two escalation options on retry budgets) and I had to propose concrete mechanics for them to react to rather than pre-packaged choices.
  - Final design: (1) identification is pure deterministic filename-substring matching against the error/output text (`extract_implicated_files()`) - no LLM call, works uniformly across compile/test/RV failures, naturally yields nothing for errors that don't name a known file (the exec-maven-plugin bug's error text never mentions `pom.xml`, so it correctly never triggers targeting). (2) Soft-scoped: target file(s) framed as the fix, every other written file included as read-only reference context - a genuine improvement over today's full-set path, which never shows the model its own previous attempt's actual content, only the error text. (3) **Two independent retry budgets**, the user's own idea after rejecting my two proposed escalation options: `retry_count` (full-set, unchanged `max(4, 1+len(chain))` formula) and a new `targeted_retry_count` (ceiling 3) - re-decided after every failure based on whether that failure named a file and targeted budget remains. (4) Targeted attempts always use the primary model, never escalate through the fallback chain - my own reasoning, user agreed: the measured 19-43s Ollama model-swap cost would directly undermine a "fast, cheap, surgical" attempt, and nothing is lost since the full-set escalation safety net still exists once the targeted budget is exhausted. (5) Targeting starts immediately on the very first failure, not deferred to a later retry.
  - **Found and fixed a real bug while implementing**: `quality_passed` was computed as `retry_count < max_retries`, which silently breaks once a run can succeed via a targeted attempt AFTER the full-set budget is already exhausted (a real, reachable case given the loop condition `retry_count < max_retries or (targeted budget available)`) - would have reported failure on an actually-successful run. Fixed with an explicit `quality_gates_succeeded` flag set only at the real success point. Caught via a dedicated regression test, not live testing.
  - Updating this also broke a real pre-existing test (`test_workflow_fallback_chain`) that assumed attempt 2 always escalates to the fallback chain - it no longer does when the failure names a known file (now correctly goes targeted first). Rewrote the test to exhaust the 3-attempt targeted budget first (all still primary model, confirmed via explicit assertions), then verified escalation resumes correctly on the full-set path afterward.
  - 12 new tests (identification, prompt-building, 3 workflow-integration scenarios covering targeted-fires-and-fixes, no-file-named-falls-back-to-full-set, and the quality_passed edge case). Full suite green (270/270).
  - **Live verification, real but partial**: 4 real scenarios against local models (2 Python tasks + a real `kriya fix` all succeeded on the first attempt, no retry to observe; a Java/Ignite task reproduced the SAME real exec-maven-plugin bug from the live-lookup A/B test across a full 4-attempt exhaustion, correctly and consistently choosing the full-set/escalation path every time since that bug's error text never names `pom.xml` - solid confirmation of the decision logic and safe-fallback behavior, and of the exhaustion-across-both-budgets logic, but the targeted branch itself was not caught firing in a fully organic live run this session - that specific mechanism (a soft-scoped single-file fix succeeding) was already proven directly by the standalone A/B test in the live-lookup entry above).
  - Committed as `791b605`, pushed.

- 2026-07-31 session (full from-scratch live validation of the real Ignite+Qpid Spring XML Java 17 messaging use case, user-requested specifically to validate skill-gap/live-lookup/targeted-retry working together against a genuinely hard goal, not a toy). User split it into incremental milestones (Broker-only → Cache-only → Wire-together) mid-discussion, a better test design than one big goal. Milestone 1 (embedded Qpid broker) took **9 live attempts** to pass - each failure was root-caused and fixed (skill content or Kriya code), never tolerated as a limitation; the user twice explicitly rejected proposals that stopped short of root-cause fixes ("why is there a missing file in the first place... this thinking is badly required" and "if we give up saying some reason is not welcoming").
  - Fixed: `exec:java` vs `exec:exec` pom.xml confusion (skill examples + `ArchitectAgent`/`RunVerifierAgent` prompts) - a real, hardcoded wrong default (`exec-maven-plugin block implies exec:exec`) was baked into `RunVerifierAgent`'s own system prompt, confirmed via code read of a sibling AntiGravity-built Kriya project hitting the identical bug.
  - New: completeness prevention (required-files checklist injected into the Developer's first prompt, from `extract_expected_files(design)`) + recovery (`IncompleteGenerationError` routes to a dedicated missing-file retry sharing Targeted Single-File Retry's budget/no-escalation philosophy) - see the target-retry entry above for the sibling mechanism this reuses.
  - Two qpid-specific root causes found via direct investigation (bytecode inspection of `qpid-broker-core` 9.2.1, and direct `-q`-vs-not comparison), neither previously suspected: Qpid's `SystemLauncher` hardcodes an uncaught `classpath:system.properties` default that only fails under `exec:java`'s isolated classloader (fix: always supply `initialSystemPropertiesLocation`); Maven's `-q` silently drops SLF4J logger output under `exec:java` (fix: goal-required observable markers must use `System.out.println`, never a logger call).
  - Real bug found and fixed independent of the milestone itself: `ignite-java17`'s tags included bare `java`/`maven`/`spring`, causing it to co-activate on unrelated qpid-only goals - concretely observed causing a live skill-content cross-contamination incident (Ignite rules auto-committed into `qpid/rules.txt`, an Ignite Spring XML example auto-committed into `qpid/examples/`) before the tags were narrowed. The underlying multi-skill-gap-prompt-routing code (which skill a single human/live-lookup answer's extracted content gets attributed to when multiple skills are flagged in one combined gap prompt) was NOT fixed at the code level - only this one instance's trigger (the bad tags) was removed. A skill with legitimately-necessary-but-broad tags could still hit the same misattribution.
  - Also found live, unrelated to the milestone script itself but while investigating: `SkillGapAgent` extraction doesn't dedupe well against near-identical rephrasings of existing rules across repeated live-lookup passes - `qpid/rules.txt` accumulated ~11 duplicate/near-duplicate rules this session, manually cleaned up twice. Not fixed at the code level.
  - Environment note, not a Kriya bug but relevant to reproducing any of this: this machine's `mvn` defaults to JDK 26 (Homebrew), which is incompatible with Qpid Broker-J 9.2.1's `Subject.doAs` usage post-SecurityManager-removal (JEP 486) - worked around via explicit `JAVA_HOME=temurin-17`. Kriya itself has no detection or guidance for this class of environment mismatch.
  - A self-inflicted mid-session mistake worth remembering: while separately asked to run/fix an unrelated sibling project (`TEST_PROJ_USING_KRIYA/spring-ignite-messaging`), a cleanup `pkill -f "exec.mainClass=com.example.BrokerServer"` for that project matched and killed the live Kriya validation subprocess too, since its goal text (passed as a CLI arg, visible in `ps`) happened to contain the identical substring. Killed a 3-minute-in run; had to restart the attempt. Lesson: `pkill -f` substring matches against full command lines including argv text, not just the intended target process name - dangerous when a goal string is passed as a literal CLI argument.
  - Milestone 1 result: **passed on the 9th live attempt, with zero retries** (compile, completeness, and runtime verification all green on the very first Developer generation) - strong signal the accumulated fixes generalize, not just patch the one observed failure. Milestones 2 (Ignite cache-only) and 3 (wire-together) not yet run.
  - 273/273 tests green. Committed as `ffda9a5`, pushed along with 39 auto-generated `Kriya: ...` skill-learning commits from the live testing itself.
  - **Updated score: 7.5/10** (same "single-developer, human-supervised, local daily-driver" framing as the first 6.5/10 score - see [[feedback-production-readiness-scoring]]). Up from 6.5 because a genuinely hard, realistic, multi-technology goal now passes end-to-end with zero manual intervention beyond Kriya's own designed-in Q&A - not just unit tests passing. Held back from higher because: only 1 of 3 planned milestones actually live-validated; the multi-skill-gap-routing bug and the duplicate-rule-extraction bug are identified but not fixed at the code level (only this session's specific triggers were removed/cleaned up); `kriya skills unverify` no-op bug (found earlier) still unfixed; zero CI coverage of any live-model behavior (inherently can't run without local LLMs, but means every "verified" claim rests on manual sessions, not an automated safety net).

- 2026-08-01 (same session continued): completed Milestones 2 (Ignite cache-only, additive to M1's workspace) and 3 (wire-together: Qpid+Ignite+Spring XML JMS messaging, the actual original real-world target scenario) - both eventually passed with **zero retries** once real root causes were fixed, but getting there required correcting a wrong assumption from earlier in the session plus two more genuinely significant bug fixes.
  - **`jvmArguments` was never a real exec-maven-plugin parameter - confirmed by extracting the actual mojo descriptor from the jar.** Established (wrongly) earlier this session that `<mainClass>+<jvmArguments>` was "the ONLY correct" exec:java shape; that verification only checked the app ran, not that flags were applied, and Qpid (the app tested) turned out not to need them in this environment - so the bug was silently harmless for M1 and only surfaced against Ignite, which genuinely needs `--add-opens=java.base/java.nio=ALL-UNNAMED`. exec:java fundamentally cannot pass JVM startup flags at all (runs inside Maven's own already-started JVM). **The user caught this independently before the live failure did** - explicitly pushed back twice ("i have been telling that is wrong") and pointed at the AntiGravity project's own correct exec:exec pattern as the reference, which is what led to actually verifying via the jar's mojo descriptor rather than continuing to trust the earlier "verified" claim. Real fix: exec:exec (`<executable>java</executable>` + `<arguments>` with every flag as its own element + `-classpath`/`<classpath/>` + `${exec.mainClass}`), rewritten across all 3 exec-maven-plugin skills and `RunVerifierAgent`'s inference guidance.
  - **A bug I introduced while fixing the above**: XML forbids `--` anywhere inside a `<!-- -->` comment, not just at the boundaries. My corrected comments said "every `--add-opens` flag..." in prose, which prematurely terminated the comment; the Developer Agent consistently copies skill-example comments verbatim into generated files, so this broke pom.xml parsing and cascaded into a false "dependencies removed" report. Fixed by rephrasing to avoid literal `--` in any XML comment; verified with a real XML parser, not just visual inspection, this time.
  - **`create_git_worktree` only ever reflected git HEAD - the most significant single finding of this whole session.** Milestone 3's "preserve existing files, add a new one" goal failed all 7 retries with `package X does not exist` for literally every import (Ignite, Qpid, Spring simultaneously) - not because the generated code was wrong, but because pom.xml (never touched by the Developer, since the goal said leave it alone) simply didn't exist in the sandbox: M1/M2's generated files were never git-committed in the test workspace, and a git worktree by design only knows about committed history, discarded completely on every fresh reset. This is NOT a narrow test-harness quirk - it's the normal state of any real project with in-progress uncommitted work, exactly when a "preserve/extend what's here" goal is most likely to be asked. Fixed with `_sync_uncommitted_changes_into_worktree()`: `git status --porcelain` (same pathspec already used for checkpoint dirty-detection) enumerates every modified/untracked/deleted file in the real workspace and copies it into the worktree after the HEAD reset. Two regression tests (uncommitted content copied in; deleted-but-uncommitted files don't linger).
  - **Skill-extraction hygiene**: (1) exact-string rule dedup missed near-duplicate rephrasings from independent extraction calls against the same reference material - `qpid/rules.txt` had accumulated ~11 duplicates across the session. Fixed with a deterministic (no LLM) content-word overlap-coefficient check, threshold tuned against real observed duplicate pairs (0.54-0.92 overlap) vs. genuinely different rules (0.08-0.11) - this fix was observed firing correctly live during M2, filtering real duplicates in real time. (2) Example-file writes were unconditionally overwriting existing files - confirmed live: a verified, hand-curated `ignite-java17/examples/pom.xml` got silently replaced by generic extracted content mid-session. Fixed: extraction never overwrites an existing example file, only adds new ones.
  - All 3 milestones passed and were **independently spot-checked by directly running each entrypoint myself** (not just trusting the RunVerifierAgent's judgment) - BrokerServer, CacheApp, and IgniteQpidApp all confirmed working correctly and coexisting in the same project after the full wire-together goal.
  - 279/279 tests green.
  - **Updated score: 8/10** (same framing as the 6.5 → 7.5 progression - see [[feedback-production-readiness-scoring]]). Up from 7.5 because the actual complete, hard, real-world target scenario (not a toy, not a single milestone) now works end-to-end with zero manual intervention beyond Kriya's own designed-in Q&A, AND because the worktree/uncommitted-changes bug fixed this round is a genuinely general, high-severity issue (not skill-specific) that would have silently affected any real user working on an in-progress project. Held back from higher because: still fundamentally validated through manual, human-guided live sessions, not an automated/unattended regression suite - no guarantee these exact fixes generalize to a different library/scenario combination without another live session catching it; the multi-skill-gap-routing bug and `kriya skills unverify` no-op bug (both identified in earlier entries) remain unfixed at the code level; zero CI coverage of any live-model behavior; only tested against one primary model (qwen3-coder:30b) with one fallback (deepseek-coder-v2:16b).

- 2026-08-01 (same session, next backlog item): fixed the multi-skill-gap-routing bug flagged unfixed above. Root cause confirmed by reading the actual code (`kriya/workflow/workflow.py`'s skill-gap resolution loop): when several skills are simultaneously unverified for one goal, Kriya combines them into a single human-facing prompt, but the manual-resolution loop reused that SAME combined, ambiguous gap description for every co-flagged skill's extraction call - no per-skill scoping at all. Fixed with two layers, both requested explicitly ("add both — the prompt scoping fix and a code-level guard"): (1) `_scoped_skill_gap_description()` narrows the extraction-time description to name only the one skill each call is for; (2) `_likely_misattributed_sibling()`/`_filter_misattributed_extraction()`, a deterministic no-LLM-call guard comparing a candidate rule/example against the target skill's identity (name+tags) vs. each co-flagged sibling's, dropping (never redirecting) anything that looks like it belongs to a sibling. Applied to both the manual and live-lookup resolution paths.
  - **Testing caught a real precision bug before it shipped**: the first version of the identity-word guard produced a false negative because "apache" is shared between `ignite-java17`'s `apache-ignite` tag and any `org.apache.qpid...` package reference - a genuinely qpid-only rule failed to flag as misattributed when targeted at ignite-java17, since "apache" alone satisfied the target-hit check. Fixed with a small generic-word exclusion (`apache`, `org`, `com`, `java`, etc.) scoped only to skill identity fingerprints, not arbitrary text - caught by a test asserting the reverse direction too, not just the originally-observed one.
  - 6 new tests including a full-workflow integration test that simulates the worst case directly (mocked extraction misbehaves identically for both co-flagged skills, as if the prompt-scoping fix had zero effect) - proves the code-level guard is real defense-in-depth, not just decorative. 285/285 tests passing. Committed as `58a3ca9`, pushed.
  - Remaining open items from the last score entry: `kriya skills unverify` no-op bug still unfixed; zero CI coverage of live-model behavior; single-model-family testing only. Not rescoring this entry on its own - it closes one specific, previously-flagged gap rather than changing the overall picture; next full score update should happen alongside the next substantive feature/milestone work, per [[feedback-production-readiness-scoring]].

- 2026-08-01 (same session, next backlog item): fixed the `kriya skills unverify` no-op bug flagged above. Root cause was purely in the CLI (`kriya/cli.py`'s `skills_unverify` command) - `SkillEngine.mark_unverified()` itself was already correct (resets both `verified` and `verification_gap_acknowledged`), but the CLI's `if not skill.verified: ... return` short-circuited before ever calling it whenever `verified` was already False, even if `verification_gap_acknowledged` was stuck True (the exact state hit live earlier this session, worked around then by hand-editing skill.yaml). Fixed by only short-circuiting when BOTH fields are already at their reset state. One-line fix; regression test reproduces the exact stuck-acknowledged scenario. 286/286 tests passing. Committed as `0752783`, pushed.
  - This closes the second (and last) of the two backlog items explicitly flagged unfixed in the 8/10 score entry. Remaining known gaps: zero CI coverage of live-model behavior, single-model-family testing only (qwen3-coder:30b + deepseek-coder-v2:16b fallback) - both structural, not quick fixes. Tagged `0.7` (pushed) covering everything through the multi-skill-gap-routing fix, just before this item.

- 2026-08-01 (new backlog item, UX rather than generation reliability - not rescoring): shipped `kriya repl` (Version A of a two-phase REPL/multi-turn plan). User's stated motivation: today's one-shot-per-invocation CLI feels like "start a fresh conversation every time"; wants it to feel more like a real session, with a nicer boxed input prompt "something like yours" (Claude Code's own). Explicitly wants Version B too ("without fail") - a genuine continuous, conversational tool-calling agent loop - but I gave an honest, evidence-grounded pushback first: this session's own multi-skill-gap misattribution bug (see the 2026-08-01 entry above) is direct evidence of the model *not* reliably self-discriminating on an ambiguous instruction without a deterministic code-level backstop, which is exactly the kind of self-directed judgment a continuous agent loop leans on *more*, not less, than Kriya's current bounded pipeline. User agreed to the sequencing: Version A now, Version B as a small bounded validation spike next (gated on what it actually shows, not deferred indefinitely).
  - Version A: `kriya repl` dispatches each typed line straight into the same Click group the CLI uses (`standalone_mode=False`) - zero duplicated parsing/logic. `--config` captured once, auto-applied per line. Boxed prompt via `prompt_toolkit` (new dependency). Typing "/" live-lists every command (enumerated from the Click group's own registry, not hardcoded) with its help text, mid-conversation feature-creep the user asked for after seeing the base REPL work ("can we add... when user gives input '/' it should list the commands"). 21 new tests, 301/301 passing. Committed `bc52cfe`, pushed.
  - Also retroactively wrote up the multi-skill-gap misattribution fix (shipped earlier the same day) into `docs/design.md` as §2.3.4e - it had never actually been documented, a real gap in the docs-reconciliation habit, found only because this feature's own design section needed to cite it accurately.
  - Version B is NOT yet started - next step when picked back up is designing the bounded validation spike itself (one narrow real task, hard turn cap, full decision logging) before any architecture commitment.

- 2026-08-01 (same session, Version B picked back up): built and ran the bounded validation spike (`spikes/version_b_routing/`, committed `ff75b8e` as a reference POC - not packaged/shipped, verified no `kriya/` source imports it and it adds zero collected pytest items), then implemented Version B for real (`kriya/routing.py`, `kriya/repl.py`, `kriya/config/config.py` - not yet committed as of this entry). User's framing throughout: "if it fails, there is no point in designing and developing it" - real go/no-go evidence before any implementation, not vibes.
  - **Embeddings-alone routing structurally cannot separate in-scope from out-of-scope input** - 0/5 on rejecting destructive/out-of-scope test inputs, across 4 local embedding models (`nomic-embed-text`, `embeddinggemma`, `nomic-embed-text-v2-moe`, `qwen3-embedding`) and 2 aggregation strategies (nearest-exemplar, centroid). A destructive request scores in the same cosine-similarity band as a real match - not a threshold-tuning problem, a structural one.
  - **Adding a narrow LLM in-scope gate fixed safety but needed two more real fixes**, both found via live testing, not guessed at: (1) few-shot prompt calibration - a gate with zero examples over- or under-rejected depending on model; (2) **gate model must be non-thinking-capable** - `qwen3:8b`'s default hidden "thinking" mode added 4-13s latency per call (confirmed via direct Ollama API testing: `/api/chat` with `think:false` cut 9.6s/414 reasoning tokens to 431ms/none) AND was observed in one isolated test rationalizing its way to an *unsafe* answer ("delete all my files" → `in_scope: true`) before self-correcting on a later run - a correctness risk, not just a latency one. Kriya's `LLMClient` goes through the OpenAI-compatible endpoint by design (portability across any backend, not just Ollama) and that endpoint does NOT honor `think:false`/`chat_template_kwargs.enable_thinking:false`/inline `/no_think` on Ollama 0.32.5 - only the native `/api/chat` does. Fix was model selection, not config: `qwen3-coder:30b` (already Kriya's default `llm.model`) has zero thinking-mode machinery in its template at all, so needed no suppression.
  - **User pushed for a much harder, larger test set before trusting the result** (expanded from the initial 37 cases to 136, "with whatever production-grade experience you have" - realistic hard negatives like "install express and add it to package.json" and "set up a CI pipeline in GitHub Actions" phrased to sound like legitimate feature requests, not obviously-silly negatives) - this caught two real, separate gaps the smaller set missed: (1) a genuinely mislabeled test case (a CI-workflow-YAML request is actually in-scope for `generate` - writing a file into the repo through the reviewable pipeline - not infra/out-of-scope; corrected after the user's own line of questioning about whether Kriya needing new capability could reduce ambiguity), and (2) the gate initially let package-install/infra-deploy-phrased requests through as `generate` until the prompt explicitly excluded "requires actually RUNNING a command or acting on a real system" with matching few-shot examples.
  - **User explicitly required "ask when uncertain" as a first-class design requirement**, not an afterthought - implemented as `AskWhenUncertainClassifier`/margin-based `CLARIFY` in the spike, ported to production as `Router.route()`'s top-2-candidates-within-margin check. This was empirically the single biggest lever: on the final 136-case test set, "effective accuracy" (a correctly-deferred CLARIFY counts as success) reached 95.6% while forced-top-1 "strict accuracy" was only 65.4% - most of the real gain came from correctly recognizing uncertainty, not from guessing better.
  - **Never assumed the winning spike config would also be Kriya's actual out-of-the-box default** - explicitly re-tested against the packaged `default_config.yaml` values before implementation. Confirmed `llm.model` default (`qwen3-coder:30b`) was already the winning gate model by coincidence, but `embedding.model` default (`nomic-embed-text`) was NOT `embeddinggemma` (the winning embed model) - re-running the full pipeline with the true default embedding model dropped effective accuracy from 95.6% to 77.2% (real categories like `review`/`skills` collapsing to ~53%, not a threshold artifact - the correct label often wasn't even in the top-2 ranking). Tested two smaller alternatives (`nomic-embed-text-v2-moe` 273MB, `qwen3-embedding:0.6b` 639MB) hoping to avoid a new dependency - neither got close to the bar, and `embeddinggemma` (621MB) turned out to be roughly the same size as the alternatives while clearly winning, so there was no real size/accuracy trade being made. Resolved by decoupling: new `routing.embed_model` config (default `embeddinggemma:latest`), deliberately separate from `embedding.model` (tuned for the RAG index, a different task - short intent phrases vs. long-form retrieval). `Router` fails loudly (`RoutingModelUnavailable`) if that model isn't pulled, rather than silently falling back to `embedding.model` and eating the accuracy loss unannounced.
  - **A real, previously-unstated assumption caught before coding, not during it** (per the user's explicit "I don't prefer unknown during coding" ask before implementation started): the spike's "the argument is always just the raw input text" assumption is true for `generate`/`ask` but false for `fix` (needs `--error`, not a positional arg) and false for `review`/`analyze` (both require an *existing path* - `click.Path(exists=True)` - raw natural language would fail Click's own validation immediately). Fixed in `build_dispatch_tokens()`: `review`/`analyze` default to `.` (repo root), consistent with both already being documented as git-status-aware for directories.
  - Config: `routing.enabled` defaults `false` (opt-in only, matching the precedent of every other trust-sensitive Kriya default - `autonomy.mode`, `web_lookup_enabled`), `routing.embed_model`/`reject_threshold`/`ask_margin` all new fields, documented in `default_config.yaml`. Gate model reuses `llm.model` - no separate config field, since the constraint (non-reasoning) is a model-selection fact discovered through testing, not something a runtime flag can fix given the OpenAI-compat-endpoint limitation above.
  - 24 new tests (`tests/test_routing.py`, plus `_route_line`/`_resolve_clarify` extracted from `run_repl()`'s loop into testable helpers in `tests/test_repl.py`) - all mocked (no live model calls in the test suite itself), plus a separate live smoke-test run against real `embeddinggemma`+`qwen3-coder:30b` confirming the production port reproduces the spike's behavior end-to-end, including the `review`/`analyze` path-argument fix working for real. 328/328 tests passing, ruff clean on every touched file. Docs updated in the same pass (README, user_guide §3.6.1, design.md §3.6a, CLAUDE.md) per the docs-reconciliation habit - not deferred.
  - Not rescoring the production-readiness number for this entry - same reasoning as Version A (UX/session-layer feature, not a change to the core generate/fix/review pipeline's code-generation reliability), though this one is closer to the line than Version A was, since it does lean on local-model judgment more directly (the in-scope gate). The distinction held: nothing here changes what `kriya generate`/`fix`/`review` actually produce, only how a natural-language line gets routed to one of them.

- 2026-08-01/02 session (CI live-model tier + a critical packaging bug, then a full qwen3.6-vs-qwen3-coder model comparison, then three real retry-loop bugs found and fixed via that comparison): closes/softens two of the longest-standing flagged gaps (zero CI coverage of live-model behavior; single-model-family testing only).
  - Built `tests/test_live_smoke.py` + a `live-model-smoke` CI job (Ollama installed fresh, pulls `qwen2.5-coder:1.5b`+`all-minilm`, `continue-on-error: true`, excluded from default `pytest` runs via `-m "not live_model"`). While finally exercising a genuinely fresh clone for this, found **`kriya/memory/` (the real source package, not just runtime data) had never been committed to git at all** - `.gitignore`'s unanchored `memory/` pattern (meant for runtime DB files) also matched the source package name. This had been silently working locally only because every dev machine already had the untracked files on disk. Fixed by anchoring the pattern (`/memory/`, `/logs/`) and force-adding the previously-excluded files; also fixed a real latent `sqlite3`-used-without-import bug in `vector.py`, found only because this was the first time the file was ever linted. Also fixed a CI pip-cache race between jobs sharing a Python version. Commits `2888b62`, `5ea8182`.
  - Ran a rigorous, real 3-way model comparison (`qwen3-coder:30b` only / hybrid `qwen3.6:35b` Plan+Architect + `qwen3-coder:30b` Developer / `qwen3.6:35b` for everything) on an isolated, skill-free Jackson-JSON-record task to separate raw model capability from skill-injected knowledge. `qwen3.6`-only failed 7/7 initially. Root-caused (not accepted as a model limitation) to two real Kriya bugs: (a) the per-file fallback path could concatenate a sibling file's content into the current file's response; (b) targeted single-file retries re-asked the model for a file list it had already been given deterministically, and the model reliably dropped the actually-broken file from that list, burning the whole retry budget without progress.
  - Fixed both, plus a `known_target_files` param on `DeveloperAgent.run_generation()` to bypass the unreliable file-list call entirely when the caller already knows the target set deterministically. Re-ran the Jackson task: passed, and independently picked a newer Jackson version (2.17.0) than the primary model's own default (2.15.2) - a real "less knowledge-gap" signal for using qwen3.6 at the Planner/Architect layer specifically.
  - **Full live re-validation of the real Qpid+Ignite Spring XML app (3 milestones, `qwen3.6:35b` for every role) surfaced two more real, general bugs**, both fixed with regression tests: (c) Maven's `-q` (needed so `System.out.println` isn't buried in build noise - several skills recommend it) also suppresses the actual exception on a runtime failure, leaving every retry with zero diagnostic signal - `_resolve_run_command()` now always injects `-e` into Maven run commands. (d) The missing-file-recovery retry path, when given `known_target_files` directly from `find_missing_expected_files()` (which only ever returns bare basenames, since the Architect's design text isn't parsed for directory structure), wrote files flat at the sandbox root instead of their real nested location - fixed via `_resolve_missing_file_paths()`, which searches the design text itself for a fuller path mention before using the deterministic-bypass shortcut. All 3 milestones then passed end-to-end (M1 2 attempts, M2 3 attempts, M3 3 attempts, all independently re-run outside Kriya to confirm), full suite 339/339, ruff clean. Committed `da0ea86`, pushed.
  - **Audited every LLM call site in the codebase afterward** (10 total) to confirm the "model drops items from a requested list" pattern doesn't recur anywhere else - it doesn't; every other structured-output call site is either a single-object response or genuinely open-ended discovery (skill-conflict/rule extraction) with no closed ground-truth set to check completeness against.
  - **Model-selection finding**: hybrid (qwen3.6 Plan+Architect / qwen3-coder Developer) beats qwen3.6-only on every measured task - fewer retries (qwen3.6 as Developer is measurably less disciplined at code output, needing more compile-error retries even with perfect diagnostic information) and much lower total latency, since the hybrid config's `agent_llms` overrides only touch Planner/Architect - Developer, the skill-conflict checker, and the run-verifier judge/grader all silently default to the fast model, while an all-qwen3.6 config pays reasoning-mode latency on every one of those (~4.5 min of pure pairwise skill-conflict-check overhead alone, observed live, for 3 co-active skills - `O(skills²)` LLM calls, paid on every `generate` call regardless of goal complexity).
  - **Tool-call-per-file Developer architecture explored as a follow-on idea, prototyped narrowly, not adopted.** Live-tested tool-calling reliability on Kriya's own Ollama setup across 3 models: `qwen3-coder:30b` (Kriya's actual current Developer model) is genuinely inconsistent (sometimes proper `tool_calls`, sometimes a malformed `<function=write_file><parameter=...>` dump left in `content`). **Initial root-cause attribution (Ollama routes it to the wrong parser, citing ollama/ollama#14493) was WRONG and was corrected in the same session after the user pushed on it** - `ollama show qwen3-coder:30b --modelfile` directly confirms `RENDERER qwen3-coder` / `PARSER qwen3-coder` are already correctly selected (issue #14493 is about a different model, "Qwen 3.5 27B", being misrouted - doesn't apply here). The real cause, found via further research: the correctly-selected `qwen3-coder` parser has its own open reliability bugs - ollama/ollama#11232 (parser mis-extracts even against correct model output), **#14570 (returns HTTP 500 outright when a tool call's arguments get truncated - directly relevant to Kriya's real use case, since a `write_file` call's argument is the entire file content, exactly the "large arguments" failure case)**, and a goose-project report (#6883) that `qwen3-coder` degrades to XML-in-text once tool count exceeds ~5. Also found ollama/ollama#16383: `qwen3.6`/`qwen3.5`'s parser has its own known intermittent failures too ("cumulatively painful for any agent issuing many tool-using calls") - meaning this session's own "3/3 reliable" result for `qwen3.6` was very likely small-sample luck, not proof of real reliability at scale. `devstral-small-2:24b` uses an older-style custom `TEMPLATE` (no RENDERER/PARSER at all) rather than the newer structured pipeline, and was 3/3 reliable in this session's sample - unconfirmed whether that generalizes better, just untested at scale here. Speed: tool-calling was ~2x faster than `json_mode` on `qwen3.6:35b` (~16s vs ~32s avg), no meaningful difference on `qwen3-coder:30b`/`devstral-small-2:24b`. **Net conclusion unchanged despite the correction**: no simple Ollama config fix exists (the right parser is already active; it's the parser implementation itself that's unreliable, especially for large arguments) - Kriya's own defensive dual-path fallback parsing (already built in the spike, proven to work every time it fired) is the correct mitigation, not a workaround for something configurable away. `qwen-agent` (official Qwen PyPI package) has its own Hermes-style parser, unconfirmed whether it would fare better against `qwen3-coder`'s actual XML dialect specifically. Lesson for future sessions: verify a specific model's actual configured behavior (`ollama show <model> --modelfile`) before attributing a live observation to a GitHub issue found by search - an issue about a similarly-named model is not evidence about this one, see [[feedback-verify-at-the-source]].
  - **Follow-up, same session: actually tried the obvious workaround (a custom `TEMPLATE`-only Modelfile bypassing `qwen3-coder`'s built-in `RENDERER`/`PARSER`) rather than leaving it theoretical - result was a clean negative, worth remembering so it isn't re-attempted blind.** Built `FROM` the raw weights blob directly (not the `qwen3-coder:30b` tag - a derived model otherwise silently inherits the parent's `RENDERER`/`PARSER` regardless of what `TEMPLATE` is specified, ollama/ollama#14560), with a hand-authored `TEMPLATE` using `qwen3-coder`'s real native tool-call syntax. **0/5 runs produced proper `tool_calls`** - strictly worse than the built-in (buggy) parser's ~50%, and the model's raw output itself became less internally consistent (drifted between `<parameter=content>` and bare `<content>` mid-response, something it didn't do under the built-in parser). Root cause: Ollama's `TEMPLATE`-only path only auto-recognizes a small, fixed set of known legacy bracket conventions - confirmed by reconciling against `devstral-small-2:24b`, which is *also* pure `TEMPLATE`-only with zero `RENDERER`/`PARSER` and got proper `tool_calls` every time, because its template uses Mistral's own `[TOOL_CALLS]`/`[ARGS]` bracket syntax, one of the recognized conventions. `qwen3-coder`'s actual native format isn't. **No config-only escape hatch exists for this model** - a dedicated parser (buggy or not) is required for structured extraction to work at all; hand-rolling one in a Modelfile isn't possible without Ollama shipping new parser code. Spike (script, Modelfile, findings) at `spikes/tool_call_developer/` (uncommitted, exploratory); the experimental `qwen3coder-customtemplate` Ollama tag itself was removed after the test.
  - **Updated score: held at 8/10.** Real, rigorous work this round (3 more general bugs found+fixed with regression tests, a full audit confirming the pattern is closed elsewhere, a genuine model-selection recommendation backed by real timing data) - but "a system already scored 8/10 kept yielding new bugs this readily under one more model's worth of stress" is itself a signal the surface area of undertested-but-reachable code is still large, consistent with the standing view that an accumulating pile of fixes isn't the same as monotonic progress (see [[feedback-production-readiness-scoring]]). Live-model CI coverage is real now but still narrow (one small model, `continue-on-error: true`, not a hard gate); multi-model testing is now backed by real manual data across 4 models total (`qwen3-coder:30b`, `qwen3.6:35b`, `devstral-small-2:24b`, plus CI's `qwen2.5-coder:1.5b`) but still not automated as a regression matrix - a new model could still surface a 4th bug of this same shape tomorrow.

- 2026-08-02 session: copied this memory file into the repo itself (`docs/kriya_production_readiness.md`, frontmatter stripped, short intro added) per explicit user request - kept in sync going forward, see [[feedback-readiness-log-in-repo]]. Also committed the tool-call spike (`spikes/tool_call_developer/`) that had been sitting uncommitted, `841eac1`.
  - User asked for 2 backlog picks specifically targeting "performance, less errors, less retries." Picked (1) skip Developer's file-list call on the initial generation too (not just retries), reusing the Architect's own already-computed `_expected_files_upfront` as `known_target_files`, and (2) a live-model CI test that actually exercises the retry/recovery paths (not started yet - deferred when a bigger finding took priority, see below). User chose to proceed with (1) first.
  - **Implementing (1) surfaced a bigger consequence than scoped**: since `all_files_written` is cumulative across the whole retry loop and attempt 1 now always writes every expected file via `known_target_files`, the entire missing-file-recovery mechanism (`IncompleteGenerationError`, `_build_missing_files_retry_prompt`, `use_missing_files`) becomes unreachable via its most common real trigger (the model dropping a file from its own list) - though NOT fully dead: `normalize_written_filepath` can still reject/discard a resolved path downstream, which is a real, narrower remaining trigger. Raised this explicitly via `AskUserQuestion` before proceeding rather than silently shipping it. **User's decision: keep the fix, keep the machinery as defense-in-depth** ("Kriya is listing the files it needs. During verification if the files created are not sufficient may be this will help us.") - correct call, since the mechanism isn't actually fully dead.
  - Implemented: initial full-set generation call (`retry_count == 0` only - later full-set retries after a real failure are left on the old ask-the-model path, since they already escalate through the fallback chain and involve real error context, not a clean slate) now passes `known_target_files=_resolve_file_paths_from_design(_expected_files_upfront, design)` when the Architect's design yielded a resolvable file list. Renamed `_resolve_missing_file_paths` -> `_resolve_file_paths_from_design` (now used at two call sites, not just missing-file recovery).
  - **Fixing this broke 7 existing tests** (call-shape changed: attempt 1 now does N independent per-file plain-text completions instead of one JSON-list-with-content call, whenever the mock design text incidentally matches a real filename - which most test fixtures did). Fixed each individually rather than blanket-patching, and separately audited for tests that were passing for the WRONG reason (mocks stale but not content-checked, e.g. `test_workflow_successful_run` was silently writing a raw JSON string as a Python file's content and passing anyway since it never asserted on content) - found and fixed one such case. Full suite 339/339, ruff clean.
  - **Live-verified against `qwen3-coder:30b` with a real 3-file goal (task-tracker CLI: models.py/storage.py/cli.py)**: confirmed via the log that the Step-1 file-list call never fired - straight to 3 "Implementing file" per-file calls. The run went on to fail after 7 attempts for a genuinely unrelated reason (see next entry) but **zero `INCOMPLETE GENERATION` failures occurred across any of the 7 attempts**, with `models.py`/`storage.py` correctly written only on full-set attempts (not rewritten during the 3 cli.py-scoped targeted retries) - confirms the fix works exactly as intended, including under real retry/escalation pressure.
  - Committed `0ad51ed`, pushed.
  - **A genuinely new, unrelated finding from the live-verification run, not a regression from this fix**: the "argparse subcommand bug" that caused all 7 attempts to fail was never actually a code bug - directly reconstructed and ran the exact generated `cli.py`/`models.py`/`storage.py` outside Kriya, and `add`/`list` both worked correctly, including persistence across separate invocations. The real cause: `RunVerifierAgent.judge()` only ever infers ONE command to run, and the actual command used every attempt was literally `python cli.py` with zero arguments - which can only ever show help text. The Reviewer's stated reason ("`Task(args.title)` invalid, missing `done`") was also wrong - `done: bool = False` has a default, confirmed by running it. **New backlog item opened as a direct result**: the Runtime Verification Gate cannot validate any goal whose correctness requires observing state across multiple sequential invocations (add-then-list, write-then-read, start-then-query) - it will burn its full retry budget and fail every time, no matter how correct the generated code is, and misreport it as a code bug (both to the retry loop's error context and the Reviewer). User asked to start on this immediately after opening it - see next entry for the fix itself once implemented.

- 2026-08-02 session (same day, continued): implemented and shipped the new backlog item immediately, per explicit user request ("open the backlog item and immediately start working on this backlog item").
  - `RunVerifierAgent.judge()` (`kriya/agents/agent.py`) changed from a single `run_command` to an ordered `run_commands` list (backward-compatible: a model still returning the old flat `["exe", "arg"]` shape gets auto-wrapped into a single-element list rather than rejected). System prompt now explicitly instructs: most goals need one command, but a goal whose correctness can only be observed across multiple invocations (add-then-list, write-then-read) must return each as its own ordered element.
  - New `PolymorphicValidator.run_app_sequence()` (`kriya/tools/validate.py`) runs each command in order in the same workspace (state persists between steps - a written file, a database), running every command regardless of an earlier one's exit code (so later output is still available as grading evidence) except on timeout, which stops the sequence immediately. `run_app()` (single command) left unchanged and still used for goals with only one step.
  - Workflow.py call site updated (`resolved_run_commands = [_resolve_run_command(cmd) for cmd in judgment["run_commands"]]`, `validator.run_app_sequence(...)`) - **caught and fixed a real bug before shipping**: the non-zero-exit short-circuit was checking only the LAST step's returncode, which can be 0 even when an earlier step failed (every step runs regardless) - fixed to check `run_res["success"]` (reflects the whole sequence) instead.
  - Updated 6 existing tests referencing the old `run_command` key/shape across `test_agents.py`/`test_workflow.py`, added 6 new ones (multi-step sequence, old-shape backward-compat, `run_app_sequence` success/continues-after-failure/timeout-stops/no-commands). 345/345 passing, ruff clean.
  - **Live-verified against the exact same task-tracker CLI goal that originally exposed the bug - first pass surfaced a second, self-inflicted bug, found and fixed before declaring done**: the judge now correctly inferred TWO sequential commands (real progress), but used a bare `cli.py` instead of the actual generated path `src/main/python/cli.py` - traced directly to the new system prompt's OWN example using a bare filename, which the model copied instead of using the real path from `files_written`. Fixed the prompt to explicitly require the exact path from "Files Generated" and warn against copying a bare filename from the example. **Second live pass: first attempt, zero retries** - correct nested path, `add "Task 1"` correctly preserved as one argument (not split), `[RESULT]`-equivalent grading passed, independently re-run outside Kriya (`add "Task 1"`, `add "Second one"`, `list`) to confirm for real, not just trusting Kriya's own grader.
  - Committed `10f47ac`, pushed.

- 2026-08-02 session: started a new, explicitly-scoped initiative per user request - a systematic per-feature validation pass across every Kriya CLI command (not just `generate`), "similar to unit testing," to surface hidden bugs the way live testing has repeatedly done all session. Stated end goal, to keep front-of-mind for every item in this pass: Kriya should be a robust code assistant generating accurate code, effectively, with minimal/no retries.
  - Inventoried the real CLI surface directly from `kriya/cli.py` (not from memory) - 17 distinct commands/groups. Split into "extensively live-tested already" (`generate`, `fix`, `skills` subcommands, `repl`) vs. "never or barely live-tested with a real model" (`review`, `ask`, `learn`, `analyze`, `traces`, `doctor`, `config`, `plugins`, `tools list/execute`, `prompt render/generate`) - working through the second list one command at a time, due diligence (read the real implementation first) before any live call.
  - **`review` (first item): found 4 real bugs via due diligence alone, before spending any live LLM time**, then a 5th (worse) confirmed live: (1) `agent_llms.reviewer` config silently ignored by the standalone command (built its own `ReviewerAgent` with no role override, unlike `generate`'s internal reviewer which correctly threads it through workflow.py); (2) Ruby (`.rb`) missing from the directory-review extension allowlist despite `PolymorphicValidator` elsewhere explicitly supporting Ruby; (3) `git status --porcelain` (no `-z`) quotes paths with spaces, and naive `.split()`+`parts[-1]` parsing silently mangled them, finding nothing and falling through to a broader recursive-scan fallback that masked the bug rather than exposing it - switched to `-z` (NUL-separated, unquoted); (4) **no context/token budget management at all** - confirmed live as a severe silent failure: a 20,000-line file got truncated from the FRONT by the backend, cutting off every `=== File: ... ===` framing marker, leaving the model an unlabeled code fragment with no idea it was being asked to review anything - it produced a confused non-review response, and Kriya still reported **exit 0 (success)** with zero warning.
  - **Caught a real bug in my OWN fix (#3) before it shipped, via a discriminator test pattern worth remembering**: my first `-z` parsing attempt got git's rename-record field order backwards (assumed old-path-first, new-path-continuation; verified directly against real `git status --porcelain -z` output via `xxd` that it's actually new-path-first, old-path-continuation) - caught because the regression test was deliberately designed to distinguish "the git-status path found this file" from "the fallback recursive scan found it by accident, masking a parser bug" (an `unrelated.py` committed-with-no-changes decoy file that must never appear in the prompt if the git-status path is genuinely doing the work) - a naive test that only checked "is the renamed file present" would have passed on the buggy code too, same as the original bug did.
  - Fix for (4): budget-aware batching reusing `workflow.py`'s existing `estimate_tokens()` heuristic and `context_window * 0.75` convention (not a new one) - the common case (a few small/medium files) still produces exactly one combined call, unchanged; degrades to multiple labeled batches only when genuinely needed; a single oversized file gets truncated with an explicit marker sent to the model plus a user-facing warning. Also added a user-facing warning for the pre-existing (previously silent) 10-file directory cap. `ReviewerAgent`'s system prompt gained a 4th guideline: explicitly call out truncation in its own report if it sees the marker (defense in depth alongside the CLI-level warning).
  - 9 new tests (`tests/test_review_command.py`), each explicitly verified to fail against the pre-fix code (not just assumed to). 354/354 full suite, ruff clean. Committed `67365f8`, pushed.
  - **`ask` (second item): found 4 more real bugs in the key-files context builder, same due-diligence-first approach.** (1) The "skip large non-entry-point files" heuristic checked only Java's `public static void main` marker, applied uniformly to `.java`/`.py`/`.rb` - that marker can never appear in Python/Ruby, so EVERY large (>3000 char) Python/Ruby file was silently excluded regardless of whether it was the actual entry point, including Kriya's own `cli.py` when asking about its own repo. Fixed with a per-extension marker table (Python: `__main__`, Ruby: `__FILE__`). (2) `requirements.txt`/`pyproject.toml` - the actual dependency/run manifests for a Python project - matched neither the special-filename set (Java/Node-centric only) nor the extension set, so were never included even for "what does this depend on?". (3) The directory walk's ignore set didn't include `.kriya`, so a leftover/active `generate` worktree (a full nested git working copy) could get walked into, polluting context with a stale duplicate copy of the same files.
  - **Also investigated (via due diligence + live test) the theoretical "no overall token budget" concern that was severe in `review`'s case - confirmed it does NOT materialize as a real problem here**, and deliberately did not add a speculative fix: `repo_context` is small bounded metadata (languages/frameworks/deps list, not a per-file dump - confirmed live at 1,586 chars for Kriya's own repo), `key_files_context` already has an effective ~30-50KB soft cap, `rag_context` is bounded by chunk size (~100 lines) × `top_k` (5). Live-verified against Kriya's own ~150-file repo: 8,764 of 32,768 tokens used, correct and accurate answer. Consistent with "don't build for scenarios that can't happen" - the difference from `review` is that `review` had a *confirmed live failure*; this one doesn't.
  - 4 new tests (`tests/test_ask_command_key_files_bugs.py`), each verified to fail against the pre-fix code. 358/358 full suite, ruff clean. Committed `1a4c312`, pushed.
  - **`learn` (third item): found 2 real bugs (one a genuine data-loss bug), plus a design question raised with the user rather than assumed.**
    - **Data-loss bug**: `--text` entries were named `"Manual Entry {N}"` by positional index within a single invocation, not a stable identity. Indexing always clears existing chunks for the same `source_name` first (intentional dedup-on-re-learn for URLs/files) - so a LATER, entirely unrelated `learn -t` call whose Nth `--text` flag landed on the same index silently DELETED an earlier, unrelated manually-taught fact. Confirmed live/in a test. Fixed with a content-hash-derived name (`Manual Entry (<hash>)`) - distinct texts get distinct identities, a genuine re-teach of the same text still correctly dedups.
    - **Silent-failure bug, same shape as `review`'s severe one**: `OllamaEmbeddingClient.get_embeddings()` silently substitutes an all-zero "dummy" vector on any embedding-server failure (a deliberate graceful-degradation choice that's harmless for `ask`'s RAG query lookup, since near-zero similarity scores get filtered naturally) - but `learn` writes that dummy vector permanently into the index and still reports "Successfully indexed." The content becomes silently unsearchable forever while the user believes it worked. Now warns clearly instead of masking it.
    - **Design question raised via `AskUserQuestion` rather than assumed**: should `learn -u <url>` be gated by `autonomy.egress_policy` (defaults to `"local_only"`, strictly enforced for `LLMClient.complete` elsewhere via `is_local_url`/`EgressViolationError`) - confirmed via direct code read that `fetch_url_text` has ZERO egress-policy awareness today, meaning a stock Kriya install (default `local_only`) currently allows unrestricted external URL fetching via `learn -u`, contradicting the "zero external cloud dependencies" positioning on the surface. **User's decision: no, leave it exempt by design** - `learn -u` is itself the user's explicit, deliberate authorization to reach that specific URL, unlike an automatic pipeline call the policy is meant to guard against. Documented the reasoning directly in the code and locked it in with a regression test, so this doesn't get "fixed" into a regression later by someone who doesn't know it was a deliberate call.
    - 6 new/updated tests, each verified to fail against pre-fix code where applicable. 362/362 full suite, ruff clean. Committed `490a94e`, pushed.
  - **`analyze` (fourth item): surfaced something bigger than a per-command bug.** Live-testing `analyze` against a scratch project whose config didn't override `paths.skills` silently wrote a new auto-generated skill into the REAL Kriya repo's own shared `skills/` directory - the SAME class of incident already documented multiple times in this project's history (Gap 3 smoke-test, live-lookup work, item 5's real-world gate), now the 4th occurrence, and it caught me despite having the specific warning in memory. That recurrence, not the individual incident, was the actionable signal.
    - **Raised with the user rather than assumed**: should Kriya build an actual safety-net CHECK, not just document the warning again? Confirmed yes, scoped broadly (every write call site found, not just the one that triggered this).
    - New `kriya/skills/skill.py::is_accidental_shared_skills_write()`/`get_global_skills_dir()`: flags exactly the "`paths.skills` silently resolved to Kriya's own shared install directory, for a workspace that isn't the Kriya install itself" signature. Wired into all 3 confirmed write call sites: `analyze`'s auto-skill-bootstrap (`analyzer.py`), `generate`'s skill-gap/conflict-resolution/lesson-staging (`workflow.py` - checked ONCE per run before any of those can fire, not repeated at each individual write), and `skills create` (`cli.py`). Each warns loudly before writing rather than blocking outright, consistent with this pass's established "warn, don't silently mask" pattern.
    - Two more `analyze`-specific bugs found via due diligence: (1) `click.Path(exists=True)` accepts a file as well as a directory, but `analyze()` walks the path as a directory (`os.walk` on a file silently yields nothing) - pointing at a single file produced an empty-looking but "successful" analysis with no error. Now a clear error. (2) `--changed` on a non-git directory: both git commands fail (returncode != 0, not an exception), and the old code silently left the changed-files set empty either way - filtering the file list down to NOTHING with no indication this was because `--changed` genuinely couldn't be honored, not because there were zero real changes. Now warns and falls back to indexing everything.
    - **A mistake happened while writing the regression tests for this, itself an instance of the exact bug being fixed** - an early draft set `cfg = AppConfig()` directly (bypassing `load_config`'s real path-resolution) and didn't mock away the actual write, causing a second real stray write into the live repo's `skills/` during test authoring. Caught immediately (`git status --short skills/`), cleaned up (untracked, no git history to unwind), and the tests were redesigned to patch `get_global_skills_dir` itself so the shared-dir scenario can be exercised without ever touching the real directory - documented as the reference pattern for any future test of this kind.
    - 7 new tests, 369/369 full suite, ruff clean. Committed `419089a`, pushed.
  - **`traces` (fifth item): found 2 real bugs, ruled out 2 more candidate concerns via direct evidence rather than assumption.**
    - **Confirmed bug**: `traces()` opened `traces.db` with a bare `sqlite3.connect(db_path)` instead of the shared `kriya.core.db.get_connection()` - the only thing that gives a connection WAL journal mode + a 30s busy timeout. `kriya/core/db.py` globally monkey-patches `sqlite3.connect` itself at import time, which raised a real question: is the patch already active by the time `traces()` runs, making this a non-issue? Verified directly (not assumed) - a fresh `import kriya.cli` followed by a `CliRunner` invocation of `traces` alone leaves `kriya.core.db` absent from `sys.modules` the whole time, so the patch is never triggered and the bare connect gets zero WAL/timeout protection, unlike every other DB access path in the app. A concurrent `generate`/`fix` run writing to the same `traces.db` could make `kriya traces` raise "database is locked" instead of waiting briefly.
    - **Confirmed bug, found live against real data**: no `LIMIT` clause on the query. Running `kriya traces` against this project's own real `traces.db` (grown from all the live testing this whole validation pass has done) dumped a 485KB, 1000+-row wall of text to the terminal in one call. Added `-n/--limit` (default 20) and `--all`, plus a "Showing N of M recorded runs" footer when truncated.
    - **Ruled out via grep, not assumed fine**: the `status.lower() == "success"` color check and the unguarded `f"{dur:.2f}s"` format both looked risky from reading the display loop alone (what if `status` or `duration_sec` don't match expectations?) - grepped every `log_run()` call site in the codebase and found exactly one (`workflow.py`), always passing `status="success"` or `status="failure"` literally and a `time.time() - start_time` computed float for `duration_sec`. Neither is a reachable bug given the actual single writer, so neither was touched - consistent with this pass's "don't fix what can't happen" discipline.
    - 4 new tests (`tests/test_traces_command.py`), each verified to fail against the pre-fix code via `git stash` (unrecognized `--all`/`-n` options, no truncation). 373/373 full suite, ruff clean. Committed `5a07529`, pushed. Docs updated (`user_guide.md` §4.5) in the same commit.
  - **`doctor` (sixth item): found 2 real bugs.**
    - **Confirmed bug**: the entire LLM connectivity check was gated on `llm.provider == "openai"` literally, but `kriya.core.llm.LLMClient` never reads that field at all - grepped every `.provider` reference in the codebase and confirmed `doctor()` is the ONLY place `cfg.llm.provider` affects behavior; `LLMClient` always talks OpenAI-compatible protocol to `base_url` regardless of its value, and `user_guide.md` documents the field as exactly that ("OpenAI-compatible client; works against Ollama, LM Studio, etc."). Verified live against a real local Ollama server: setting `provider="ollama"` (a natural, reasonable value describing the actual backend) made the entire Testing-connection/SUCCESS/ERROR block for the LLM section vanish silently, while the embedding check (correctly unconditional) succeeded against the same server. Fixed by always running the check.
    - **Confirmed bug**: `doctor()` never returned non-zero regardless of how many `[ERROR]` findings it reported - undermines its own README framing ("Ensure local Ollama models and server links are connected") for any scripted/CI use (`kriya doctor && kriya generate ...`). Now tracks whether any `[ERROR]`-level check failed and exits 1 if so; `[WARNING]`-level findings (e.g. configured model not in the server's list) deliberately don't affect the exit code, reusing the severity labeling already present in the output rather than inventing a new one.
    - 4 new tests (`tests/test_doctor_command.py`), each verified to fail against pre-fix code via `git stash`. 377/377 full suite, ruff clean. Committed `8f15a03`, pushed. README updated with the new exit-code contract in the same commit.
  - **`config` (seventh item): found a real secret-exposure bug, more severe in class than the prior silent-skip/silent-success bugs found in this pass.** `config()` printed `cfg.model_dump_json()` verbatim with zero redaction anywhere in the codebase (grepped for any existing masking/redact logic first - none exists). This dumps every `api_key` field in plaintext - top-level `llm.api_key`, every `llm_chain[]` fallback entry, every `agent_llms.<role>.llm`/`llm_chain` override, each with its own independent `api_key` - plus every MCP server's `env` dict, which commonly carries real subprocess credentials (e.g. `GITHUB_TOKEN`). Not theoretical: `user_guide.md` explicitly documents `llm.api_key` as holding a real credential for remote endpoints ("set a real key for remote endpoints"). Verified live before fixing - a fake API key and a fake MCP token both appeared verbatim in real command output.
    - Fixed with a recursive `_redact_secrets()` walk (any dict key literally named `api_key` -> `***REDACTED***`; every value inside a dict keyed `env` -> redacted, key names kept for diagnostic visibility) rather than enumerating every nested location by shape - naturally covers current and future nesting (llm_chain entries, every agent_llms role) without needing updates when new config sections are added.
    - 4 new tests (`tests/test_config_command.py`), each verified to fail against pre-fix code via `git stash`. 381/381 full suite, ruff clean. Committed `53c3723`, pushed. README gained a short "Inspect Resolved Configuration" section (the `config` command was previously undocumented entirely).
    - **User follow-up question, verified rather than assumed**: does the fix mean secrets are never logged anywhere, not just in `config`'s own output? Static grep first (no `logger.*` call anywhere references `api_key`/`cfg`/`self.config` directly; checkpoint fingerprinting SHA-256-hashes the full config dict, one-way, never logs the raw value; trace logs' `model_hops` only ever records bare model-name strings). One real remaining caveat flagged honestly rather than assumed away: could `logging.level: DEBUG` make the underlying `httpx`/`openai` SDK client emit the `Authorization` header itself? **Verified live against a real local model**, not just reasoned about: ran `LLMClient.complete()` with Kriya's own `configure_logging` set to DEBUG and a real fake secret in `llm.api_key`, captured all log output - the key never appeared. The `openai` SDK's own debug logging explicitly redacts it (`'security': {'bearer_auth': True}` - a boolean flag, not the header value), and `httpcore`'s DEBUG-level logs only emit request/response *events* (`send_request_headers.started`/`.complete`), never header contents. Confirmed clean, no further fix needed - this was a due-diligence check that came back negative, not every finding in this pass is a bug.
  - **`plugins` (eighth item): found a real "diagnostic gives false confidence" bug, same class as several earlier items in this pass.** `plugins()` called `PluginManager.discover_and_load()` but never `initialize_all()` - grepped every `PluginManager` call site and confirmed it's the ONLY one that skips it; `tools_list`/`tools_execute` (and real `generate`/kernel startup) both call it. `discover_and_load()` only proves a plugin's `__init__` succeeds; `initialize()` is the step where a plugin actually registers its tools/listeners, and can fail independently. Verified live with a deliberately broken test plugin (raises in `initialize()`): `kriya plugins` showed it as cleanly loaded, while `kriya tools list` against the same directory failed outright.
    - **Second related finding, flagged but deliberately not fixed in this item**: that same live test showed `kriya tools list` doesn't just fail on the broken plugin - it fails *entirely* and never lists the unrelated working plugin's tools either, since `PluginManager.initialize_all()` raises on the first failure and aborts the loop. Left for the `tools list/execute` item (next in this pass) since changing `initialize_all()`'s fail-fast contract affects real pipeline paths (not just a diagnostic command) and deserves its own scoping decision - consistent with this pass's discipline of not silently bundling a second, differently-scoped fix into the current item.
    - Fix: `plugins` now starts the kernel and attempts each plugin's `initialize()` directly via its own tolerant per-plugin loop (not `PluginManager.initialize_all()`, deliberately not reused here so one broken plugin can't hide every other plugin's real status) - shows `[INITIALIZED]`/`[FAILED: <reason>]` per plugin, exits non-zero if any failed.
    - 3 new tests (`tests/test_plugins_command.py`), each verified to fail against pre-fix code via `git stash`. 384/384 full suite, ruff clean. Committed `8c75b27`, pushed. README gained a "Check Plugin Health" section (previously undocumented).
  - **`tools list`/`tools execute` (ninth item): found the two bugs anticipated from the `plugins` item, plus one more of the same "happy-path-only cleanup" shape.**
    - **Confirmed bug (the flagged one)**: both commands called `PluginManager.initialize_all()`, which raises on the first plugin init failure and aborts before later plugins are attempted. Verified live with a broken plugin alongside a working one: `tools list` reported zero tools and an error (hiding the working plugin's tool entirely); `tools execute <working-tool>` failed outright even though the requested tool had nothing to do with the broken plugin. Fixed with a new shared `_initialize_plugins_tolerant()` helper (also adopted by `plugins`, replacing its own inline copy of the same logic) - each plugin's `initialize()` is attempted independently; a broken one now only makes its own tools unavailable (`tools list` shows a `[WARNING]` naming which plugin(s) failed), everything else keeps working. `PluginManager.initialize_all()` itself left untouched (still fail-fast) - that's still the right contract for other call sites; only these diagnostic/utility commands needed tolerant behavior, a deliberately narrow fix matching the deliberately narrow question raised in the `plugins` item.
    - **Second confirmed bug, same "diagnostic-command cleanup" shape as skipped in `traces`/other items' spirit but here about resource cleanup, not display**: `kernel.start()` spawns real MCP subprocess servers (`asyncio.create_subprocess_exec` - genuine child processes, not reaped when the CLI process exits) but `pm.shutdown_all()`/`kernel.stop()` were only ever called on the happy path. `tools list`'s "No tools registered" early return, and any exception between a successful kernel start and the end of either command's body (a malformed tool schema while listing, invalid JSON in `tools execute`'s `arguments_json`), skipped cleanup entirely - a real orphaned-process leak. Fixed with `try/finally` in both commands, which also let three duplicated pre-exit cleanup call sites in `tools_execute` collapse into one `finally` block.
    - 6 new tests (`tests/test_tools_command.py`) - two exercise the fail-fast fix with real dynamically-loaded plugins, two spy on `Kernel.stop` to prove cleanup runs even when the command body raises - each verified to fail against pre-fix code via `git stash`. 388/388 full suite, ruff clean. README updated with the new resilience behavior.
    - **Committed locally (`37c73b2`) but NOT pushed** - user asked mid-turn to check in before pushing going forward; holding all of this item's commits (and the docs-sync commit for it) until confirmed.
  - **`prompt render`/`prompt generate` (tenth and final item of the original inventory): found one real "dead code, no way to reach it" gap, of a different shape than the rest of this pass's findings.** `PromptEngine` (`kriya/prompt/engine.py`) has always supported a `template_dir` constructor arg for custom `<name>.jinja` files (checked before the 4 built-in defaults) - already covered by its own unit tests in `tests/test_prompt.py`, so the underlying feature genuinely works. But grepping every `PromptEngine(` construction in the codebase shows `prompt render` is the ONLY call site, and it always built `PromptEngine()` with zero arguments - no CLI flag, no config field, and `generate`/`fix`'s real pipeline doesn't use `PromptEngine` at all. Confirmed live before fixing: a real `.jinja` file on disk produced "Template not found" no matter what - a fully-implemented, unit-tested feature with zero way to actually invoke it from the CLI.
    - Fixed with a `--template-dir`/`-t` option on `prompt render`, matching the existing `--var` option's per-invocation style rather than inventing a new persistent config field for what is a standalone ad-hoc utility disconnected from the real generation pipeline.
    - `prompt generate` checked too and found clean: goes through `LLMClient.complete()` like every other real LLM call site, so it already inherits egress-policy enforcement; has no per-role `agent_llms` config concept to silently ignore since (unlike reviewer/planner/etc.) no such config exists for this ad-hoc command - ruled out, not a repeat of `review`'s original bug shape.
    - 2 new tests (`tests/test_prompt_command.py`), the custom-template-dir one confirmed to fail against pre-fix code via `git stash`. 390/390 full suite, ruff clean. README gained a "Render & Generate Prompt Templates" section (previously undocumented).
    - **Committed locally (`754007c`) but NOT pushed yet** - holding per the standing [[feedback-check-before-push]] preference; check with the user before pushing.
  - **This closes the original 10-command inventory from the 2026-08-02 "systematic per-feature validation pass" kickoff** (`review`, `ask`, `learn`, `analyze`, `traces`, `doctor`, `config`, `plugins`, `tools list/execute`, `prompt render/generate`) - every one found and fixed at least one real bug via due diligence + live/deterministic verification, several found bugs of a genuinely new shape not seen in earlier passes (secret exposure in `config`, dead/unreachable code in `prompt render`, resource leaks in `tools list/execute`). Next step is to check with the user on what's next: a fresh command inventory pass, revisiting a flagged-but-deferred item, or a different focus entirely.

- 2026-08-02 session (REPL first-contact UX, prompted by a user question rather than the command-by-command pass, but treated with the same rigor): user asked how to accomplish "generate a prompt, then use it to build the app" - answering it surfaced a real bug (see the deferred entry just above) and, when asked to think out loud about the bigger picture ("first he wants to generate a prompt or he may straight away ask generate so and so app for me thinking it will work like you? What will he notice?"), a much bigger first-contact UX gap in `kriya repl` itself.
  - Traced precisely (not guessed) what a first-time REPL user hits typing plain English with `routing.enabled` at its shipped default (`false`): `kriya/repl.py::_route_line`'s `tokens[0] in cli_main.commands or router is None` short-circuit passes raw tokens straight to Click regardless of shape when routing is off, producing a bare "No such command" or (if the sentence happened to start with a real command word, e.g. "generate a REST API for todos" unquoted) `generate`'s own "Got unexpected extra arguments" - either way a raw, unguided CLI parsing error, hitting exactly the scenario the user described.
  - Presented options rather than picking one unilaterally: flip the default vs. leave it off and only improve the REPL's failure messaging. **User's explicit decision**: flip the default to `true` AND add the friendly-error catch - both together, not either alone.
  - **Verified precisely, live, before implementing**: confirmed an explicit command always bypasses routing regardless of its enabled state (`prompt` is itself a registered top-level command name, so `_route_line` returns immediately with `router.route()` never called - proven via a mock that raises if called) - directly answered the user's follow-up question ("will enabling routing break `prompt generate \"...\"`?") with evidence, not assumption.
  - **A sharper realization surfaced mid-scoping, not assumed from the start**: flipping the default alone does NOT close the user's original scenario, because `_route_line`'s explicit-command-always-wins check happens BEFORE the router is ever consulted - a line starting with a real command word followed by prose (`generate a REST API for todos`, unquoted) bypasses routing entirely no matter what `routing.enabled` is set to. This is arguably the MORE likely first-contact failure, since starting a sentence with "generate" is the natural thing to do - which is exactly why the user's paired ask (routing on + a `_dispatch`-level UsageError catch) was necessary, not redundant with each other.
  - Implemented: `RoutingConfig.enabled` default flipped to `True` in both `kriya/config/config.py` (pydantic field) and `kriya/config/default_config.yaml` (the packaged yaml `load_config()` actually applies - both needed). `kriya/repl.py::_dispatch` gained a `click.exceptions.UsageError` catch (covers both `NoSuchCommand` and the "real command, wrong argument shape" case, since `NoSuchCommand` is itself a `UsageError` subclass) - shows Click's own specific error first, then appends one short, deliberately generic quoting tip rather than trying to sniff "natural language" vs. "genuine typo on an intentional command" from the error text (judged too brittle/Click-version-fragile). `_print_banner()` gained a routing-state-dependent hint line, shown proactively before a user's first attempt rather than only reactively after a failed one.
  - **Caught and fixed a real inconsistency during implementation, not from live testing**: the new tip line initially used plain `click.secho(...)` with no `err=True`, landing on stdout while Click's own error (via `e.show()`) goes to stderr - same logical error report split across two streams, which would confuse anything piping/filtering stderr specifically. Fixed before writing tests.
  - **Verified end-to-end against real local models**: with the real (no override) default config, `routing.enabled` resolves to `True` via `load_config()`; a genuinely natural sentence with no command-name prefix ("build me a small todo app with tests") correctly routed to `generate` through the real `embeddinggemma` classifier + `qwen3-coder:30b` in-scope gate, ~5s, confirming the new default actually works end-to-end, not just at the config level.
  - 8 new tests (`tests/test_repl.py`, `tests/test_config.py`), the behavior-critical ones confirmed to fail against pre-fix code via `git stash`. 396/396 full suite, ruff clean. Committed `56c49b5`, **not yet pushed** (holding per [[feedback-check-before-push]]).
  - Docs reconciliation caught 3 real, now-stale mismatches from the old "off by default" framing (README's routing bullet, user_guide.md §3.6.1, design.md's Version B write-up) - all corrected in the same commit, plus a new user_guide.md callout for the "starts with a real command word" edge case with a worked example of the new tip.
  - Not rescored - this is UX/onboarding, not a change to what `generate`/`fix`/`review` actually produce, consistent with how Version A/Version B REPL work was treated earlier in this project's history.

- 2026-08-02 session (closing the deferred `prompt generate` stdout bug, scoped properly before any code per explicit user request): the fix was scoped as two parts before writing anything - Part 1 (stdout/stderr cleanup, benefits standalone shell-pipe users) and Part 2 (auto-save + `--file` hint, the actual fix for REPL chaining, since a real shell pipe was never available between two typed REPL lines regardless of the routing-default change). User explicitly asked to also check other commands for the same bug shape first.
  - **Audit found 2 more real, confirmed instances, not just the original one**: `analyze` printed its JSON payload FIRST, then progress/status chrome AFTER on the same stdout stream (would corrupt `kriya analyze . | jq .` with trailing garbage past the JSON's closing brace) - actually the worse shape of the two. `review` mixed "Scanning directory..."/"Reviewing N files..."/the "=== Code Review Report ===" header into stdout alongside the actual streamed review text. `ask`/`prompt render`/`config`/`tools execute` were checked and confirmed already clean; `doctor`/`plugins`/`traces` ruled out as not applicable (no other Kriya command consumes their output as input, unlike `analyze`'s JSON or `review`'s report).
  - **User asked directly whether this would change LLM behavior** - verified precisely per-command before implementing: in all three cases the moved click.secho/echo calls are structurally separate from the actual system_prompt/user_prompt/batch_prompt construction, confirmed by reading each call site rather than asserting it. Pure output-stream routing change, zero effect on any model input.
  - **A second, sharper user question mid-scoping**: since `kriya` now defaults to the REPL on bare invocation, does that break the standalone shell-pipe fix? Verified precisely rather than assumed: the REPL-default gate (`ctx.invoked_subcommand is None`) only fires on BARE `kriya` with zero arguments - proved via a mock asserting `run_repl` is never called for `prompt render ...`, and via real piped subprocesses (`kriya prompt render ... | wc -l`, `echo "..." | kriya generate`) showing completely normal behavior with no REPL interference, confirmed no stray state left in the repo afterward. The real, correctly-identified gap remains what Part 2 addresses - piping *within* one REPL session between two typed lines, which was never possible in the first place, REPL-default or not.
  - **A deeper instance of the SAME bug found only because the regression test exercised the real indexing path (mocked LLM + embeddings) rather than a stub**: `kriya/analyzer/analyzer.py`'s own auto-skill-bootstrap side effect (triggered whenever `analyze` indexes a repo without an existing `auto-<repo>` skill) has its own narration and streamed convention-extraction output, also unguarded stdout - a shallower test that avoided the real indexing path would have missed this entirely. Fixed the same way (err=True throughout).
  - Implemented: `prompt generate`'s status/header chrome moved to stderr; auto-saves to `.kriya/last_prompt.md`, prints `generate --file .kriya/last_prompt.md -y` as the next step - a failed save warns instead of silently claiming success (caught and fixed before writing tests: the first draft silently swallowed a save failure at debug-log level only). `analyze`'s JSON stays the only stdout content, progress bar redirected via `file=sys.stderr`. `review`'s narration/headers moved to stderr, only the streamed review text stays on stdout.
  - **Found and fixed a related, pre-existing gap while touching this area**: `.kriya/` (already used by `checkpoint.py`/worktree sandbox before this session) was never in `.gitignore` - any real dogfooding run showed untracked files. Added `/.kriya/` to this repo's own `.gitignore`, scoped to Kriya's own repo (not a general "auto-manage a third-party project's gitignore" feature, which would be a separate, bigger question).
  - 10 new/changed tests across `tests/test_prompt_command.py`/`tests/test_analyze_command.py`/`tests/test_review_command.py`, each verified to fail against pre-fix code via `git stash`. 401/401 full suite, ruff clean. Committed `f6554c1`.
  - **Follow-up user request, same session**: add a concrete worked example to the help surfaces for this exact use case. Added to both `kriya repl`'s `/help` text and `kriya prompt generate --help`'s own docstring (using Click's `\b` marker to stop automatic text-rewrapping from mangling the two-line example - verified live it renders correctly). 1 new test, confirmed to fail pre-fix via `git stash`. 402/402 full suite, ruff clean. Committed `131daea`.
  - All 5 commits from this REPL-UX/prompt-chaining thread (`56c49b5`, `c56e255`, `f6554c1`, `131daea`, plus the doc-sync commit) pushed to `origin/main` together after user confirmation, per [[feedback-check-before-push]].

- 2026-08-02 session (back to the golden use case - Ignite+Qpid+SpringXML - after closing the CLI validation pass): user asked for a model recommendation first. Answered with real prior data rather than re-testing: qwen3-coder:30b for every role (only model to complete the full 3-milestone scenario end-to-end before; model-swapping on this machine measured 3.8x slower for a mixed-tier config) - explicitly deferred trying the qwen3.6-hybrid-Planner/Architect idea until after a clean qwen3-coder-only baseline, so a model change wouldn't confound whether *this session's* bug fixes actually reduced retries.
  - **Wrote 3 fresh milestone goals** (not reusing the old, unrecoverable exact wording - the prior goal text was never persisted anywhere, only passed as a CLI arg each time) building incrementally in the same project: M1 Ignite-only (embedded node + cache via Spring XML), M2 Qpid-only (embedded broker + JMS client via Spring XML, separate main class), M3 real integration (a Qpid message written into the Ignite cache, both started from the same Spring XML context/main method) - deliberately NOT three independent greenfield generations, to also test whether Kriya can extend a growing codebase without breaking it.
  - **Skills audit before running** (user's explicit ask: "we need to provide proper skills information"): confirmed `qpid`/`ignite-java17` both `verified: true` with real content and examples; confirmed they load as GLOBAL skills (this validation project's own local `./skills/` is empty) since `SkillEngine` always loads global + project-local; found 2 minor hygiene issues (4 stale/orphaned entries in `qpid/rule_provenance.json` referencing Ignite text never actually in `qpid/rules.txt`; `ignite-java17/examples/` has only `pom.xml` vs qpid's 6 files) - neither blocked the run, deliberately left skills unchanged before the M1 run to keep the model-fixed-vs-skills-fixed comparison clean.
  - Found the project directory (`~/WorkingDirectory/AI/ClaudeCode/kriya-full-validation-ignite-qpid`) still had the *previous* validation's complete 3-milestone codebase sitting untracked in git - moved it aside (not deleted, nothing here is git-tracked) into `_prior_run_backup_20260731/` before starting fresh, also cleared stale `.kriya/checkpoints`/`.kriya/worktree`/Ignite's own `ignite/` runtime-work directory. Reused the existing `orchestrate.sh` harness (a working, already-proven non-interactive `kriya generate` runner that watches the log and auto-answers skill-gap/conflict/approval prompts via a FIFO) rather than rebuilding anything - passed `-y` directly to `kriya generate` instead of relying on orchestrate.sh's text-pattern-matching for approval, since that regex is fragile against any wording changes this session's own CLI-text edits might have introduced.
  - **M1 attempt 1: real bug, found and fixed.** Quality Gates failed after all 7 retries (`KRIYA_EXIT=0` despite this - reconfirmed the CLI's exit code doesn't reflect Quality Gates outcome, only that the workflow ran to completion; must always check the actual log, not the exit code). Root-caused precisely: every attempt generated `import org.apache.ignite.cache.IgniteCache;`, verified WRONG by unzipping the real `ignite-core-2.18.0.jar` directly - the actual class lives at `org.apache.ignite.IgniteCache` (top-level package; `org.apache.ignite.cache` holds other real types like `CacheMode`/`CacheAtomicityMode`, so the wrong guess is plausible, not random). Same exact compiler error repeated byte-for-byte across all 7 attempts - the model never once self-corrected despite unambiguous compiler feedback every time. Also found `kriya/tools/resolver.py`'s Maven-Central-search dependency-suggestion feature actively injecting misleading noise (`c:"IgniteCache"` simple-class-name search matched an unrelated `cc.mashroom:mashroom-plugin`) - flagged, not fixed (secondary, not the primary blocker). **Fix**: added an explicit rule to `skills/ignite-java17/rules.txt` stating the correct import path and why the wrong guess is plausible. Committed `64e62b8`.
  - **M1 attempt 2: the skill fix helped (compilation got past the import bug into runtime verification) but surfaced a second, more architecturally significant bug.** `RunVerifierAgent` inferred `mvn -e exec:java` against a `pom.xml` correctly written in the `exec:exec`-only `<arguments>`/`<classpath/>` shape (Ignite's `--add-opens` flags need `exec:exec`, since `exec:java` runs inside Maven's own already-started JVM and can never apply JVM startup flags - documented in the skill). **User asked directly why this could happen given the skills were clean** - answered with precise evidence, not a guess: read the actual `>>> Step: Architect Design <<<` log output and found it was JUST a bare file list (27 output tokens) - the Planner's own plan text, one step earlier, correctly said "use exec:exec" three separate times, but the Architect's design (deliberately minimized in an earlier, separately-justified fix, to prevent unnecessary file/class splitting) is ALL `RunVerifierAgent.judge()` ever sees (`goal`, `design`, `files_written` paths only - confirmed by reading the actual call site) - the correct guidance never survived to reach it. Since `judge()` gets called with identical inputs every attempt, the same wrong command repeated 3 times running, burning the entire targeted-retry budget on a problem that was never in the generated code, forcing an expensive full-set regeneration that regressed the already-fixed `IgniteCache` import bug as a side effect (both under the primary model and after escalating to `deepseek-coder-v2:16b`).
  - **Fix, scoped properly before implementing (user's explicit ask)**: extended `_resolve_run_command()` (kriya/workflow/workflow.py) - already the established home for exactly this kind of deterministic post-hoc correction (bare-`python`-substitution, unconditional Maven `-e` injection already live there) - with an optional `workspace_path` param. When given, checks the real `pom.xml` on disk for the exec:exec-only `<classpath/>` signature and corrects `exec:java`→`exec:exec` if found - ground truth from the actual file, independent of whether skill/plan/design guidance survived the pipeline. Deliberately one-directional (only the confirmed, observed failure), explicitly chosen over the two alternatives raised (richer `judge()` context, a run-command retry-feedback loop) since it's zero-new-LLM-calls and matches the existing pattern exactly. 6 new tests, confirmed to fail pre-fix via `git stash`, PLUS verified directly against the exact real `pom.xml` content extracted from the failing run's own log (not just a hand-built fixture). 407/407 full suite, ruff clean. Committed `e1e4cd2`.
  - **M1 attempt 3: real, verified success.** `Quality Gates: PASSED` in 2 attempts total (attempt 1 still made the wrong-import mistake initially - the skill rule doesn't guarantee first-try correctness - but this time the retry loop got an uncontaminated budget to actually fix it via real compiler feedback, since the exec:exec fix meant no attempts were wasted on the unrelated runtime-verification mismatch). Confirmed `mvn -e exec:exec` was correctly chosen this time. **Independently re-verified outside Kriya entirely**: ran `mvn -q compile exec:exec -Dexec.mainClass=com.example.App` by hand against the actual applied files - a real Ignite node started, and printed the exact expected `[IGNITE-RESULT] Order-1001-Placed`.
  - Both fixes NOT yet pushed (`64e62b8`, `e1e4cd2` on top of the earlier-pushed `60c4b07`) - holding per [[feedback-check-before-push]].
  - Next: M2 (Qpid standalone), then M3 (real integration) - same fresh-goal, same-model, skills-unchanged discipline, only fixing what a real run actually surfaces.

- 2026-08-02 session (M2, Qpid-only milestone, continuing the golden-use-case validation from M1): same fresh-goal/same-model/skills-unchanged discipline. Found and fixed 3 genuine Kriya architecture bugs across the first 3 attempts, then a real toolchain-drift issue in attempt 4 that was NOT a Kriya bug.
  - **M2 attempt 1: `pom.xml` never touched.** All 7 retries `COMPILATION FAILURE` because `pom.xml` (needing new Qpid dependencies) never appeared in the Developer's written-files list. Root cause: `ArchitectAgent.system_prompt`'s `"## Files to Create"` heading, combined with `DeveloperAgent`'s "implement ALL files, don't omit any" instruction, structurally excluded already-existing files needing modification - the Architect's design simply never listed `pom.xml` since M1 had already "created" it. Fixed: heading renamed to `"## Files to Create or Modify"` with an explicit instruction to check Workspace Context for existing files needing real changes, not just new ones.
  - **M2 attempt 1, secondary finding**: a stray `cc.mashroom-mashroom-plugin-None` skill folder was found auto-created in the validation project's local `skills/` dir by the "auto-accrual" mechanism (Phase 3 of `workflow.py`) with zero verification the Maven-Central-search suggestion was ever actually used in the final applied code (same root issue as M1's flagged-not-fixed resolver noise, now confirmed to have a real downstream consequence). Fixed: auto-accrual now only scaffolds a skill if the suggested coordinate's `artifactId` actually appears in the FINAL applied file content, not just somewhere in the retry history.
  - **M2 attempt 2 (with fixes 1-2 applied): `pom.xml` now correctly touched, no more mashroom noise - both confirmed.** New failure: `RunVerifierAgent` inferred a malformed run command (`-Dexec.executable=java -Dexec.args=-cp %classpath` - bypasses the pom's own `<arguments>` entirely, uses invalid `%classpath` syntax exec-maven-plugin doesn't support). Root cause: same class of gap as M1's exec:java/exec:exec bug - `judge()` never sees the real pom.xml, only the Architect's minimized design. Fixed by passing actual `pom.xml` content (read from the worktree) into `judge()`'s prompt as ground truth, alongside the existing goal/design/files-written inputs.
  - **Also investigated, confirmed NOT a bug**: a "Dependency regression" check (`validate.py`, compares new pom.xml deps against `original_workspace_path`'s) triggered a tug-of-war over `ignite-indexing` during this attempt. Traced precisely: the "budgets_exhausted" code path does NOT silently apply broken/regressed files to the real workspace - confirmed by reading the actual apply-gate code, not assumed. Real friction (wastes retry budget) but not a correctness bug - left as known friction, not fixed.
  - **M2 attempt 3 (with fix 3 applied): `mvn -e exec:exec` now correctly chosen - confirmed.** Runtime Verification failed with `UnsupportedOperationException: getSubject is not supported` inside `SystemLauncher.startup()`. Root-caused via manual reproduction outside Kriya: a genuine Java 17.0.10+/Qpid Broker-J compatibility issue (Qpid's own internal startup logging calls a JDK method deprecated for removal), fixed via `-Djava.security.manager=allow`. Added to `skills/qpid/rules.txt`. Confirmed the SAME exception recurred identically across 3 real Kriya retry attempts within that run - the model never resolved it on its own.
  - **User's sharp challenge, verbatim: "Are we in a loop here?" - is this becoming targeted per-app patches instead of genuine Kriya product fixes, after 3 days of touching the same skill.** Answered with genuine reflective analysis: of the fixes across M1+M2 so far, 6 were genuine Kriya architecture/code fixes (Files-to-Create heading, auto-accrual verification gate, RunVerifier pom-content grounding, exec:java/exec:exec correction, resolver false-positive removal, and - found in direct response to this question - the live-lookup fix below) vs. 2 were skill-content-only fixes (the IgniteCache import path, the qpid security-manager flag). Investigating the "why does the human keep having to hand-fix the skill" question further (not just asserting the ratio was fine) surfaced a NEW, concrete, generalizable Kriya bug: **the repeated-identical-failure live-lookup trigger (meant to let Kriya try resolving exactly this class of problem autonomously) never fires for errors with no extractable Maven groupId:artifactId coordinate** (e.g. a bare Java stack trace) - it falls back to comparing the FULL raw error text, which embeds Maven's own always-different build-timing lines (`Total time:`, `Finished at:`), so two occurrences of the byte-for-byte-identical Qpid exception never compared equal, and live lookup silently never engaged across all 3 repeat attempts. This directly explains why a human kept needing to hand-fix this specific class of skill gap. **User explicitly authorized**: "Yes, scope it and implement it."
  - **Fix**: new `_normalize_error_for_repeat_detection()` (`kriya/workflow/workflow.py`) strips the two confirmed-noisy Maven lines before raw error text is used as the fallback repeated-failure signature (used only when `extract_error_search_terms` found no coordinate). Deliberately narrow, evidence-grounded fix (only the two noise patterns actually confirmed present in the real captured failing output) rather than speculatively stripping broader noise (PIDs, other timestamps) never actually observed mattering in this real case. 2 new regression tests using the real captured error text (byte-for-byte from the failing log, only timing/timestamp values varied), confirmed to fail pre-fix (`ImportError`, function didn't exist). 417/417 full suite, ruff clean. Committed `5ad1f27`, not yet pushed.
  - **M2 attempt 4 (with all 4 fixes + qpid skill fix applied): a NEW failure, and this time NOT a Kriya bug.** `mvn exec:exec` crashed at VM startup: `"A command line option has attempted to allow or enable the Security Manager. Enabling a Security Manager is not supported."` under `java.base@26.0.1` - a completely different, opposite-shaped error from attempt 3's fix target. Root-caused: this machine's Homebrew `mvn` hardcodes `JAVA_HOME` to Homebrew's `openjdk` symlink whenever `JAVA_HOME` is unset in the environment, and that symlink had been silently upgraded to JDK 26.0.1 at some point since the qpid skill rule was written and verified. JDK 24+ *permanently removed* the Security Manager (JEP 486) - the exact flag that was the correct, verified fix for 17.0.10 is now a fatal VM-startup error on 24+. The earlier manual reproduction (that verified the flag as the fix) had invoked plain `java` directly, which resolves via `/usr/bin/java` → Temurin 17.0.10 (a completely separate resolution path from `mvn`'s own JAVA_HOME override) - so it never hit this discrepancy. **Correctly identified as validation-environment toolchain drift, not a product or skill defect** - did not touch Kriya code or the skill again; fixed by pinning `JAVA_HOME` to the Temurin 17 install for the validation run's shell environment instead. Also reconfirmed in this same attempt: the `ignite-indexing` dependency-regression tug-of-war recurred, worse than attempt 2 (all 3 Ignite deps dropped by attempt 7) - still just friction, not re-scoped this session.
  - **M2 attempt 5 (JAVA_HOME pinned to Temurin 17): real, verified pass.** `Quality Gates: PASSED` in 2 attempts (attempt 1 hit an unrelated runtime-verification issue, self-corrected on attempt 2). Real runtime verification output: `[QPID-RESULT] Order-1001-Placed`. Independently re-verified outside Kriya by hand (`mvn -q compile exec:exec -Dexec.mainClass=com.example.QpidClientApp` with JAVA_HOME pinned) - the real embedded broker started (`BRK-1004 : Qpid Broker Ready`, JVM 17.0.10+7), message sent and consumed synchronously, correct `[QPID-RESULT] Order-1001-Placed` printed. Note: the (separate, non-gating) Reviewer pass reported "Status: Rejected" on this same run with several critique points - confirmed this is expected, informational-only behavior per the architecture (Reviewer never overrides the actual Quality Gates pass/fail signal), not a contradiction.
  - **User immediately caught a repeated-mistake pattern in how this JDK issue was handled**: pointed out (correctly) that "mvn resolves to JDK 26 instead of 17" was NOT a new finding - it was already discovered and documented 2 days earlier (2026-07-31 entry above, this same file: "Kriya itself has no detection or guidance for this class of environment mismatch"), and this session re-discovered and re-worked-around it from scratch instead of checking memory first or closing the already-flagged gap. Acknowledged directly, no defensiveness. When the user then pushed further ("do 1 and 2 together actually solve the problem?"), recognized a pure `kriya doctor` diagnostic (option 1 alone) is disconnected from the actual `generate` retry loop and wouldn't have prevented today's wasted retries even if it existed - refined into two connected layers. **This time, actually implemented and closed the gap** (not a third re-flag/re-workaround):
    - `classify_environment_failure()` (`kriya/workflow/workflow.py`): detects JDK's own generic fatal-VM-init messages (`"Error occurred during initialization of VM"` etc. - not app/skill-specific) and `PolymorphicValidator`'s own `"Failed to invoke/execute ...: [Errno 2] No such file or directory"` wrapper text (deliberately not a bare substring match anywhere in output, to avoid misclassifying a generated app's own legitimate, code-fixable `FileNotFoundError`).
    - Wired as an explicit circuit breaker into the Quality Gates retry loop (`budgets_exhausted` gains an early-exit path + explicit `break`, since this can fire on attempt 1, well before `retry_count` naturally exhausts) - stops burning retries on a failure no code regeneration could ever fix. New `environment_failure` field on the workflow result, surfaced distinctly by both `generate` and `fix` in the CLI (a dedicated `[ENVIRONMENT/TOOLCHAIN ISSUE]` block pointing at `kriya doctor`).
    - **Also fixed a related, previously-undiscovered bug while implementing**: `PolymorphicValidator.run_compile_check()`'s missing-`mvn`/missing-`gradle` handling silently logged the `FileNotFoundError` at debug level and fell through to a raw `javac`-only fallback - for any project with real Maven dependencies (exactly this Ignite/Qpid case), this produces a misleading "cannot find symbol" error indistinguishable from a real code/import bug. Now returns a clear, distinct failure instead.
    - New `check_java_toolchain()` (`kriya/tools/validate.py`), shared by both the circuit breaker and a new `kriya doctor` section: resolves whether `java`/`mvn` target different JDK major versions, warns (doesn't fail) if so, skips cleanly if neither tool is present.
    - **Verified live, not just via mocked tests**: with `JAVA_HOME` unset on this exact machine, `kriya doctor` correctly reported `java`->17 / `mvn`->26 with the mismatch warning, exit code still 0.
    - 12 new tests across `tests/test_workflow.py` (classifier unit tests including a deliberate false-positive-avoidance test for a generated app's own `FileNotFoundError`, plus a full workflow integration test proving the retry loop stops after exactly 1 attempt), `tests/test_polymorphic_validation.py` (missing-mvn no-javac-fallback, `check_java_toolchain` mismatch/match/neither-found), `tests/test_doctor_command.py` (mismatch warning doesn't fail exit code, clean skip when neither tool found) - all confirmed to fail pre-fix via `git stash`. 428/428 full suite, ruff clean. README/design.md/user_guide.md updated in the same pass. Committed `cb1d055`, pushed to `origin/main`.

- 2026-08-02 session (stepping back before M3, per user request): before continuing the golden-use-case milestones, user asked to think architecturally about Kriya's *ideal* end-to-end flow for a task like this - "at every stage right from the initial prompt how the flow should be... we will iron it out to see what all use cases fall out of it." Built a two-diagram Mermaid flowchart (overall pipeline + a zoomed Generate & Verify retry loop, since that's where nearly every real bug this session has lived), each node tagged done/partial/gap, published as an artifact meant to be iterated on live rather than a one-shot deliverable. Surfaced 5 concrete open questions from the mapping exercise (not yet fixes, just candidates): (1) toolchain preflight was manual-only; (2) skills can't express environment-conditional facts (the JDK 17-vs-24+ security-manager flag being the concrete example); (3) the dependency-regression tug-of-war was never actually fixed, just flagged 3 times; (4) the run command is re-inferred from scratch every retry instead of declared once as a structured contract; (5) failure reporting isn't uniform across knowledge-gap/skill-gap/approval-rejected/environment-failure.
  - **User picked #1 to iron out first.** Scoped by tracing the actual retry-loop code before proposing anything: the diagram's first-draft placement (preflight before repository analysis, i.e. before Plan) turned out not to be achievable - for a brand-new Java project, no `pom.xml` exists that early, so "is this Java" is unknowable until the Developer's first generation has actually run. Corrected placement: the check now runs once, the first time a `PolymorphicValidator` inside the retry loop confirms `stack == "java"` (right after attempt 1's Developer generation, before the compile check) - reliable for both a fresh Java project and one extending existing Java code, still before any retry is spent.
  - Implemented `_check_java_toolchain_mismatch()` (`kriya/workflow/workflow.py`), gated by a one-shot `toolchain_checked` flag shared across both `PolymorphicValidator` construction sites in the loop (main compile check + regression check, so the resumed-checkpoint path is covered too) so the real `java -version`/`mvn -version` subprocess check never repeats across retries. Surfaced two ways: `logger.warning` in real time, and a new `toolchain_warning` result field printed by both `generate` and `fix` **regardless of pass/fail** - a mismatch that didn't bite this particular goal could still bite on a different machine or a later JVM-flag-sensitive one.
  - 7 new tests (pure-helper unit tests, a success-path test proving the warning shows even when quality gates pass, a called-once-across-retries test, a non-Java-stack skip test), confirmed to fail pre-fix via `git stash`. 434/434 full suite, ruff clean. Committed `89df4f8`, pushed to `origin/main`.
  - **User picked #2 next: skills can't express environment-conditional facts.** Presented 3 real design options via `AskUserQuestion` rather than picking one: (a) surface the resolved JDK as a plain fact, let the model reason against a rule already written to be genuinely conditional in prose - no schema change; (b) structured per-rule `[jdk<24]`-style condition syntax in `rules.txt`, parsed and deterministically included/excluded - more general, code-enforced, real schema/parser work; (c) purely reactive - auto-flag a suspect rule for human review only after the environment-failure circuit breaker fires, no proactive mechanism. **User chose (a)** - matches this whole session's "don't build for scenarios that can't happen" discipline, since only one real instance has ever justified this.
  - New `_java_toolchain_fact()` (`kriya/workflow/workflow.py`) reuses `check_java_toolchain()`, injects `"Target JVM (resolved on this machine via 'mvn'/'java'): JDK X."` into `convention_prompt` once, early (before the skill-matching loop, so Planner/Architect/Developer all see it via the same prompt block skill rules already ride on) - unconditional but cheap (a Python-only goal on a Java-free machine pays nothing, `shutil.which` short-circuits before any subprocess spawns).
  - `skills/qpid/rules.txt`'s security-manager rule rewritten to state both halves explicitly (required 17.0.10-23.x, forbidden 24+) instead of only the half true when first verified. **Caught a second, less obvious consequence while fixing it, not from live testing**: the exec:java-vs-exec:exec guidance (a separate rule) was *entirely derived* from "the security-manager flag is always needed for broker-embedding" - so it was also quietly stale for JDK 24+, where that flag is now forbidden and (absent Ignite in the same class) typically no other flag is needed either, meaning plain exec:java is fine there too, not just exec:exec-with-zero-extra-flags. Fixed in the same pass rather than left for a third rediscovery later.
  - 9 new/updated tests (fact-function unit tests, Planner-prompt-injection verified via mocked LLM call args, 2 existing toolchain tests updated for the new call-count now that `check_java_toolchain()` is invoked twice per Java run - once for the fact, once for the retry-loop's own mismatch check - instead of once), confirmed to fail pre-fix via `git stash`. 439/439 full suite, ruff clean. `design.md` updated in the same commit. Committed `d967a47`, pushed to `origin/main`.
  - **User asked a sharper question, prioritized ahead of items #3-5**: "on a fresh install of kriya where there are no skills, if the user tries to do the messaging app... what will happen?" - a direct challenge that everything validated this session sits on top of skills hand-refined over 3+ days, which a brand-new user wouldn't have. Traced precisely via code reading + one direct empirical check (`extract_library_versions()` run against the real M1/M2 goal text), not assumed.
    - **Good news, confirmed**: "no skill exists" detection genuinely works - `extract_library_versions()` correctly identifies `org.apache.ignite:ignite-core` and `org.apache.qpid:qpid-broker-core` from natural-language goal text with ZERO version numbers present (verified live against the exact M1/M2 goal strings), and correctly cross-references skill tags to flag them as missing when no covering skill exists.
    - **The real gap**: live lookup only runs if `autonomy.web_lookup_enabled` AND `search.base_url` are both configured (a fresh install has neither by default); even when enabled, this project's own documented history shows it failed to extract anything usable from Qpid's docs 3/3 real attempts (Apache's doc site isn't readthedocs-structured, unlike the one case - `requests` - that ever worked autonomously). Under `-y` (this whole session's own usage mode, and any CI/automated usage), read `on_skill_gap`'s code directly: `if yes: ...return None` - the human-ask fallback is silently skipped, marked only by a dim console line. Generation then proceeds "best-effort," meaning the model must already independently know essentially every one of the ~9-15 real, often genuinely obscure gotchas (wrong `IgniteCache` import path, exec:java-vs-exec:exec + JVM flags, the `initialSystemPropertiesLocation` classloader quirk, wrong `modelVersion`, missing `virtualhostaliases`/`nodeAutoCreationPolicies`, wrong delivery mode, the JDK 24+ Security Manager removal) that took 3+ days of live iteration to discover *with* Kriya's help - several found only via bytecode inspection or direct reproduction, not things a base model plausibly already knows. **Nothing downstream connects a resulting failure (or a shaky success with silent correctness gaps) back to "this ran with an unresolved skill gap"** - not the retry loop, not the final report, not the trace log.
    - Updated the flowchart artifact (same URL) with the real branching (gap detection → live-lookup-if-configured → human-ask-unless--y → silent best-effort proceed) replacing the earlier, overly-optimistic single "skill_gap_callback" node, and the failure-report node upgraded from 🟡 partial to 🔴 gap to reflect that skill-gap status is invisible in reporting entirely. Added as open question #6, deliberately NOT scoped/implemented yet - three different fix angles exist (expectation-setting up front, reporting after the fact, or actually improving live-lookup's real capability for non-readthedocs docs, a much bigger effort) and the user hasn't picked one.
  - **User asked how other coding agents (Claude Code, Cline, Cursor, Copilot) handle this same class of gap.** Answered with an explicit hedge - direct code-reading confidence for Kriya, general public-architecture knowledge (not source access) for the others. Core finding: those tools mostly close unfamiliar-library gaps via open-by-default web access woven into the normal agentic loop (search/fetch as an ordinary tool call, the agent's own judgment on when to use it, no fixed retry budget, full real-time visibility to the user) rather than anything like Kriya's durable, verified skill ledger - which exists specifically *because* Kriya deliberately doesn't default to open internet access (the hard "proprietary content never leaves the machine via search" constraint from the original Live Lookup design). The actionable gap: those tools never have a *silent* failure mode here (the agent's reasoning IS the loop, visible live); Kriya's retry loop treated "nobody taught me this" the same as "typo," with a single dim console line if the gap went unresolved. Kriya's skills architecture is arguably more ambitious than what these tools do; the failure-mode UX around it was less transparent than theirs.
  - **User proposed a concrete direction: improve live-lookup capability, and add a confirmation before any search leaves the node.** Traced the actual code first rather than assuming: query CONTENT was already hard-restricted (bare technology-name strings only, enforced structurally in `_resolve_via_web_lookup()`, confirmed by reading it directly) for both trigger points. But a real, previously-unexamined gap surfaced: `on_web_lookup`'s confirmation fires AFTER the search already happened - it only gates whether fetched results get USED, not whether the query gets SENT. Under `-y` (this whole session's own usage mode), the search fires regardless and gets silently discarded, with zero human visibility into what left the machine, for zero benefit. Split the user's two-part ask into two differently-sized problems: (1) confirm-before-send - well-scoped, implemented this turn; (2) improve live-lookup capability - a separate, harder, still-open problem (already tried once via multiple-candidate retry + immediate-extraction, still landed at 0/3 for Qpid).
  - **Scoped via `AskUserQuestion` before implementing** (two real design forks, both resolved by the user): (a) interactive mode - two sequential prompts (search authorization, then separately whether to use results) vs. one merged prompt; user chose two, since they're genuinely different concerns (outbound authorization vs. inbound trust) - Recommended chosen. (b) should the retry loop's error-triggered lookup (originally designed with NO confirmation at all, deliberately, "since the retry loop is already unattended") also require the same gate; user chose yes, uniform everywhere - Recommended chosen.
  - **Implemented**: new `autonomy.web_lookup_auto_approve` config flag (default `false`). New `WorkflowEngine._approve_web_lookup()`: fails closed - a search only fires with either the persistent opt-in or a real-time callback shown the exact terms and target URL before sending. Wired into all 3 live-lookup call sites uniformly (goal-stage, design-stage, retry-loop). A declined/unauthorized search falls through to the existing human-ask fallback exactly as if lookup had found nothing.
  - **Found and fixed a real bug in my own first draft of the Stage 2B (design-derived) wiring, caught via careful tracing before it shipped**: initially wrapped the ENTIRE design-derived-gap block (including its own downstream human-ask fallback) behind the new approval check, meaning a declined/unauthorized search would have skipped asking a human entirely for design-derived gaps - inconsistent with Stage 1.2's careful "declined lookup still falls through to human-ask" behavior. Fixed by restructuring to match Stage 1.2's pattern before writing any tests.
  - **Found and explicitly did NOT fix, flagged instead**: while tracing Stage 2B precisely, discovered a real, PRE-EXISTING (not introduced by this change) asymmetry - its own human-ask fallback for design-derived gaps only fires when live lookup actually returns non-empty results; unlike Stage 1.2's more robust pattern, if search legitimately finds nothing at all, Stage 2B silently skips asking a human too, even in the original pre-existing code. Confirmed this isn't something my change made worse - out of scope for this pass, told to the user directly rather than silently expanded into.
  - 21 new tests, confirmed to fail pre-fix via `git stash`. 451/451 full suite, ruff clean. README/design.md/user_guide.md updated, including correcting a now-stale claim in design.md that the retry-loop lookup had no confirmation gate "by design." Committed `81eef4a`, not yet pushed.
  - Flowchart artifact updated in place: SKILLGAP subtree gained the pre-send confirmation node; open question #6 marked "partially addressed, not resolved" - the safety/authorization gap is closed, the capability gap (Qpid's 0/3 live-lookup track record) is explicitly still open and unscoped.

- 2026-08-02 session (completing open question #6 - improving live-lookup capability, the half deliberately left unscoped in the prior increment): user said "Yes lets complete #6."
  - **A real, running self-hosted SearXNG instance was already available in this environment** (`kriya-searxng` Docker container, port 8080) - confirmed responsive via a direct curl before designing anything, enabling genuine live testing.
  - **Root-caused the "landing-page problem" precisely**: `curl`'d SearXNG directly with Kriya's actual query shape (`"apache qpid documentation"`) and confirmed live it returns `qpid.apache.org/documentation.html` and `qpid.apache.org/` as the top 2 results - both landing/index pages, matching the project's own previously-documented finding exactly.
  - **User interjected mid-investigation to also check how Claude Code's own web-search tooling behaves** - pivoted to testing `WebSearch`/`WebFetch` directly, phrasing queries naturally, as an independent data point.
  - **Key empirical finding, validated two independent ways**: the exact minimal query change actually worth shipping (`"{term} example"` instead of `"{term} documentation"`, using the literal Maven-coordinate term shape `_resolve_via_web_lookup()` sends) surfaced a GitHub example config file and an official Ignite quick-start code sample correctly importing `IgniteCache` from its real top-level package - the exact fact `skills/ignite-java17/rules.txt` had to be hand-written for earlier this session after manually unzipping a JAR. Independently re-verified the Qpid how-to page extracts cleanly through Kriya's own `fetch_url_text()`, not just a promising search snippet.
  - **Hit a real operational constraint that shaped the fix's scope**: repeated SearXNG queries triggered actual rate-limiting/CAPTCHA suspension on the instance's own upstream engines. Implemented a single, minimal query-template change (not a multi-variant fallback chain) as a result.
  - **Implemented**: changed `_resolve_via_web_lookup()`'s query from `f"{term} documentation"` to `f"{term} example"` - a one-line fix, used by both trigger points uniformly.
  - 3 existing tests asserting the exact query string sent updated accordingly, confirmed to fail pre-fix via `git stash`. 451/451 full suite, ruff clean. README/design.md/user_guide.md updated with the concrete live-testing evidence. Committed `e2baec3`, not yet pushed.
  - Flowchart artifact updated in place: open question #6 fully marked resolved (both increments), LOOKUP1 node upgraded from 🟡 partial to 🟢 done, with an explicit caveat that this corrects a systematically bad query verified against two real examples, not a guarantee of universal live-lookup coverage.

**How to apply:** When resuming this work, check current backlog state against the code before assuming an item is still open — this list can go stale. Don't re-propose full upfront planning for each backlog item; the established working pattern is small, verified, one-item-at-a-time passes (see [[feedback-incremental-scoping]]).
