# Kriya Backlog and Lessons Learned

**Current, kept live and edited in place** — not a chronological log. When an item closes it moves out of the backlog; when a new one opens it's added here. For the full session-by-session narrative behind any of this, see the archived [`kriya_production_readiness.md`](kriya_production_readiness.md) (frozen as of tag `0.10`).

Score as of tag `0.10`: **8.5/10**. Recalibrated mid-session (2026-08-03) to **7-7.5/10** after repeated live cold-start M3 failures - the infrastructure fixes below are real and well-tested, but end-to-end reliability on a moderately complex, novel goal is not yet where it needs to be; don't let a closed backlog item count substitute for outcome evidence.

**Status as of 2026-08-03 end of day**: `accuracy-improvements` branch (18 commits ahead of `main`, all pushed, 529 tests passing, ruff clean) has six real, evidenced, regression-tested structural fixes from today (JAVA_HOME crash, stack-detection false positive, LSP visibility x2, runtime-verification file-scoping, process-group leak). Deliberately NOT merged into `main` yet - explicitly deferred to next session. Open question before merging (or after - it doesn't block, but should be tracked): whether the retry-loop improvements (LSP grounding, `likely_files` scoping) measurably improve M3-class success rate, not just that they engage correctly - not yet observed live, since every M3 run today either passed on the first attempt (mechanisms unexercised) or failed for a root cause the mechanisms got a chance to help with but the run still exhausted its retry budget before recovering.

**`kriya-protocol-parser-app` closed 2026-08-07** - the standalone Java 17/Spring-XML binary-protocol-parser validation exercise set up 2026-08-04, open across three sessions (first attempt never run; second hung mid-generation), finally passed clean (`run_id 04b3c204`, verified via `traces.db` and the actual workspace files, not just the log). Took 7 chained real bug fixes across this session and the two before it to get there (`_java_toolchain_fact()` leak, Ignite security-manager flag, missing build manifest, Java test-class-name bug, missing stack-trace source grounding, gutter-regex space-count mismatch, the `failed_content`/`attempted_edits` forensics gap that found the gutter bug). One data point, not a reliability claim on its own - see the score note above about not letting a single closed item substitute for outcome evidence; the natural next check is a fresh eval-harness batch to see whether the earlier fixes in this same session (sanitization, resource-lifecycle invariant, non-binary RV grading) hold up too, since no batch has cleanly isolated those yet.

## Current backlog

Confirmed still open, checked against actual code as of tag `0.10`:

- **CLOSED: `RunVerifierAgent.judge()`'s inferred `exec:java` never checked the pom's `mainClass` against reality (2026-08-11)**. Found live (staged-build validation): two distinct failure modes, same root gap - `pom.xml` missing `mainClass` entirely (`PluginParameterException`) and `pom.xml` naming a class that was never actually generated that attempt (`ClassNotFoundException: com.example.MainApp`) - both persisted identically across 8 straight retry attempts including model escalation, since `pom.xml` was correctly implicated every time but the real problem (the `mainClass` string itself, not the file's general shape) was never checked against the actual generated source tree. Fixed: new `_resolve_maven_main_class()` (`kriya/workflow/file_resolution.py`) scans `src/main/java` for the one class with a real `main` method and returns its fully-qualified name; wired into `_resolve_run_command()`'s existing deterministic-correction chain as `-Dexec.mainClass=<resolved>` (exec-maven-plugin's own documented override, never edits `pom.xml`), applied only when exactly one real entry point is found - deliberately doesn't guess when 0 or 2+ candidates exist. 9 new tests. See `docs/design.md` §2.7 for the full writeup.
- **CLOSED: Developer always regenerated files from scratch even when Planner's own plan already contained complete, correct code (2026-08-11)**. Planner's prompt never asks for full code, but the Planner model routinely over-delivers it in fenced code blocks anyway for small-enough goals - Developer previously ignored this entirely and paid a full completion per file regenerating the same content. Fixed: new `extract_planner_code_blocks()` (`kriya/workflow/file_resolution.py`) matches each fenced block in the plan text to its expected filepath via closest-preceding-mention (`rfind()`-based, not first-match - a real bug found and fixed mid-implementation, see `docs/design.md` §2.6 for the mis-attribution case). Wired into `run_attempt()` (`kriya/workflow/attempt.py`): on attempt 1 only, and only when the plan covers *every* expected file (all-or-nothing), those blocks are used directly and `DeveloperAgent.run_generation()` is skipped for that attempt - every downstream gate (compile/test/Runtime Verification) treats the result identically regardless of source, so a bad reuse is caught exactly like a bad Developer generation would be. 19 new tests. See `docs/design.md` §2.6 for the full writeup.
- **CLOSED: `_sync_uncommitted_changes_into_worktree()` silently dropped an entire untracked directory from the worktree sandbox (2026-08-11)**. Found live (staged-build workspace): a goal asking to "extend the existing... preserve the existing classes" failed every retry with `cannot find symbol: class Protocol` - `Protocol.java`/`ProtocolParser.java` were genuinely never copied into the sandbox, not a model mistake. Root cause: `git status --porcelain`'s default mode collapses a directory with zero committed content anywhere under it into one summary line (`?? src/`) instead of one line per file, and the sync function's per-line copy loop treated that single directory line as a no-op (`if os.path.isdir(src): continue`) rather than recursing into it. Fixed: added `--untracked-files=all` to the `git status` call so every file is always enumerated individually - existing per-file copy logic needed no other change. 1 new regression test, confirmed failing pre-fix. See `docs/design.md` §2.10 for the full writeup.
- **CLOSED: deliberate audit pass over the worktree-sync and anchored-edit/content-sanitization layers found and fixed 4 more real bugs, all Kriya's own machinery (2026-08-11)**. Not found live this time - reviewed `edit_safety.py`/`agent.py`'s parsing functions line by line after a direct question about what open ends remain once local-model unpredictability is treated as an accepted cost, not a Kriya defect. (1) `_TRAILING_FILE_CONTENT_RE` matched "file content" + a colon ANYWHERE in a line, truncating real code that happened to mention the phrase (e.g. a log statement) - fixed by requiring the colon to be the last thing on its line. (2) The gutter-stripping regex's "  N:" branch matched any 2-plus-space "digit:" line, colliding with legitimate YAML/properties syntax - narrowed to the real, exact 3-space format `_build_error_source_context()` emits (several existing tests had hand-typed the wrong 2-space count as an unverified guess, corrected alongside this). The ">>" branch's digit requirement was tried as mandatory too, but reverted - a real 2026-08-04 incident depends on catching a bare `>>` with the line number dropped, structurally indistinguishable from a bit-shift code line by pattern alone. (3) `apply_anchored_edits()`'s uniqueness check (whole-file, blank-lines-collapsed) could disagree with its own application logic (fixed-size raw-line window) whenever a search block's blank-line count differed from the content's - fixed by making both use the same blank-line-tolerant subsequence match. (4) `Failure.to_gate_outcome()` dropped the grader's reasoning from every persisted FAILED `run_verification` trace (present on every passed one) - fixed by enriching the persisted output the same way the success path already does. 12 new/corrected tests. See `docs/design.md` §2.10 for the full writeup.
- **CLOSED: "Runtime Verification PASSED" did not mean the delivered project would actually run afterward without Kriya's help (2026-08-11)**. Found live, immediately after the audit above: the user manually ran `mvn exec:exec` in a workspace Kriya had just reported success on - crashed instantly (`-Djava.security.manager=allow`, fatal on JDK 24+), then, once that flag was removed, crashed again on a deeper Qpid Broker-J 9.2.1 incompatibility (`Subject.getSubject()`, unsupported without an active Security Manager). Root cause: on a machine where `java`/`mvn` resolve to different JDK majors, Kriya forces `JAVA_HOME` onto the goal-stated target JDK for its OWN verification subprocess calls only - `_strip_jdk_incompatible_jvm_flags()` correctly decided the flag was safe under that override, but the override itself was never written anywhere in the delivered `pom.xml`, which still had a bare `<executable>java</executable>` resolved via PATH at whatever JDK is default for whoever runs it later. Fixed: new `_pin_exec_plugin_executable_to_resolved_jdk()` (`kriya/workflow/toolchain.py`) pins `exec-maven-plugin`'s `<executable>` to the absolute path of the JDK Kriya's own verification actually used, whenever `java_home_override` is active - the general, durable counterpart to the flag-strip fix (which only closed the first symptom, not the underlying drift). 6 new tests. See `docs/design.md` §2.7/§2.10 for the full writeup.
- **CLOSED (partial - top 5 of 27): deliberate SME-style architecture review found 27 more real bugs across RAG/memory, tools/sandboxing, skills, and CLI/plugin subsystems - 5 highest-severity fixed (2026-08-12)**. Requested directly ("take the hat of a subject matter expert... find any architectural add-ons or hidden bugs"), run as 4 parallel deep-review passes. (1) `is_local_url()` (`kriya/core/llm.py`) failed OPEN on a hostname-less URL, contradicting its own documented fail-closed egress boundary - fixed. (2) `fetch_url_text()` (`kriya/tools/web.py`) had zero SSRF protection - fixed with a scheme/public-IP guard, manual per-hop redirect re-validation, and a streamed byte cap. (3) `DependencyGraph.clear_file()` (`kriya/analyzer/graph.py`) could delete a DIFFERENT file's graph relations on a symbol-name collision (bare, unqualified names, no file scoping) - fixed with a new `source_file` column. (4) A failed embedding call got cached as a permanent "success" (`kriya/analyzer/analyzer.py`) - a transient embedding-server hiccup permanently corrupted a file's RAG entries with no way to recover short of `--force` - fixed by not updating cache metadata on a zero-vector chunk, so the next run retries automatically. (5) `SkillEngine`'s skill-directory precedence (`kriya/skills/skill.py`) was backwards - the implicit CWD-guess directory silently beat an explicitly configured `paths.skills` - fixed by reordering. 12 new tests. The remaining 22 findings (ranked, most-severe-first, reported via the code-review findings mechanism) are still open - see `docs/design.md` §2.10 for the full writeup and the 22-item list.
- **CLOSED: skill staleness nudge - first of 3 "fits in easily" architectural add-ons from the same review (2026-08-12)**. `Skill.verified_context` already recorded what version a skill was verified against, and its own field docstring already named the exact reason ("a pinned version gets yanked, a new major version changes the config shape") - nothing ever read it back to check against a later goal's mentioned version. New `_skill_staleness_warning()` (`kriya/workflow/skill_extraction.py`) compares the two, reusing the same `(lib, ver)` extraction already done for `supported_versions` range-checking. Purely observational - never blocks generation or un-verifies the skill, never touches the model's prompt - surfaced as a new `skill_staleness_warnings` result field and a `[SKILL STALENESS]` CLI line on both `generate`/`fix`. 6 new tests. See `docs/design.md` §2.10 for the full writeup.
- **CLOSED: `kriya completion` command - second of 3 "fits in easily" architectural add-ons from the same review (2026-08-12)**. Click already generates a working shell-completion script for free via a magic `_KRIYA_COMPLETE=<shell>_source` env var - confirmed live for bash/zsh/fish with zero `kriya`-side code needed. Nothing surfaced that this already worked, so `kriya completion <bash|zsh|fish>` prints the enable-now one-liner plus the exact rc-file line to make it permanent. Pure discoverability wrapper. 3 new tests. See `docs/design.md` §2.10 for the full writeup.
- **CLOSED: `kriya generate --json` - third of 3 "fits in easily" architectural add-ons from the same review (2026-08-12)**. `generate` only had rich narrative output, unusable for CI/scripting. New `--json` flag swaps `sys.stdout` to `sys.stderr` for the whole command body (Click resolves `sys.stdout` dynamically, so this one swap covers every existing `click.echo`/`secho` call site for free), then restores stdout to print only the final result dict as JSON, exit code reflecting `quality_gates_passed`. Known v1 gap: two early-`sys.exit()` paths (strict knowledge-gap block, declined-scaffold block) don't get JSON output, matching their pre-existing non-structured signal. 3 new tests. See `docs/design.md` §2.10 for the full writeup.
- **CLOSED: re-ranking retrieval - first of two deferred architectural add-ons the user picked next (2026-08-12)**. Graph RAG's context assembly was unranked: BFS neighbor hits all weighted equally regardless of relation type/hop distance, seeds were a filename-stem guess despite the graph already having each file's real symbols, and tier degradation (full→skeleton→signatures) was purely categorical with no per-file relevance signal. Shipped, additive/backward-compatible, zero new dependency or LLM call: `DependencyGraph.get_symbols_for_file()` replaces the seed guess; `get_neighborhood()` now scores/sorts hits by relation-type weight over hop distance; `build_code_context()` gained an optional `file_scores` param that degrades the lowest-scored file first regardless of matched/related category, falling back to the exact prior categorical behavior when omitted. Explicitly deferred: adaptive re-retrieval using retry-attempt error text (retrieval runs once, pre-retry-loop, documented as loop-invariant - a bigger structural change with no evidence yet it's worth the added latency). 9 new tests, including one full `run_generation_workflow()` test confirming the real wiring, not just the pure units. See `docs/design.md` §2.10 for the full writeup.
- **CLOSED: AST-aware editing - second of two, scoped down after research + one piece tried and reverted (2026-08-12)**. The literal ask (real parse→locate→splice replacing `apply_anchored_edits()`) was rejected up front: all 7 documented corruption incidents are Java text-extraction bugs a parser would never have caught, and it only helps Python without a new dependency Kriya has repeatedly avoided. Shipped instead: `_find_duplicate_top_level_type()` (`kriya/workflow/edit_safety.py`) closes the one concretely-named still-open gap - a complete, brace-balanced duplicate top-level Java class/interface/enum, which the existing brace-balance check structurally cannot catch. A `.py` `ast.parse()` branch was also shipped, then **reverted the same session**: since Python's real compile gate is already just `compile()` (100% overlap with `ast.parse()`, unlike Java where the brace check only intercepts one narrow shape), adding it would silently downgrade every Python syntax error's targeted retry from content-shown (compile gate reads the already-written file) to error-text-only (this check runs pre-write, so the file never reaches `all_files_written`) - for zero cost savings, since `compile()` is already as free as `ast.parse()`. Caught by two tests actually regressing once the branch was added, not a hypothetical. 4 new tests. See `docs/design.md` §2.10 for the full writeup.
- **CLOSED: findings #20-27 (tail of the 27-finding SME review) fixed as a batch (2026-08-12)**. User pasted the review's full text and said "Lets pick up from 20." 8 correctness bugs, verified against actual code before fixing: `JdtlsClient.shutdown()` missing a SIGKILL fallback (same leaked-orphan-subprocess class already fixed for `validate.py`); `_request()`'s pending-response future never cleaned up on timeout; a malformed Content-Length header crashing the LSP read loop silently at DEBUG (now WARNING); `EventSystem.emit()` re-raising mid-loop and skipping later handlers (now runs every handler, still raises the first exception after, per an existing test's documented intent); `_augment_error_with_live_lookup()` only ever reading the first lookup candidate instead of trying each until one is usable; neither `analyzer.py`'s Java chunker nor `context_budget.py`'s `skeletonize_braced_code()` being comment/string-literal-aware when brace-counting (both now use a comment/string-stripped mirror for brace detection only); `estimate_tokens()`'s word-split heuristic undercounting punctuation-heavy code identifiers (replaced with a char-count heuristic, ~4 chars/token); the post-SIGKILL `process.communicate()` reap call having no timeout of its own (could hang forever on a D-state child). 12 new/updated tests, all confirmed failing pre-fix. Full suite: 784 passed, 8 pre-existing failures (confirmed identical via stash, unrelated to this batch). See `docs/design.md` §2.10 for the full writeup.
- **CLOSED: 8 more findings fixed, continuing down the ranked list (2026-08-12, same day)**. User: "more of the remaining 13." `PluginManager.discover_and_load()` re-raised on a broken plugin folder with nothing wrapping it at any real call site (unlike `initialize()`), risking silently losing Kriya's own built-in tools plugin - now fault-isolated, plus a stale docstring claiming real callers for the fail-fast `initialize_all()` (there are none, only its own test) corrected rather than left to mislead the next reader. Rule/conflict text written to rules.txt/staged_rules.txt with no newline-stripping, but every reader parses strictly line-by-line - new shared `_sanitize_for_flat_file_line()` applied at all 3 real write sites (found via grep, not just the one named). `_likely_misattributed_sibling()` could leave a target skill's identity word set empty (e.g. a bare "java" skill), making the guard maximally aggressive instead of abstaining - now abstains on an empty target identity. `is_version_supported()`'s range parser dropped a comma-separated upper bound with no space (`">=2.15.0,<3.0.0"`) entirely. `parse_version_parts()` could silently zero out a whole version component when a qualifier ended in a digit (`"1.0.5-rc1"` → wrongly `(1,0,0)` instead of `(1,0,5)`). `LocalVectorStore.verify_model()` checked only one arbitrary row - now checks every distinct model/dimension combination, warning (not silently passing) on a partial mismatch. A real doc/code mismatch in `CLAUDE.md` itself (still claimed `sqlite-vec`-backed search, contradicting both the code and `docs/design.md`'s own already-correct description) corrected. 9 new tests, all confirmed failing pre-fix. Full suite: 793 passed, 8 pre-existing failures (same baseline, reconfirmed). See `docs/design.md` §2.10 for the full writeup.
- **CLOSED: the last 4 findings from the ranked list, all in `analyzer.py` (2026-08-12, same day)**. User: "continue with the analyzer.py." `target_extensions` hardcoded to 4 languages despite `EXTENSION_MAP` supporting many more - fixed to the union of both (JS/TS/Go/Rust/C#/etc. now get real Graph RAG coverage instead of zero). A `with store.conn, graph.conn:` block looked like a transaction but wasn't one (both connections' write methods auto-commit internally) - removed the misleading wrapper and documented the real per-file failure-isolation behavior instead of overclaiming atomicity. Incremental `--changed` indexing never ran `git diff --cached`, silently missing a staged-but-unmodified-since-staging file - added. The same Java method-signature regex was duplicated 3x across `analyzer.py`/`graph.py`/`context_budget.py` - extracted into one shared `JAVA_METHOD_SIGNATURE_CORE` constant. 6 new tests, all confirmed failing pre-fix. Full suite: 796 passed, 8 pre-existing failures (same baseline). See `docs/design.md` §2.10 for the full writeup.
- **CLOSED: the final 2 findings, closing all 27 (2026-08-12, same day)**. User: "pick up the last 2 findings and fix them too." `LLMClient.complete()`'s reasoning-model retry triggered on ANY exception (connection errors, auth failures, rate limits), not just response_format incompatibility - fixed with an exclusion list (`_LLM_RETRY_EXCLUDED_EXCEPTIONS`) of clearly-unrelated `openai` exception types, deliberately not an allowlist, so an unrecognized exception still retries. `get_neighborhood()`'s BFS had no per-result cap - a common method name shared across many unrelated classes could pull in every unrelated definition as "related" context, unbounded - fixed with a `max_results: int = 30` param, capping the final score-sorted output (not the internal traversal) so the highest-scoring hits survive. 5 new tests, all confirmed failing pre-fix. Full suite: 800 passed, 8 pre-existing failures (same baseline, reconfirmed across every batch this session). **Closes all 27 of the original 2026-08-12 SME-review bug findings.** See `docs/design.md` §2.10 for the full writeup.
- **CLOSED: `kriya/workflow/workflow.py` mechanically modularized (2026-08-11)**. Had grown to ~4700 lines, making both navigation and full-file context loading during development increasingly costly - flagged directly by the user before starting the verification-contract work below ("the workflow now is unmanagable... before we add something more to it, we need to refactor it properly into self contained modules"). Extracted ~55 pure helper functions into 9 focused modules under `kriya/workflow/` (`worktree.py`, `context_budget.py`, `edit_safety.py`, `file_resolution.py`, `skill_extraction.py`, `live_lookup.py`, `failure_grounding.py`, `toolchain.py`, `lsp_integration.py`, `retry_prompts.py`), each re-exported back into `workflow.py`'s own namespace so every existing import and `unittest.mock.patch("kriya.workflow.workflow.X", ...)` call site kept working unchanged - a pure move, not a rewrite. `workflow.py` itself dropped to ~2600 lines; `run_generation_workflow()`'s own internal orchestration logic is untouched and deliberately deferred as separate, higher-risk follow-up work (Opportunity 2 - restructuring the stage-by-stage control flow itself, not just where the helpers live). Verified behavior-preserving with zero downstream code changes needed anywhere (`tests/test_workflow.py`, `tests/test_agents.py`, `kriya/cli.py`, `spikes/fix_alignment/run_alignment_test.py` all import cleanly against the new layout). See `docs/design.md` §2.10 for the full module list.
- **CLOSED: Opportunity 2 (`run_generation_workflow()`'s stateful orchestration), all 3 slices shipped (2026-08-11)** - user's explicit follow-up ask after Opportunity 1 shipped: "a system which is modularized, easily maintainable with targeted fixes, and unit testcases... a specific behavior like a service." Planned via a formal plan-mode session (`GenerationState` first, then an isolable attempt-executor, then a pure retry-decision function, full pipeline-as-stages deliberately last/deferred - see `docs/design.md` §2.10 for the sliced rationale). `workflow.py` itself: ~4700 lines before Opportunity 1, ~1570 lines after all of Opportunity 2. The originally-scoped Slice 4 (pre-loop pipeline as explicit stage objects) remains deliberately deferred, unscoped - the highest-risk, lowest-urgency piece per the user's own prioritization (cost/testability first).

  Slice 1: new `kriya/workflow/state.py` (`GenerationState`/`RetryBudgets`) replaces ~25 closure variables threaded through the retry loop with one explicit object - a mechanical, scripted rewrite (245 substitution sites), not a rewrite of any logic. Two real bugs surfaced by actually running the test suite (not caught by `ast.parse` or a naive grep-diff): a reference-before-construction ordering bug (`state.error_context` used before `state` existed - fixed by constructing `state` at the very top of the method) and an accidental deletion of unrelated statements that happened to sit inside the span being consolidated (`required_files_prompt_block`/`ecosystem_invariant_block`/etc. - restored verbatim). Verified against the FULL `tests/test_workflow.py` suite (251 tests) via a standalone runner: 243/251 pass, the other 8 confirmed pre-existing/unrelated - zero regressions.

  Slice 2: new `kriya/workflow/attempt.py` (`AttemptContext` + `run_attempt()`) extracts the retry loop's happy-path body (Developer invocation through the Runtime Verification gate, ~780 lines) out of `workflow.py` entirely - the loop body shrinks to build a context object once, then `await run_attempt(state, ctx)`. Two NEW bug classes surfaced this time, both invisible to a grep-diff since they're not about missed rename sites: (1) a local import (`PolymorphicValidator`) that used to stay in scope for the rest of the enclosing function (Python's function-level, not block-level, scoping) broke once the importing code moved to a different function - fixed with a normal top-level import in `workflow.py`; (2) three retry-mode booleans and three model-override values, computed only inside the extracted range but consumed by code left behind (retry-budget accounting, lesson-extraction model selection) - recomputing them from `state` afterward would be actively WRONG, not just redundant, since `fallback_targeted_attempted` gets mutated inside the very branch that needs to be identified - fixed by adding `last_attempt_mode`/`last_model_override`/`last_base_url_override`/`last_api_key_override` to `GenerationState`, set once inside `run_attempt()`. Two existing tests also needed their `unittest.mock.patch` targets repointed from `kriya.workflow.workflow.X` to `kriya.workflow.attempt.X`, since the real call moved - one failed loudly when this wasn't done, the other passed anyway by silently falling through to the real (unmocked) function on a machine with a real JDK installed, which is arguably worse (a test that looks like it's checking something it no longer is). 2 new tests added that exercise `run_attempt()` in true isolation - no `WorkflowEngine`, no Planner/Architect/RAG mocks, no worktree - the actual payoff this slice exists for. Verified against the full 253-test suite: 245 pass, same pre-existing/unrelated failures as Slice 1's baseline, zero regressions.

  Slice 3: new `kriya/workflow/retry_strategy.py` (`handle_attempt_failure()`) extracts the `except Exception as e:` block's failure-handling logic (~220 lines: Failure classification, error-triggered live lookup, LSP grounding, retry-budget accounting, the terminal-vs-continue decision). Deliberately NOT split into a smaller "pure decision" function plus inline leftovers, despite that being this slice's original description - reading the real code showed it doesn't decompose that cleanly (the live-lookup gate, LSP client lifecycle, and budget accounting share real data dependencies threaded through one continuous sequence); splitting it would have meant re-deriving already-computed values from the caller's side, the same bug class Slice 2 already ran into. Moved as one cohesive unit instead, reusing the SAME `AttemptContext` object `run_attempt()` already gets (extended with 3 more fields: `max_retries`, `web_lookup_query_callback`, and a bound-method callable for `WorkflowEngine._approve_web_lookup`) rather than inventing a second, overlapping context class. Verified against the full 255-test suite: 247 pass, same pre-existing/unrelated failures as every prior slice's baseline, zero regressions. 2 new tests exercise `handle_attempt_failure()` in isolation with a real `QualityGateFailure` - no `WorkflowEngine`, no retry loop, no worktree.

  **All 3 slices done.** `workflow.py`: ~4700 -> ~1570 lines. Slice 4 (pre-loop pipeline as explicit stages) remains unscoped and deferred - not started.
- **Not yet scoped: Runtime Verification against a genuinely long-running, backgrounded server (2026-08-11 discussion)**. Today's Runtime Verification gate only handles a self-contained, self-terminating smoke test - server and client (or the goal's own equivalent) run in one process/command sequence and the process exits on its own within `run_verification_timeout_seconds`. A goal shaped like "start a server, then verify it responds" where the server is meant to keep running independently (a true external client hitting a backgrounded process) isn't supported - discussed directly with the user, who pushed back on treating this as a permanent limitation rather than an accepted one ("why does Kriya have to live with this?"). Assessed as genuinely solvable, not fundamental: would need real process-lifecycle engineering (`Popen` the server detached, poll for readiness - a port opening or a log line - run the client/verification step separately, then guarantee cleanup via a process-group kill even on failure/timeout) rather than the current single-blocking-command-sequence model `PolymorphicValidator.run_app_sequence()` uses. Deliberately NOT bundled with the verification-contract work (§2.7) - the risk/complexity profile is different (new subprocess lifecycle surface vs. a purely additive prompt convention + regex scan) and conflating the two would have made the lower-risk, higher-confidence fix wait on the higher-risk one. Explicitly sequenced as its own future epic, after Opportunity 2 (`run_generation_workflow()` orchestration refactor) is scoped/started, not before.

- **Not yet scoped: a document-ingestion knowledge-gap-filling system (PDF/TXT/MD/logs -> skill knowledge, less hand-holding than writing rules.txt from scratch).** Explored 2026-08-08 by reviewing a sibling project (`eie`/`eie-validation`, a separate, independently-developed "deterministic engineering pack compiler" that ingests real technology docs via a local LLM and produces structured knowledge - dependencies, APIs, rules, examples, etc.). Direct, evidence-based findings from reading its actual generated output for Apache Ignite 2.18.0 (the same technology Kriya's own skill covers) against real PDFs: dependency-coordinate extraction is genuinely reliable (`ignite-core`/`ignite-indexing`/`ignite-spring`, all correct) and it correctly flagged the real `--add-opens` JVM-flags landmine Kriya's own skill also has a dedicated section for - but API-surface extraction was completely empty (`apis: []`) in the most recent generated pack, and the same fact (e.g. "requires Java 11 or 17") was independently duplicated near-verbatim across 3-4 different category files rather than canonicalized once, despite that project's own ADRs explicitly committing to deterministic canonicalization. Conclusion: a "feed it docs, it maintains skills, no hand-holding" system is NOT yet achievable at Kriya's current hand-written-rule quality bar for the hardest category (API usage) - rated ~4/10 for a literal zero-human-review version, ~7/10 if scoped down to "AI drafts candidate rules, human does a cheap approve/reject" reusing the EXISTING `staged_rules.txt` -> `kriya skills approve` mechanism rather than requiring full automation. Dependency-coordinate extraction specifically looks close to solvable today; API-signature extraction needs either much richer source docs (real API references, not getting-started guides) or a human/live-failure-driven step, matching how Kriya's own skills got their density. Not scoped into a concrete plan yet - explicitly deferred, to be discussed again.
- **`extract_target_test()` substring-vs-basename matching** - does substring matching against the full filepath, not basename, for "is this a test file" detection. Minor, low blast-radius, still present.
- **Stage 2B's human-ask fallback asymmetry** - only falls back to asking a human when live-lookup returns *some* result but nothing usable; if it returns literally nothing, the fallback doesn't fire (unlike Stage 1.2's more robust pattern). Pre-existing, narrow, not yet fixed.
- **"Enabling reasoning" / retry-prompt de-noising** - HIGH PRIORITY, narrowed further. Several cheap instances closed (redundant-dependency checklist; `retry_temperature` opt-in + javac `-Xlint:rawtypes,unchecked` diagnostics; LSP grounding for import/symbol mistakes). Reasoning-model routing specifically is de-prioritized - a direct A/B test showed zero correctness benefit for a fact-recall-class error (13x slower for the same answer), and cited research (LLMs have "a very strong bias to generate code based on their priors," struggle to override it via in-context info "especially for smaller, openly-accessible models") suggests prompting alone - reasoning or not - isn't the right lever for that class of mistake; deterministic grounding (LSP) is.
- **Not yet scoped: revisit reasoning-model routing for Planner/Architect specifically, not Developer - new locally-available models, real plumbing gap found first (2026-08-15)**. The item above still stands as-is for the Developer retry loop (fact-recall-class error, compounding retry cost, zero benefit found) - not being revisited. But that result was never tested for Planner/Architect-shaped work (multi-step decomposition/design), a structurally different problem class where reasoning is more plausible to help, and it doesn't compound the way a retry loop does (paid once, not N times). Found live while benchmarking locally pulled models (`spikes/model_speed_poc/`, `curl` traces against both Ollama's native `/api/chat` and its OpenAI-compatible `/v1/chat/completions`): `qwen3.6:27b` (Kriya's own configured default model) is a genuine thinking model that signals reasoning via a real structured field (`message.thinking` / `delta.reasoning`) - a different convention than the inline `<think>...</think>` tags `LLMClient.complete()` (`kriya/core/llm.py`) actually has code for. Confirmed: that method currently has **zero code path reading the real field at all**, in either the streaming or non-streaming request path - its existing regex-strip is a no-op for this model family since `content` never contains inline tags to strip. Not yet causing visible breakage in practice (the `llm.max_tokens` bump 4096→16384 made earlier this session, for an unrelated reason, happens to give real headroom), but it's a latent gap: a tighter `max_tokens` or an unusually long reasoning chain against this model can silently return empty/truncated content today with nothing in the code to detect or explain why - reproduced live twice (a `prompt generate` run and the bench spike). Two things needed before a fair re-test is even possible: (1) actually read the model's real reasoning delta/field instead of ignoring it, (2) mark `qwen3.6:27b` (and any other locally-thinking-capable model) as a reasoning model in config so the existing `max(base_max_tokens, 12288)` floor applies to it. Once that plumbing exists, the real test: a moderately complex Planner/Architect decomposition goal, reasoning-on vs reasoning-off, judged on plan/design quality - not just speed - the task class the original negative result never covered. **Explicitly scoped only, not implemented** - direct instruction (2026-08-15) to hold off on any code change until this is evaluated.
  - **UPDATE (2026-08-15, same day): real reasoning-on vs reasoning-off A/B run for the Developer-shaped prompts, and it reinforces the original decision rather than reopening it.** `spikes/model_speed_poc/bench.py` extended to send explicit `think:true`/`think:false` per run (the first pass never toggled it - Ollama defaults thinking on for a thinking-capable model, so that run only ever measured "thinking on, budget-starved") plus a real +4096-token reasoning headroom. On the one prompt with a real functional-correctness check (`short_fn`, both modes actually completed): `qwen3.6:27b` no_think 0.94s/PASS vs think **80.5s**/PASS; `qwen3.6:35b-a3b-q4_K_M` no_think 0.47s/PASS vs think **33.6s**/PASS - same correctness outcome, near-identical output length, 71-85x slower with reasoning on. Matches (more dramatically than) the original 13x-slower/zero-benefit finding. On the two harder prompts (`medium_refactor`, `longer_task`), the think arm didn't even finish for either model - reasoning alone consumed 2,618-4,717+ tokens and hit the budget wall before producing a single content token, even with the generous headroom. One inconclusive loose thread: `qwen3.6:35b-a3b-q4_K_M`'s no_think mode produced genuinely invalid syntax on `medium_refactor` - a real miss - but its think-mode counterpart starved before reaching any content, so there's no evidence reasoning would have caught it, just an open question. **Conclusion: this data closes the Developer-shaped-task question more firmly (newer models, same result, worse ratio) - it does NOT touch the actual open question (Planner/Architect decomposition quality), which remains untested.** Real constraint learned for whenever that test happens: 4096 tokens of reasoning headroom is not reliably enough for this model family even on "write a class"-level complexity - a Planner/Architect test needs to budget for reasoning chains that can run 4,000-5,000+ tokens without finishing.
  - **UPDATE (2026-08-15, same day): the actual Planner/Architect A/B ran (`spikes/model_speed_poc/planner_reasoning_poc.py`, real `ignite_qpid_protocol` goal, real skill-rule conventions ~34KB/~8500 tokens, isolated variable - Architect fixed to think:false in both arms so only Planner's reasoning setting varied) - result: keep reasoning OFF for Planner too, closing this question rather than reopening it.** `no_think` Planner: 3393 content tokens, correctly applied this session's own hard-won time-unit rule (`System.currentTimeMillis() / 1000`) with zero reasoning. `think` Planner: burned 10,015 thinking tokens (real, server-reported chunk counts - not a wall-clock artifact) before content ran out of budget and got cut off mid-`ProtocolApp.java`, *before ever reaching* the sample-data construction where that same time-unit bug lives - so reasoning didn't fail the check, it never got there. The one real positive signal - `extract_planner_code_blocks()` found 5 complete reusable files in the `think` plan vs 0 in `no_think` - doesn't cash out to anything today: the existing all-or-nothing reuse gate needs every expected file covered, and 2 of 7 were missing/incomplete (one of them *because of* the truncation above). Net: a fixed token budget forces reasoning and content to compete, and the loser is whatever content comes last - which in a real multi-layer goal is often the highest-risk orchestration code, not filler. Third consecutive real test (two Developer-loop, now one properly-isolated Planner test) where reasoning hasn't earned its cost in this pipeline. Separately confirmed via two direct `curl` A/B tests (bypassing Python/Ollama entirely) that ~295s of the originally-reported ~730s "no_think" wall-clock was actually a bug in the benchmark script's own streaming loop (`http.client` line-iteration overhead on a many-thousand-chunk response, not Ollama/model/Kriya) - fixed in `bench.py` (buffered reader + list-append/join instead of string `+=`). Only wall-clock (`total_time_s`) was affected; `generation_tps` (server-reported) and every token-count/content finding above were never touched by that bug and stand as measured.
- **Not yet scoped: incremental/milestone-based goal decomposition - does Kriya split a big goal into sequential sub-goals today, and would it help? (2026-08-15)**. Confirmed via code search (`grep -rn "milestone" kriya/`, 2 comment-only hits, no mechanism) that Kriya does NOT do this - `ignite_qpid_protocol`'s "LAYER 1/2/3" structure is a prompt-level hint only; Planner/Architect/Developer still see and must handle all three layers in one combined call. Raised directly by the user after the Planner reasoning A/B: real production tools do this incrementally, and the exact failure mode just measured (a fixed budget forcing reasoning and content to compete, cutting off the highest-risk section) is a direct symptom of sending the whole goal at once - splitting into smaller per-layer calls would shrink that risk structurally, not just via more budget. Sketch if built: a decomposition step orders the goal into milestones; each runs the existing Architect->Developer->Quality-Gates pipeline scoped to just that layer, extending the REAL already-generated files from the prior milestone (not a hypothetical); a final full-scope verification pass runs after the last milestone. Real, evidence-backed connection to reasoning: since per-call scope would be much smaller, reasoning's budget-competition problem (the reason it lost the Planner A/B above) would be far less severe - worth reconsidering reasoning-for-Planner IF this ships, not before.
  - **Follow-up question the user raised, answered with real evidence, not guessed: does Kriya have anything for the "Phase 2 breaks Phase 1" integration-bug class this implies?** Partially, and better than expected on the detection side, with one already-known gap on the self-healing side. Confirmed by reading the actual code (`kriya/workflow/workflow.py:1487-1532`): every successful `kriya generate` attempt already runs an UNCONDITIONAL, unfiltered full regression test suite (`PolymorphicValidator.run_tests()`, no target filter) before declaring success - real protection against new code silently breaking old code, already shipped, today. Two real caveats: (1) this has only ever run within ONE monolithic call's own attempt lifecycle, never yet across separate phase-boundary calls, since milestone decomposition doesn't exist; (2) this net is only as strong as actual generated test coverage - a goal relying on Kriya's `[VERIFICATION] PASS`-style runtime-check convention (like `ignite_qpid_protocol` itself) rather than real JUnit/pytest suites could leave `run_tests()` with little or nothing to actually re-run, meaning weaker protection than it looks for exactly this goal shape. And the specifically relevant existing gap: `attribution.py`'s own taxonomy (researched via real SWE-bench data + Aider/Cline/Copilot GitHub issues, see the "PENDING file-attribution-consolidation" work above) already documents Row 2 - multi-file coordination - as "architecturally supported, untested": targeted retries CAN touch files beyond the ones a locator names, but nothing has ever verified Kriya reliably produces a correct coordinated fix spanning a file the current attempt didn't originally write. That's exactly "fix Phase 2's break in Phase 1's code" - so today's honest worst case is Kriya correctly DETECTS a cross-phase regression via the full-suite gate but may not reliably self-heal it, falling back to reporting failure rather than silently shipping broken code (safe, not self-healing).
  - **Proposed POC, NOT YET BUILT - discussion paused by the user before committing ("Hold hold")**: reuse the goal's own existing Layer 1/2/3 split, run 3 sequential real `kriya generate <layer-N-goal> -y` calls against the same growing workspace (100% real production code paths, unlike the reasoning spikes which had to bypass `LLMClient`), compare wall-clock/retry-count/pass-fail per layer against the existing monolithic-run history for this same goal. Expected cost: 30-90+ minutes per approach given this goal's documented difficulty - not started.
- **Stage 4/6 SME review (RunVerifierAgent), 2026-08-15: independent brutal review by a fresh, context-free agent found 9 real findings - #1 (most severe) fixed and live-validated; #2-9 remain open.** Deliberately delegated to a fresh agent with zero session context specifically to avoid this session's own accumulated bias toward "looks solid" - it verified every claim against the actual current code before reporting, not just pattern-matching on comments. Ranked most-severe-first:
  1. **CLOSED: the most common runtime failure shape (plain nonzero exit, no hang) bypassed grading entirely.** `attempt.py`'s `elif not run_res["success"]:` branch (a step failed, not a timeout) was the ONLY one of the three outcome branches (clean run / timed out / plain failure) that never called `extract_contract_verdict()` or `RunVerifierAgent.grade()` - it short-circuited straight to a synthetic "one or more steps failed" message, discarding a generated program's own rich `[VERIFICATION] FAIL: <reason>` self-diagnosis and always leaving `likely_files` empty (zero file-attribution signal for exactly the failure class `grade()` exists to localize). Confirmed via grep: this branch had ZERO test coverage anywhere in the repo, and the one existing test that looked like it covered it (`test_run_attempt_persists_grader_reasoning_on_run_verification_failure`) actually mocked a `success=True/returncode=1` shape the real `run_app_sequence()` can never produce, so it was silently exercising the WRONG branch - corrected. **Fixed** by mirroring the clean-run branch exactly: check the deterministic marker first, fall back to `grade()` only if genuinely absent. **Validated two ways**: (a) a corrected/new workflow-level test with the real `success=False` shape, confirming `grade()` is never called when the marker is present; (b) a deterministic, LLM-free integration test (`test_run_app_sequence_plain_crash_output_feeds_the_real_contract_parser`, `tests/test_polymorphic_validation.py`) that runs a REAL (non-mocked) subprocess exiting nonzero after printing a FAIL marker, feeding the REAL captured output through the REAL `extract_contract_verdict()` - confirming the actual integration boundary, not just the branch's control-flow logic in isolation. Chosen specifically because reproducing the *original* live incident that prompted this fix (see the pom.xml entry below) proved impractical given LLM non-determinism - this gets real confidence without needing a live model at all.
  2. **CLOSED, 2026-08-15**: `command_source` (`"goal_explicit"` vs `"inferred"`) is self-reported by `judge()`'s own untrusted JSON response with zero validation against the actual goal text - and the human-in-the-loop approval gate (`attempt.py`) only fires for `"inferred"`. A model mislabeling a guessed command as `"goal_explicit"` skips the one safety check that exists, even in strict `human-in-the-loop` mode. **Fixed deliberately, weighing several angles before choosing an approach** (user explicitly asked for caution, not a quick patch): new `downgrade_ungrounded_goal_explicit_commands()` (`kriya/workflow/file_resolution.py`, same "never trust an LLM claim without an independent check" pattern already used for `grade()`'s `likely_files`/`check_skill_conflicts()`'s index bounds) checks whether a claimed-`goal_explicit` command's executable (basename, case-insensitive) actually appears in the goal text - `judge()`'s own system prompt already instructs it to "extract that exact command" when the goal states one explicitly, so a genuine claim should already satisfy this by construction. Design choices made explicitly, not by default: (a) placed in the workflow layer, not inside `RunVerifierAgent` itself, matching the established precedent for correcting `judge()`'s output (`_resolve_run_command()`'s Bundler/Maven corrections live the same way); (b) checks ONLY the executable, not the full command line/arguments - a stricter match would catch more but produce more false positives for a legitimate goal-stated command Kriya's own downstream corrections (`-e`, `-Dexec.mainClass`) then extend; (c) deliberately, consciously accepts a known false-positive class (a goal saying "run using Maven" rather than naming `mvn` literally would fail the check) - chosen because that failure mode is a cheap, safe degrade (one extra approval prompt) vs. the false-negative failure mode being the actual security-relevant gap; (d) for a multi-command sequence, ANY ungrounded command downgrades the WHOLE sequence (same AND-semantics precedent as `extract_contract_verdict()`'s multi-step FAIL) - always resolves toward MORE approval-gating, never less; (e) applied unconditionally (corrects the cached judgment itself, before the approval-gate check even runs), not just inside the `human-in-the-loop`-mode branch, so the judgment dict is internally consistent for any future consumer, not just this one call site. 7 new tests: grounded-stays-explicit (no false positive on the ordinary case), ungrounded-downgrades + original-dict-never-mutated, case-insensitivity, executable-basename handling, multi-command-any-ungrounded-downgrades-all, already-inferred-is-a-no-op, and a full `run_attempt()` integration test confirming `approval_callback` is actually invoked for a real mislabeled case (not just the pure function in isolation). 519 passed (512+7), same 6 pre-existing/unrelated failures - zero new regressions.
  3. **DEPRIORITIZED, 2026-08-15, explicit user decision - not closed, not ignored, deferred until the main pipeline stabilizes.** Execution has no real sandbox - `kriya/tools/sandbox.py`'s own docstring admits it doesn't stop reading credential files or reaching the network; resource limits are best-effort, not a hard boundary. Considered building the narrow, in-scope mitigation (an executable allowlist/denylist for `judge()`-inferred commands - no new dependency, doesn't touch the existing "Full containerized (Docker) isolation... explicitly out of scope" decision a few lines below) vs. revisiting that Docker decision itself vs. deferring - user chose to defer, reasoning agreed as sound: finding #2 (just closed) already removes the most direct exploitation path (a mislabeled/malicious command skipping human review entirely) - what remains is a lower-probability, now-partially-mitigated risk, not an open gap. Kriya's actual threat model here (a single local operator running it on their own machine) is also meaningfully different from a multi-tenant service running untrusted code from strangers, which the finding's framing implicitly assumes. The eventual fix (the allowlist/denylist) stays small and well-scoped whenever picked back up - deferring doesn't let complexity accumulate in the meantime. Revisiting the Docker decision, if ever wanted, deserves its own focused discussion, not a side effect of working through review findings.
  4. **CLOSED, 2026-08-15**: the `[VERIFICATION] PASS` marker was trusted from the same (possibly buggy) model that wrote the implementation, with no independent semantic check that the PASS is actually gated behind a real comparison. **Genuine tension weighed before fixing, not a one-sided bug**: `extract_contract_verdict()` exists specifically BECAUSE `RunVerifierAgent.grade()` (the LLM grader) once hallucinated a wrong "expected" value and rejected genuinely correct code - so any fix had to avoid casually reintroducing that exact problem for the common case. Design: `VERIFICATION_CONTRACT_HEADER`'s own instruction requires BOTH a PASS and FAIL path ("PASS if... FAIL: `<reason>` otherwise") - so any genuine implementation, however structured (if/else, ternary, whatever), has to literally write the `"[VERIFICATION] FAIL"` string somewhere in source for that branch to exist at all. New `pass_verdict_is_grounded()` (`kriya/workflow/verification_contract.py`) checks the written files' real source for that string; if absent, the PASS verdict is discarded and the run falls through to `grade()` instead - same "never worse than today's behavior" fallback the module's own docstring already promises for a non-compliant program, just extended to cover an unconvincingly compliant one too. Deliberately scoped to PASS only (a FAIL verdict has no equivalent risk - worst case is one unnecessary retry, not shipping broken code as "verified"), and honestly NOT overclaimed as airtight: a model could still write dead code like `if (true)` and pass this check - same "cheap, low-false-positive tripwire, not a semantic guarantee" scope every other structural check in this codebase already accepts, and the fallback (`grade()`) is itself imperfect, just strictly better than zero scrutiny. Wired into all THREE of `attempt.py`'s `extract_contract_verdict()` call sites (clean run / timed out / plain nonzero exit - the same three branches Finding #1 touched) via one new shared helper (`_extract_grounded_contract_verdict()`), not three separate edits that could drift out of sync. Caught and fixed a real pre-existing test-fixture gap while validating: two existing tests mocked Developer output with no FAIL branch in the file content at all, which would have broken under the new check for the wrong reason (an under-specified fixture, not a real regression) - corrected both to a realistically-compliant if/else fixture. 5 new tests (4 pure `pass_verdict_is_grounded()` cases: grounded, ungrounded, checks-across-all-files, empty input; 1 full workflow-level test confirming `grade()` genuinely gets called for a real ungrounded case, not just the pure function in isolation). 524 passed (519+5), same 6 pre-existing/unrelated failures - zero new regressions.
  5. **NOT YET FIXED**: Developer/Judge/Grader all silently default to the same model unless a project explicitly sets `agent_llms` (confirmed: zero entries in `default_config.yaml`) - directly compounds #4, since there's no independent second opinion by default.
  6. **NOT YET FIXED**: no output-size cap before feeding captured stdout/stderr into the grader prompt (contrast `SkillGapAgent`'s explicit 20K-char cap) - a silently truncated prompt on a verbose-output goal could cut the actual success marker with nothing detecting it.
  7. **NOT YET FIXED**: non-UTF-8 captured output raises inside `communicate()`, caught by a generic `except Exception` one level up - doesn't crash, but burns a retry on a generic message with zero real diagnostic signal.
  8. **NOT YET FIXED**: `judge()`'s decision is cached once per run (`state.cached_run_verification_judgment`), but `files_written`/live `pom.xml` content can drift across retries - a stale inferred entrypoint path from attempt 1 can persist even after the file layout changes.
  9. **SUSPECTED, not confirmed**: concurrent runs sharing the single reused `.kriya/worktree` could collide (files, ports) - plausible from the code's own precedent comments, not traced to a concrete repro.
  - **Checked and found genuinely solid, not just unexamined**: the timeout-kill mechanism (process-group SIGKILL + bounded reap), `grade()`'s hallucinated-`likely_files` filtering, fail-closed behavior on unparseable JSON in both `judge()`/`grade()`, and the timed-out-but-output-shows-success handling.
- **Two logging gaps found and fixed while forensically investigating a real, still-unresolved live incident (2026-08-15) - see the pom.xml entry below for the incident itself.**
  1. `DeveloperAgent._fill_missing_content()`'s per-file loop had ZERO logging for its "entry already has content/edits, skip regeneration" pass-through path - not even gated behind the optional `stream_callback`, which the "actively generating" branch also relied on exclusively (no `logger` fallback either). A real investigation had no way to tell, from any log, whether a file was silently reused vs. freshly regenerated. Fixed: both branches now log at INFO unconditionally.
  2. The only `trace_logger.log_run()` call for a normal (non-early-exit) run happened AFTER the Reviewer call - a single LLM completion that can itself hang or get killed. When that happens, `state.gate_outcomes` (the full per-attempt forensic history, already complete in memory once Quality Gates conclude) was entirely lost - confirmed live: a real run's trace row stayed at `in_progress`/0 attempts/empty `gate_outcomes` despite ~40 minutes of real retry activity, because the log itself ended abruptly mid-Reviewer-call. Fixed: an intermediate checkpoint now writes `gate_outcomes`/`model_hops`/etc. right after Quality Gates conclude, before Reviewer runs - safe and idempotent (`run_id` is the table's PRIMARY KEY, `log_run()` always does `INSERT OR REPLACE`), so the real final write afterward simply replaces it.
- **NOT YET RESOLVED: the original live incident that prompted the two logging fixes above - `ignite_qpid_protocol` hit `STRUCTURAL CORRUPTION in pom.xml: malformed XML: no element found: line 1, column 0` (i.e. genuinely empty content) identically across attempts 5-8, with the retry-mode escalating (fallback_targeted -> full-set x3) but the corruption never clearing.** Could not conclusively determine, from that run's logs alone (pre-dating the logging fixes above), whether this was a Kriya-side bug (stale/rejected content somehow reused instead of fresh regeneration) or a genuinely unusual, repeated model failure - ruled out several hypotheses (both write-loop paths check-before-write, so a rejected edit's content can't reach disk; `_resolve_step1_file_list()` always returns fresh `content: None, edits: None`; Planner-code-block reuse is explicitly gated to attempt 1 only) without reaching a definitive answer. A same-day re-run with the new logging in place did NOT reproduce this specific failure (LLM non-determinism - a different, unrelated bug surfaced instead, see the multi-module entry below), so the original hypothesis remains neither confirmed nor ruled out. The new logging is real, permanent infrastructure regardless - it will give a decisive answer the next time this (or a similar) corruption recurs, live or in the eval harness.
- **CLOSED, 2026-08-15: PlannerAgent had no minimalism constraint, and a goal explicitly requiring ONE entry-point class got planned as three separate Maven modules with three separate entry-point classes.** Found live (same re-run as above, different bug than the one being chased): the checkpointed plan text's very first design decision was a Maven `<modules>` block (`protocol-layer`, `ignite-layer`, `qpid-layer`), directly contradicting the goal's own explicit "Put all startup/orchestration logic in ONE entry-point class... do not split this across multiple orchestration classes" instruction - confirmed this originated in Planner's own output, not a later stage; Architect/Developer just faithfully implemented what a wrong plan already specified. `ArchitectAgent` already has an equivalent MINIMALISM principle (added independently, in an earlier session) but runs AFTER Planner, by which point the decision was already locked in. Separately checked whether this session's own Target-2 fix (the skill-conventions reminder added to Planner's prompt, ~54-109 tokens) contributed to this run's slowness (465s for the Planner call alone, 5,896 output tokens) - confirmed NOT a meaningful factor: the reminder is 0.3-0.6% of this run's actual 17,912-token Planner input; the real time/token sink was the unnecessarily large wrong plan itself. Fixed: added a MINIMALISM instruction to `PlannerAgent`'s system prompt (mirroring Architect's), explicitly citing the confirmed incident and stating a goal's own multi-part description (layers/phases) must NOT be read as suggesting a multi-module answer. 1 new test (`test_planner_agent_prompt_forbids_unrequested_multi_module_structure`).
- **Stage 5/6 (SkillGapAgent) SME review, 2026-08-15.** Reviewed `SkillGapAgent.extract_skill_update()` and `check_skill_conflicts()` end-to-end (correctness + downstream consumers in `kriya/workflow/skill_extraction.py`/`workflow.py`). Three findings:
  A. **NOT YET FIXED - the Skill Conflict Checker's index-based redesign hasn't actually closed the failure mode it was built to fix.** `SkillGapAgent`'s Skill Conflict Checker (`kriya/agents/agent.py`, ~line 1560-1648) compares two co-active skills' numbered rule lists and asks the model for `{"rule_a_index": <int>, "rule_b_index": <int>}` conflict pairs - a redesign explicitly motivated by an earlier, worse version that asked for verbatim rule-text quotes and (per that redesign's own code comment) saw "up to 28 conflicts discarded from a SINGLE call... zero real conflicts surfacing." Confirmed live, same failure class recurring under the fixed version: a real `ignite-java17`/`qpid` run produced 22+ consecutive `out-of-range index` warnings in the same log burst (`idx_a=34` fixed, `idx_b` sweeping 23->44). Diagnosed this session by cross-checking against the real files: `ignite-java17` currently parses to 44 real rules (`idx_a=34` was actually a *valid* position - only `idx_b` was ever out of range against qpid's 22). One index held constant while the other counts up by exactly 1, 22 times in a row with no genuine per-pair reasoning behind any of them, is the signature of an LLM stuck in a **repetition-loop degenerate generation** under `json_mode` (pattern-completing "same object, incremented field" instead of judging each pair) - a model-reliability problem, not a code-correctness bug; the bounds-check already discards every one of these safely, the cost is pure waste (tokens/latency) plus a skill pair whose conflict detection silently always returns empty. Three mitigation options identified, not yet chosen: (1) an anomaly guard - if the returned `conflicts` list is anomalously long against the prompt's own stated expectation ("most skill pairs have none"), discard the whole response as suspected-degenerate with one aggregated warning instead of N individual ones (cheap, code-only, doesn't reduce wasted generation time); (2) cap `max_tokens` for this specific call so a repetition loop hits a hard ceiling early (the only lever that actually reduces waste, but `call_with_escalation`'s no-override path - the common case, no `agent_llms` configured - currently passes no `max_tokens_override` at all); (3) leave as-is (not incorrect, just occasionally wasteful, confirmed on exactly one skill pair so far). Needs live verification either way since it's fundamentally live-model behavior - deferred pending which mitigation (if any) is chosen.
  B. **CLOSED: `extract_skill_update()`'s `conflicts` field had no per-item shape validation - a real crash risk.** Unlike `check_skill_conflicts()` (which strictly validates every returned conflict entry's dict-ness/index bounds), `extract_skill_update()` only checked `isinstance(conflicts, list)`, nothing about what's inside. That list flowed straight into `_stage_skill_conflicts()` (`kriya/workflow/skill_extraction.py`), which unconditionally calls `c.get("candidate_rule", "")` etc. on every item - and the entire skill-gap-resolution block in `workflow.py` (~lines 500-662) has no enclosing try/except. A model returning `"conflicts": ["some string"]` instead of `[{"candidate_rule": ...}]` (a plausible JSON-shape mistake, same failure class as the already-fixed index-hallucination issue) would raise an uncaught `AttributeError` and abort the entire generation run, not just skip skill-gap resolution. Fixed: each `conflicts` entry is now validated (must be a dict, must have a non-empty string `candidate_rule`; `conflicts_with`/`reason` coerced to `""` if not strings, all three stripped) before being trusted - same "never trust an LLM claim without an independent check" trust boundary already used for `check_skill_conflicts()`'s own index bounds and `grade()`'s `likely_files`. The mutual-exclusivity block (dedupes a rule already flagged as conflicting out of `rules`) was simplified to rely on this upstream guarantee rather than re-checking `isinstance(c, dict)` itself.
  C. **CLOSED: `examples` field validation was all-or-nothing.** `examples if isinstance(examples, dict) and all(isinstance(v, str) for v in examples.values()) else {}` - one malformed example value (e.g. a model nesting a dict/list for a single file) discarded *every* example in the response, even genuinely valid ones, inconsistent with `rules`' own per-item filtering three lines above it in the same function. Fixed: per-item filtering, same pattern as `rules`.
  - **Independent review** (fresh subagent, no shared context with the author - per explicit user instruction to always review as a third party going forward, not self-assess): confirmed both B and C correct with no bugs - traced the `valid_conflicts = []` pre-initialization (non-list `conflicts` falls through safely, not just wrong-shaped list items), the `isinstance(c, dict)` guard before any `.get()` call, the whitespace-only `candidate_rule` rejection (`not candidate_rule.strip()`, not just exact-empty), and confirmed the mutual-exclusivity block's reliance on the upstream guarantee is genuinely safe. Flagged one cosmetic-only inconsistency (fixed: `candidate_rule`/`conflicts_with`/`reason` are now stored `.strip()`ped, matching what the mutual-exclusivity comparison already compared against) and 3 test-coverage gaps (examples-not-a-dict-at-all, conflicts-not-a-list-at-all, whitespace-only `candidate_rule` as a case distinct from plain-empty) - all closed with new tests. 6 new tests total in `tests/test_agents.py`. Confirmed via `git stash` that the 8-failure full-suite baseline (see below) is unaffected - zero new regressions from B/C.
- **Stage 5 "brutal third-person" full-module review, 2026-08-15 - same treatment as RunVerifierAgent got in stage 4** (explicit user request: "can we get a third person review like we did for developer?"). Fresh subagent, no shared context with anything fixed above, told to review the whole `SkillGapAgent` stage end-to-end (both methods, both system prompts, plus real downstream consumers - `kriya/workflow/skill_extraction.py`, the full `workflow.py` orchestration, and `kriya/skills/skill.py`'s conflict-resolution registry) - not scoped to just the A/B/C diff. Found 6 new findings, ranked by severity; user asked to fix 1 and 2 (3-6 logged, not yet fixed):
  1. **CLOSED - `kriya skills approve` was promoting conflict diagnostics as if they were real rules, unconditionally, every time.** `_stage_skill_conflicts()` (`kriya/workflow/skill_extraction.py`) is the ONLY writer of `staged_rules.txt` in the current codebase (confirmed via grep - the old free-text lesson-extraction path that used to also write there was migrated to structured `staged_knowledge.json`, see `kriya/knowledge/staging.py`'s own docstring), and every line it writes has the shape `[CONFLICT] {candidate} -- conflicts with existing rule: '{existing}' ({reason})`. `skills_approve` (`kriya/cli.py`) read every staged line and appended it VERBATIM to `rules.txt`, then deleted the staged file - no `[CONFLICT]` check, no filtering, no way to pick a side. This wasn't a rare input shape - it fired on every single flagged conflict, always, and sits directly in the human-review path `_stage_skill_conflicts()`'s own docstring says exists "so a human notices and resolves them." Fixed: `skills_approve` now splits staged lines into `promotable` (auto-promoted as before) and `unresolved_conflicts` (`[CONFLICT]`-prefixed - rewritten back into `staged_rules.txt` instead of being promoted or deleted, so they survive for manual resolution); the file is only removed once zero conflicts remain. `skills_list` also renders `[CONFLICT]` lines in red for visual distinction. 3 new tests in `tests/test_cli_smoke.py`.
  2. **CLOSED - the cross-skill conflict check (section 1.3) ran against a stale `SkillEngine` cache, missing rules extracted moments earlier in the SAME run (section 1.2).** `se.get_skill(name).rules` reads the in-memory cache built by `discover_and_load()`; `_write_skill_extraction()` (called from 1.2, immediately before 1.3) appends new rules straight to `rules.txt` on disk but never refreshes that cache, and nothing reloads it before 1.3 runs. The codebase's own author already fixed this EXACT staleness pattern for a different consumer later in the pipeline (`workflow.py` ~line 1141, "Reload from disk first - extraction writes... append directly to rules.txt without refreshing SkillEngine's in-memory cache") but missed this call site. Concrete trigger: a goal resolving two unverified/missing skills in one run via live-lookup or human-supplied reference - a genuine contradiction between the two freshly-extracted rule sets goes completely undetected in that run (either the pair gets skipped via the `if not skill_a.rules or not skill_b.rules: continue` guard, or compared against stale/empty content), only ever catchable, if at all, on some future run. Fixed: one `se.discover_and_load()` call added at the start of the 1.3 block, before the pairwise comparison loop - cheap (filesystem walk + YAML parse, no network/LLM calls), same pattern as the existing precedent. 1 new test in `tests/test_workflow.py` (`test_workflow_skill_conflict_check_sees_rule_extracted_earlier_in_same_run`) confirming the conflict is actually caught in the SAME run, not a future one.
  - **Independent review** (fresh subagent) confirmed both root-causes and mechanics correct (ran the new tests directly, confirmed `test_skills_approve_does_not_promote_flagged_conflicts` genuinely fails against the pre-fix code via a scoped `git stash`), but surfaced two follow-ons, both closed: (a) `test_skills_list_marks_conflict_lines_distinctly` asserted on `result.output`, but `CliRunner`'s captured stream is never a real tty so click strips ANSI color regardless of what `fg=` was passed - the test would have passed even with the red-coloring branch broken. Rewritten to patch `click.secho` (`wraps=`, so real rendering still happens) and assert the `fg="red"` kwarg directly. (b) Finding 2's fix exposed a real, previously-unreachable gap: section 1.2's `convention_prompt` embeds `extraction["rules"]` RAW (unsanitized), while section 1.3's conflict-resolution removal (`convention_prompt.replace(f"- {rule}\n", "")`) now compares against `skill.rules` freshly reloaded from disk - which IS sanitized (`_write_skill_extraction()`'s own `_sanitize_for_flat_file_line()` call). A raw/sanitized mismatch (e.g. embedded newlines in the LLM's rule text, a documented real occurrence) would make the string-replace silently no-op, leaving a "resolved" losing rule still visible to Planner/Architect despite the conflict having been correctly detected and a resolution recorded. Fixed: the `convention_prompt` embedding now sanitizes with the identical `_sanitize_for_flat_file_line()` call, so both sides always match byte-for-byte.
  - Findings 3-6, NOT YET FIXED, logged for later: (3) design-derived (stage 2B) skills never get a cross-skill conflict-check pass at all - section 1.3 only runs once, before the Architect design exists, a pure ordering gap. (4) conflict resolution's `convention_prompt.replace()` is unscoped - if two independently-active skills happen to share byte-identical rule text, resolving a conflict between two OTHER skills would silently also strip that line from the third, uninvolved skill's section. (5) no cross-run memoization for "these two skills' current content already checked clean" - `check_skill_conflicts` pays a full LLM round-trip for every active skill pair on every single run, even when neither skill's rules have changed since the last run already cleared that exact pair (compounds Finding A's degenerate-generation cost, since a wasteful call repeats indefinitely). (6) `_write_skill_extraction`'s exact-duplicate check uses a frozen `existing` snapshot that isn't updated per-iteration (only the near-duplicate check's `effective_existing` is), so a single extraction response containing the same SHORT (< 4 content words) duplicate rule twice gets both copies written to `rules.txt` - narrow input shape, cosmetic duplication only.
  - **Reviewer explicitly confirmed genuinely solid** (not just unexamined): both system prompts (clear, unambiguous for a weaker local model, worked examples included), the index-bounds defense in `check_skill_conflicts`, the mutual-exclusivity enforcement in `extract_skill_update`, `_likely_misattributed_sibling`'s implementation matching its own documented "false negative cheaper than false positive" intent line-by-line, and `find_conflict_resolution`/`record_conflict_resolution`'s order-independent, fail-safe, no-TOCTOU design.
- **CLOSED, 2026-08-15: the 2 `test_knowledge.py` auto-accrual failures were a test-authoring bug, not a Kriya application bug - root-caused and fixed.** `PolymorphicValidator` (`kriya/tools/validate.py`) is constructed at TWO separate sites during a successful `run_generation_workflow()` run: `attempt.py`'s per-attempt compile/test gate uses a deferred, function-LOCAL `from kriya.tools.validate import PolymorphicValidator` inside `run_attempt()` (re-resolves the name from `kriya.tools.validate`'s module namespace fresh on every call), while `workflow.py`'s own separate final "full regression test suite" check (deliberately a SECOND validator, pointed at the real post-worktree-reset workspace - see the comment at workflow.py ~line 1554-1561) imports it ONCE at module load via a top-level `from kriya.tools.validate import PolymorphicValidator`, binding a frozen reference to the real class that no later patching of `kriya.tools.validate.PolymorphicValidator` can reach. `test_workflow_auto_accrual`/`::_skipped_when_suggestion_never_actually_used` both did `patch("kriya.tools.validate.PolymorphicValidator")` (replacing the WHOLE class) - only ever affects the first call site; the second silently fell through to the REAL, unmocked class, hit real Maven against the test's minimal mocked pom.xml content, and produced genuine `'modelVersion' is missing`-shaped validation errors. Every other test in `tests/test_workflow.py` already uses the correct pattern (confirmed via grep, dozens of examples, e.g. `test_workflow_checks_toolchain_only_once_across_retries`'s own "regardless of which of the two PolymorphicValidator construction sites reaches it first" comment) - patch the METHODS directly on the class object (`patch("kriya.tools.validate.PolymorphicValidator.run_compile_check", ...)`), which affects every instance regardless of which module's reference constructed it, since method lookup at call time always goes through the shared class object. Fixed both tests to match that established pattern; also dropped the no-longer-meaningful `mock_validator.stack = "java"` line (there's no whole-class mock instance to attach it to anymore) - verified safe: with only the two methods patched, `PolymorphicValidator.__init__`'s real `_detect_stack()` runs genuinely, `.stack` is only ever read for toolchain-preflight logging/`JAVA_HOME` override (never by the mocked `run_compile_check`/`run_tests`), and `state.toolchain_checked` (shared across both construction sites) flips `True` after the FIRST preflight, so workflow.py's own second construction site never touches `.stack` at all in the success path. Independent review (fresh subagent) confirmed the root-cause diagnosis, the class-vs-instance method-patching mechanics (no unexpected `self` argument breaking the mocked `side_effect` sequence), and the `.stack` removal's safety - flagged one pre-existing, unchanged-by-this-fix side note (both the old and new mocking still let a real `java -version`/`mvn -version` subprocess call fire for the toolchain-mismatch check, never asserted on, not a regression). Both tests confirmed passing. **Corrects the full-suite pre-existing-failure baseline back to 6** (the JDK-path mismatch group only - see below) now that this fix is in place.
- **NOT YET FIXED - a second, DIFFERENT pre-existing defect found hiding inside 3 of the 6 "known JDK-path baseline" failures, root-caused 2026-08-15 while investigating why their failure SIGNATURE changed after the stage-6 Reviewer-ordering fix.** `test_workflow_full_set_prompt_includes_existing_dependencies_checklist`, `test_workflow_surfaces_toolchain_warning_even_on_success`, and `test_workflow_checks_toolchain_only_once_across_retries` were reported failing with `assert False is True` instead of their previously-tracked `StopAsyncIteration` signature right after the stage-6 fixes landed - investigated with a scoped `git stash` (isolating every uncommitted change from this entire session, not just today's) to determine whether the Reviewer-reorder fix caused a real regression. It did not: the exact same standalone repro fails identically against the clean, last-committed baseline too, just via an UNCAUGHT `StopAsyncIteration` crash at the old (pre-fix) Reviewer call site instead of a caught, gracefully-retried failure - proof the underlying defect predates this entire session's work. Root cause (traced with temporary print-tracing directly in `find_structural_corruption`/`DeveloperAgent.run_generation`/`run_attempt`, all reverted after - `kriya/workflow/edit_safety.py` confirmed zero diff afterward): `attempt.py`'s pre-existing `known_target_files = ctx.expected_files_upfront` logic (`kriya/workflow/attempt.py` ~line 392-394, unrelated to anything touched this session) sets `known_target_files=["pom.xml"]` on attempt 1 for these three tests (Architect's heuristic file-list extraction resolves it), which forces `DeveloperAgent.run_generation()`'s EARLY-RETURN branch (`if known_target_files: ... return await self._fill_missing_content(...)`) - a per-file, RAW-file-content generation path, not the "Step 1 JSON file list" path these tests' 3rd mocked `llm.complete` response was actually written for. The mocked JSON-array string (`'[{"filepath": "pom.xml", "content": "<project></project>"}]'`) gets written to `pom.xml` VERBATIM as literal file content (JSON syntax and all) - `find_structural_corruption()` correctly flags it as malformed XML (`ET.fromstring` on `'[{"filepath"...'` raises exactly `"syntax error: line 1, column 0"`, confirmed by direct testing), and the retry loop churns through its budget on a file that can never be written correctly, eventually exhausting the fixed-length mock response list. This is a genuine test-authoring bug (the mock's 3rd response was written assuming the "Step 1 file list" JSON-parse path would be reached, but `known_target_files` being set on attempt 1 - true whenever Architect resolves a file list at all - means that path is never actually exercised for these tests), not a Kriya application defect - `known_target_files` behaves exactly as its own extensive docstring says it should. Not yet fixed (each test's 3rd mock response needs to return plain, direct file content instead of a JSON-list-shaped string) - flagged here so a future session doesn't re-investigate from scratch, and doesn't mistake this for a regression the next time these 3 tests' failure text happens to shift for an unrelated reason (their failure signature is apparently sensitive to exactly which LLM call in the pipeline first hits mock exhaustion, which can shift innocuously any time the surrounding pipeline's call sequence changes).
- **CLOSED, live-verified 2026-08-15/16: `_fill_missing_content()`'s per-file generation loop showed sibling files only by NAME, never their actual content - confirmed as a real, repeated live compile-failure cause and fixed.** Found while forensically investigating a real `ignite_qpid_protocol` eval-harness run that TIMED OUT after the full 2400s without ever resolving: `Protocol.java` (package `com.example`) and `ProtocolParser.java` (package `com.example.protocol`, no import for `Protocol`) each independently invented a plausible-but-inconsistent package with nothing to reconcile them, because the per-file prompt's "Other Files In This Batch" section explicitly captioned sibling files "context only - do NOT output their content here" - filenames only, never content, even for a sibling already fully generated earlier in the SAME batch. A second, independent instance of the identical failure class in the SAME run: `ProtocolApp.java` called `Protocol`'s constructor with a signature that didn't match what `Protocol.java` actually declared, for the same underlying reason. Traced precisely before fixing: checked the actual checkpointed Plan/Design text (neither ever named a package, despite `ArchitectAgent`'s own system prompt explicitly saying defining "packages" is its job) and confirmed via `attempt.py` that `existing_code_context` is built ONCE before any file in an attempt is generated, so it's empty of sibling content for a first attempt regardless. Fixed: `_fill_missing_content()` now shows each file the REAL content of any sibling already written earlier in the same batch (`files_out`, accumulated so far in the same loop, checked before the current file's own append) - a sibling not yet reached still falls back to a name-only list, since there's genuinely nothing to show yet, but this closes the gap for the common case where the Architect's own file list already orders data classes before their consumers. New regression test reproduces the exact two-file scenario (`test_fill_missing_content_shows_freshly_generated_sibling_content_not_just_name`); one existing test's assumption updated to match the improved behavior. **Live-reverified overnight**: re-ran the identical goal end-to-end (`spikes/eval_harness/runs/b-4-sibling-fix-verify`) - zero package/import-mismatch errors anywhere in the entire run (previously: cascading failures across all 5 attempts). The run still timed out, but on a completely different, unrelated issue (see next entry) - the specific bug this fix targets did not recur even once.
- **NOT YET FIXED, live-observed 2026-08-16, confidence too low to guess a fix without more evidence: a correctly-diagnosed, syntactically-valid one-line anchored-edit fix was proposed identically 3 times in a row and never actually took effect.** Found in the SAME overnight re-verification run above, after the sibling-content fix closed the original bug: `ProtocolApp.java:68` read a value back from an `IgniteCache` without a cast (`Protocol cachedProtocol = cache.get(1);` instead of `(Protocol) cache.get(1)`) - `incompatible types: java.lang.Object cannot be converted to com.example.Protocol`. The model's own fix-analysis text correctly named the cause identically across all 3 targeted retries, and its proposed `SEARCH:`/`REPLACE:` block each time was genuinely correct, compiling Java (`Protocol cachedProtocol = (Protocol) cache.get(1);`) - yet the IDENTICAL compile error recurred after every attempt, including a subsequent bounded self-correction micro-loop (`run_self_correction_loop`, which also failed on every single attempt in this run, not just this one - "model stopped calling tools without a passing recompile") and a final fallback-model escalation. No `ANCHORED EDIT FAILURE` (the mechanism's own explicit rejection log line, confirmed present and working correctly earlier the same night on a different file) was ever logged for this specific edit - `apply_anchored_edits()` reported no error, meaning it believes the edit was applied. Root cause NOT confirmed: the harness's `--timeout-per-goal 2400` killed the process mid-write on the final fallback attempt, leaving `ProtocolApp.java` on disk as a 0-byte empty file - destroying the one piece of evidence (the file's real post-edit content at the moment of the recurring failure) that would settle whether the edit silently applied to the wrong location, got overwritten by something else before the compile check ran, or the compile itself referenced stale `target/` bytecode from an earlier attempt (the worktree's compile-cache-reuse-across-retries design, `.kriya/worktree/target`, survives between attempts by design - a plausible but unconfirmed contributing factor). Deliberately not guess-fixed and not re-verified with a third live run tonight - explicit user instruction to conserve GPU time after a full day of live-model runs, and this doesn't clear the same evidentiary bar the sibling-content fix did (that one was confirmed via the checkpoint files, direct process tracing, and two independent same-run data points; this one has a single, now-unrecoverable data point). Recommended before touching this: add explicit logging of the anchored edit's actual post-application file content (or a content-diff before/after) right at the point `apply_anchored_edits()` returns successfully, closing the same "can't tell what actually happened from the log alone" gap the earlier STRUCTURAL CORRUPTION investigation (see above) was fixed for.
- **CLOSED, 2026-08-16: generated/edited source files were written non-atomically - a process killed mid-write (the exact mechanism that destroyed the evidence for the finding above) left a file at 0 bytes with its prior content already gone. Fixed with a shared atomic-write helper, at every site that writes generated content.** Explicit user ask after seeing this happen "many times so far": don't lose debuggable evidence to a timeout kill. Root mechanism confirmed precisely: the eval harness uses `subprocess.run(..., timeout=...)`, whose timeout handling calls `Popen.kill()` - SIGKILL on POSIX, uncatchable by any signal handler or cleanup code, so no amount of try/finally or atexit logic could ever help. Plain `open(path, "w")` truncates a file to empty BEFORE writing new content - a kill in that window (confirmed as the actual mechanism behind the empty `ProtocolApp.java` in the finding above) leaves 0 bytes on disk, with whatever was there before destroyed, regardless of whether the new content was ever fully written. Fixed: new `atomic_write_file()` (`kriya/workflow/edit_safety.py`, the natural shared home - both call sites already import from this module, and the alternative of one importing from the other would fight `attempt.py`'s existing deliberate deferred-import boundary around `self_correction.py`) writes to a temp file in the same directory, then `os.replace()` (atomic on both POSIX and Windows NTFS) - the file on disk is always EITHER the complete old content OR the complete new content, never an in-between state, no matter when the kill lands. Applied at all three real write sites for generated/restored content found via `grep`: `kriya/workflow/attempt.py`'s two per-file write sites (the normal per-attempt generation path, both the anchored-edit and full-content branches), `kriya/workflow/self_correction.py`'s own tool-driven patch-application write (the self-correction micro-loop, confirmed live to run and fail on every single attempt in the same overnight run), and `kriya/workflow/workflow.py`'s human-rejection rollback path - the highest-stakes of the three, since that one restores the user's REAL project file directly with no worktree isolation layer, so a kill there would have corrupted the user's actual pre-existing source, not just a scratch sandbox file. Deliberately did not chase every other `open(..., "w")` in the codebase (e.g. `shutil.copy2`'s own internal write during the worktree-to-workspace apply step) - lower risk since the source of truth (the worktree file) survives a kill there untouched, unlike the three sites fixed, where the in-flight write WAS the only copy of the content. 3 new tests (`tests/test_workflow.py`): correct-content write, no leftover temp file, and old content fully replaced (not left partially mixed) on a second write to the same path.
- **CLOSED (stage 3/6, DeveloperAgent, 2026-08-15): `run_generation()`'s single-stage-generation fallback was silently dropping ALL retry context - fixed.** Found during the per-agent-stage SME review: when `_resolve_step1_file_list()` produces nothing usable (documented by its own docstring as a real, expected outcome - the same malformed-response risk already established for `ArchitectAgent`'s identical `parse_file_list()` contract - not a theoretical edge case, and confirmed to have zero prior test coverage), `run_generation()` fell through to a separate "single-stage generation (original implementation)" prompt that never threaded `prior_error_context`/`extra_fix_instruction`/`retry_temperature` through at all, unlike the well-engineered `_fill_missing_content()` path every other branch uses. Real consequence: if this fired DURING a retry - exactly when error context matters most - the model was asked to "generate the complete files" with zero idea what it was fixing, and would plausibly just regenerate the identical mistake, burning the retry for nothing. Fixed by threading `prior_error_context`/`extra_fix_instruction` into this fallback's own prompt (a `=== Prior Attempt Failed - Fix This Error ===` block, conditional on `prior_error_context` actually being set) and `retry_temperature` into the completion call (`temperature_override`). Deliberately did NOT thread `implicated_files`/`error_source_context`/`files_with_current_content` through - those scope PER-FILE behavior (`_fill_missing_content`'s own per-file loop, and an edits/anchored-patch output shape this single-call path doesn't have at all, only full file `content`), so they don't map cleanly onto one batched completion; this is the minimal fix for the actual harm (zero error context), not a rewrite to replicate `_fill_missing_content`'s full fix-analysis machinery. 2 new tests (`tests/test_agents.py`): confirms the error block and `temperature_override` both land in the fallback's prompt/call when a retry hits this path, and confirms no fabricated error block appears on a clean first attempt that happens to hit the same fallback.
- **Stage 6/6 (ReviewerAgent, final stage) "brutal third-person" full-module review, 2026-08-15 - same treatment as RunVerifierAgent/SkillGapAgent got.** `ReviewerAgent` itself is a thin wrapper (just a system prompt, `BaseAgent.run()`), so the review focused on its two real consumers: the embedded pipeline's Reviewer stage (`workflow.py`) and the standalone `kriya review` CLI command (`kriya/cli.py`). Found 6 findings, ranked by severity; user asked to fix 1+2 first, then (this entry) 3 and 5, with 4 explicitly deferred and 6 assessed as minor:
  1. **CLOSED - the Reviewer's approve/reject verdict had zero enforcement anywhere, in ANY autonomy mode, and it was structurally too late even if parsed.** `quality_gates_passed` came entirely from the compile/test retry loop; the human-approval gate ran BEFORE the Reviewer stage - so even in strict `human-in-the-loop` mode, a human approved based only on a truncated raw diff, files got copied into the real workspace and the worktree torn down, and ONLY THEN did the Reviewer possibly say "REJECTED - critical issue," which changed nothing at that point (exit code 0, files already on disk, verdict never persisted to `traces.db`). Fixed: the Reviewer now runs BEFORE the approval decision, but ONLY when `need_human_approval and approval_callback` both hold (same gate the real `approval_callback` invocation already uses) - zero added cost in autonomous mode. Its verdict is folded into `escalation_reason` (a plain string `on_approval` already prints verbatim) rather than changing `approval_callback`'s shared signature, which two OTHER, differently-shaped approval decisions elsewhere (Stage 2A knowledge-gap, Runtime-Verification command approval) and any external/REPL/MCP caller also depend on. The later "5. Reviewer" step reuses the result (`state.pre_approval_review`) instead of a redundant second LLM call. **Independent review caught a real bug in this fix**: the original reset (`state.pre_approval_review = None`) was scoped INSIDE the "4.5" approval section, so it only ran if a later loop iteration reached that section again - but `run_attempt()` raises `QualityGateFailure` directly from many sites (compile/targeted-test/anchored-edit failures) that skip "4.5" entirely, the ordinary way an attempt fails. A stale review from an earlier, now-superseded attempt could survive and get returned as if it described the final content - reproduced concretely by the reviewer. Fixed by moving the reset to run unconditionally, once per loop iteration, before `run_attempt()` is even called - structurally guaranteed, no exception path can skip it. New regression test reproducing the exact scenario (`test_workflow_stale_pre_approval_review_not_reused_after_later_attempt_fails`).
  2. **CLOSED - the embedded Reviewer call had zero context-budget protection, the exact bug class the standalone `kriya review` CLI command was already fixed for.** `workflow.py`'s Reviewer stage concatenated every file's full raw content with no token-budget check at all; `kriya review` already had budget-aware chunking/batching with an explicit `TRUNCATED` marker, built specifically because unbounded content silently overflowing the context window produced a confused non-review with zero warning. Fixed: extracted that logic into a new shared `kriya/workflow/review_context.py::build_review_batches()`, used at both the new early-Reviewer call site and the existing later one - also closes a dead-instruction gap, since the shared system prompt's "Truncation awareness" rule previously only ever had a `TRUNCATED` marker to react to from the CLI path. **Independent review caught a real, newly-reachable bug this exposed**: section 1.2's `convention_prompt` embeds `extraction["rules"]` RAW (unsanitized), while section 1.3's conflict-resolution removal (`convention_prompt.replace(f"- {rule}\n", "")`) compares against `skill.rules` freshly reloaded from disk - which IS sanitized. A raw/sanitized mismatch (e.g. embedded newlines, a documented real occurrence) would make the string-replace silently no-op, leaving a "resolved" losing rule still visible to Planner/Architect despite the conflict having been correctly detected. Fixed: the `convention_prompt` embedding now sanitizes with the identical `_sanitize_for_flat_file_line()` call, so both sides always match byte-for-byte.
  3. **CLOSED - standalone `kriya review` sent bare file blobs with zero framing, a real mismatch with `ReviewerAgent`'s own system prompt.** That prompt assumes a goal exists (to calibrate its "don't reject for missing tests the goal never asked for" leniency rule) and assumes one coherent generated application (for its mandatory "How to Run" section) - neither is true for standalone review of arbitrary pre-existing code, often just a git-modified subset of a larger unrelated project. Fixed: a new `review_context_header` explicitly tells the model there's no goal, not to invent requirements, and to describe what's determinable from a partial file set rather than assuming a whole application - prepended to every batch (not just the first). Deliberately NOT a fabricated fake goal, which would just invite the model to invent requirements the goal doesn't state.
  4. **DEFERRED, not fixed - `kriya review` can't function as a CI gate despite looking like one (no exit-code signal for a rejected review, no aggregate warning for unreadable files).** Assessed and explicitly declined for now: fixing this properly means a real new structured-output contract (a `[REVIEW] PASS/FAIL`-shaped marker, mirroring the `[VERIFICATION]` pattern already built for RunVerifierAgent) plus prompt changes plus CLI parsing plus exit-code wiring - a genuine feature addition, not a bug fix. Unlike `generate --json` (explicitly built for CI/scripting, with its own documented precedent), nothing suggests `kriya review` was ever meant to be a pass/fail gate - its own help text just says "run code review agent on a file or folder." No one has hit this as an actual pain point in this review. Logged as an open backlog item for if/when a CI-gate story for `review` is actually wanted.
  5. **CLOSED - `kriya fix` truncated the Reviewer report to 300 chars mid-stream with no full reprint at the end, unlike `generate`.** `fix`'s `step_cb` truncates EVERY step's content (including the Reviewer's full report) to 300 chars; `generate` already reprints `res.get("review")` in full at the end under its own header, `fix` never did - the mandatory "How to Run the Application" section and most findings were simply lost after the truncated preview scrolled by. Fixed by adding the same reprint to `fix`. **Independent review caught a real gap**: `generate`'s reprint is gated on `res.get("files")` (non-empty), not just `res.get("review")` alone - a human-rejected approval-gate run sets `review` to a one-line rejection notice ("Rejected by user during approval gate review.") with `files: []`, which `generate` correctly suppresses but my first pass at `fix`'s reprint would have mislabeled as a "Reviewer Report". Fixed by adding the same `res.get("files")` guard; new regression test (`test_fix_does_not_mislabel_a_human_rejection_as_a_reviewer_report`) locks this in.
  6. **Assessed, not fixed - `REVIEW_EXTENSIONS = (".py", ".java", ".xml", ".rb")` is narrow, but consistent with Kriya's actual supported stacks (Java/Ruby/Python auto-detection), not an arbitrary oversight.** A TypeScript/Go/Rust project pointed at `kriya review` gets silently incomplete coverage with no "N files skipped by extension" notice - real but low-value to fix given nothing else in Kriya supports those stacks either. Left open, low priority.
  - **Reviewer explicitly confirmed genuinely solid**: model/chain threading identical across both call sites, git rename/copy `-z` status parsing (verified live against real git output, not just trusted), and the batch-packing design (a single oversized file never spans batches).
  - Independent reviews for all of the above: fresh subagents, no shared context with the author, per standing user instruction to always review as a third party rather than self-assess.
- **CLOSED: durable "verified project facts" ledger (2026-08-11)**. Turned out to already exist (Autonomous Skill Accrual / lesson extraction, `skills/auto-<repo-slug>/staged_rules.txt` - per-project, not per-library, exactly as this item asked for) but was gated on `state.last_model_override and chain`, so it only ever fired for a fallback-model escalation rescue - never for an ordinary primary-model retry that self-corrected, and never at all for a project with no `llm_chain` configured, meaning most real projects could never accrue a lesson regardless of how hard-won the fix was. A first fix (bare `error_context` truthiness) was too broad - fires on literally any retry-then-succeed run, confirmed by ~20 broken tests, and would flood staged rules with noise from trivial single-retry fixes. Settled on `state.last_model_override or state.budgets.retry_count >= 2` - fires for escalation (unchanged) or a primary-model run needing 2+ full-set rewrites, a genuine "this was actually hard" bar. 2 new tests plus 4 existing tests updated for the new expected call. See `docs/design.md` §2.10 for the full writeup.
- **Extending `extract_error_search_terms()` to more compiler error shapes** - only 2 shapes handled (coordinate, wrong-import). Partially superseded by the newer, more general file:line:col source-context mechanism, but not fully closed - explicitly requested to stay logged as backlog.
- **Model-mixing within the Developer pipeline** - json_mode for large/full-file content (proven reliable across every model tested), native tool-calling specifically for small anchored-edit-sized targeted fixes (reliable for 2/3 models even on large-argument tests). Real, evidence-backed, not implemented.
- **Standalone `kriya skills audit` command** - explicitly deferred multiple times.
- **Full containerized (Docker) isolation** for quality-gate execution - explicitly out of scope; process-level sandboxing (env allowlist + resource limits) is what exists today.
- **mypy/type-checking** - deferred pending a concrete pain point.
- **Goal-level semantic requirements aren't enforced by compile/test/LSP grounding (2026-08-03)** - found live: a milestone goal explicitly said "consume it back synchronously" (a JMS message), but the model's generated code never called a real `MessageConsumer`/`receive()` at all - it just reused the original locally-created object's own getters as if that were the received value. Different retry attempts papered over this differently: one attempt referenced the original object from a method scope where it didn't exist (a real, LSP-catchable "cannot find symbol" compile error), another attempt's cache lookup simply returned nothing at runtime (a real, RUN VERIFICATION-catchable failure) - but neither compile checks, LSP grounding, nor lint flags can ever catch "you skipped an explicitly-requested step but produced plausible-looking code around its absence," because the code that exists is syntactically/type-correct. This is a different failure class from every other one logged so far (missing instruction, wrong/ignored fact, lost self-diagnosis) - a genuinely missing implementation step masked by a workaround. Not fully fixed (see the retry-loop fix below, which addresses WHY the model got zero course-correction across 3 identical attempts, not the underlying model behavior itself) - would still benefit from a goal-completion-specific reviewer check distinct from the general code-quality Reviewer pass.
- **LSP grounding still gave zero log trace on a real, live-reproducible catch - fixed (2026-08-03)** - the very next cold-start run after the debug→warning visibility fix had a real "cannot find symbol: class Logger" (missing imports) compile error, exactly the shape LSP grounding is built for - and again, zero "jdtls"/"LSP" trace anywhere in the log. Re-ran the real production functions directly against the actual broken file: jdtls started in 1.6s and correctly caught every missing-import diagnostic. So the WARNING-level fix wasn't enough - a successful check that finds zero diagnostics produces zero log output BY DESIGN (not a failure), which meant "never engaged this run" and "engaged, found nothing" were still indistinguishable. Fixed: `_get_or_start_jdtls_client()` now logs a one-time INFO note on both the not-found path (previously fully silent) and the successful-start path (previously silent on success) - a run's log can now affirmatively confirm LSP grounding was active. Still open: whether jdtls was actually invoked during that specific cold-start run remains unconfirmed - this closes the observability gap for the NEXT run, not a retroactive diagnosis of this one.
- **Same symptom, different root cause across two different cold-start runs (2026-08-03)** - "No Person found in cache" recurred in a second cold-start run, but the actual generated code was completely different this time: a real `MessageConsumer.receive()` call WAS present and correctly threaded into the cache write (unlike the earlier run's missing-receive bug) - the actual bug was `storeInIgniteCache()` and `readAndLogFromCache()` each independently calling `Ignition.start()` inside their own try-with-resources block, so the write's node auto-closed before the read's brand-new node ever existed - two separate, non-communicating Ignite instances. Unlike the missing-receive() case, this IS the kind of fact a skill rule can catch (a concrete "don't do X" pattern, not "did you skip an entire step") - added to `skills/ignite-java17/rules.txt`. Reinforces the existing "not every wrong case needs the same fix" lesson: two failures with an identical user-visible symptom had entirely different root causes and entirely different appropriate fixes.
- **Anchored-edit parsing swallowed a redundant trailing FILE CONTENT: block into the applied patch - fixed (2026-08-04)** - M1 (previously 100% reliable across every prior run) failed for the first time in a manual overnight re-run. Root-caused: attempt 1 hit the classic `org.apache.ignite.cache.IgniteCache` wrong-import bug; attempt 2's retry (with LSP grounding active, confirmed via the INFO trace) correctly self-diagnosed AND correctly fixed it via a clean 3-line SEARCH/REPLACE - but ALSO appended a redundant, unasked-for FILE CONTENT: block after it. `_split_fix_analysis_edit()`'s `replace_block = text[replace_match.end():].strip()` had no bound at a subsequent "file content:" marker, so the entire redundant full-file text (plus the literal "FILE CONTENT:" string) got swallowed into the applied patch, duplicating the package/import/class declaration mid-file and producing "class, interface, enum, or record expected". That corruption then cascaded: every remaining attempt (3-6) failed with "Anchor matching failed... matched 0 times" against the now-unexpectedly-shaped file, burning the entire retry budget on a self-inflicted parsing bug rather than a real model or environment issue. Genuinely reassuring finding buried in a bad outcome: the model's own SEARCH/REPLACE diagnosis was entirely correct - LSP grounding and the Ignite skill rule both did their job; Kriya's own parsing corrupted a good answer. Fixed: truncate `replace_block` at any trailing "file content:" marker. Confirmed failing pre-fix (reproduced the exact real response shape), passing post-fix.
- **Anchored-edit SEARCH/REPLACE blocks sometimes contained Kriya's own copied error-source gutter formatting - fixed (2026-08-04)** - found live in the very next validation run after the FILE CONTENT truncation fix above (same failure class, different mechanism). `_build_error_source_context()` shows the model real source lines prefixed with `>> N: ` (the reported error line) or `  N: ` (surrounding context), so the model can see exactly what's broken - but a real response copied that display formatting directly into its SEARCH block (`>> import org.apache.ignite.cache.IgniteCache;`, keeping the `>>` marker, dropping the line number). That text can never match the real file's plain `import ...;` line, guaranteeing "Anchor matching failed... matched 0 times" regardless of whether the model's intended fix was otherwise correct - a self-inflicted retry-budget waste, not a model reasoning failure. Fixed: `_split_fix_analysis_edit()` now strips both `>>`/line-number gutter prefixes and wrapping ```lang fences from SEARCH/REPLACE blocks before they're used, matching the exact format `_build_error_source_context()` itself emits (safe against false positives - no real source line legitimately starts with `>>`, and the non-highlighted gutter form is only stripped when followed by a colon-terminated line number, never just an ordinary 2-space indent). A subtle ordering bug surfaced while testing this: the pre-existing `.strip()` on the raw extracted block ran before gutter-stripping and silently ate a "  N: " gutter's leading spaces on whichever line landed first - fixed by deferring all stripping until after gutter-removal. 3 new regression tests (real gutter shape, line-numbered gutter shape, confirms ordinary indented code is untouched), all confirmed failing pre-fix, passing post-fix.
- **Three compounding anchored-edit/retry-loop gaps found via `ignite_qpid_person` intermittently flipping pass/fail across an identical goal - all fixed (2026-08-07)**. Investigation started from the getBean-by-type bug fix (see the ignite-java17 skill entry above) but continued once the user asked "why did it pass 5 times in a row, then fail again" - the answer turned out not to be a regression at all. Checked the cache-read code across 3 sampled PASSING runs (130621, 153420, 162827): every single one hit the IDENTICAL first-attempt compile error (`var cache = ignite.cache(CACHE_NAME); ... Person person = cache.get(1);` - raw/erased generics, `incompatible types: Object cannot be converted to Person`) as the failing run did. What differed wasn't the bug occurring - it was whether the model's ONE-SHOT targeted-retry self-correction happened to land. In the passing runs it did (`Person cachedPerson = (Person) cache.get(1);` - correct cast, one retry, done). In the failing run (20260807-193054) the model's own FIX ANALYSIS said "properly handle the generic types" but the actual diff only renamed `ignite.cache()` to `ignite.getOrCreateCache()` - a plausible-sounding but incomplete self-diagnosis that never touched the actually-reported line - and the identical error recurred, cascading into the anchor-mismatch/fallback-model spiral. Three fixes shipped together, all in `kriya/agents/agent.py`/`kriya/workflow/workflow.py`, all regression-tested against the real captured log content (not synthetic reproductions):
  1. **Multi-pair SEARCH/REPLACE corruption** (a DIFFERENT, previously-unfixed gap from the 2026-08-04 FILE CONTENT-truncation fix above): a real response for `MainApp.java` (a separate `ignite_qpid_person` run, 20260807-190127) returned THREE separate SEARCH/REPLACE pairs for one file despite the prompt saying "include only the lines that actually need to change" (singular) - `_split_fix_analysis_edit()` only ever recognized the FIRST pair and folded pairs 2/3 (markers and all) verbatim into pair 1's own replace text, so applying that "edit" spliced literal `SEARCH:`/`REPLACE:` strings and duplicate code into the file. `apply_anchored_edits()` already applies a LIST of edits in sequence (confirmed by reading it, not assumed) - fixed by actually parsing and returning every pair as its own edit instead of corrupting the first one with the rest.
  2. **General "incompatible types" prompt scaffold**: rather than a narrow Ignite skill rule, added a language-level instruction (`DeveloperAgent._build_incompatible_types_scaffold()`) that fires whenever `prior_error_context` matches javac's `incompatible types: X cannot be converted to Y` shape - names the actual X/Y from the real error text and states the only two universally-correct fixes (an explicit cast, or explicit generic type parameters instead of `var`/raw types), explicitly forbidding an unrelated change from counting as the fix. Generic across any library with the same erasure footgun, not Ignite-specific.
  3. **Layer 1: locator-touch pre-flight check** (`find_edits_ignoring_reported_line()`, new in `kriya/workflow/workflow.py`) - the highest-leverage fix, and the one that would have caught the 193054 failure outright even without fix #2 working. javac's error already names the exact `file:[line,col]` where the problem is (the same locator `extract_error_source_locations()`/`_build_error_source_context()` already parse). After an edit is applied but BEFORE the expensive compile-and-fail cycle spends itself confirming it didn't work, checks whether any edit whose own SEARCH block already spanned the reported line left that exact line byte-identical between SEARCH and REPLACE - a model contradicting its own claimed scope, not just "the file didn't change." Deliberately narrower than "did the file change anywhere": an edit whose search block never spans the reported line at all is a legitimate alternative fix (e.g. correcting a variable's generic-typed declaration several lines above instead of its usage site) and is never flagged - verified against exactly this case in a test, so the check can't regress into rejecting valid fixes. On a match, raises a new `Failure.type == "unaddressed_error_location"` BEFORE writing to disk (saving the compile round-trip entirely) with a message that both names the ignored line(s) precisely AND preserves the original error's `file:[line,col]` locator verbatim (so a subsequent attempt's source-context grounding and this same check don't lose their anchor) - and is included in the repeated-failure signature live lookup escalates on, so a model stuck on the same unaddressed location twice reaches live lookup on the same schedule a repeated compile failure would, not a slower one. 7 new tests across both files (multi-pair parsing incl. a real captured 3-pair reproduction, the scaffold's dedup + wiring into the actual prompt, the locator-touch check's true-positive/false-positive/no-locator cases, and a full end-to-end workflow test asserting the compile mock is called exactly twice - proving the rejected attempt never reached it).
- **New eval-harness goal (`ignite_qpid_protocol`) found a genuinely new failure class on its first run, and confirmed Layer 1 generalizes to runtime exceptions - scaffolded, fixed (2026-08-08)**. Added deliberately to combine two previously, independently-proven components (Ignite+Qpid orchestration; a hand-rolled binary `Protocol`/`ProtocolParser` wire format) in a combination never exercised before, specifically to avoid over-reading the prior batches' clean streaks (see the "identical goal intermittently passing/failing is not evidence of a regression" lesson below - the same caution applies to reading too much into a streak on a *fixed* goal set). First real run surfaced a `java.nio.BufferOverflowException` in the model's own `encode()` - unrelated to Ignite/Qpid, a genuinely new class. The model's own FIX ANALYSIS correctly diagnosed it in words but the diff never touched the reported line - caught by Layer 1, which had never been built or tested against a *runtime* exception before, only compile errors. It fired correctly anyway, because it sits on `extract_error_source_locations()`, which already recognizes a JVM stack trace's `(File.java:line)` shape from an unrelated earlier fix (2026-08-07) - two fixes composing correctly without either being written with the other in mind. Fixed with the same shape as the incompatible-types scaffold: `DeveloperAgent._build_buffer_capacity_scaffold()` fires on `java.nio.Buffer{Overflow,Underflow}Exception`, names the two things that must both be checked (non-standard field widths need manual bit-shifting, not a fixed-width `ByteBuffer` primitive; the allocated buffer size must equal the real summed wire width), and forbids a partial fix from counting. Deliberately general, not scoped to this one goal - confirmed relevant via a SECOND, independent, unrelated live occurrence of the sibling exception (`BufferUnderflowException` in `kriya-protocol-parser-app`'s own hand-rolled decode(), found 2026-08-07, well before this goal existed) showing the same root cause recurs across genuinely different codebases. 5 new tests, one verified directly against the real captured stack trace. See `docs/design.md` §2.4 for the fuller writeup.
- **CLOSED: `spikes/fix_alignment/` real batch run, nudge promoted to always-on (2026-08-08 built, 2026-08-10 run + wired)**. Built to measure the "correct diagnosis, failed execution" pattern directly with real repeated LLM calls (two byte-faithful fixtures, real `DeveloperAgent.run_generation()` calls, real `apply_anchored_edits()` scoring against the resulting file). First real batch (40 calls, `qwen3-coder:30b`) found the self-consistency nudge closes the gap for a one-line fix (3/10 -> 1/10 diag-ok/exec-no) but does NOTHING for a multi-line manual bit-shifting fix (0/10 in both conditions) - see the durable lesson below for the full interpretation. Since the nudge never hurt and sometimes helped, promoted to always-on: `DeveloperAgent.SELF_CONSISTENCY_NUDGE` (`kriya/agents/agent.py`) is now passed as `extra_fix_instruction` at both of `kriya/workflow/workflow.py`'s retry-loop call sites where a fix-analysis is meaningful (targeted retry, full-set retry) - not gated behind a config flag. The missing-file-recovery call site deliberately does not pass it (never sets `prior_error_context`, so it would be a no-op there). 3 new workflow-level tests confirming the nudge reaches both wired call sites and is correctly absent from the third. Full findings in `spikes/fix_alignment/README.md`.
- **A one-shot fallback-model targeted fix, tried before the expensive full-set regeneration (2026-08-10)** - once the primary-model targeted budget (deliberately non-escalating - a measured 19-43s Ollama model-swap cost made "swap on every targeted attempt" a bad trade) exhausts, the only remaining option was a full-set regeneration: every file rewritten, a fresh Step 1 file-list call. Confirmed live it took ~13 minutes rewriting 6 files when only 2 actually needed fixing. Since a full-set escalation already pays a model-swap cost anyway, trying ONE targeted fix on that same fallback model first spends a cost that was coming regardless on a much cheaper shot. Implemented as a genuinely separate one-shot step (`use_fallback_targeted`, `kriya/workflow/workflow.py`) counting against neither the targeted budget nor the full-set budget - reuses the exact same targeted-retry prompt shape, just on `chain[0]` instead of the primary model. Found and fixed a related pre-existing imprecision while testing it: the lesson-extraction trigger was gated on `retry_count > 0 and chain` (really means "some full-set attempt failed earlier in this run," not "this successful attempt used a fallback"), tightened to `model_override and chain`. 3 new tests plus one pre-existing test (`test_workflow_fallback_chain`) updated for the new intermediate step in its scenario.
- **`RunVerifierAgent.grade()` could hallucinate its OWN "expected" value and reject genuinely correct code on that basis - with real downstream regression risk (2026-08-10, prompt-fix recurred and was superseded 2026-08-11)** - found live during a staged (incremental) build test: a round-trip program printed `dataLength=15, bodyLength=15, equals=true` (a real self-check the program performed on real decoded data, which passed), but the grader rejected it, claiming the body string "Hello, Protocol" should be 13 bytes - verified directly (`len("Hello, Protocol".encode("utf-8"))` is 15), the grader's own arithmetic was simply wrong, and the code had been correct since attempt 1. Worse than a wasted retry: the Developer model's own next fix-analysis nearly caught the real problem ("I think there's a misunderstanding in my analysis... the issue is not in my code") before deferring to the grader's reported "error" over its own correct reasoning, then spent several attempts actively breaking already-correct encode/decode logic chasing the phantom bug. First fix (2026-08-10): the grader's system prompt now instructs it to treat a program's own printed self-verification (`equals=true`/`MATCH`/`PASS`) as strong, primary evidence, and not independently recompute an expected numeric value that could contradict it - deliberately a prompt instruction, not a hard bypass, since trusting any printed `equals=true` unconditionally would risk masking a genuinely broken comparison implementation elsewhere.

  **That prompt fix did not hold** - confirmed live on a re-run: with the instruction verifiably reaching the prompt, `grade()` independently hallucinated the identical wrong byte-length value again and rejected genuinely correct code a second time. A prompt instruction can only tell the grader to weight the program's self-check more heavily - it still has to correctly *identify* that self-check inside flattened captured stdout, which is itself a re-derivation the grader can get wrong. Superseded (2026-08-11) with a verification-contract approach that removes the LLM from this decision for any goal that complies: a new standing Developer prompt convention, `VERIFICATION_CONTRACT_HEADER` (`kriya/workflow/retry_prompts.py`), asks the generated entrypoint to print a `[VERIFICATION] PASS`/`[VERIFICATION] FAIL: <reason>` verdict line based on a real comparison it performs itself; a new deterministic function, `extract_contract_verdict()` (`kriya/workflow/verification_contract.py`), regex-scans captured Runtime Verification output for that marker and, when present, returns a verdict directly with zero LLM involvement - `grade()` is only called when no marker is present, so a goal whose generated code doesn't comply gets exactly today's (still-improved) behavior. See `docs/design.md` §2.7 for the full writeup; 8 new tests (6 unit, 2 workflow-level proving `grade()` is never invoked when the marker is present, for both PASS and FAIL). Not yet re-validated against the real live scenario that found this bug in the first place - that's the next live run.
- **`spikes/eval_harness/run_harness.py` never configured `search.base_url`, so every harness run's `autonomy.web_lookup_enabled: true` was silently a no-op - fixed (2026-08-07)**. Flagged (not fixed) in the anchored-edit/retry-loop entry above; the user asked for the fix as an explicit follow-up. `kriya/config/config.py::SearchConfig` deliberately makes `autonomy.web_lookup_enabled` and `search.base_url` two separate switches specifically "so a config merge/copy-paste can't silently enable outbound search" - `_write_config()` set the first from the very first version of this file but never the second, so the retry loop's error-triggered live-lookup gate (`kriya/workflow/workflow.py`, `and self.kernel.config.search.base_url`) was false on every single batch run to date, and no root `kriya.yaml` in this repo set it either. Fixed: a new `--search-base-url` flag (`KRIYA_LIVE_SEARCH_BASE_URL` env override), defaulting to `http://localhost:8080` - the same self-hosted SearXNG convention `docs/user_guide.md` §4.6 already documents and was live-tested against, not a guess. Verified end-to-end against the REAL `AppConfig` loader (not just that the YAML looks right): `search.base_url` and both `autonomy` switches all resolve correctly through `load_config()` from a `_write_config()`-generated file. No SearXNG instance is actually running on this machine right now (confirmed via a direct connection check) - the fix makes live lookup reach for the network again, but an actual search backend still needs to be stood up (`docker run -d -p 8080:8080 searxng/searxng`) for it to find anything; `search_web()` already degrades safely either way (empty base_url is a no-op, a connection failure is a logged WARNING + empty results, never a crash), so this default can't break a run that doesn't have one.
- **`kriya generate -y`'s double `run_generation_workflow()` invocation on a knowledge-gap goal could leave a misleading trace row behind - fixed (2026-08-07)**. Root-caused first, then fixed once asked to: `kriya generate -y` calls `run_generation_workflow()` TWICE when a knowledge gap fires - once that hits the gap and returns immediately (writing a `status=knowledge_gap` trace row), then the CLI auto-confirms and calls it AGAIN with `knowledge_risk_confirmed=True` - the second call is the real run. Confirmed directly from a real batch's stdout log: after "[Auto-Confirming knowledge risk]", the second call ran Planner/Architect/Developer for real (started both Ignite and an embedded Qpid broker, sent/received a JMS message, printed `[RESULT] Person successfully processed and stored`), then hung and was killed by Kriya's own 90s Runtime Verification timeout, triggering fallback-model escalation that pushed total wall time past the harness's `--timeout-per-goal` (1200s default) - which killed the subprocess before the second call's own final trace row was ever written, leaving only the harmless first row (`knowledge_gap`, <1s) behind. `report.py` only ever sees whichever row happened to persist, so a `knowledge_gap`-labeled row in a report can be an artifact of exactly this race, not the real failure reason.

  Fixed without touching WHEN or WHERE either call writes its trace row (the lowest-risk option considered - see the design discussion in `docs/design.md` §4.4 for the alternatives weighed and why this one was chosen): `run_generation_workflow()` gained a new, purely additive `trace_id_override` parameter (defaults to `None`, preserving today's exact "fresh uuid every call" behavior for every other caller - `fix`, tests, and any future direct caller are all unaffected). `kriya/cli.py`'s two retry call sites (permissive/fully-acked, and warn-then-confirmed) now pass the FIRST call's own returned `run_id` back in as `trace_id_override` on the retry. Since `traces.db`'s `run_id` is the `runs` table's PRIMARY KEY and `TraceLogger.log_run()` already does `INSERT OR REPLACE` (confirmed by reading it, not assumed - and directly verified with a standalone repro: two `log_run()` calls sharing a `run_id` produce exactly one surviving row, the later write winning), the retry's own eventual real trace row cleanly supersedes the transient `knowledge_gap` one instead of leaving two independent rows behind - and if the retry is itself killed before writing (the exact harness-timeout scenario above), the original row simply remains, no worse than today's behavior. 1 new end-to-end test (mocks the exact CLI double-call pattern, asserts exactly one row survives with the retry's real final status, not `knowledge_gap`) plus confirmed the existing single-call `test_workflow_traces_knowledge_gap` (asserting the FIRST row's own correctness) is untouched and still passes unchanged. `fix` (the other `generate`-adjacent command) never hits this code path at all - confirmed via grep, it calls `run_generation_workflow()` exactly once, no knowledge-gap handling.
- **Fallback-model context-window overflow root-caused and fixed (2026-08-07)** - the remaining half of items (1)/(3) from the `ignite_qpid_person` triage. `deepseek-coder-v2:16b` (16384-token window) hit real `400 - prompt is longer than the context length` errors from the inference server, plus a truncated/malformed anchored edit on the very next retry against the same fallback. Root cause, confirmed via direct measurement: every retry path computed the graph-RAG code-context budget as a flat `context_window * 0.75` fraction, then separately prepended `skills_prompt` (this goal's 5 active skills' rules/instructions) on top - unaccounted for, and measured at ~6800 tokens, over half the fallback's entire 12288-token budget before any code context was even added. See `docs/design.md` §2.5 for the full writeup and the `_reserve_graph_context_budget()` fix. The malformed-`"REPLACE:"`-literal edit symptom from the original triage note couldn't be independently re-located in the available logs to confirm byte-for-byte, but is mechanistically consistent with the same root cause (a model producing degenerate/truncated output when starved of real completion headroom by an oversized prompt) - flagged as the likely explanation, not proven with the same certainty as the 400 errors.
- **Ignite Method B (Spring-managed IgniteSpringBean) resource-lifecycle hang - fixed (2026-08-07)**, confirmed as a genuine, distinct recurrence of the "prints correct output, then hangs" bug class (2026-08-04 entry above), not the same cause repeating - that entry's fix and skill-rule guidance were written entirely around Method A (`Ignition.start()`/`ignite.close()`); Method B was never covered at all. Root cause, found directly in a real `ignite_qpid_person` run (20260807-203804) after `RunVerifierAgent`'s own non-binary grading correctly flagged it as a resource-lifecycle defect (not a wrong-output one - "the goal's described output WAS produced correctly, but the process never exited on its own"): the generated `main()` declared its Spring context as the bare `ApplicationContext` interface and never closed it. A second, sharper finding while root-causing this: the skill's OWN existing example code (`skills/ignite-java17/rules.txt`, `instructions.md`) declared that same variable as `ApplicationContext` inside a `try (ApplicationContext context = ...)` block - which **does not compile**, since `ApplicationContext` doesn't extend `Closeable`/`AutoCloseable` (only `ConfigurableApplicationContext` does). A model hitting that compile error while otherwise correctly following the skill's own template would rationally drop the try-with-resources to keep the code building - and then never close the context by any other means either, since the skill never showed one. Fixed: both files now declare `ConfigurableApplicationContext` (compiles, and is genuinely closeable), wrapped in try-with-resources; the resource-lifecycle rule extended with an explicit paragraph covering Method B specifically (closing the SPRING CONTEXT via `context.close()`, not `ignite.close()` directly - the intended mechanism differs between the two approaches even though the underlying symptom is identical). Same investigation this run also surfaced a real, still-open, out-of-scope finding: the same log's earlier retries hit a DIFFERENT "Ignite instance already been started" variant (a helper method re-instantiating its own `ClassPathXmlApplicationContext` instead of receiving the already-created one as a parameter) - the run's own Layer 1 pre-flight check (see the anchored-edit entry above) correctly caught and rejected a non-fix for it mid-run, a live confirmation that fix is working as designed, separate from the fix documented here.
- **Root-caused the "silent exit code 1 right after Qpid Broker Ready" mystery (2026-08-04)** - reproduced the exact failing attempt in a real standalone Maven project (not mocked) to get the real, unfiltered stderr Kriya's own captured output was missing. Real cause: the model's hand-rolled JSON serialization (`"{\"name\":\"" + name + "\",\"age\":" + age + "}"`) parsed back via unbounded `substring(indexOf(...) + N)` calls - the age field's substring has no upper bound, so it always includes the trailing `}`, and `Integer.parseInt("1}")` throws `NumberFormatException` on literally the first message every time (not an edge case - the last field is always followed by the closing brace). Confirmed this is a genuine model-generated code bug, not a Kriya bug - added a concrete rule to `skills/qpid/rules.txt` recommending Jackson's `ObjectMapper` (or explicit substring bounds) instead of unbounded ad-hoc string parsing. Separately confirmed Kriya's own output-capture mechanism (including yesterday's process-group fix) correctly captures this exact exception when re-tested directly - the original run's missing exception text looks like an intermittent capture race, not a deterministic bug; not chased further given no reliable repro.
- **Root-caused the "app finishes all work, prints the correct result, then times out anyway" mystery (2026-08-04)** - the model's own fix-analysis blamed a slow Qpid broker shutdown; verified this directly instead of trusting it. Reproduced with a minimal, isolated test: a bare `SystemLauncher.startup()`/`.shutdown()` with no Ignite involved exits cleanly in ~1s. Adding an `Ignite ignite = Ignition.start()` that's never explicitly closed reproduced the exact hang - the process printed everything expected (including the shutdown-returned marker) and then hung indefinitely, confirmed by letting it run past a 30s timeout with nothing further happening. Real cause: yesterday's node-lifecycle fix (share ONE Ignite instance across methods instead of starting a new one per operation) correctly fixed the "separate nodes, read returns null" bug, but left the natural follow-on question - "closed how, and when?" - unaddressed, and the generated code that surfaced this never explicitly closed the shared instance. Not a Kriya bug; a real, reproducible Ignite lifecycle gotcha (background threads keep the JVM alive) that the retry loop was burning budget on for the wrong diagnosis (the model kept trying to add Maven exec-plugin timeout settings, which can't fix a genuine application hang). Updated the same skill rule from yesterday to cover both halves: start once, AND close exactly once (try-with-resources wrapping the whole main() body, or an explicit `ignite.close()` alongside the Qpid shutdown call).
- **Timed-out commands leaked child processes across entirely separate, later runs - fixed (2026-08-03)** - a "port in use" Runtime Verification failure in one validation folder was traced to a stray `java` process from a DIFFERENT, already-finished validation folder, still alive and holding the port over an hour later. Root cause: `_run_cmd_with_timeout()` used `subprocess.run(..., timeout=...)`, whose built-in timeout handling only kills the DIRECT child process. `mvn exec:exec` forks its own separate `java` process to actually run the app (the entire point of exec:exec over exec:java) - killing `mvn` on timeout left that `java` child (and the embedded Qpid broker it started) running indefinitely, orphaned, in the background. Fixed: the subprocess is now started with `start_new_session=True` (POSIX `setsid`, putting it in its own process group) and a timeout now kills the WHOLE process group (`os.killpg(..., SIGKILL)`), not just the direct child. New regression test spawns a real child process from within the timed-out command and confirms it actually stops (a heartbeat file stops updating) rather than surviving orphaned - confirmed failing pre-fix (child kept running for the full duration) and passing post-fix.
- **Runtime Verification failures got weaker retry-loop treatment than compile failures - fixed (2026-08-03)** - root-caused why the same model, same goal, same prompt failed 3 consecutive attempts with zero self-correction: `extract_implicated_files()` scopes a retry by finding a known file's basename literally inside the failure text. A compile error always contains that (`file:[line,col]`). A Runtime Verification failure's text is an LLM judge's prose plus captured stdout (broker banners, SLF4J lines with no `.java` suffix) - confirmed by inspecting the actual captured output from the real failing run, no filename ever appears. So `last_implicated_files` came back empty for every runtime failure, which silently disabled the FIX ANALYSIS forcing instruction, the anchored-edit preference, and file-scoped LSP grounding - for this entire failure class, on every project, regardless of any skill (this is the general, cold-start-friendly fix, as opposed to a narrow per-domain skill rule). Fixed: `RunVerifierAgent.grade()` now also receives `files_written` and returns a `likely_files` field (validated against the real file list - same trust-boundary pattern as the skill-conflict-checker's index validation), which gets folded into the raised failure text ("Likely responsible file(s): ...") - the existing basename-matching mechanism picks it up with no other plumbing needed, so the file-scoped retry, FIX ANALYSIS, and LSP grounding all now apply to runtime-verification failures the same way they already did for compile failures.
- **Breadth beyond Java/Maven** - real compile/test/LSP support only exists for Java (Maven/Gradle + jdtls), Ruby, and Python; anything else (most obviously Node/TypeScript, given prevalence) gets `"unknown"`-stack handling (honest "not validated" skip, see the false-positive fix above) rather than an actual quality gate. Closing this for one more stack means three separate pieces of work: real compile/test invocation (`PolymorphicValidator`), an LSP client for that stack's language server (mirroring `kriya/tools/lsp.py`'s jdtls client, e.g. `typescript-language-server`), and dependency-checklist/regression logic for that stack's manifest (mirroring `get_pom_dependencies()` for `package.json`). Not started - scope as its own increment, not a quick fix.
- **Rule-level verification granularity** - a passing Runtime Verification run marks *every* rule in the active-context snapshot verified, not just the ones the generated code actually exercised. Known limitation of the Gap 6 skill-trust design, not fixed.
- **Remaining items from the second (prompt-quality) external review, 2026-08-15/16 - assessed, none currently forced by evidence.** Six of the review's ten numbered recommendations are done (§7.14, §7.15, §7.19) or explicitly closed as not needed after live evidence (the "exactly two fixes" wording turned out fine once the illegal-combination gap, §7.15, was closed). What's left, each with an honest verdict on whether it's actually pushing us to act now:
  - **Whole-prompt budgeting beyond graph-RAG/sibling-content** - the one item with REAL, repeated live evidence behind it: `"Escalating compilation attempt to fallback model: qwen3.6:35b-a3b-q4_K_M (Limit: 1000 tokens)"` showed up multiple times across `b-7`/`b-9`/`b-10`/`b-10a` - skill-prompt bloat eating almost the entire graph-RAG budget on the fallback model, every escalation. Confirmed as a real, current CONDITION - not yet confirmed as the direct CAUSE of any single specific failure this session (each of today's 8 fixes had its own more specific, already-identified root cause instead). A genuinely bigger, riskier change than anything shipped today - touches how the whole context-assembly budget gets allocated across every retry path, not a single function. Worth its own dedicated scoping session, not a reactive tack-on. Not started.
  - **Structured/typed validation for `edits`/`content` shapes** - extends the Finding-5 `_coerce_bool_field` philosophy beyond booleans. No live incident behind this one at all - `apply_anchored_edits()`'s own uniqueness-match requirement already fails loudly on a malformed edit dict today. Theoretical/preventive, not evidenced. Not started.
  - **Shorten Planner/Architect prompts** - reasonable in principle, no live incident forcing it. Not started.
  - **Stage-specific eval sets** (file-list completeness, JSON schema compliance, prompt-injection resistance, etc., run against multiple model tiers) - the biggest lift of anything on this list, genuinely valuable as infrastructure, not blocking anything today. Not started.
  - **Interface ledger replacing full sibling content** - explicitly assessed and declined earlier this session: the review's own proposed schema (methods only, no constructors) would regress the live-verified 2026-08-15 sibling-content fix, whose second half was exactly a wrong constructor call. Would need a materially more complete schema before reconsidering - not a "pick this up next" item as originally proposed.
  - **Reviewer verdict as a binding/structured gate** - already assessed and deliberately deferred back in the stage-6 per-agent SME review, before this second review even ran: the Reviewer already runs before the approval decision and its text reaches the human approver; making the VERDICT ITSELF machine-parsed is a real feature addition for a CI/fully-autonomous use case, and "no one has hit this as an actual pain point" for the human-in-the-loop default still holds. Not started, not currently worth revisiting.

Resolved, NOT backlog (don't re-open without new evidence):
- **`ruby_word_count`'s goal wording was self-contradictory, same bug class as `python_task_tracker`'s "in-memory" issue (2026-08-07)** - regressed for the first time all session (`quality_gates_exhausted`, 6 attempts) after passing reliably since 2026-08-04, confirmed unrelated to anything shipped that day. Root cause read directly from the model's own first attempt: the goal asked for "a Gemfile (no external gems needed)" while ALSO requiring RSpec tests - self-contradictory, since rspec is itself an external gem. The model took the instruction literally (`# Empty file - no external gems needed`), then flailed through 6 attempts of increasingly confused fixes - including pinning `gem 'bundler', '~> 2.0'` in its own Gemfile, which then collided with this machine's older system bundler (`1.17.2`) - never reaching the real fix. Not a Kriya bug - fixed by rewording the goal (`spikes/eval_harness/goals.py`): "no external gems" now explicitly scopes to the library implementation only, and the Gemfile is explicitly told to declare `rspec` for the test suite. Not yet re-validated live.
- **`RepositoryAnalyzer` reported empty/Kriya-owned directories as real project structure, causing a genuine hallucination (2026-08-07)** - found live via `django_healthcheck_gap`: Architect's own reasoning explicitly said "There's a `skills` directory (Django app)... There's a `manage.py` in the root" in a fresh, empty repo - neither existed. Not a schema-validation bug (the file list itself validated cleanly, no fallback engaged) - a data-quality bug in the context feeding the model. Confirmed directly: `RepositoryAnalyzer.analyze()`'s `top_level_folders` added ANY directory it walked into, even a completely empty one, and the eval harness (like most real projects, by the same default config) materializes an empty `./skills` directory (`paths.skills`'s default) before generation starts - `repo_context` genuinely told the model `"top_level_folders": ["skills"]`. Fixed at the source rather than adding a corrective prompt instruction (the standard instinct for hallucination caused by bad retrieval/context - fix the pipeline, don't compensate for it): (1) a directory only counts as real project structure if it actually contains a file, not merely by being walked into; (2) Kriya's own reserved `skills`/`memory`/`logs` default directory names are now unconditionally excluded, even when they contain real content (they're Kriya's own bookkeeping, never the user's application). Verified no regression to existing architecture-detection tests (MVC detection only needs one of three marker directories present). 2 new tests in `tests/test_analyzer.py`. Known limitation: hardcoded default names, not threaded from a project's actual configured `paths.skills`, matching an existing precedent (`_has_any_py_file()`'s hardcoded `.kriya` exclusion).

**Follow-up, same day**: explicitly checked whether this traced back to the original global+project-specific skill-loading design, not just this one failure, per the user's request. It didn't - `active_skills` was already confirmed empty for the failing run (no skill content reached the prompt), and `SkillEngine.discover_and_load()` never creates `paths.skills` itself (gracefully skips it if missing; every write path under it creates it on demand via `os.makedirs(exist_ok=True)`). The empty `./skills` that actually triggered this was `spikes/eval_harness/run_harness.py`'s own `_write_config()` unconditionally pre-creating it - dead scaffolding, removed. This reframes the fix's real justification: a genuinely fresh, non-harness run would never have hit this specific empty-directory case at all; the durable value is the "exclude Kriya's reserved dirs regardless of content" half, which protects any real, iteratively-used project where these directories legitimately fill up with content after even one prior run. Not yet re-validated live.
- **Developer-stage eval harness + extracted `_resolve_step1_file_list()` (2026-08-07)** - the same per-stage eval pattern as `architect_eval.py`, one stage further: `spikes/eval_harness/developer_eval.py` runs Planner -> Architect -> Developer's Step-1 file-list query (no per-file content generation, no Quality Gates) and reports which of the 3 paths resolved the list (`contract`/`fallback`/`none`), plus overlap with Architect's own list as a basic consistency signal. Required extracting Step 1's inlined logic out of `run_generation()` into `DeveloperAgent._resolve_step1_file_list()` first - previously there was no way to observe WHICH path resolved the list without hooking logging, since `run_generation()` only ever returned the end result. The extraction preserves `run_generation()`'s exact prior behavior (confirmed: the try/except that used to wrap the whole Step-1 block now wraps the call to the extracted method instead, and the method internally catches `_extract_json_value()`'s raise-on-total-failure so it never raises itself either - same "always returns a tuple" contract `parse_file_list()` follows, with the only observable difference being one now-silent WARNING log line for the total-parse-failure case, which no test asserted on). 3 new direct tests for the extracted method's three return paths, alongside the 4 from the earlier Developer contract slice. **Re-validated live (2026-08-07)**: run against all 5 goals - 5/5 contract-validated, 0/5 fell back, 0/5 needed single-stage generation. Matches the Architect contract's own 100% hit rate exactly.
- **Developer stage contract - generalized the Architect file-list schema to a second, live call site (2026-08-07)** - before implementing, checked whether the Architect fix had made Developer's own file-list step redundant: it hadn't. `known_target_files` (sourced from Architect's list) is only supplied on attempt 1 and targeted/missing-file retries; on every FULL-SET retry (any compile/test failure not narrowly scoped - common, not an edge case) it's unset again, and `DeveloperAgent.run_generation()`'s own Step 1 "which files do I need" query fires for real - structurally the identical problem `kriya/agents/contracts.py`'s `FileList`/`parse_file_list()` already solved for Architect (a bare list of paths, previously recovered only by `_extract_json_value()`+`_normalize_file_entries()` with zero schema validation). Renamed `ArchitectFileList`/`parse_architect_file_list` to `FileList`/`parse_file_list` (mechanical, no behavior change) since the contract is now shared, not Architect-specific. Developer's system prompt now asks for the same `{"files": [...]}` shape; `parse_file_list()` is tried first against the raw response. Deliberately layered, not a replacement: `_extract_json_value()`/`_normalize_file_entries()` stay fully intact as a fallback, preserving a real existing behavior the stricter schema can't replicate on its own (a model that over-delivers full `{filepath, content}` objects to the file-*list* prompt gets that content used directly instead of being rejected for not matching a paths-only shape - confirmed by tracing every one of the ~15 existing `run_generation`-exercising tests in `tests/test_agents.py` against the new code path; none use the new `{"files": [...]}` shape, so all fall through to the unchanged old extraction exactly as before). Single-shot fallback (asking for full file objects with content in one blob) stays out of scope, unchanged - its own ad hoc normalization is separate, larger work. 4 new tests in `tests/test_agent_contracts.py` covering the new path succeeding, falling through for a bare array, falling through while still using over-delivered content, and confirming an unsafe path in the new shape gets rejected by the contract rather than silently passed through (still recovered via the old, non-validating fallback - same pre-existing behavior for that response shape, not a new gap). Not yet re-validated live.
- **Architect-stage-only eval harness (2026-08-07)** - the other half of the phase-wise-validation ask: not just a contract per stage, but a way to CHECK each stage independently without paying for the full pipeline. `spikes/eval_harness/architect_eval.py` runs only Planner -> Architect in-process for each `goals.py` goal (no `kriya generate` subprocess, no worktree, no compile/test/run-verification) and checks whether the structured JSON file list validated, plus whether a Maven/Gradle/Bundler-establishing goal actually got its manifest listed - the exact class `_detect_missing_build_manifest()` catches reactively, now checkable proactively in seconds instead of minutes. Reuses `goals.py`'s shared fixture set rather than duplicating it, so a regression here is directly comparable against a full-pipeline regression on the identical goal. No pytest tests written for it - matches the established, zero-test precedent for `run_harness.py`/`goals.py` (live-model-driven manual tools under `spikes/`, hand-validated in the user's own terminal, not part of Kriya's own package). Verified statically only (py_compile, ruff, `--help` from both the repo root and `spikes/eval_harness/`). Template for doing the same to Developer/Reviewer once they get contracts of their own. Not yet run live.
- **`ignite-java17` skill rule on Spring-managed Ignite startup was incomplete, letting the model combine two mutually-exclusive start mechanisms (2026-08-07)** - found live re-running `ignite_qpid_person`: attempt 1 crashed at runtime with `Ignite instance with this name has already been started: ignite-server-node`, and every subsequent retry hit the SAME crash or a new compile error, never converging. Root cause confirmed directly from the actual generated files: `applicationContext.xml` defined an `IgniteSpringBean` (auto-starts an Ignite node the instant the Spring context loads) AND `MainApp.java` separately called `Ignition.start("applicationContext.xml")` - which parses that same XML as a second, independent Spring context internally, auto-starting the bean again under the identical instance name. The model's own fix-analysis correctly diagnosed "the Ignite node is being started twice" on every single attempt, but the fix kept flip-flopping between reverting straight back to the exact `Ignition.start()` call that caused the crash, or switching to `context.getBean(...)` while leaving `context` itself never constructed (a new compile error) - never landing on one complete, correct implementation. The existing rule in `skills/ignite-java17/rules.txt` was the real gap: it listed "direct Ignition" vs. "Spring Bean" as two options without ever saying not to combine them, and its Spring-Bean example was a bare `context.getBean(...)` call with no `context` construction shown - exactly the two mistakes the model kept making. Fixed by rewriting the rule: explicit "use exactly one, never both" framing, plus the complete working code for the Spring-Bean path (the `ClassPathXmlApplicationContext` import/construction that kept getting dropped). Not a Kriya code change - a skill-content fix, matching the established precedent of strengthening a rule to be maximally explicit before ever reaching for a deterministic code-level correction. **Re-validated live same day**: re-ran the same goal, passed clean in 430s using only the primary model - no fallback escalation, no trace of the duplicate-start crash.

**The fix was incomplete - re-recurred days later as a DIFFERENT symptom, traced to two more stale references the original fix missed.** User's instinct, unprompted: "i think for this we have a skill or rule update this is not new. i believe this got somehow reintroduced with some fix... Check closely." A later `ignite_qpid_person` run failed with `NoSuchBeanDefinitionException: No qualifying bean of type 'org.apache.ignite.Ignite' available` - the by-TYPE lookup (`context.getBean(Ignite.class)`) the original fix explicitly replaced with a by-NAME lookup (`context.getBean("igniteNode")`). Grepped the whole repo rather than assuming the one rule already fixed was the only place this pattern lived: found it live in TWO more places the original fix never touched - `skills/ignite-java17/rules.txt` line 12 (a completely different rule, about variable-scoping discipline, whose own parenthetical example still said `context.getBean(Ignite.class)`) and `skills/ignite-java17/instructions.md`'s "Method B" code example (a complete, illustrative code sample - likely more influential to the model than a single rule line - still showing the by-type lookup verbatim). Both are actively rendered into the same prompt block as the already-fixed rule (`Skill.instructions`, loaded from `instructions.md`, gets folded into `convention_prompt` alongside `rules` - confirmed by reading `kriya/workflow/workflow.py`'s skill-prompt-assembly code, not assumed), so the model was being shown BOTH the corrected guidance and the old, contradicting example in the same generation call. Also found a third, non-live reference - `skills/qpid/rule_provenance.json` had an orphaned provenance record (dated 2026-07-31, `verified: false`, no longer matching any live rule in that skill's own `rules.txt`) still carrying the old by-type example; confirmed via grep it isn't served to the model, but corrected its text anyway so a future audit read isn't misled. Fixed all three references to consistently use the by-name lookup, and added the same "these are mutually exclusive, never combine them" framing to `instructions.md`'s "Method A/B" section that the rules.txt fix already had, since the instructions document's own "two valid ways" phrasing never carried that warning either. Not yet re-validated live.
- **Python had no per-project dependency install step, unlike Ruby (2026-08-07)** - found live immediately after the layout-invention fix cleared the way for `django_healthcheck_gap` to actually reach its test gate: every attempt failed with `ModuleNotFoundError: No module named 'django'`, confirmed structurally unwinnable since `PolymorphicValidator.run_tests()` ran pytest via `sys.executable` (Kriya's OWN interpreter) - no generated project ever got its own `requirements.txt` installed anywhere. The same class of gap Ruby's `bundle install` fix (2026-08-04) closed for that stack. Fixed: `PolymorphicValidator._ensure_project_venv()` (`kriya/tools/validate.py`) creates/reuses an ISOLATED project-local venv at `.kriya/venv` (deliberately not `sys.executable` directly - installing an arbitrary project's deps into Kriya's own environment risks breaking Kriya itself) and installs `requirements.txt` plus `pytest` into it whenever a real, non-empty requirements.txt exists; `run_tests()` runs pytest under that venv's interpreter instead. Mirrors Ruby's failure handling: a genuine `pip install` failure fails the gate (real dependency problem, retry loop should see it), but venv creation failing degrades silently to the old `sys.executable` behavior (infrastructure problem, not code-fixable). Deliberately scoped to `run_tests()` only - `run_app_sequence()`/Runtime Verification still isn't isolated, no live evidence yet that it needs to be. 5 new tests (`tests/test_polymorphic_validation.py`) mirroring the Ruby precedent's mocking style. **Re-validated live (2026-08-07), confirmed the predicted gap**: after the `RepositoryAnalyzer` fix let `django_healthcheck_gap` reach Runtime Verification for the first time, it hit the identical `No module named django` one gate later - `run_app_sequence()` never reused `run_tests()`'s own isolated venv. Fixed: shared resolution extracted into `PolymorphicValidator._resolve_python_interpreter()`, used by both `run_tests()` and a new `_substitute_python_interpreter()` that `run_app()`/`run_app_sequence()` call before executing any command - only rewrites a command's executable position (index 0) when it's literally `"python"`/`sys.executable`. Same failure handling as `run_tests()`: a genuine pip install failure fails the whole sequence immediately (every command would fail identically against a broken environment anyway), venv creation failure degrades silently to `sys.executable`. 3 new tests. Not yet re-validated live with this specific fix in place.
- **Architect file-list contract - first phase-wise structured-output slice (2026-08-07)** - prompted by an external architecture review's `FINDING-ARCH-002`/`ACTION-CONTRACT-001` ("architect file-list extraction based on text... coupled to heading syntax") and a broader discussion about validating each pipeline stage's output against a contract instead of only end-to-end. Checked what Planner's/Architect's output actually feeds downstream before picking a target: Planner's `plan` is NEVER parsed (pure prose, string-interpolated into later prompts only) - contract-izing it buys nothing today. Architect's `design` IS already load-bearing structured data in disguise: `extract_expected_files()` (a blanket regex over the whole design's prose matching any `word.ext` token, no way to distinguish a real requirement from an incidental mention) feeds `IncompleteGenerationError`'s completeness check and the Developer's upfront required-files checklist - directly implicated in the missing-pom.xml bug that took 3 sessions to root-cause. Architect was the higher-leverage target. Fixed: `kriya/agents/contracts.py` (new module) - `ArchitectFileList` Pydantic schema (non-empty, workspace-relative, no `..`/absolute paths) plus `parse_architect_file_list()` (extracts the LAST fenced or bare JSON `{"files": [...]}` block in the design, never raises). `ArchitectAgent.system_prompt` now requires this JSON block instead of the old `## Files to Create or Modify` markdown-bullet convention; `ArchitectAgent.run_with_file_list()` runs the design completion once and validates it - same call count as before, deliberately no corrective-retry call yet (its actual hit rate is unmeasured, and it would have shifted every one of the 93 `side_effect=[...]`-based tests in `test_workflow.py` by one position - premature complexity, not worth the blast radius before live data justifies it). On validation failure, degrades to the OLD heuristic (`extract_expected_files`/`_resolve_file_paths_from_design`, kept as this path's fallback, not removed) rather than failing the run. Also refactored the two consumption sites in `kriya/workflow/workflow.py` to use the structured/resolved file list directly (`known_target_files`, and a new `_architect_basename_to_path` lookup for missing-file recovery) instead of re-deriving paths from prose via regex on every use - collapsing two independent fragile regex passes into one validated call plus one deterministic fallback. Deliberately does NOT request schema-constrained decoding (`response_format: json_schema/strict`) - Kriya targets "Ollama or any OpenAI-compatible server," and whether a given backend actually enforces that is backend-dependent and unverifiable in general, so the Pydantic validation step stays the permanent, backend-agnostic floor regardless. 25 new tests (`tests/test_agent_contracts.py`) using realistic failure shapes observed this session (trailing chatty prose, an illustrative JSON snippet before the real list, single-quoted pseudo-JSON, adversarial/traversal paths), plus 2 new workflow-level tests - one demonstrating the actual value-add (a nested path never mentioned in prose, which the old heuristic would have gotten wrong) and one confirming the fallback still reproduces old behavior exactly. **Re-validated live (2026-08-07)**: full 5-goal eval-harness batch, zero fallback engagements across every goal including `ignite_qpid_person` (6 nested files, correct paths first try, no bare-basename guessing) - 100% structured-success rate on this batch. No evidence yet that the deferred corrective-retry is needed.
- **A bare `pytest` from the repo root swept up live-generated eval-harness output and failed collection entirely (2026-08-07)** - found live: after a `python_task_tracker` batch, running the suite failed immediately with `ModuleNotFoundError` on `spikes/eval_harness/runs/<batch>/workspaces/python_task_tracker/src/main/python/tests/test_store.py` - a real, live-generated file from the harness run, not a Kriya test. Root cause: `spikes/eval_harness/runs/` is gitignored (never reaches CI) but NOT excluded from pytest's own filesystem walk, and `pyproject.toml` had no `testpaths` restriction, so any `test_*.py`-named file anywhere under the repo got swept into collection - including whatever a live model happened to generate, in whatever layout it happened to invent, which naturally can't import cleanly relative to Kriya's own test suite. `"Interrupted: 1 error during collection"` meant zero real tests ran at all, not just one failure. Fixed at the root instead of chasing this one directory: `testpaths = ["tests"]` in `pyproject.toml` scopes default collection to where Kriya's own suite actually lives (matches the two documented `pytest`/`pytest tests/test_workflow.py` commands in `CLAUDE.md`) - explicit paths on the command line still override it as before, `live_model`-marked tests still resolve since they already live under `tests/`. Confirmed via `pytest --collect-only`: 611/614 collected cleanly post-fix, versus a hard collection-abort pre-fix.
- **JDK toolchain mismatch was only ever warned about, never actually corrected (2026-08-07)** - `maven.compiler.source/target` in `pom.xml` controls the Java *language* version javac targets, not which JDK `mvn` itself runs under; a goal explicitly saying "targeting Java 17" had no way to make Maven honor that when `mvn` defaulted to a different, genuinely incompatible JDK on the machine (the same JDK 26 that triggered the Qpid `getSubject` incompatibility above). Fixed: `_resolve_java_home_override()` (`kriya/workflow/workflow.py`) - when a real mismatch is detected and the goal names a version matching the side that's already correct (`java`, not `mvn`), derives that JDK's real home directory and forces every Maven subprocess call onto it via `JAVA_HOME`/`PATH` (`PolymorphicValidator.java_home_override`, applied in `_run_cmd_with_timeout()`). Deliberately narrow - never guesses, does nothing on a single-JDK machine or when neither side of the mismatch matches what the goal asked for. 8 new tests. Superseded by the `/usr` fix immediately below the same day - the resolver itself had a platform bug.
- **The JDK-home resolver's first implementation computed `/usr` as a "JDK home" on macOS, hanging a real `mvn` subprocess indefinitely (2026-08-07)** - found live on the very next batch after the JAVA_HOME-enforcement fix above shipped: `ignite_qpid_person`'s log showed `JVM toolchain enforcement: forcing Maven subprocess calls to run under JAVA_HOME=/usr`, and `ps aux` confirmed the `mvn clean compile` subprocess was genuinely stuck (near-zero CPU time for hours), not just slow. Root cause: `_resolve_jdk_home_for_java_on_path()` used one heuristic on every OS - resolve `java` on PATH through symlinks, trust the `<JDK home>/bin/java` layout convention - but `/usr/bin/java` on macOS is Apple's own real, non-symlinked, root-owned dispatcher stub, never a symlink into an actual JDK; `os.path.realpath()` returned it unchanged, its parent directory's basename genuinely is `bin`, so the heuristic confidently computed `/usr`. Fixed: renamed to `_resolve_jdk_home_for_version(version)`, now platform-aware - on `sys.platform == "darwin"` it shells out to `/usr/libexec/java_home -v <version>` (macOS's own JDK-discovery tool), falling back to the old PATH-symlink heuristic only on non-macOS platforms where it's actually reliable. 5 new tests covering both platform paths. User manually interrupted the hung process. **Re-validated live (2026-08-07)**: re-ran `ignite_qpid_person`, `JAVA_HOME` resolved to a real Temurin 17 install (`/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home`), and the run's own `run_verification` output confirmed the subprocess genuinely executed under it (`Platform : JVM : Eclipse Adoptium version: 17.0.10+7`) - the run passed clean end to end (goal fully satisfied per the grader).
- **`_strip_jdk_incompatible_jvm_flags()` decided against mvn's untouched default JDK instead of the JDK `java_home_override` was actually forcing execution onto (2026-08-07)** - found on the SAME `ignite_qpid_person` re-run that confirmed the `/usr` fix above: the log showed `"...resolved target: JDK 26"` for the flag-strip decision, even though `JAVA_HOME` was simultaneously being forced onto JDK 17 for every Maven subprocess in the same run. Root cause: the function called `check_java_toolchain()` fresh, which reports `mvn`'s untouched default (26 on this machine), with no awareness that `java_home_override` (computed earlier in the same retry loop) was about to override that default. Harmless this time - the flag is optional either way on JDK 17 - but a real logic gap: two independent "what JDK is this?" computations that can disagree about the one that actually matters. Fixed: `_strip_jdk_incompatible_jvm_flags()` now takes an optional `java_home_override` param; when set, it uses `toolchain['java_version']` (the side `_resolve_java_home_override` always overrides onto) instead of `mvn_java_version`. The one call site (inside the run-verification branch) now passes the already-in-scope `java_home_override` variable through. 4 new tests (2 unit-level proving the decision flips correctly in both directions with an override active, 1 workflow-level end-to-end). Not yet re-validated live with this specific fix in place - the next `ignite_qpid_person` batch is the natural check, though low urgency since the prior behavior was confirmed harmless in the one case observed so far.
- **Qpid Broker-J's `Subject.getSubject()` call crashes on JDK 24+ regardless of the security-manager flag (2026-08-07)** - found live, `ignite_qpid_person`, immediately after the `_strip_jdk_incompatible_jvm_flags()` fix started correctly stripping the now-forbidden flag on a resolved JDK 26 target: the broker crashed on a different, previously-unseen error instead (`UnsupportedOperationException: getSubject is not supported`, inside Qpid Broker-J's own internal logging). A genuine Qpid-Broker-J-version-vs-JDK-24+ incompatibility - not fixable by adding OR omitting the flag, not a code defect, and not something any retry can resolve. Without classification, the retry loop burned two full attempts trying to code-fix it before ever escalating past it. Fixed: added as a third recognized class in `classify_environment_failure()` (`kriya/workflow/workflow.py`), with an actionable message naming the real fix (a newer Qpid Broker-J release, or an older JDK) since Kriya can't self-heal this the way it can strip a wrong flag. Not yet re-validated live.
- **Eval harness's per-goal `kriya.yaml` silently inherited Kriya's own reasoning-model fallback, burning `--timeout-per-goal` (2026-08-07)** - `run_harness.py`'s `_write_config()` never set `llm_chain` at all, so every goal fell back to the packaged `default_config.yaml` chain (`deepseek-r1:32b`, `reasoning: true`) on escalation - confirmed live as the cause of `python_task_tracker` and `ignite_qpid_person` both hitting the harness's own wall-clock timeout (individual fallback completions took 2-5+ minutes), consistent with the separately-cited finding that a reasoning model gave zero correctness benefit for a fact-recall-class retry while being 13x slower. Fixed: an explicit `--fallback-model` (default `deepseek-coder-v2:16b`, the same fast non-reasoning model `kriya-protocol-parser-app`'s own `kriya.yaml` had already independently validated live in this exact role) is now written into every goal's `llm_chain`. **Re-validated live (2026-08-07)**: re-ran `python_task_tracker` (previously timed out at 1200s/2400s/2400s/3600s/1200s across 5 straight attempts, never once completing) - this run finished in 166.7s across 7 quality-gate attempts, escalating to the fallback model partway through with no multi-minute stalls. Confirmed fixed; the goal still failed, but for the separate, already-known layout-invention reason below, not a timeout.
- **Gutter-stripping regex matched an exact 2-space prefix; the real format is 3 spaces (2026-08-07)** - found by re-running `kriya-protocol-parser-app` after the `failed_content`/`attempted_edits` forensics fix below and reading the actual persisted SEARCH text for the first time, instead of guessing: `_build_error_source_context()`'s non-highlighted gutter format string (`f"{'  '} {i+1}: ..."`) actually emits three leading spaces (the two-space placeholder plus the f-string's own separator space), but `_GUTTER_PREFIX_RE` (`kriya/agents/agent.py`) only ever matched exactly two - so a real SEARCH block that copied the gutter verbatim (confirmed directly: `"   58:         // Extract body..."` sitting unstripped in a persisted `attempted_edits` entry) guaranteed "matched 0 times" regardless of whether the model's intended fix was otherwise correct. Fixed: widened to `[ ]{2,}` (2 or more). New regression test generates the gutter via the real function rather than a hand-typed guess at its format - the same kind of mismatch that let this go unnoticed originally. Not yet re-validated live. A second, separate finding in the same data (a fallback-model edit for one file containing another file's code plus trailing unpaired-fence prose) was flagged, not fixed - looks like model reliability, not a clean Kriya bug.
- **`failed_content`/attempted-edit forensics gap: captured but never persisted (2026-08-07)** - `Failure.failed_content` was added when the failure-grounding abstraction shipped specifically to close a forensics gap, but `to_gate_outcome()` silently dropped it before it ever reached `gate_outcomes`/`traces.db` - a deferred gap that turned out to matter live: a real anchor-match failure ("the search block matched 0 times", `kriya-protocol-parser-app`) was undiagnosable after the fact, since neither the original file content nor the model's actual attempted search/replace text survived anywhere. Fixed: `to_gate_outcome()` now persists both `failed_content` and a new `attempted_edits` field (the anchored-edit raise site's own `edits`, post-sanitization). 3 new/extended tests. Paid off the same session - see the gutter-regex fix above, found directly from this data.
- **Compile-error-only source-line grounding regex never recognized a JUnit/JVM stack trace's own file:line locator (2026-08-07)** - found on the SAME `kriya-protocol-parser-app` re-run that confirmed the Surefire class-name fix worked: a real test failure (`BufferUnderflowException`) had a precise stack trace, but `_ERROR_LOCATION_PATTERN` only matched javac's `File.java:[line,col]` shape, not a stack trace's `(File.java:60)` shape - so the test failure got zero source-line grounding and no anchored-edit preference, unlike a compile error. The model's own fix-analysis correctly diagnosed the bug but, with no precise anchor, fell back to full-file regeneration and reintroduced an equivalent bug on the very next attempt. Fixed: `extract_error_source_locations()` now recognizes both shapes (purely additive regex extension). Explicitly verified this does NOT affect the separate, narrowly-scoped live-lookup mechanism (`extract_error_search_terms()`) - confirmed by a new regression test that a real stack trace still yields zero external-search terms, addressing the user's explicit concern about proprietary content never leaving the node. 5 new tests. Not yet re-validated live.
- **Java targeted-test gate passed a raw file path to Maven's -Dtest=/Gradle's --tests, which both expect a class name (2026-08-07)** - found immediately on the SAME `kriya-protocol-parser-app` re-run that confirmed the missing-manifest fix worked: once `pom.xml` existed and compiled cleanly, the very next gate (`PolymorphicValidator.run_tests()`) failed every attempt with `"No tests matching pattern ... were executed!"` because `extract_target_test()`'s raw path (`src/test/java/com/example/ProtocolTest.java`) was passed straight into `-Dtest=`/`--tests` verbatim. Structurally unable to ever pass regardless of the generated test's own correctness - confirmed the model's own test file was fine, this was purely a Kriya-side invocation bug. Fixed: derive the bare class name via `os.path.splitext(os.path.basename(target_test))[0]` before building either command - resolves via classpath scanning regardless of package, no src-root guessing needed. 3 new tests. Not yet re-validated live.
- **A required build manifest the Architect never asked for couldn't be recovered by any existing mechanism (2026-08-07)** - found running the long-open `kriya-protocol-parser-app` standalone validation (open since 2026-08-04, finally run this session): `pom.xml` was never written across two entire generation runs days apart - the Architect's design never listed it, so `IncompleteGenerationError`'s missing-file recovery (which only catches a file the design DID list but the Developer dropped) never fired, and `extract_implicated_files()` could never implicate it either, since "package X does not exist" never names the missing manifest that's the real cause. The model's own fix-analysis correctly diagnosed the root cause on every single retry and explicitly declined to fix it, since a targeted retry is told to stay in scope. Fixed two ways: `ArchitectAgent`'s prompt now explicitly requires the build manifest as its own listed file whenever Maven/Gradle is established by the goal; `_detect_missing_build_manifest()` (`kriya/workflow/workflow.py`) is a structural safety net that redirects the retry loop at generating `pom.xml` from the error shape alone (no pom.xml/build.gradle present + a "package X does not exist" compile error), independent of whether the Architect's prompt fix actually works. Not yet re-validated live - `kriya-protocol-parser-app` is the natural next place to confirm it.
- **Ignite/Qpid's recurring JVM Security Manager crash on JDK 24+, open since 2026-08-04 (2026-08-07)** - confirmed via a live batch that this was NOT a missing/weak rule: `skills/qpid/rules.txt` already stated the correct, JDK-version-conditional instruction, and `_java_toolchain_fact()` already surfaced the correct resolved-JDK fact (`active_skills` confirmed both reached the model) - the model added the forbidden `-Djava.security.manager=allow` flag on every attempt anyway. Prompting had hit its ceiling on this one. Fixed with deterministic correction instead: `_strip_jdk_incompatible_jvm_flags()` (`kriya/workflow/workflow.py`) - a small, explicitly-not-generic table of known JDK-incompatible flags (one entry today) - strips a match from the worktree's `pom.xml` right before run-verification executes, surfaced via `toolchain_warning`. Not yet re-validated live.
- **`_java_toolchain_fact()` injected an unrelated "Target JVM" fact into every prompt on any machine with Java installed, regardless of the goal's actual stack (2026-08-06)** - found live via an eval-harness batch (`ignite_qpid_person` knowledge_gap+known env-failure recurrence, `ruby_word_count` pass, but `django_healthcheck_gap` fully regressed to writing Java/Spring code for a Django goal). `prompt_rendered` in `traces.db` showed the smoking gun: `=== Environment Fact ===\nTarget JVM ... JDK 26.` sitting directly in a Django goal's Planner prompt, contradicting the ecosystem-preservation invariant in the very same prompt. This is the likely real root cause of the "partial fix" mystery logged in the ecosystem-invariant entry above/design.md §2.3.4k - not a saliency/prompt-length issue as originally suspected. Fixed: new `_goal_or_repo_targets_java()` gates the fact on real evidence (existing `pom.xml`/`build.gradle`, or the goal text naming Java/Maven/Gradle/Spring/the JVM) instead of "is Java installed on this machine at all." Not yet re-validated live as of this writing - the next eval-harness batch will show whether this was the dominant cause.
- **Unified failure-grounding abstraction (`kriya/workflow/failure.py`)** - one `Failure`/`QualityGateFailure` shape (type/message/file_locations/likely_files/failed_content/attempt) populated at each of the 6 raise sites (compile, targeted_test, test, run_verification, regression_test, anchored_edit), replacing per-source hand-built retry-scoping paths with asymmetric capabilities. Closes a real gap: an anchored-edit anchor-match failure previously never captured its own filepath at all. See design.md §2.3.4j.
- **Ecosystem-preservation invariant** - a standing checklist instruction (`ECOSYSTEM_INVARIANT_HEADER`) threaded through all 3 Developer prompt builders, after a Django/Python goal produced Java/Spring code and a Python goal invented a Maven-style layout with zero active skills involved (confirmed via `traces.db`). Live-validated as a real but **partial** fix: the language/framework substitution case is fixed, the directory-layout-invention case still recurred on a more complex multi-file goal. See design.md §2.3.4k. **The open question is now resolved (2026-08-07)** - see the layout-invention entry below: confirmed a genuine prompting-ceiling case (the invariant text already names this exact anti-pattern verbatim, 7/7 attempts across two models still did it anyway), fixed deterministically instead of via more prompting.
- **Directory-layout invention (`src/main/python` for pure-Python goals) made every `python_task_tracker` attempt fail, regardless of prompting (2026-08-07)** - `python_task_tracker` had never completed even once across 5 prior batches (all timeouts). After the `--fallback-model` fix resolved the timeout, a fresh run finished in 166s but still failed (`quality_gates_exhausted`): all 7 attempts, across both `qwen3-coder:30b` and the `deepseek-coder-v2:16b` fallback, wrote to `src/main/python/tasks/...` - a Maven/Gradle-style nesting - for a goal with zero Java/Maven mention, despite `ECOSYSTEM_INVARIANT_HEADER` already naming this exact anti-pattern verbatim ("do not invent a Maven-style src/main/src/test directory layout"). Every resulting `ModuleNotFoundError: No module named 'tasks'` was misdiagnosed by the model's own fix-analysis as a sys.path problem on all 7 attempts, never as a layout problem - retries never escaped the pattern. A genuine prompting-ceiling case (same shape as the Ignite Security Manager flag), not an under-specified prompt. Fixed deterministically instead of adding more prompt text: `PolymorphicValidator.run_tests()`'s Python branch (`kriya/tools/validate.py`) already appended `<workspace>` and `<workspace>/src` as sys.path fallbacks for test collection (an earlier src-layout fix); now also conditionally appends `src/main/python`, `src/main`, `src/test/python`, `src/test` when they actually exist - the specific nesting shape observed, not a general recursive layout-discovery mechanism. Doesn't stop the model from writing the odd-looking layout (a real, open cosmetic gap - the invariant stays in the prompt since it's free and may still reduce frequency), but stops that choice from being fatal to the goal - mirrors the Java test-class-name fix's "meet the model where it is via classpath scanning" philosophy. Runtime verification (`python <script>.py`) was never actually broken by this - Python's own script-execution sys.path[0] insertion already resolves sibling packages regardless of nesting depth; only pytest's collection path needed the fix. 1 new regression test reproducing the exact observed layout end-to-end. **Re-validated live (2026-08-07)**: re-ran `python_task_tracker` - compile and targeted tests now pass cleanly on all 7 attempts (`7 passed`, every time), the `ModuleNotFoundError` completely gone. First time this goal has ever gotten past test collection.
- **`python_task_tracker`'s original goal wording was self-contradictory once run-verification exercises it, unrelated to any Kriya bug (2026-08-07)** - found on the SAME re-run that confirmed the layout-invention fix above: `run_verification` failed on every attempt with tasks not persisting between commands (`add "Task 1"` then `add "Task 2"` both got id 1; `done 1`/`list` saw nothing). Root cause: each `python cli.py <cmd>` in the inferred run sequence is a separate process, and the goal explicitly asked for an "in-memory" `TaskStore` - genuinely in-memory state cannot survive a fresh process regardless of how correct the generated code is. The passing test suite proves this: it correctly exercises `TaskStore` within one process, which is all "in-memory" can mean. Not a Kriya pipeline bug and not a model bug - the goal text itself asked for two things ("in-memory" + CLI commands implying cross-invocation persistence) that can't both be literally true. Fixed by rewording the goal (`spikes/eval_harness/goals.py`), not the pipeline: now explicit that `TaskStore` persists to a JSON file between CLI invocations (how a real CLI tool would need to work) while staying plain in-memory within any single process/test. **Re-validated live (2026-08-07)**: re-ran once more, `run_id 97786725`, `status: success`, first attempt, no fallback escalation, 70s. Every gate passed - compile, targeted test, run_verification (grader: "task state persisted between invocations as demonstrated by the load/save functionality"), regression test. First clean pass this goal has ever had, closing a thread open since 2026-08-04 across three fixes (fallback-model timeout, layout-invention, goal wording).
- **Resource-lifecycle invariant** - stack-agnostic generalization of `skills/ignite-java17/rules.txt`'s Ignite-specific "start once, reuse, close once" rule, added generically to every Developer prompt so a goal using any other stateful resource (DB connection, broker client, thread pool) gets the equivalent guidance cold-start. See design.md §2.3.4k.
- **Non-binary Runtime Verification grading on timeout** - previously a timed-out run short-circuited straight to a flat "Run timed out" message without ever grading the output `_run_cmd_with_timeout()` already captures before the forced kill. `RunVerifierAgent.grade()` now grades that captured output on timeout too (`timed_out=True`, so the grader doesn't read the forced-kill exit code as evidence of failure). A hang stays disqualifying either way, but is now categorized distinctly (`gate_outcomes` type `"run_verification_hung"` vs plain `"run_verification"`) with a retry message pointing at the resource-lifecycle invariant above instead of a content-free timeout notice. Directly generalizes the real Ignite hang bug logged below (2026-08-04 entry: "app finishes all work, prints correct result, then times out anyway"). See design.md §2.7.
- **Tool-calling-based agent loop architecture** - empirically tested twice (small task, then realistic large-file-argument task) against 3 local models, confirmed not viable today via any backend (Ollama or vLLM), externally corroborated by Qwen's own recommended stack and by Aider's real production choice. **Refined, not reversed (2026-08-12)**: a general Developer-stage rewrite around tool-calling stays a closed "no" (large tool-call arguments are still unreliable, re-confirmed by re-running the exact same spike unmodified on current Ollama) - but native tool-calling restricted to SMALL arguments only is reliable, and was shipped narrowly on that boundary as a bounded compile-gate self-correction micro-loop. See the dated entry below and design.md's "Agentic tool-use loop" writeup.
- **Bounded self-correction micro-loop on compile-gate failure, shipped opt-in (2026-08-12, `tool-use` branch)** - closes part of the "agentic tool-use loop" architectural recommendation from the 2026-08-12 SME review, narrowly. New `LLMClient.complete_with_tools()` (`kriya/core/llm.py`) and `kriya/workflow/self_correction.py::run_self_correction_loop()`, wired into `run_attempt()` (`kriya/workflow/attempt.py`) only on a compile-gate failure: gives the model 4 small-argument-only tools (`read_file`/`list_files`, scoped strictly to files already in the attempt's sandbox; `apply_patch`, reusing the existing `apply_anchored_edits()` unmodified; `recompile`, reusing the existing `PolymorphicValidator.run_compile_check()` instance) and a bounded turn budget (`autonomy.self_correction_loop_max_turns`, default 4) to fix the failure interactively before falling back to today's full-set regeneration. `apply_patch` can only touch files already in scope, never create a new one - keeps `state.all_files_written`/the completeness check untouched by this feature entirely; a fix needing a new file just exhausts the budget and degrades safely to the existing retry path. Opt-in via `autonomy.self_correction_loop_enabled` (default `False`) - off by default, zero behavior change when off or when the loop fails, confirmed via a dedicated test asserting the new module isn't even imported in the flag-off path. Explicit scope boundary, not an oversight: compile gate only (not test/run-verification - less mechanically "obviously fixable"), and not a general Developer rewrite (see the refined finding above - that idea stays closed). 11 new tests across `tests/test_llm_extra.py`, `tests/test_self_correction.py` (new file), `tests/test_workflow.py`. See design.md's dated "Agentic tool-use loop" entry for full detail.
- **Same-day follow-up: a live eval-harness run (`ignite_qpid_person`, new `--self-correction` flag on `run_harness.py`) confirmed the fallback-safety property live, and also found the same forensics gap this session's OWN durable lesson (below, "Verify a regression test...") already warns about.** The loop fired on a real compile failure, made its full 4-turn budget of genuine diagnostic tool calls, didn't resolve it, and fell through cleanly to the existing retry path with zero disruption - but its transcript was silently discarded on that path (only ever persisted on success), invisible in `gate_outcomes`/`traces.db`. Fixed: new `Failure.self_correction_attempt` field (`kriya/workflow/failure.py`, same precedent as `attempted_edits`), persisted unconditionally by `to_gate_outcome()`, set on the compile-failure branch whenever the loop ran at all. 1 new regression test.
- **The SAME eval-harness run's underlying compile failure turned out to be a genuine, unrelated Kriya bug in `extract_planner_code_blocks()` (2026-08-12)** - traced from `traces.db`'s `failed_content` directly: `IgniteQpidPersonDemo.java`'s entire content was the literal string `mvn -q compile exec:exec -Dexec.mainClass=com.example.IgniteQpidPersonDemo` (a real, documented run-command template baked into both `skills/qpid/rules.txt` and `skills/ignite-java17/rules.txt`), not Java source at all. Root cause: `attempt.py`'s Planner-code-reuse optimization (skips a real Developer call when the Planner's own draft already contains a fenced block for every expected file) fired because the Planner mentioned `IgniteQpidPersonDemo.java` in its plan text, then - within `extract_planner_code_blocks()`'s lookback window - wrote a fenced "here's how to run this" snippet quoting that skill's own run-command example, correctly substituted with the real class name. The extraction function had zero content-type validation - it matched purely on filename proximity, with nothing checking that the matched fence plausibly contained Java at all - so a legitimate "how to run it" note got reused as the file's entire source, written straight to disk with no Developer review, only caught afterward by the (much more expensive) compile gate. Fixed: a new, small, explicitly-extensible `_MIN_PLAUSIBLE_CODE_PATTERN` table (`kriya/workflow/file_resolution.py`, one entry today: `.java` requires a `class`/`interface`/`enum`/`record` keyword, matching javac's own error vocabulary for this exact incident) rejects a matched fence that fails the check for its extension - the caller's existing all-or-nothing safety net (only reuse Planner code when EVERY expected file matched) then safely falls through to a real Developer generation for the whole set, exactly the existing degradation path, no new one invented. 2 new tests (rejects the exact incident shape, confirmed failing pre-fix; a sibling test confirms genuinely valid Java content is still accepted normally).
- **New `binary-wire-protocol` skill (2026-08-12), closing the actual underlying model-capability gap the extract_planner_code_blocks fix above only worked around.** The BufferOverflowException itself (`ProtocolParser.encode()` writing a 3-byte `dataLength` wire field via `buffer.putInt()`, which always writes 4 bytes) is a long-standing, previously-documented failure this model has never once fixed via a successful anchored edit in any real run (see "Correct diagnosis, failed execution" durable lesson below) - only a clean first attempt or full-regeneration escalation ever got it right. User pointed at a working, manually-verified reference (`kriya-staged-protocol-ignite-qpid`'s own `ProtocolParser.java`, using explicit bit-shift/mask byte packing instead of `ByteBuffer.putInt()`) and asked whether the prompt needed the same update. New skill `skills/binary-wire-protocol/` (goal-text tag-activated: "wire format", "big-endian", "little-endian", "binary protocol", "byte-level protocol" - confirmed live to activate on the exact `ignite_qpid_protocol` goal text via `SkillEngine`) gives the model the exact correct pattern directly (rules.txt + instructions.md + a worked `examples/WireProtocolCodec.java`) rather than leaving it to rediscover the fix under retry pressure every time. **CONFIRMED live, same day**: re-ran `ignite_qpid_protocol` with the skill in place - passed clean on attempt 1 (compile, run_verification, regression all first-try, zero retries), `active_skills` confirmed `binary-wire-protocol,qpid,ignite-java17`, and the actual generated `ProtocolParser.java` uses exactly the taught pattern (manual bit-shift/mask packing for the 3-byte `dataLength` field, zero `ByteBuffer` calls anywhere). Real, direct evidence this specific model-capability ceiling is addressable by giving the correct pattern upfront (attempt 1) rather than only reactively after a failure (the pre-existing `_build_buffer_capacity_scaffold()`, retry-only, evidenced 0/10 execution-correct in `spikes/fix_alignment/`) - the intervention POINT mattered, not just the guidance content, which was nearly identical between the two. Kriya's own pipeline auto-marked the skill `verified: true` via the real Runtime Verification Gate pass, committing directly to the branch (`git_commit_if_tracked()`, 2 commits: `verification_gap_acknowledged=True` then `verified=True`) - the existing skill-verification mechanism working exactly as designed against genuinely new skill content. One data point, not a guaranteed fix for every future run of this goal (this bug class was previously intermittent, not 100% reproducing every time either) - worth a couple more batches before calling it fully closed, but a real, confirmed positive result today.
- **Two more `ignite_qpid_protocol` re-runs (same day) confirmed the skill holding and found one new, unrelated, self-healed bug.** A 2nd run passed clean on attempt 1 again (3/3 clean compiles in a row now with the skill active - the byte-packing bug hasn't recurred once). A 3rd run's compile also passed clean on attempt 1 (still 0 byte-packing recurrences), but hit a genuinely different `run_verification` failure: `IgniteCheckedException: Ignite instance with this name has already been started` - the model's own attempt-1 `IgniteProtocolApp.java` created a SEPARATE `new ClassPathXmlApplicationContext("applicationContext.xml")` inside both `sendProtocolViaJms()` and `receiveProtocolViaJms()`, each re-triggering `Ignition.start("ignite-server-node")` and colliding with the instance already started in `main()`'s own context (confirmed directly from the model's own FIX ANALYSIS text and the corrected file, which now takes the existing `context` as a parameter into both helper methods instead of creating its own). Correctly diagnosed AND correctly fixed by the EXISTING targeted-retry mechanism in exactly one retry - no wasted attempts, no self-correction involvement (a `run_verification` failure is out of that gate's scope by design). **Not fixed further, deliberately** - user asked what a fix would look like (a small addition to `RESOURCE_LIFECYCLE_HEADER` telling the Developer to pass an already-started stateful resource/context into helper methods rather than re-instantiating it) but explicitly held off building it: one occurrence that self-healed cleanly in a single retry isn't the same evidence bar as the byte-packing bug's documented, repeated, 0/10-retry-success history before ITS fix was built - logged here as a real, interesting data point, not (yet) actioned. Revisit only if this specific shape (duplicate stateful-resource/context instantiation across helper methods) recurs.
- **Standalone experiment (same day, no Kriya involved): does real Claude Code, routed entirely through local `qwen3-coder:30b` via Ollama, generate the identical `ignite_qpid_protocol` goal successfully?** New isolated repo `~/WorkingDirectory/AI/ClaudeCode/claude-code-qwen3-ignite-qpid-protocol/`, real `claude` CLI (not a Kriya-orchestrated call) invoked with `ANTHROPIC_BASE_URL=http://localhost:11434` (no `/v1` suffix - confirmed live via a smoke test first that including it 404s, since Claude Code's client appends its own path), `ANTHROPIC_API_KEY=ollama`, `ANTHROPIC_MODEL=qwen3-coder:30b`, `CLAUDE_CODE_MAX_CONTEXT_TOKENS=32768` (`ollama ps` confirmed this model is actually served at a 32768 context window). Smoke test (a trivial one-line prompt) succeeded cleanly, confirming the plumbing genuinely works end-to-end against the local model, not just superficially.
  The real run did NOT succeed - only 1 of ~7 expected files (`pom.xml`) got written before the session terminated on a synthetic `invalid_request` error, `"Prompt is too long"`. Root cause, read directly from the real session transcript (`~/.claude/projects/.../*.jsonl`, same auto-saved mechanism as any Claude Code session): the FIRST real turn already carries 22,787 input tokens - Claude Code's own system prompt plus built-in tool definitions, before any task-specific content - already ~70% of the model's real 32768-token window. Writing one file (whose content gets echoed back into the conversation via the tool result) was enough to push the running total past what Ollama would accept; the request was rejected outright with no successful auto-compaction and no graceful recovery, and the session just stopped.
  **This is a genuinely different failure than what this session's own earlier spikes predicted** (large-tool-call-argument corruption during a `Write` call) - the `pom.xml` write itself succeeded with zero corruption. The real barrier is more fundamental: Claude Code's baseline operating overhead alone barely fits a 32K-context local model, independent of whether any individual tool call is well-formed or not. Directly reinforces why Kriya's own architecture (tightly token-budgeted context allocation via `context_budget.py`'s skeletonization tiers, no large fixed built-in-tool-definition tax, deliberately economical prompts) is a structural necessity for operating inside a window this size at all, not an arbitrary design choice made in isolation from this constraint.
- **Same experiment, re-run with `CLAUDE_CODE_MAX_CONTEXT_TOKENS=62000` (2x the prior budget) - got much further, but surfaced a different, arguably more important finding than raw context budget.** More headroom didn't prevent a real mess, it just gave the agent more room to make one before running out: once it started hitting real errors (a Maven POM dependency-resolution failure, the same class of "guessed wrong library version" mistake `skills/qpid/rules.txt` explicitly exists to prevent), it began creating loose duplicate copies of `Protocol.java`/`ProtocolParser.java`/`Main.java` directly at the repo root (apparently to test faster with plain `javac` instead of waiting on Maven), which diverged from the real copies already correctly nested under `src/main/java/com/example/`. It then got confused about which copy it was looking at - an `Edit` call failed with `"String to replace not found in file"` (structurally identical to Kriya's own well-documented "matched 0 times" anchor-match failures) because the file it thought it was editing wasn't the one actually on disk at that path. Tool-error count climbed from 14 to 26 across the observed window, and 4 different scattered ad hoc test files (`SimpleTest.java`, `FinalIntegrationTest.java`, `ProtocolParserTest.java`, `TestProtocolParser.java`) accumulated at the root before the run was manually stopped (user judged it had run long enough, ~46s CPU time, hundreds of turns).
  **Why this matters for Kriya specifically**: this is a real, concrete demonstration that even a considerably more sophisticated agentic harness (Claude Code itself) has no structural defense against file-state drift once it starts improvising workarounds under repeated errors - it has no forced canonical file-set tracking, no isolated-and-resettable sandbox per attempt, nothing playing the role `GenerationState.all_files_written`/the completeness check/the per-attempt git worktree reset play in Kriya. Kriya's structural rigidity (bounded retries, one isolated worktree per attempt, an explicit expected-vs-actual file check) is often the thing that looks like a limitation next to a "real" freely-improvising agent - this run is direct, live evidence that the rigidity is actively preventing a real failure class, not just adding process overhead. Also reinforces (does not newly establish - already known probabilistic) that the byte-packing bug isn't a hard ceiling: this run's `ProtocolParser.encode()` used the correct manual bit-shift pattern for the 3-byte `dataLength` field on its own, no skill/prompt guidance involved, a positive but not novel data point given this bug was already known to be intermittent, not 100% reproducing.
- **Closed the run-verification grader-reliability gap's coverage hole (2026-08-12) - the deterministic contract mechanism was real but under-complied-with, not broken.** User asked for a production-readiness score scoped specifically to Kriya's "local LLM coding per node" mission; the LLM-graded correctness signal (`RunVerifierAgent.grade()`, documented hallucination history) was named as the single highest-leverage remaining gap. Investigation found the deterministic alternative (`VERIFICATION_CONTRACT_HEADER`/`extract_contract_verdict()`, shipped 2026-08-11) already reaches attempt 1's prompt correctly - traced `_build_full_set_retry_prompt()` and confirmed it's used for both the first attempt and every full-set retry - but grepping this session's own real captured output from two clean first-attempt `ignite_qpid_protocol` successes found zero `[VERIFICATION]` markers in either; `grade()` ran instead both times. Root cause: the header is folded near the *top* of a per-file prompt that keeps growing after it, the same failure shape the "only this file" instruction already solved by being repeated at the very end, right before generation - the verification-contract instruction never got that treatment. Fixed by giving it one (`kriya/agents/agent.py::_fill_missing_content()`, a compact reminder appended right after the existing "only this file" line) - reusing an already-validated pattern, not inventing a new mechanism. Also added `"graded_by": "contract"/"llm"` to the persisted `run_verification` gate_outcome (`kriya/workflow/attempt.py`) so a future eval-harness batch can measure whether this actually moved compliance by querying `traces.db` directly, instead of grepping raw stdout logs by hand - which is what diagnosing the gap itself required, repeatedly, this session. Considered and explicitly deferred: moving `RunVerifierAgent.judge()` earlier to make the marker mandatory rather than model-self-assessed - real blocker, `judge()`'s own prompt is built around `files_written`/`build_file_content` which don't exist yet on attempt 1; not justified until this cheaper, precedented fix is tried and measured to actually need it. 3 new tests, each confirmed failing pre-fix. Not yet re-validated live - the natural next check is a few eval-harness batches of `ignite_qpid_person`/`ignite_qpid_protocol` to see whether `graded_by: contract` becomes more common.
- **`check_skill_conflicts()` O(skills²) waste (2026-08-03)** - fixed by switching from verbatim-rule-text matching to index-based referencing (an index is either valid or it isn't - no "almost right" case for a fuzzy match to fail on), plus a short worked example in the prompt.
- **Unresolved skill gaps invisible in the final report (2026-08-03)** - fixed: new `unresolved_skill_gaps` field, tracked everywhere a gap can go unaddressed (no callback wired, human decline, empty extraction, the pre-existing Stage 2B asymmetry), surfaced by both `generate`/`fix` regardless of pass/fail.
- **`PolymorphicValidator` silent Python-default false positive (2026-08-03)** - `_detect_stack()` used to fall back to `"python"` for ANY workspace not matching Java/Ruby markers, so a genuinely unsupported stack (JS/TS, Go, Rust, C#, ...) silently ran the Python compile-check branch, matched zero `.py` files, and reported a false-positive `success: True, "Python files compiled successfully"` - a quality gate that never actually checked anything. Fixed: Python is now detected the same way Java/Ruby are, by real markers (`requirements.txt`/`pyproject.toml`/`setup.py`/`setup.cfg`/`Pipfile`/any `.py` file present); anything matching none of the three is `"unknown"`, and `run_compile_check`/`run_tests` return `success: True` (so an unsupported stack doesn't fail the retry loop forever on a gate that can never run) with an explicit "no validation available, NOT confirmed" message instead of a false claim. This does NOT add real compile/test support for any new stack - that's still the separate, larger "breadth beyond Java/Maven" item below; this closes only the false-positive correctness bug.
- **LSP integration (`jdtls` for Java) (2026-08-03)** - implemented (`kriya/tools/lsp.py`, hand-rolled async JSON-RPC client - no Python library actually supports jdtls diagnostics) and live-validated end-to-end. First real install-and-run check found and fixed a real bug before validation could even proceed: jdtls's launcher refuses to start under an inherited `JAVA_HOME` pinned below Java 21, and Kriya's own Maven workflows routinely export exactly that (e.g. Temurin 17) for the target project's own reasons - `JdtlsClient.start()` now strips `JAVA_HOME` from the child process env. With that fixed, a real end-to-end run against a real Maven project (`kriya-validation-lint-temp-check`) with `JAVA_HOME` pinned to Temurin 17 (reproducing the exact bug conditions) confirmed jdtls starts cleanly, correctly flags a real `org.apache.ignite.cache.IgniteCache` wrong-import mistake with two error diagnostics, and produces zero false positives on the correct import. Confirmed live that this exact wrong-import mistake recurs across a SECOND model (`deepseek-coder-v2:16b`, not just `qwen3-coder:30b`) - a broad training-data habit, not a single-model quirk, which LSP grounding now catches deterministically regardless of which model is generating.
- **LSP grounding silent-failure visibility gap (2026-08-03)** - found live, same day, in the very next real run: a genuine "cannot find symbol" compile error occurred in a Maven project with jdtls installed - exactly the shape LSP grounding exists to catch - and it never reached the retry prompt, with zero trace anywhere in the run's own log. Root cause: `_get_or_start_jdtls_client()`'s start() failure and `_build_lsp_diagnostics_context()`'s per-file check failure were both logged at `debug` (invisible at the default level), so "jdtls not installed" (expected, silent) and "jdtls installed but broken" (a real problem) were indistinguishable from the log. Re-running the exact real production functions (`extract_implicated_files`, `_get_or_start_jdtls_client`, `_build_lsp_diagnostics_context`) directly against the actual broken file in isolation worked correctly and produced the right diagnostic - so the mechanism itself is sound; something specific to the live run's context swallowed it. Fixed the visibility gap: both failure paths now log at `warning`, and a new `lsp_warning` result field (mirroring `toolchain_warning`) is set the first time jdtls is found but fails to start, surfaced by both `generate`/`fix` regardless of pass/fail. Does NOT fix the underlying "why didn't it fire in the live run" question - the next real run with this fix in place will show the actual reason in the log if it recurs.
- **A prose-phrased trailing-marker corruption slipped past the "FILE CONTENT:" truncation regex entirely, duplicating top-level Java declarations (2026-08-08)** - `ignite_qpid_protocol` run `20260808-053604` produced `ProtocolParser.java` with two `package` statements and two `public class` declarations (a real 23-error `illegal start of expression`/`class expected` javac cascade), root-caused by replaying the exact captured raw model response through the real `DeveloperAgent._split_fix_analysis_edit()`: the model announced its redundant, unasked-for trailing full-file dump as `"Corrected file content for '...':"`, not the literal `"FILE CONTENT:"` marker the existing `_TRAILING_FILE_CONTENT_RE` required, so it truncated nothing and the whole duplicate declaration got folded verbatim into the applied edit. Fixed at the root - broadened the shared regex to `r"^[^\n]*?file content[^\n:]{0,60}:"` (multiline, consumes the whole announcement line from wherever "file content" starts through the colon), applied uniformly at all three call sites (`sanitize_generated_content`, `_split_fix_analysis`, `_split_fix_analysis_edit`) - plus a defense-in-depth safety net, `find_structural_corruption()` (`kriya/workflow/workflow.py`): a cheap, deterministic Java brace-balance check (comment/string-aware) and XML well-formedness check, wired into both of the retry loop's file-write choke points as a new `Failure.type == "structural_corruption"`, raised BEFORE the file is written to disk. Confirmed the safety net catches the case pure regex truncation can't (a *complete*, self-closed duplicate class is brace-count-balanced by construction) - specifically, a redundant dump that gets cut off mid-generation before its own duplicate class closes.
- **`extract_implicated_files()` unconditionally implicated pom.xml on effectively every Maven Java retry (2026-08-10)** - found live, `ignite_qpid_protocol` run `20260810-111517`: a targeted retry burned a full extra per-file completion call correctly diagnosing a real `BufferOverflowException` in `ProtocolParser.encode()` under `"Developer fix analysis for 'pom.xml'"`, then regenerated pom.xml with nothing useful to change. Root cause: the old bare `basename in error_text` substring check treated pom.xml as implicated whenever its name appeared anywhere in the captured output, and Maven's own reactor startup banner (`"[INFO]   from pom.xml"`) prints on every single build, success or failure, regardless of what actually broke - confirmed directly against two real captured cases from the same run (a compile error and a stack trace, both naming a genuinely-implicated file's own `file:line` locator alongside the banner). Fixed by preferring a precise file:line locator (reusing `extract_error_source_locations()`, the same mechanism `_resolve_file_locations()` already relies on - no pom.xml/Maven-specific special-casing) over a bare substring match: when at least one known file has a locator, only locator-backed files are returned; bare substring matching is used only when NO known file has a locator at all, preserving a genuine pom.xml-specific failure's correct implication (e.g. a real dependency-resolution error with no other file's locator competing).
- **A file legitimately co-implicated by a shared error had no way to say "I don't need to change" - wasted a whole retry attempt twice in the same run (2026-08-10)** - same `ignite_qpid_protocol` run: the `BufferOverflowException` stack trace correctly implicated BOTH `ProtocolParser.java` (the actual bug) and `ProtocolApp.java` (its caller), each getting its own per-file completion. Root-caused directly from the raw captured completion: `ProtocolApp.java`'s own response wrote a correct FIX ANALYSIS about `ProtocolParser.encode()`'s bug, then a SEARCH block that was `ProtocolParser.java`'s `encode()` method body copied verbatim - which can never match `ProtocolApp.java`'s real content, producing "Anchor matching failed... matched 0 times" (confirmed twice in the same run). The fix-analysis instruction had no way for the model to say "the cause is real, but the fix belongs in a different file - this one doesn't need to change." Fixed: both fix-analysis instruction variants (`DeveloperAgent._fill_missing_content()`) now offer a `"NO CHANGE NEEDED:"` opt-out, checked first (before SEARCH/REPLACE or FILE CONTENT extraction, taking priority over a possibly-contradictory stray edit) in both parsing functions - returns `content=None`, which the write loop's pre-existing `if content is None: continue` already treats as "leave this file as-is." Also closes a related risk: without this marker, a model volunteering "no change needed" prose with nothing else would have been silently written as the file's new literal content by the plain fallback path.

## Durable lessons learned (pattern-level, not session narrative)

- **Passive reference material is never enough - the model needs explicit checklists/instructions.** Confirmed repeatedly: showing the model its own prior file content, or existing dependencies, as passive context did NOT stop dependency drops, missing files, or redundant/wrong additions. An explicit "you must preserve/must not add X" instruction did. This generalizes: never assume a model will infer a constraint from context alone if the constraint is important.
- **A retry loop needs a forcing function, not just "here's the error, try again."** Single-shot, non-reasoning completion will regenerate byte-for-byte identical broken code across many retries even with the exact error present in every prompt, because nothing requires it to engage with that error before writing code. Structured "explain the fix, then write the code" instructions work as a prompting-level substitute for genuine reasoning.
- **Scope error-specific instructions to the file(s) actually implicated, always - even in a "regenerate everything" retry.** Broadcasting an error-specific instruction to every file in a batch produces confused/wrong output for unrelated files and dilutes the one response that mattered. Whatever mechanism identifies "which file is broken" should gate every downstream per-file signal, not just decide targeted-vs-full-set routing.
- **A model that already knows the right answer in a clean prompt can still get it wrong in the real noisy retry loop.** Don't assume a wrong answer means a knowledge gap - test the same question in isolation before concluding the model needs more capability (reasoning, a bigger model, live lookup). Often the fix is making the prompt say the right thing more clearly, not giving the model more power.
- **Test capability claims at realistic scale, not on a toy task.** Tool-calling reliability that looked solid (3/3) on a tiny Stack-class task collapsed completely on a realistically-sized file. Any "is X reliable" check needs to match the real workload's shape (size, complexity), or the result doesn't transfer.
- **Verify a regression test actually fails pre-fix, every time - a passing-for-the-wrong-reason test is a real, recurring risk.** Caught more than once: a test that looked like it validated a fix instead passed on both old and new code because of an unrelated mocking gap. `git stash` + confirm the failure mode matches the bug, every time, no exceptions.
- **Environment/toolchain mismatches are easy to re-discover from scratch - check memory before re-diagnosing.** JAVA_HOME/JDK version drift has bitten multiple times; the fix each time was already recorded from a prior session.
- **Real, live validation surfaces bugs that a fully-mocked test suite structurally cannot.** Nearly every significant bug found this whole effort came from an actual end-to-end run against real local models and real tools, not from the (extensive, always-green) mocked test suite. Mocked tests verify the code does what it's supposed to do; live runs verify the code's assumptions about the outside world are correct.
- **A "durable fact injected into the prompt" is a proven, reusable pattern** (skills, toolchain facts, dependency checklists) - reach for this shape again before inventing something new when the problem is "the model doesn't reliably know/apply X."
- **Structural/deterministic grounding beats more or better prompting for some error classes.** Real research found production coding agents increasingly favor deterministic tools - LSPs, linters, the compiler's own lint flags - over bigger models or cleverer prompts for catching import-path and type mistakes specifically. Matches this session's own evidence: a reasoning model gave zero benefit over prompting for a fact-recall error, and a prose skill rule stated verbatim in every prompt still didn't stop a recurring wrong-import mistake. When a mistake is the kind a compiler/tool could verify with certainty, reach for that before reaching for a smarter/slower model.
- **Lower temperature, not higher, for a stuck retry.** Counter to intuition, cited research found code-gen success rate drops as temperature rises even within a low range - "add randomness to shake it loose" is not supported by evidence.
- **A correct self-diagnosis can still fail to become a correct edit - and full-file regeneration is where it gets lost.** Found live: a model's own FIX ANALYSIS text named the exact right fix, then the regenerated file didn't contain it. Not a knowledge or grounding problem (LSP wouldn't help this one) - a small, anchored patch instead of a full rewrite has no unrelated content for a stated intention to get lost inside. When a precise fix location is known, prefer patching over regenerating.
- **An optional feature's failure path must never log below the default level - or its failures are undebuggable from the field.** Found live: LSP grounding's own start()/check failure paths logged at `debug`, so when jdtls silently failed to catch a real compile error it should have caught, the log gave zero clue why - "not installed" and "installed but broken" looked identical from the outside. Any optional/best-effort capability (a local tool, an LSP client, a linter) needs its degrade-to-off path logged at a level that actually survives to the operator, matching the precedent already set by `toolchain_warning`/`unresolved_skill_gaps` - both a debug log line AND a persisted result field, not one or the other.
- **A general retry-loop mechanism can silently only work for the failure class it was first built against - check it against every failure class, not just that one.** `extract_implicated_files()`'s basename-in-text matching was built and validated against compile errors (which always name a file), then reused as the single gate for FIX ANALYSIS/anchored-edit/LSP scoping generally - but a Runtime Verification failure's text structurally never names a file, so the same mechanism silently never engaged for that whole class, on every project, regardless of skills. This is also the right lever for a cold-start user with zero pre-existing skills: a domain-specific skill rule only ever fixes the one pattern it was written for and does nothing for the next novel goal, but a structural retry-loop fix like this one helps every goal in that failure class immediately, skill or no skill.
- **Any display formatting Kriya adds when SHOWING the model something (line-number gutters, `>>` highlight markers, etc.) is at risk of being echoed back verbatim into a field that needs the raw, unmarked original.** Found twice now in the same mechanism: a model asked to produce an exact-match SEARCH block copied the visual formatting Kriya itself added for readability, not just the underlying content. Any prompt section that displays code/text with added-for-humans decoration, when the model is later asked to reproduce a piece of it exactly, needs either an explicit "these markers are not part of the file" instruction, defensive stripping on the way back in, or both.
- **Parse defensively for a model that over-delivers, not just one that complies exactly.** The anchored-edit prompt asks for SEARCH/REPLACE OR FILE CONTENT, not both - but a real response included both anyway (a redundant, unasked-for full-file regeneration appended after a perfectly correct small patch). The parsing code assumed "replace_block runs to the end of the model's response" was a safe bound; it wasn't, the moment a model does more than exactly what was asked. Every marker-based parser in this codebase should assume a well-intentioned model may add extra structure beyond what's requested, not just omit what's asked for.
- **Separate validation folders aren't actually isolated from each other at the OS level - a leaked process from one can silently corrupt a completely different run, an hour later.** Found live: a "port in use" failure in one validation folder was actually caused by a stray process from a DIFFERENT, already-finished folder that Kriya's own timeout handling failed to fully kill. Each folder has its own git worktree/filesystem sandbox, but they all share the same machine's process table and network ports - a resource leak in one run's execution is a real, cross-contaminating risk to every other run sharing the machine, not a contained failure.
- **An identical goal intermittently passing and failing across separate runs is not, by itself, evidence of a regression - check whether the SAME first-draft mistake is happening every time before concluding anything changed.** Found live: `ignite_qpid_person` passed 5 batches in a row, then failed - the instinctive read was "something we shipped broke it." Actual cause: the model made the identical first-attempt compile error in EVERY sampled run, passing and failing alike; what varied was whether its one-shot retry self-correction happened to land. A "passing streak" on a system with a non-deterministic single point of recovery is a small sample landing on the likely outcome repeatedly, not proof of reliability - the failure was always one unlucky retry away.
- **A model's own stated self-diagnosis can be internally consistent, cite the right symptom, and still implement the wrong fix - verify the diff against the diagnosis mechanically where possible, not just trust the prose.** Found live: FIX ANALYSIS text said "properly handle the generic types" (correct), the diff renamed an unrelated method call (incomplete/wrong), and nothing checked that the two actually matched before spending a full compile-and-fail cycle finding out. Where the error carries a precise, parseable locator (a compiler's `file:[line,col]`, not most runtime/logic errors), a cheap, deterministic check - did the diff actually change the reported line, regardless of what else it touched or what the analysis claimed - catches this class of self-contradiction for free, before ever re-running the expensive gate.
- **A marker-detection regex tied to one exact phrasing will miss a paraphrased announcement of the same thing - match the structural pattern, not the literal string.** Found live: the "redundant trailing FILE CONTENT: block" defense (lesson above) only ever matched the literal marker text; a real response phrased the identical over-delivery as "Corrected file content for '...':" instead, no colon immediately after "content," and the truncation silently did nothing. Broadened to match from the start of whatever line contains the phrase "file content" through its own colon, not the exact marker string - the general fix for "the model said the same thing a different way" is to loosen the match to the underlying shape, not to enumerate every observed phrasing as its own case.
- **A cheap structural heuristic (brace-counting, etc.) has a real, describable blind spot - know it, don't oversell the check as complete.** `find_structural_corruption()`'s Java brace-balance check cannot detect a corruption where the redundant/duplicated content is itself fully self-closed (a complete second class declaration sums to zero net braces same as valid code) - only genuinely asymmetric corruption (a dump truncated mid-generation, an unclosed brace) trips it. This is fine as defense-in-depth for the cases the root-cause fix doesn't reach, but doesn't replace fixing the root cause, and a test suite exercising a "safety net" heuristic should include a case proving where the net's holes actually are, not just a case where it happens to catch something.
- **Not every "the model got it wrong" case needs the same fix.** This session found several different root causes behind superficially similar retry failures - a missing instruction (redundant dependency), a missing/ignored fact the model won't override on its own (wrong import), and a correct fact that didn't survive a full rewrite (missing Serializable). Diagnose which one before reaching for a fix - they don't share a solution.
- **A build tool's own boilerplate output can spuriously satisfy a naive "does this filename appear anywhere in the text" check - prefer a precision signal (a real file:line locator) over a presence signal (bare substring) whenever both are available.** Found live: `extract_implicated_files()`'s substring match treated pom.xml as implicated on effectively every Maven retry, because Maven's own reactor startup banner names pom.xml on every build regardless of outcome. The fix wasn't a pom.xml-specific exclusion (that's the hardcoded trap) - it was recognizing that a file with a real, precise locator is much stronger evidence than a filename appearing anywhere in noisy tool output, and this codebase already had exactly that distinction built (`extract_error_source_locations()`) for a different purpose. When a "does X appear in this text" heuristic misfires, check whether a more precise signal already exists elsewhere in the codebase before inventing a new special case.
- **A structurally-forced instruction ("every implicated file must produce a fix") needs an explicit escape hatch, or the model will invent one badly.** Correctly scoping a retry to every file a shared error genuinely implicates (a bug's actual location AND its caller, say) is right - but if the per-file prompt demands "identify the cause and what you're changing" with no way to decline, a model whose true fix belongs to a sibling file will either restate that sibling's edit verbatim (an anchor match against the wrong file's content, guaranteed to fail) or silently overwrite this file with prose explaining why there's nothing to do. The fix isn't narrower implication scoping - both files WERE genuinely relevant - it's giving the model a legitimate way to say "correctly diagnosed, doesn't apply here."
- **"Correct diagnosis, failed execution" is not one phenomenon - it's at least two, and only one of them is nudge-able.** `spikes/fix_alignment/`'s first real batch (2026-08-10, 40 calls, `qwen3-coder:30b`) measured this directly rather than guessing: a self-consistency nudge ("re-read your own diagnosis before writing the edit") closed the gap from 3/10 to 1/10 for a one-line fix (add a cast/generic type), but did NOTHING for a multi-line manual bit-shifting fix - 0/10 execution-correct in both the baseline AND nudge conditions, not one success across 20 attempts. Converges exactly with live eval-harness evidence: this model never once fixed the buffer-capacity bug via a successful anchored edit in any real run either, only via a clean first attempt or a full-regeneration escalation to a different model. Before reaching for a prompting fix to a "the model knows the right answer but doesn't execute it" problem, check whether the required fix is a small mechanical change (a nudge plausibly helps) or a genuinely complex multi-step transformation (a nudge plausibly won't) - conflating the two wastes effort on the wrong lever.
- **Testing a new branch is also how you find latent imprecision in the branches next to it - a gating condition that "usually" means the right thing isn't the same as one that always does.** Found while adding the fallback-targeted-fix step: lesson extraction gated on `retry_count > 0 and chain`, intended to mean "this success came from an escalated fallback model," actually just meant "some full-set attempt failed at some earlier point in this run" - true for a later PRIMARY-model targeted success too, which was never really a fallback-model resolution. The new branch (which sets a real model override without touching `retry_count`) made this a common, easily-observed case for the first time, but the imprecision predated it. When adding a new state to an existing set of branches, re-check whether an adjacent condition's variable actually means what its usage assumes, not just whether the new branch itself behaves correctly in isolation.
- **An LLM grader given full latitude to independently re-derive "the expected value" can invent a wrong one and then treat its own invention as ground truth - and a Developer model asked to fix a reported failure will defer to that invented ground truth even over its own correct reasoning.** Found live: the grader miscounted a string's UTF-8 byte length, the generated code was actually correct, and the fix-analysis trail shows the Developer model getting *closer* to the truth ("the issue is not in my code") before still concluding it must be wrong, because the reported error carried more apparent authority than its own arithmetic. When a captured output already contains a deterministic self-check the program computed itself (an `equals`/`MATCH`/`PASS` comparing real runtime data), a grader should be told to weight that over its own independent recomputation - the same "prefer a precision/deterministic signal over the LLM's own judgment" principle that's shown up multiple times this session, just applied to the grading step instead of the retry-scoping step this time.
- **A coding agent's tool-calling reliability at small argument sizes doesn't mean its whole architecture transfers to a small-context local model - the agent's own fixed baseline overhead may not fit at all, independent of tool-calling quality.** Standalone test: real Claude Code CLI, routed entirely through local `qwen3-coder:30b`, given the identical `ignite_qpid_protocol` goal, no Kriya involved. It didn't fail on a corrupted tool-call argument (the one file it did write came out clean) - it failed because Claude Code's own system prompt plus built-in tool definitions alone consume ~70% of a 32K-token local context window before any task content exists at all, so writing even one file pushed the conversation past what the model could accept and the session died outright with no recovery. A capability finding at one layer (tool-calling works fine at small argument sizes) doesn't automatically generalize to a DIFFERENT layer (does the whole surrounding agent fit in the budget at all) - check both before assuming an architecture "should" transfer.
- **Structural rigidity that looks like a limitation next to a freely-improvising agent can be the thing actively preventing a real failure class, not just process overhead.** The same standalone Claude-Code-on-local-model experiment, re-run with 2x the context budget, got much further but started creating loose duplicate files outside the project's real source tree once it hit real errors, then got confused about which copy was current (an `Edit` failure structurally identical to Kriya's own "matched 0 times" anchor-match failures). Claude Code itself - a considerably more capable harness than anything Kriya runs - has no forced canonical-file-set tracking or resettable per-attempt sandbox, and visibly drifted once it started improvising workarounds under repeated errors. Kriya's bounded retries, isolated per-attempt git worktree, and explicit expected-vs-actual completeness check aren't just overhead next to a "real" agent's freedom - they're a structural defense against a failure class a more sophisticated but less constrained agent hit live, in front of us, in under a thousand turns.

## 2026-08-13 session: failure attribution module (`kriya/workflow/attribution.py`)

Shipped: see `docs/design.md` §7 for full design/motivation/testing detail. Summary - root-caused a real `ignite_qpid_protocol` batch timeout (`spikes/eval_harness/runs/20260812-222120`) to a structural gap, not a one-off bug: four independently-reinvented "which file is this failure about" call sites, with a deterministic verification-contract FAIL (which deliberately declines to guess a file, by design) falling through to a blind, unordered full-set walk that never reached the actually-broken file before an external timeout. New `kriya/workflow/attribution.py`: one tested entry point (`attribute_failure()`), three tiers (`locator`/`judge` reused unchanged, a new cheap `triage` classification call that rides the SAME model-escalation ladder generation already uses rather than a separate "fast model" policy, and an honest `full_set` fallback). Persists `attribution_tier`/`attribution_confidence`/`attribution_reasoning` onto `gate_outcomes`/`traces.db`, same `graded_by`-style pattern, zero schema migration. 885 tests passed, same 8 pre-existing/unrelated failures as baseline (confirmed via `git stash`) - zero new regressions.

- **A single-candidate-file guard was needed for the new triage tier, found only by running the FULL test suite, not the new module's own tests.** `tests/test_attribution.py` passed cleanly in isolation, but a full suite run surfaced 24 newly-failing tests elsewhere - triage was firing (and consuming/exhausting scripted `AsyncMock` completion sequences) on trivial single-file test fixtures where there was never any real ambiguity to resolve. Not a flaw in the tiered design itself, but a real scope gap: skip triage whenever there's at most one candidate file, since the answer (if any) is already unambiguous by elimination and an LLM call adds cost with zero possible benefit. Reinforces the standing lesson two entries up ("check a new mechanism against every failure class, not just the one it was built against") - here it needed checking against every EXISTING TEST's failure shape too, not just new ones.
- **A field added to a shared, mutated-then-serialized object can still miss the persisted snapshot if something else serializes it first.** Found live, via a new end-to-end test (not assumed): `Failure.attribution_tier` was set correctly in `retry_strategy.py`, but for a `QualityGateFailure` type that appends its OWN `gate_outcome` at the raise site (compile/test/etc., inside `attempt.py`, well before `retry_strategy.py` ever runs), that `to_gate_outcome()` snapshot was already taken and appended - the later mutation had nothing left to affect. The general shape: when the SAME logical object gets serialized at more than one point in a control-flow path, a field set between those points needs to either move earlier (before the first serialization) or patch the already-serialized copy in place - setting it "correctly" on the live object isn't sufficient if nothing re-reads that object afterward.

## 2026-08-13, same day: self-diagnosis feedback (`kriya/workflow/attribution.py`'s `self_diagnosis` tier)

Shipped: see `docs/design.md` §7.1 for full design/motivation/testing detail. Summary - live re-validation of the attribution module (§7 above) found a real, generalizable gap in its own `locator` tier on a DIFFERENT bug than the one that motivated it: a Qpid AMQP connection failure's stack-trace locator confidently routed every retry to `ProtocolApp.java` (where the exception was thrown), while the model's own FIX ANALYSIS text correctly named `qpid-initial-config.json` (where the actual misconfiguration lived, already documented in the active `qpid` skill) - discarded after being logged, never read again. User explicitly rejected a narrower "config vs. code file" heuristic in favor of reusing information Kriya already generates for free. New `self_diagnosis` tier, ranked above `locator`/`judge`/`triage`: `DeveloperAgent.run_generation()` now threads its FIX ANALYSIS text out as an optional `analysis` key per file; a new `extract_self_diagnosed_files()` checks whether it names a DIFFERENT known file, gated (via the existing repeat-failure-signature machinery, reused for a second purpose) to only apply on a CONFIRMED repeat of the exact failure the diagnosis responded to - never on an unrelated new failure. 895 tests passed (885 + 10 new), same 8 pre-existing/unrelated failures as every baseline this session - zero new regressions.

- **The SAME class of bug (a field set on a live object but never reaching its persisted snapshot) recurred one increment later, on a DIFFERENT field, within the same session.** The new end-to-end test failed on its `likely_files` assertion even though `attribution_tier` was correct and the real retry correctly targeted the redirected file - `failure.likely_files` itself was never overwritten with the final attribution result, only the newer `attribution_*` fields were (the retry loop used a local `implicated` variable correctly; the persisted copy didn't). Confirms the entry directly above this one as a durable, generalizable pattern, not a one-off - every field on a mutated-then-serialized object needs its OWN check for "does this actually reach the persisted copy," adding one field correctly doesn't retroactively fix a sibling field with the identical structural gap.
- **Debating a fix's generalization BEFORE building it caught a wrong turn for free.** The user pushed back on an early instinct (a "config file vs. code file" heuristic) as too narrow - it only covers one axis of "root cause differs from surfacing site," not the general case (a bug in a producer, an exception in a consumer, no config involved at all). The shipped design (reuse the model's own stated diagnosis, checked against what it was asked to fix) covers both without needing to know anything about file types. Worth the debate time before writing code, not after.

## 2026-08-13, same day, third increment: diagnosis-vs-diff check (`find_edits_ignoring_own_diagnosis`)

Shipped: see `docs/design.md` §7.2 for full design/motivation/testing detail. Summary - a THIRD live validation run (`attribution-fix-validation-3`) found a compile error recurring verbatim across 3 consecutive targeted retries, each with a textbook-correct FIX ANALYSIS naming the exact fix (`` `IgniteCache<Integer, Protocol>` `` instead of `` `var` ``) - yet the worktree file never actually changed. Traced to a precise gap in the existing `find_edits_ignoring_reported_line()` (2026-08-07): it only checks the *compiler's* reported line, and deliberately permits a fix at a different, legitimate line (exactly this bug's shape - the real fix was one line above the two reported error lines) without ever checking that the alternative fix actually happened. New sibling function extracts backtick-quoted spans from the analysis and checks whether any appear in the NEW content but not the OLD - resolving the "diagnosis quotes both the problem and the fix" ambiguity precisely rather than guessing from sentence structure. Wired into the exact same pre-flight pattern as its sibling check, so it reuses everything the first two increments built (attribution routing, targeted-retry scoping) for free. 903 tests passed (895 + 8 new), same 8 pre-existing/unrelated failures as every baseline this session - zero new regressions.

- **A user's explicit question ("what's lacking?") after a symptom repeated is a request for root-cause architecture, not a bug report - answer at that altitude or the question wasn't actually answered.** Asked directly why a "days-ago-fixed" error was reappearing. The honest, complete answer required identifying precisely which existing mechanism should have caught this and precisely why it didn't (a documented, deliberate design choice in a sibling check, not a bug in it) - not just "here's a new check to add." Matches [[feedback_architectural_thinking]]'s standing instruction, reapplied to a live-debugging context rather than a post-batch-of-fixes retrospective.
- **A sibling check's own docstring can name the exact gap it deliberately leaves open, months before that gap gets hit live.** `find_edits_ignoring_reported_line()`'s docstring already said, almost verbatim, "correcting a variable's generic-typed declaration several lines above instead of its usage site... is never flagged" - written 2026-08-07 to justify NOT flagging a legitimate case, not anticipating this exact bug shape would exploit that same permitted gap 6 days later with zero follow-through. When a deterministic check's docstring explains what it deliberately doesn't catch, that's a specification for its sibling check, not just documentation.

## 2026-08-13, same day, fourth increment: triage token-budget fix (`_TRIAGE_MAX_TOKENS`)

Shipped: see `docs/design.md` §7's "Token-budget fix" addendum. Summary - a live `gpt-oss:20b` validation run hit `attribution.py`'s `triage` tier returning empty `content` under `json_mode` (`json.loads` → `Expecting value: line 1 column 1 (char 0)`), initially diagnosed as a json_mode/reasoning-field routing incompatibility and suspected fixable by migrating to native tool-calling. Two POCs (`spikes/tool_call_developer/run_spike_small_arg_triage.py`, `run_spike_real_triage_shape.py`) tested that hypothesis directly against real models and the real prompt shape rather than accepting it on plausibility. Tool-calling turned out LESS reliable than `json_mode` for the exact model that motivated the investigation (0/5 proper tool calls for `gpt-oss:20b` vs. 5/5 for `json_mode`, in the first POC) - the opposite of the predicted outcome. The second POC, calling `LLMClient.complete()` directly with the real `_TRIAGE_SYSTEM_PROMPT` and the real `max_tokens_override=300`, reproduced the actual live failure deterministically (3/3, exactly 300/300 output tokens used, empty content every time) and pinned the true root cause: `gpt-oss:20b` reasons internally before writing JSON regardless of Kriya's own `reasoning` config classification of it, and 300 tokens wasn't enough budget to cover that reasoning AND the answer. Fixed by raising the budget to 2000 (`_TRIAGE_MAX_TOKENS`, `kriya/workflow/attribution.py`) - clean 3/3 in the same reproduction. 903 tests passed, same 8 pre-existing/unrelated failures as baseline (confirmed via `git stash`) - zero new regressions, single-constant change.

- **A plausible-sounding fix for a live bug can be wrong even when the diagnosis of the SYMPTOM was right - test the fix against real models before committing to it, especially when the fix implies an architecture change.** The "gpt-oss routes output to an unread reasoning field" diagnosis was accurate as far as it went (that field does exist, and does sometimes carry the real answer - confirmed independently by the first POC's tool-calling misses), but the natural-sounding fix built on top of it (migrate to tool-calling, since a `tool_use`-equivalent block is structurally separate from any reasoning channel) turned out to solve a different problem than the one that actually occurred. The real bug was one layer more specific (a token budget, not a field-routing problem) and the POC found that by testing, not by reasoning harder about the same diagnosis.
- **A simplified reproduction that gets a different result than the live failure isn't "close enough" - it's testing a different bug, and the gap between them can be the actual finding.** The first POC's `json_mode` call for `gpt-oss:20b` succeeded 5/5 with no `max_tokens` cap; the real `_tier_triage()` call caps at 300. That single difference was the entire explanation - a reproduction that doesn't match the real failure needs to be tightened to real fidelity (exact prompt, exact call parameters, ideally the exact production code path) before concluding anything about the discrepancy, rather than shipping a fix validated against the wrong shape.
- **A config flag that says "this model doesn't reason" only controls what KRIYA does about reasoning (tag-stripping, a token floor) - it says nothing about whether the model actually reasons.** `gpt-oss:20b` was classified `reasoning: False` in the eval harness's `llm_chain` (a reasonable classification - it doesn't emit `<think>` tags, Kriya's own signal for "this model reasons") and still spent its entire completion budget on invisible internal reasoning before ever committing to the answer. Any token budget sized "just enough for the expected answer" is implicitly assuming the model won't do unaccounted-for work first - that assumption needs to be evidenced per-model, not inferred from a classification flag built for a different purpose.

## 2026-08-13, later same day: `.py` coverage for the Planner-code-reuse plausibility check

Shipped: see `docs/design.md` §2's `extract_planner_code_blocks()` entry for full design/motivation/testing detail. Summary - a live eval-harness `python_greeter` goal, reproduced identically across two separate runs, took 5 retry attempts (previously: a clean 5-6 second pass) because a Planner-drafted `greet.py` had Kriya's own `"[VERIFICATION] PASS"` runtime-verification marker embedded as a bare, unquoted line instead of inside a `print()` call - genuinely invalid Python, written straight to disk unreviewed. The existing plausibility-check table (`_MIN_PLAUSIBLE_CODE_PATTERN`, shipped 2026-08-12 for the exact same underlying bug class but scoped only to `.java`) had zero entry for `.py`, so nothing caught it. Generalized the table from regex-only (`Dict[str, re.Pattern]`) to a validator-function table (`Dict[str, Callable[[str], bool]]`, renamed `_MIN_PLAUSIBLE_CODE_CHECK`) - `.java` unchanged (keyword regex, no stdlib parser available), `.py` gets `ast.parse()` wrapped in try/except, a real syntax check rather than a heuristic. 905 tests passed (903 + 2 new), same 8 pre-existing/unrelated failures as every baseline this session - zero new regressions.

- **A keyword-presence heuristic that works for one language can be silently insufficient for the next one, in a way that isn't obvious until it's tested against that language's own failure shape.** The `.java` check (search for `class`/`interface`/`enum`/`record`) works because real Java structurally requires one of those keywords at the top level. Python has no equivalent requirement - a completely valid file can be nothing but top-level statements - so the SAME kind of check (search for `def`/`class`/`import`) would have looked reasonable by analogy but would NOT have caught this exact bug: the broken `greet.py` still contained a real `def` elsewhere in the file, just alongside one syntactically invalid line. The right generalization wasn't "port the same pattern to a new language," it was recognizing Python has a stronger, exact check available for free (`ast.parse()`) that Java doesn't, and using it instead of forcing a weaker heuristic to match the existing shape.
- **The same root cause recurring in a new language is still worth writing as its own regression test, not just extending the existing one's data.** Rather than parametrizing the Java test to also cover Python, a dedicated sibling test pair (reject/accept) was added mirroring the Java pair's exact structure - keeps each language's regression test self-contained and readable in isolation, matching this file's own established "one entry per concretely-observed incident" precedent for the underlying table itself.

## 2026-08-13, later same day: `.py` coverage for `extract_error_source_locations()`

Shipped: see `docs/design.md` §2's `extract_error_source_locations()` entry for full design/motivation/testing detail. Summary - the `.py` plausibility-check fix (previous entry) closed the Planner-reuse bypass, but the SAME `python_greeter` goal still failed a full run afterward (`quality_gates_exhausted`, 8 raw retry attempts): `qwen3-coder:30b`'s *normal* Developer generation produced the identical `[VERIFICATION] PASS`-as-bare-invalid-syntax bug directly (no Planner-reuse involved at all), then denied the error was real across 4 straight attempts; once escalated, `glm-4.7-flash` correctly diagnosed the exact one-line fix in its own FIX ANALYSIS text on 4 consecutive retries and never once implemented it. Traced to `extract_error_source_locations()` - the function that decides whether a retry gets to show the model the exact broken line and ask for a small anchored patch, versus falling back to blind full-file regeneration - being `.java`-only. Python's own compile-check error format (`"Syntax error in {file} line {N}: ..."`, `kriya/tools/validate.py`) matched neither of the two existing alternatives, so every Python retry got the full-file-regeneration prompt, unconditionally - the exact same "no anchor -> full-file regeneration -> stated fix gets lost in the rewrite" mechanism already documented and fixed once for a JVM stack-trace shape (2026-08-07), just never extended past Java at all. Fixed by adding a third alternation for Python's shape, switching the pattern from positional to named capture groups in the process (continuing to renumber `group(1) or group(3) or group(5)` was already fragile at two alternatives, and a third made it worse). 908 tests passed (905 + 3 new), same 8 pre-existing/unrelated failures as every baseline this session - zero new regressions.

- **A model repeatedly stating the correct diagnosis and repeatedly failing to execute it is a symptom worth tracing to its OWN root cause, not a data point to just accept as "some fixes aren't nudge-able."** The instinct after seeing `glm-4.7-flash` correctly diagnose the same one-line fix four times in a row and apply none of them could easily have been "this confirms the known correct-diagnosis-failed-execution phenomenon, move on." Digging one level further found a structural, fixable reason specific to this case: the model was never shown the broken line and never asked for a small patch, because the grounding mechanism that would have enabled that request silently didn't recognize Python's error format at all. Not every instance of that phenomenon has a root cause this fixable, but assuming none of them do closes off exactly the cases that are.
- **A fix shipped for one language can leave an identical, un-investigated gap for the next language that hits the same code path - and the fact that a SIBLING fix was already made for a DIFFERENT non-Java shape (the JVM stack-trace extension) is itself the strongest signal to check.** `extract_error_source_locations()` had already been extended once, specifically because "compile-error-only" turned out to be too narrow (a JVM stack trace needed the same treatment). That fix's own docstring described the general shape of the problem ("this failure class got zero source-line grounding... just a different textual shape") without generalizing the actual pattern - the next non-`.java` shape to arrive (Python, a different language entirely, not just a different JVM error format) hit the exact same wall. When a matching function has already been extended once for "this shape wasn't covered," that's a standing prompt to check what ELSE isn't covered, not just to fix the specific shape in front of you.

## 2026-08-13, later same day: `BareVerificationMarkerCheck` (`kriya/workflow/static_checks.py`)

Shipped: see `docs/design.md` §6.1's new paragraph for full design/motivation/testing detail. Summary - after the fifth and sixth increments fixed how the retry loop *recovers* from this bug (the `.py` plausibility check, the Python source-location grounding fix), the user asked directly whether anything could address the *generation-time habit itself* - why `qwen3-coder:30b` writes Kriya's own `"[VERIFICATION] PASS"` verdict-line instruction as bare, unwrapped source text in the first place, explicitly asking for a generic fix, not a Python-specific patch. Added a third check to the existing deterministic static-checks scanner (already proven, 2026-08-12, for two Ignite-specific anti-patterns) - unlike those two, the new check is language-generic by construction: it scans every written file regardless of extension for a stripped line starting bare with the marker text, since the mistake shape (verdict text not wrapped in an actual print/output call) is identical no matter the target language, and nothing guarantees it's a compile error in every language the way it happens to be in Python. Deliberately scoped to only the confirmed shape - a plausible sibling mistake (correctly quoted but never printed) is noted as a known, not-yet-observed gap rather than speculatively covered. 6 new tests, including clean-pass fixtures for both Python AND Java to prove the language-genericity claim, not just assert it. 914 tests passed (908 + 6 new), same 8 pre-existing/unrelated failures - zero new regressions.

- **"Catch it fast and redirect" and "prevent it from happening" are two different fixes for the same bug, and the user's own framing ("the fix should be generic") pointed specifically at the SHAPE of fix, not just its presence.** The fifth/sixth increments already technically "did something" about this bug (made recovery fast and reliable once it happened) - but the user's follow-up question named the exact gap those fixes left open: nothing addressed why the mistake gets made on attempt 1, before any retry context exists. The right answer wasn't a third recovery-path improvement, it was recognizing this needed a pre-flight *prevention* check, and that the existing `static_checks.py` module - built for a completely unrelated Ignite-specific bug - was already exactly the right home for it, generalized correctly on the first attempt (scans every file, not just `.java`) because the underlying mistake pattern was never actually Java-specific to begin with, only the two existing checks happened to be.

## 2026-08-13, later same day: false-positive fix in `find_edits_ignoring_own_diagnosis()`

Shipped: see `docs/design.md` §7.2's new addendum for full design/motivation/testing detail. Summary - live re-validation of the seventh increment's static check showed it correctly catching the bug on attempt 1, then SIX consecutive `DIAGNOSIS MISMATCH` failures across two different models (`qwen3-coder:30b`, `glm-4.7-flash`), each one quoting the exact correct one-line fix. Initially read (in the prior turn) as "not every fix is nudge-able" - direct reproduction of the check function against the exact scenario instead proved the check ITSELF was wrong: a correct WRAP edit (`X` -> `print(X)`) leaves a quoted span byte-identical by construction, which the check's original "must be new" signal can never satisfy - a false positive that had been actively rejecting correct fixes, not a model capability gap at all. Fixed by adding a second, narrowly-scoped signal (the quote must equal the ENTIRE old search text, not just appear somewhere within it) after a first, looser attempt was caught reintroducing the original bug via the existing golden regression test. 916 tests passed (914 + 2 new), same 8 pre-existing/unrelated failures - zero new regressions.

- **A conclusion reached by reasoning about a model's behavior from the OUTSIDE (repeated correct diagnosis, repeated failed execution) can be flatly wrong when the actual mechanism is checkable directly - always verify against the code before accepting a plausible-sounding behavioral explanation.** "Some fixes aren't nudge-able" was a real, previously-documented phenomenon (`spikes/fix_alignment`), which made it an easy, plausible-sounding explanation to reach for again here. It was wrong this time - the actual cause was two calls deep in Kriya's own code, invisible from the log alone, and only became certain by directly calling the check function with the exact live inputs and reading its source. The lesson isn't "don't trust prior findings" - it's that a prior finding explains ONE mechanism, and a superficially similar symptom deserves the same direct-verification standard as a first occurrence, not an inherited conclusion.
- **A deterministic pre-flight check that reduces false negatives can introduce false positives in the exact same motion, and the fix for one can very plausibly reintroduce the other if tested with only the new case in mind.** The first attempted fix here (broaden "old text survives in new text") passed the new wrap-fix test cleanly but broke an EXISTING, unrelated golden regression test - both failure modes live in the same boolean condition, pulling in opposite directions. Running the full existing test suite for the function (not just the new test) is what caught this before it shipped; a narrower "does my new test pass" check would have shipped a regression in the same commit that fixed the original bug.
- **The most valuable review of a check you shipped today is running it against real, DIFFERENT live traffic, not just its own test suite.** This bug was found only because the user re-ran the exact same goal that had already been "fixed" twice this session and looked closely at what happened next, rather than treating a static check firing correctly (attempt 1, the bare-marker catch) as confirmation the whole retry sequence was healthy. The static check's own test suite was never wrong; it never exercised what happens to the retry loop AFTER it fires.

## 2026-08-13, later same day: `static_rule_violation` file-scoping fix (`kriya/workflow/attempt.py`)

Shipped: see `docs/design.md` §6.1's new addendum for full design/motivation/testing detail. Summary - a fresh live `ignite_qpid_protocol` run (immediately following the seventh/eighth increments) showed a targeted retry broadcasting to all 7 written files after an `ignite_unclosed_resource` violation that only ever named one (`ProtocolApp.java`) - 6 wasted completions, one of which applied a genuinely unrelated edit. Traced to the raise site constructing its `Failure` directly (`likely_files=list(state.all_files_written)`) instead of routing through `_build_quality_gate_failure()`, the shared constructor every OTHER gate type (compile/test/run_verification/regression_test) already uses - whose `extract_implicated_files()` call would have scoped correctly, since every `StaticCheck`'s own message already names its implicated file(s) by exact path. Confirmed directly via `extract_implicated_files()` against the real captured violation text before writing the fix, not assumed. Fixed by switching to the shared constructor - no new scoping logic invented, an existing mechanism simply applied where it was already the norm everywhere else. Also improves `IgniteMethodMixingCheck`'s scoping for free (it was broadcasting too, despite only ever needing 2 files). 917 tests passed (916 + 1 new, plus one strengthened existing assertion), same 8 pre-existing/unrelated failures - zero new regressions.

- **When one raise site in a family of similar raise sites does something differently, ask why before assuming it's intentional.** Every OTHER Quality Gate failure type already went through `_build_quality_gate_failure()` for exactly this scoping reason - `static_rule_violation` was the ONE type that bypassed it with a bespoke, broader `Failure(...)` construction. The fix wasn't inventing new scoping logic; it was noticing the inconsistency and asking why this one call site was different, then finding no good answer (the `run_static_checks()` violation text already has everything `extract_implicated_files()` needs - it always did).
- **A bug found by digging into "why does this take so long" can turn out to be upstream of a bug already fixed today, not a new unrelated one.** This scoping bug is what caused the FIRST occurrence of the `ignite_unclosed_resource` violation to broadcast in the first place - the same broadcast pattern that then required the eighth increment's false-positive fix in `find_edits_ignoring_own_diagnosis()` to even have a chance of resolving cleanly on a later retry. Fixing the root (correct scoping) makes today's earlier fixes (the false-positive fix, the self-diagnosis mechanism) less load-bearing for this specific failure shape going forward - a reminder that a good day's chain of fixes can still have an earlier, un-found root cause underneath the fixes already shipped.

## 2026-08-14: misdirected-edit detection (`kriya/workflow/edit_safety.py`)

Shipped: see `docs/design.md` §7.3 for full design/motivation/testing detail. Summary - eval-harness batch `a-3`'s `ignite_qpid_protocol` run TIMED OUT at 2400s. Traced precisely: a runtime verification failure's stack trace named only `ProtocolApp.java` (the file with an explicit `if (!equals) throw` check), scoping the targeted retry there - but the real bug was in `ProtocolParser.encode()`, a different file whose method returns wrong data silently rather than throwing, so it never appears in any stack trace at all. The model's own diagnosis correctly named the real cause, but its edit's search block - lifted from `ProtocolParser.encode()`'s own body - could never match `ProtocolApp.java`'s content, burning the retry before a slow fallback-model escalation ran out the clock. New `find_misdirected_edit_target()` checks, on an anchor-match failure, whether the failed search block exists verbatim (whitespace-tolerant) inside a DIFFERENT already-written file; if so, widens the next retry's scope to both files via the existing `attribute_failure()` locator tier - no new plumbing. Purely additive: falls through unchanged to the prior generic failure when no other file matches. Considered and rejected extending tier 0's `self_diagnosis` mechanism instead - traced why it didn't already fire here (its confirmed-repeat signature gate never matched, since the diagnosis-bearing attempt failed on a NEW failure type, not a repeat) and confirmed the content-based check is strictly more robust anyway, since it doesn't depend on the model naming the file correctly in prose. 923 tests passed (917 + 6 new), same 8 pre-existing/unrelated failures - zero new regressions.

- **A locator that's reliable for compile errors and thrown exceptions has a structural blind spot for silent-failure bugs, and no amount of improving the locator-extraction logic itself can close it.** `extract_implicated_files()` and its stack-trace/compile-error locators are all built on "the file that surfaces the failure names itself in the error text." An explicit `if (check) throw` runtime-verification pattern - the exact shape Kriya's own prompts encourage every goal to write - only ever names the file with the CHECK, never the file whose logic silently computed the wrong value being checked. The fix couldn't be "extend the locator regex further"; it had to use different evidence entirely (the edit's own content, cross-referenced against real file contents) precisely because the error text structurally never contains the answer.
- **Two mechanisms can look like they should overlap (self-diagnosis text naming a file vs. the edit's content matching a file) without one making the other redundant - trace exactly why the existing one didn't fire before building a new one next to it.** The model's analysis text DID name `ProtocolParser` by name in the live incident, which made "why doesn't self_diagnosis already handle this" the right question to answer before writing new code. The answer (a confirmed-repeat gate designed for a different scenario) confirmed this was a genuinely separate gap, not a rediscovery of an existing one - and also confirmed the new content-based signal is more robust on its own merits, not just non-overlapping.

## 2026-08-14, later same day: targeted-budget exhaustion forcing an unscoped full-set walk

Shipped: see `docs/design.md` §7.4 for full design/motivation/testing detail. Summary - two consecutive live re-validation runs of the misdirected-edit fix (`a-3`, `a-4`) both timed out at 2400s. `a-4` traced precisely: `RetryBudgets.targeted_retry_count`/`fallback_targeted_attempted` are single counters shared across the WHOLE run, not per-failure - a run that spends its entire targeted budget correctly resolving one bug (here: 4 targeted attempts + 1 fallback-targeted fixing an unclosed Ignite resource, `diagnosis_mismatch` correctly rejecting two verbatim-repeated non-fixes along the way) has zero scoped-retry runway left for the NEXT, completely different, freshly-diagnosed failure - even one with a precise, single-file javac locator (`Protocol.java:[17,5] variable dataLength might not have been initialized`). That brand-new failure fell straight into an unscoped full-file-set walk on the slow fallback model (one multi-minute completion per file, ~9 files, for a fix that needed exactly one), and the run was killed by the timeout mid-walk. Fixed by reusing the already-existing `known_target_files` mechanism (already trusted unconditionally by every targeted/fallback_targeted branch, and by the full-set branch's own attempt-1 `expected_files_upfront` optimization) one branch later: the FIRST full-set attempt, if it already has real attribution evidence (`state.last_implicated_files`), gets scoped too - deliberately gated to `retry_count == 0` only, so a failure that genuinely resists narrow scoping even after this extra scoped shot still gets the existing full clean-slate regeneration escape hatch on every attempt after. 925 tests passed (923 + 2 new), same 8 pre-existing/unrelated failures - zero new regressions.

- **A shared, run-global budget counter can silently penalize an unrelated later failure for an earlier failure's cost, and the fix isn't always "give it more budget" - sometimes it's "spend the remaining budget smarter."** Increasing `targeted_max_retries` would have papered over this specific case without addressing the structural issue: any run pathological enough could still exhaust a larger budget the same way. The actual fix left every existing budget/termination guarantee completely untouched (deliberately rejected a per-failure-signature budget redesign, which would have required re-reasoning the outer loop's entire termination safety) and instead made the one already-existing "first full-set attempt" moment smarter about what it asks for, when real evidence already exists.
- **When a live run repeats the exact same diagnosis text verbatim across consecutive attempts without adapting, that's worth watching closely, but it isn't automatically a Kriya bug** - `a-4`'s `diagnosis_mismatch` check correctly rejected the identical non-fix twice in a row and did not block the eventual real fix on the third attempt, a clean confirmation the check's precision holds up under a genuinely repeated non-fix (not a repeat of increment 8's false-positive class). The run's actual failure came from a completely different, later mechanism (budget exhaustion cascading into an unscoped walk) - a reminder to keep tracing the WHOLE run's arc rather than stopping at the first interesting-looking pattern.

## 2026-08-14, later same day: stale-removal signal in the diagnosis-vs-diff check

Shipped: see `docs/design.md` §7.5 for full design/motivation/testing detail. Summary - a structured investigation built specifically to find leads for improving Kriya (`spikes/protocol_bug_pocs/`, isolated POCs + an incremental goal-composition POC) reproduced, independently of any live Kriya run, the exact `BufferOverflowException` bug `skills/binary-wire-protocol/rules.txt` already documents: generated code containing BOTH the wrong `buffer.putInt(dataLength)` call and the correct manual byte-shift replacement in the same method - a half-finished migration. Confirmed via the real logs that this skill IS loaded and injected into every single live `ignite_qpid_protocol` run this session, so the gap isn't missing knowledge - it's that a retry loop encountering this exact half-finished shape would have passed it: §7.2's diagnosis-vs-diff check only ever validated a diagnosis's positive claim ("add Y"), never its negative one ("instead of X" also means X should be GONE). Added a second, independent signal to the same function: a quoted span immediately preceded by an explicit removal-signaling phrase ("instead of `X`", "never `X`", etc.) that's STILL present in the new content is direct evidence of an incomplete fix, checked before and independent of the existing addition signal. A real bug in the new code was found and fixed during its own test-writing: a naive `\b...\b` word-boundary wrapper (needed to stop a short flagged token like `var` false-matching inside `variable`) itself failed to match a legitimately-punctuation-ending span like `buffer.putInt(dataLength)` followed by `;` in real code, since `\b` can never fire between two non-word characters - fixed with a new `_still_contains()` helper that only anchors `\b` at whichever edge of the span is actually a word character. 930 tests passed (925 + 5 new), same 8 pre-existing/unrelated failures - zero new regressions.

- **A skill's own documented "known incident" text is a goldmine for finding what the retry loop should specifically detect and handle - it's a list of bugs already proven to recur, with the exact fix already spelled out.** `skills/binary-wire-protocol/rules.txt` didn't just describe the bug in the abstract; it named the exact API call, the exact exception, and the exact correct replacement - reproducing it in isolation and then asking "would today's retry-loop checks actually catch a half-finished version of this specific fix" led directly to the gap, rather than needing to invent a hypothetical failure mode from scratch.
- **A dedicated, no-time-pressure test-writing pass for a new check can surface a real bug in that SAME check's own new logic, and the fix should be found and closed before shipping, not deferred.** The `\b` word-boundary bug here wasn't found by reasoning about the regex in the abstract - it was found by writing a realistic test fixture (a call ending in `)` immediately followed by `;`, exactly how the real bug's code actually reads) and watching it fail. The lesson from earlier this session ("run the full test suite, not just your new test") extends one step further: write test fixtures that mirror the REAL shape of the problem as closely as possible, not a simplified version that happens to avoid exercising the exact edge that matters.

## 2026-08-14, later same day: skill-conventions reminder at generation time

Shipped: see `docs/design.md` §7.6 for full design/motivation/testing detail. Summary - user asked to pursue the skill-application-reliability lead after initially setting it aside. Investigating where skill content actually lands in the real prompt found an exact, already-solved precedent sitting right there in `_fill_missing_content()`'s own comments: `VERIFICATION_CONTRACT_HEADER` had this EXACT problem once before (reached the prompt correctly, still wasn't reliably followed) and was already fixed by repeating a short reminder at the very end, right before generation - the same fix already proven for the "only this file" instruction before THAT. Skill conventions sit even earlier in the prompt (the very first section) and had never received the same treatment. Added a conditional reminder (only when a skill section is actually present, zero cost otherwise) in the exact same place/style as the existing verification-contract reminder - no new plumbing, reuses a substring check against a string the function already has. 932 tests passed (930 + 2 new), same 8 pre-existing/unrelated failures - zero new regressions.

- **When investigating "why doesn't the model follow this instruction," check whether the exact same shape of problem has already been solved once for a DIFFERENT piece of content in this codebase, before designing something new.** The fix here isn't a novel idea - it's the third application of one already-proven pattern ("only this file" -> verification contract -> skill conventions), each one found by reading the PREVIOUS fix's own comment, which explicitly named the general shape ("a single early mention buried under everything the prompt adds after it") rather than just describing its own specific case. Comments that generalize the lesson, not just document the incident, are what made this third application fast to find.
- **A live incident's own root-cause investigation can be redirected mid-stream by the user (dismiss now, pursue later) without losing the analysis already done - the memory/docs written for the "dismissed" version are exactly what made picking it back up later fast.** The finding, the two confirmed instances (qpid, binary-wire-protocol), and the fact that no fix was obvious yet were all already captured before the user asked to drop it; when asked to pick it back up, no re-diagnosis was needed - just the next step (find where the content lands) on top of already-solid ground.

## 2026-08-14, later same day: new `binary-wire-protocol` rule for absolute-vs-relative ByteBuffer addressing

Shipped: see `docs/design.md` §7.7 for full design/motivation/testing detail. Summary - continued re-runs of the incremental-composition POC surfaced a SECOND, distinct wire-format bug mechanism (confirmed twice): `buffer.putInt(0, bodyLength)`, an absolute-indexed write at a hard-coded offset that silently overwrites already-written earlier fields, producing no compile error and no `BufferOverflowException` - the corruption only ever surfaces as an unrelated-looking downstream validation error. A new static check was considered and rejected: absolute-indexed ByteBuffer writes are legitimate for one real shape (a "backpatch" - write a placeholder, correct it once a later value is known), and distinguishing that from the bad shape reliably from source text alone wasn't achievable with the same confidence every other static check this session has. Documented as two new rules in the already-verified `skills/binary-wire-protocol/rules.txt` instead (6 rules total now), reusing the skill-matching + reminder infrastructure already shipped and proven today rather than adding new detection logic with real false-positive risk.

- **Not every well-evidenced bug needs a code fix - sometimes the right-sized fix is documentation fed into an already-working channel.** The instinct after confirming a bug mechanism twice is to reach for a static check, matching the pattern of most of today's other increments. Here that instinct was checked against a real cost (false positives on a legitimate, recognizable "backpatch" ByteBuffer pattern) and found wanting - the skill-rule route was chosen specifically because it was BETTER justified for this shape, not because it was easier.
- **Two independent reproductions of the exact same code-level mistake, from a small isolated POC with no connection to any specific live Kriya run, is strong enough evidence to act on.** Neither confirmed instance of this bug came from a live `ignite_qpid_protocol` failure - both came from the POC harness built to find leads. The evidence bar for "this is real and recurring" didn't require waiting for a third live incident; a controlled, repeated reproduction outside the noisy full pipeline was sufficient on its own.

## 2026-08-14, later same day: file-attribution consolidation

Shipped: see `docs/design.md` §7.8 for full design/motivation/testing detail. Summary - the "which file does this concern" logic had grown correct but scattered across three files (`attribution.py` for failure→file, `edit_safety.py` for edit→file, `toolchain.py` for missing-manifest detection), the exact shape that motivated `attribution.py`'s own creation back on 2026-08-13. Built a 9-row taxonomy (SWE-bench-style fault-localization research + real Aider/Cline/Copilot issues, cross-referenced against Kriya's actual code) covering every "which file" question the retry loop asks, then moved every edit→file and missing-manifest function into `attribution.py` alongside the existing failure→file tiers, rewriting the module's own docstring to literally BE the taxonomy table so a future gap is visible by inspection rather than by the next live failure. `edit_safety.py` now holds only mechanical edit-safety (does an edit apply, does the result look structurally sound), never "which file." Found and fixed one real, previously-undiscovered bug along the way, by code inspection rather than a live incident: two call sites (`_build_error_source_context`, `_resolve_file_locations`, both in `failure_grounding.py`) independently built a basename→file dict that silently dropped all but the last file when two known files shared a basename - fixed with one shared helper, `_files_by_basename()`, that keeps every match. 935 tests passed (932 + 3 new), same 8 pre-existing/unrelated failures - zero new regressions. Two taxonomy gaps found but deliberately left untested this increment (multi-file coordination, chained-exception depth) - documented in `attribution.py`'s own docstring rather than silently dropped, not acted on since neither has a confirmed live incident behind it yet.

- **A taxonomy built from external research plus a codebase's own actual functions can find a real bug with zero live incident behind it - inspection-driven review is a legitimate way to find bugs, not just a way to organize known ones.** The basename-collision bug wasn't hunted for; it fell out of asking "does every function answering this class of question actually answer it correctly" while building the taxonomy table. The same discipline that motivated consolidating scattered logic (so gaps are visible by inspection) also caught a bug via that exact inspection, before it ever needed a live run to surface.
- **When two independently-written functions build the same kind of lookup (here: basename → file), a lossy shortcut in one of them tends to exist in the other too, and a third correctly-written sibling elsewhere in the same codebase is the tell.** `extract_implicated_files()`'s list-comprehension scan (correct - keeps every match) sat right next to two dict-comprehension versions (lossy - keeps one) doing conceptually the same lookup. Once one lossy version is spotted, checking every other function answering the same shape of question is worth the few extra minutes - both actually-broken instances were found this way, not independently rediscovered later.

## 2026-08-14, later same day: widen anchored-edit preference beyond compile-locator failures

Shipped: see `docs/design.md` §7.9 for full design/motivation/testing detail. Summary - a fresh live `ignite_qpid_protocol` run (`a-6`) fixed an import-path compile error via a targeted retry, then hit an unrelated runtime-verification failure (no exception, no compiler locator) on the same file - the next retry's fix analysis correctly addressed the runtime bug, but the resulting full-file rewrite silently reverted the already-fixed import, reintroducing the identical compile error. Traced to `_fill_missing_content()`'s `prefer_anchored_edit` gate requiring a precise compiler-style line locator, which a runtime-verification failure never has - forcing every locator-less failure into a full `FILE CONTENT:` rewrite, even though the file's current, correct content was already sitting in the prompt (`_build_targeted_retry_prompt`/`_build_full_set_retry_prompt` both embed it unconditionally). Widened the gate with a new `files_with_current_content` parameter - any file already written this run (and therefore guaranteed to have its real content embedded in the prompt) now qualifies a small anchored patch as well-grounded, not just a file with a compiler-reported line. 937 tests passed (935 + 2 new), same 8 pre-existing/unrelated failures - zero new regressions.

- **A gate built for one failure shape (compile errors with precise locators) can silently exclude a whole different failure category (runtime/logic failures) without anyone noticing, because the excluded category just quietly falls back to already-existing behavior (full-file regeneration) instead of erroring.** Nothing about `prefer_anchored_edit` staying `False` for a runtime-verification failure looked broken in isolation - full-file regeneration is a normal, working code path. The actual cost (a full rewrite silently dropping unrelated correct content) only became visible by tracing a live run end to end, not by reading the gate's own condition and asking "is this correct" in the abstract.
- **The real signal a locator-based gate is trying to approximate is "do we have real content to copy from," not "does the compiler happen to report a line number."** The locator was always a proxy for a more general, already-available fact (the file's current content is in the prompt) - once that was named precisely, the fix was a strict widening of an existing condition, not a new mechanism.

## 2026-08-14, later same day: new `binary-wire-protocol` rule for sample-value units vs. declared wire width

Shipped: see `docs/design.md` §7.10 for full design/motivation/testing detail. Summary - direct inspection of `a-6`'s actual generated code (not just its log) found the `time` field's encode/decode byte-shifting logic was correct; the bug was one call site earlier, where the sample `Protocol`'s `time` value was populated via `System.currentTimeMillis()` (milliseconds) against a wire field documented as 32-bit seconds-since-epoch - a unit mismatch that deterministically breaks the round-trip regardless of how correct the packing arithmetic is. Directly compared the goal's own text against the isolated POC that passes 20/20 and found them near-identical - the real difference is that the isolated POC's test harness supplies the sample value itself, never giving the model a chance to pick the wrong-unit idiom, while both the composed POC and the real goal require the model to author its own sample data. Added as a seventh rule to the already-verified `binary-wire-protocol` skill, same reasoning and citation style as the six existing rules - reuses proven infrastructure rather than adding new detection logic.

- **A symptom that looks identical to a known bug class can have a completely different root cause one call site away - verify against the actual generated code, not just the failure signature.** "[VERIFICATION] FAIL: Protocols do not match" with no exception looks exactly like an encode/decode arithmetic bug, the shape this skill's first six rules already cover. Reading `ProtocolParser.java` directly (not assumed from the error text) showed the packing logic was correct - the bug was in `ProtocolApp.java`'s own sample-data construction, a different file and a different mistake entirely.
- **"Why did this pass in an isolated test but fail in the full goal" is answerable by diffing what each version actually asks the model to write, not just diffing the prompt text.** The wire-format description was nearly identical in both places; the isolated version simply never gave the model an opportunity to make this specific mistake, because it never asked the model to construct the sample value at all. A text diff alone would have missed this - the real difference was in scope of responsibility, not wording.

## 2026-08-14, later same day: static check for a test contradicting the verification marker

Shipped: see `docs/design.md` §7.11 for full design/motivation/testing detail. Summary - a hand-run `wordcount.py` demo goal exhausted its whole retry budget and failed, even though the generated app was directly confirmed correct by running it manually. The real cause: the model's own `test_empty_file` asserted subprocess stdout was exactly empty, while the same completion's `wordcount.py` (correctly) always prints `[VERIFICATION] PASS` per the standing convention - an unsatisfiable contradiction between two files the model wrote itself, not a defect in either one read alone. Checking the eval harness's own goal-set history for the same risk class found two already-fixed precedents (`ruby_word_count`'s Gemfile/RSpec self-contradiction, `python_task_tracker`'s in-memory-store-vs-per-invocation-subprocess mismatch) and confirmed `ignite_qpid_protocol` structurally can't hit this specific mechanism (no separate test file to contradict its own printed verdict). New `TestContradictsVerificationMarkerCheck` added to the existing static-checks family: flags a `subprocess.run(...)` stdout-exact-empty assertion only when the SPECIFIC script it invokes is confirmed (by content, not just by being in the same batch) to itself print the verification marker. 944 tests passed (937 + 7 new, including a golden regression fixture reproducing the real incident byte-for-byte), same 8 pre-existing/unrelated failures - zero new regressions.

- **Not every Quality Gate failure means the generated application is broken - sometimes Kriya's own generated test is the thing that's wrong, and the fastest way to tell the difference is to just run the deliverable yourself.** Running `wordcount.py` directly took seconds and settled the question immediately; everything after that was tracing WHY the gate was wrong, not whether it was. Worth defaulting to this check first whenever a Quality Gate failure looks confusing relative to how simple the underlying goal was.
- **The same underlying pattern - a goal or a generated artifact making two demands that can never both be satisfied - shows up in at least three different places in this codebase's own history, and recognizing it as one pattern instead of three separate incidents is what made today's fix fast.** A self-contradictory GOAL (`ruby_word_count`), a verification METHOD mismatched to how the app is actually invoked (`python_task_tracker`), and now a generated TEST contradicting a generated IMPLEMENTATION's own required behavior are the same shape at three different layers - useful to keep in mind as a named category, not just three unrelated bugs, when the next one shows up somewhere else again.

## 2026-08-15: external adversarial review, Findings 5 and 2 - `bool()` truthy coercion and a fail-open approval gate

Shipped: see `docs/design.md` §7.12 for full design/motivation/testing detail. Summary - a commissioned external SME review (independent agent, no code-change authority, tasked to be brutal about objective/architecture/prompts) produced 12 ranked findings; two were spot-checked directly against source before trusting them (per this session's standing verify-at-the-source discipline) and confirmed real. **Finding 5**: `RunVerifierAgent.judge()`/`.grade()` wrapped model-authored JSON fields in bare `bool(...)` - confirmed live that `bool("false")` is `True` in Python, so a model writing the JSON string `"false"` instead of the literal `false` got silently treated as a pass. Fixed with a new `_coerce_bool_field()` helper (`kriya/agents/agent.py`), string-matching true/false variants explicitly and defaulting `False` (fail-safe) on anything unrecognized. **Finding 2**: the Pre-Apply Human Approval Gate (`kriya/workflow/workflow.py`) had no `else`/`elif` branch for "approval required but no callback wired" - it fell through and applied the changes anyway, silently bypassing the gate. Fixed with a new `elif` branch and a shared `_abort_without_applying()` helper, matching the existing `knowledge_gap`/`human_rejected` early-return pattern, returning a new `approval_required` status.

**The Finding 2 fix surfaced a much bigger latent problem**: the full suite went from the known 6-failure baseline to 109 failures. Root cause: `AutonomyConfig.mode` defaults to `"human-in-the-loop"`, and 103 tests (102 in `test_workflow.py`, 1 in `test_milestone3_4.py` - including one written earlier this same session, believed at the time to be testing autonomous-mode behavior) used bare `cfg = AppConfig()` with no override and no `approval_callback`, unknowingly depending on the exact fail-open bug just fixed to reach their real assertions. Per explicit user instruction to confirm this was a real, addressable bug before mass-fixing, checked every actual caller of `run_generation_workflow()`: only `kriya/cli.py`'s `generate`/`fix` exist today and both already wire a real callback (`kriya/mcp/server.py` doesn't call it at all yet) - so the bug was structurally real and would silently bypass approval for any future caller that omits the callback, but wasn't currently exploitable through either shipped CLI path. Reported that nuanced picture back before proceeding. All 103 fixed via a scripted, backed-up, function-boundary-aware edit (insert `cfg.autonomy.mode = "guardrails"` after each function's own `cfg = AppConfig()`, skip anything already explicit, bottom-up per file) - chosen over changing the global test default so each test's real intent (most have nothing to do with approval) stays legible at the call site. Full suite after: 1004 passed, back to the exact same 6 known baseline failures.

- **A safety-boundary bug found by direct code inspection (not a live incident) can still be the most consequential finding of a review, if the test suite has been silently building on top of the exact hole it patches.** The bug itself was a two-line diff (a missing `elif`); the real work was discovering that 103 tests had quietly normalized the broken behavior as "how this works," which would have made the correct fix look like it broke the suite if the fallout hadn't been traced to its actual root cause first.
- **Before mass-fixing a large batch of newly-failing tests, check whether the underlying bug is *currently reachable* through real, shipped call sites - not just theoretically possible.** The answer here was nuanced (real and structurally confirmed, but not exploitable via either shipped CLI entrypoint today) rather than a flat yes/no, and reporting that precise nuance was what the user explicitly asked for before authorizing the fix - a flattened "yes it's a real bug" or "no it's just tests" would have both been misleading.

## 2026-08-15, later same day: external adversarial review, Finding 8 - zero token budgeting on the Developer's sibling-content section

Shipped: see `docs/design.md` §7.13 for full design/motivation/testing detail. Summary - the third and final of the three findings authorized together ("kick of 5 2 and 8"; 5 and 2 above). `DeveloperAgent._fill_missing_content()`'s "Already-Written File This Batch" sibling section (itself a fix shipped earlier this same session, for a real cross-file-consistency bug) concatenated every already-written sibling's FULL content unconditionally - the exact same unbounded-auxiliary-text bug class `_reserve_graph_context_budget()` was already built to fix for `skills_prompt`/`learned_rag_context` (2026-08-07 incident), just unaddressed in this other part of the same prompt. Fixed with a symmetric new `_reserve_sibling_content_budget()` (`kriya/workflow/context_budget.py`, `0.15` fraction vs. the graph budget's `0.75` - reference-only material, not the primary content being generated - floored at 500 tokens), threaded through `_fill_missing_content()`/`run_generation()` as a new `sibling_content_budget` param and wired at all four of `attempt.py`'s retry-loop call sites, each scaled to that site's own active model's context window (primary vs. fallback), matching the existing per-attempt budget-scaling convention. A sibling that doesn't fit the remaining budget now falls into a new, explicit "contents omitted - budget reached; filenames only" notice - kept distinct from the pre-existing "not yet written" notice, since conflating the two would misleadingly imply a file with real content doesn't exist yet. 6 new tests (3 pure-function, 2 `_fill_missing_content()`-level, 1 full-workflow wiring assertion).

- **The same class of bug can recur in a different part of the same file's own prompt, even right after being fixed once elsewhere.** `_reserve_graph_context_budget()` closed this exact "unbounded auxiliary text silently added to a budgeted prompt" gap for skills/RAG content back on 2026-08-07; the sibling-content section (built LATER, same session as this fix, by a different increment entirely focused on cross-file consistency) reintroduced the identical shape of bug without anyone connecting the two until the external review's Finding 8 named it directly. Worth checking any newly-added prompt-concatenation site against this specific, already-known failure mode, not just against its own immediate goal.

## 2026-08-15/16: second external review (prompt quality/LLM-consistency focus) validated and partially adopted - HELD pending live test, not committed

A second, separately-commissioned external review, this one scoped specifically to prompt quality and small-local-model reliability (distinct from the architecture-focused review above), was checked claim-by-claim against actual source before accepting any of it (learned_rag_context's existing untrusted-fencing, contradicted the review's blanket "no RAG is fenced" claim; the Java "two universally-correct fixes" scaffold, confirmed real but under-contextualized by the review as a casual overclaim when it's actually a targeted response to a specific, replayed 2026-08-07 incident). See `docs/design.md` §7.14 for the one item implemented so far.

- **Shipped, HELD**: `DeveloperAgent`'s per-file system prompt (`file_sys_prompt`) used to unconditionally claim "Return ONLY the raw file content" even during a retry, directly contradicting the SAME completion's user-message instruction to instead write a FIX ANALYSIS/SEARCH/REPLACE/FILE CONTENT/NO CHANGE NEEDED response - confirmed real by direct code read, not accepted at face value. A second, identical contradiction existed one level down inside the user message's own "Please generate..."/"Return ONLY the content of..." line. Both fixed: `file_sys_prompt` now built as one of three mode-aware variants (CREATE_FULL_FILE / REPAIR-with-anchored-edit-offered / REPAIR-without), decided deterministically from `apply_fix_analysis`/`prefer_anchored_edit` (moved earlier in the function so they're known before the prompt is built) rather than left for the model to infer; the user message's equivalent line replaced with a new mode-aware `generation_directive`. Kept short and stable, not a second copy of `fix_analysis_instruction`'s full detail - matches this module's own already-validated "repeat critical instructions near the generation point" pattern instead of duplicating the whole spec. Marker parsing itself (`_split_fix_analysis`, `apply_anchored_edits()`, etc.) is completely untouched - pure prompt-wording fix, zero parsing-code risk. 3 new tests in `tests/test_agents.py`.
- **Two self-introduced regressions surfaced while shipping this - caught at two different points, worth recording honestly rather than glossing over.** (1) A first pass of the REPAIR-mode `generation_directive` dropped the "do not touch any other file" reminder the surrounding comment documents as confirmed live-necessary for a reasoning model - caught during implementation, before any test ran, by rereading that comment. (2) A second, NOT self-caught regression: `generation_directive` initially gated on `apply_fix_analysis` alone, so it mentioned SEARCH:/REPLACE: on every retry regardless of `prefer_anchored_edit` - silently reintroducing an anchor mention even with no source location to ground it. **This one was only caught because the user ran the mocked suite before any live test, per the standing "hold for live test" instruction** - two EXISTING tests (written well before this session, not new regression tests) failed: `test_fill_missing_content_no_anchored_edit_preference_without_source_context`, `..._when_file_not_in_current_content_set`. Fixed by giving `generation_directive` the same three-way branch `fix_analysis_instruction` already had. Direct, concrete validation that "hold for live test" is the right call even before reaching the live-model stage - the mocked suite alone caught a real regression a code-only review missed.
- **Explicitly rejected from the source review**: converting SEARCH:/REPLACE: to a JSON envelope (this codebase has repeated, direct evidence that local/quantized models are unreliable at JSON-escaping large multiline source, even for far simpler fields - multiple fallback parsers already exist for short boolean/string-list JSON responses); splitting NO CHANGE NEEDED: into a separate upfront LLM call (already works as a same-call marker, confirmed by the 2026-08-10 co-implication incident it was built to fix, no evidence the combined-call version is unreliable).
- **HELD, NOT COMMITTED - explicit user instruction**: this touches the single most heavily-exercised code path in the whole pipeline (every per-file Developer generation, first-attempt and retry alike). Mocked suite alone (`.venv/bin/pytest`, confirmed 1004 passed/6 known-baseline-failures) is not sufficient sign-off for this one - holding for a live-model test run before check-in, matching this session's own standing live-testing-before-ship practice.
- **Other findings from this review, not yet acted on**: fence `RunVerifierAgent.grade()`'s captured stdout as untrusted (confirmed real - stdout is embedded raw, no framing, directly ahead of the pass/fail decision question; highest remaining priority, cheap); loosen the Java scaffold's "exactly two universally-correct fixes" wording (partially valid - the line-locator enforcement is evidence-backed and should stay, but there's a real third fix shape - changing what produces the value - the current wording forbids); whole-prompt budgeting beyond graph-RAG/sibling-content; structured validation for `edits`/`content` shapes (extends this session's own Finding-5 `_coerce_bool_field` philosophy). Assessed as reasonable but not committed to a timeline.
- **Rejected/deferred, with reasoning already given to the user**: an "interface ledger" replacing full sibling content (their proposed schema omits constructors - would regress the live-confirmed 2026-08-15 sibling-content fix, whose second half was exactly a wrong constructor call); Reviewer verdict as a binding structured gate (already consciously deferred in the earlier per-agent-stage review, no pain point yet for the human-in-the-loop default).

- **A review's specific numbered "Finding N" labels don't carry across two separately-commissioned reviews - don't conflate them.** This session now has two distinct external reviews, each with its own "Finding 2," "Finding 5," etc. - the architecture review's Finding 2 (fail-open approval gate) and this prompt-quality review's Finding 2 (system-prompt self-contradiction) are unrelated bugs in unrelated files. Worth being explicit about which review a "Finding N" reference belongs to in any future note, since the numbers alone are ambiguous.

## 2026-08-16: live-test of the above (run b-7) surfaced its own real bug - fixed, HELD alongside it

While live-validating the §7.14 mode-split fix (`ignite_qpid_person`, run `b-7`), a targeted retry's FIX ANALYSIS correctly named the cause (raw Ignite cache type) and correctly named both of `_build_incompatible_types_scaffold`'s options, but the actual anchored edit combined them illegally: `var cache = (org.apache.ignite.IgniteCache<Integer, Person>) ignite.cache(CACHE_NAME);` - `var` (option 2) plus an explicit cast (option 1), wrapping a generic method call. Confirmed directly from the real post-attempt worktree file (recoverable this time thanks to the 2026-08-15 atomic-write fix, unlike an earlier evidence-destroyed incident this session had to leave unresolved). Root cause confirmed precisely: casting a generic method's result when it's called as a cast operand (not a plain typed assignment) gives Java no target type to infer from, so the type parameters default to Object erasure - casting THAT to a differently-parameterized generic type is a genuine compile error under generics invariance, not just an unchecked warning, even though it looks like a valid application of option 1.

**This is the first live confirmation of a nuance flagged (but not acted on) during the second external review's validation**: the "exactly two universally-correct fixes" framing was assessed as a real overclaim - not because the two fixes are wrong, but because a model can combine them into a third, illegal hybrid neither option's own example shows. Fixed: one new warning paragraph added to the shared, library-agnostic scaffold (not an Ignite-specific skill rule - this is a general Java generics footgun that just happened to surface via `ignite.cache()`), naming the illegal pattern explicitly and restating the correct cast-free form of option 2. 1 new test; existing scaffold tests confirmed unaffected by inspection (original two-option text unchanged, only appended to). See `docs/design.md` §7.15.

- **HELD, same reason as the mode-split fix**: implemented while `b-7` was still in progress, deliberately NOT used to restart/supersede that run (would waste the fallback model's remaining attempts and muddy the already-in-flight fix's own signal). Queued for its own live validation once `b-7` concludes, per the user's explicit "check after 30 minutes, fix if warranted, rerun only one" instruction.
- **NOT acted on, for lack of evidence**: `b-7` also hit a second, recurring "ANCHORED EDIT FAILURE - search block elided in skeletonized context" on the SAME file across two different models. `traces.db` only persists a completed run's summary, not raw per-attempt completions - there's a real, twice-confirmed symptom here but no recoverable evidence for its mechanism. Not guessed at; left open for a future run with better raw-completion capture.
- **A live-model test run finding its own new, real, unrelated bug isn't a distraction from validating the change under test - it's exactly what live testing is for.** The mocked suite already caught one regression in the change under test itself (see the entry above); this run caught a completely different, pre-existing bug in a part of the codebase the change under test never touched. Both are direct payoff from the "hold for live test" discipline, not scope creep.

## 2026-08-16, same day: a fresh live run (b-8) found and fixed a THIRD, structural bug - the Architect's own file list left one file's path unprefixed

A fresh single-goal run (`ignite_qpid_person`, run `b-8`) launched to live-validate the two HELD fixes above instead hit a novel, unrelated failure: "Could not find or load main class PersonApp," recurring identically across 7 attempts and 2 models (including 2 DIAGNOSIS MISMATCH catches - the model kept correctly diagnosing a misplaced-import syntax error, but its edits never touched it). Traced to the real filesystem: `PersonApp.java` was sitting at the worktree ROOT instead of `src/main/java/PersonApp.java` - Maven only compiles `src/main/java/**`, so the file was structurally outside the build from attempt 1 on; `target/classes/` only ever had `Person.class`, never `PersonApp.class`. No content-level fix at that path could ever have worked, no matter how correct.

Root cause traced to its exact origin via the run's own `design`-stage checkpoint: the Architect's raw JSON file list correctly pathed every file except the entrypoint - `"PersonApp.java"` came back bare while `"src/main/java/Person.java"` etc. were correct. `FileList`'s existing validators only check per-path syntactic sanity, nothing checked a file list's own INTERNAL consistency, so this passed cleanly and was written literally where told.

**Fixed**: new `_normalize_file_list_paths()` (`kriya/agents/contracts.py`), called from the shared `parse_file_list()` choke point (covers both `ArchitectAgent` and `DeveloperAgent`'s Step-1 call automatically). Deliberately narrow, tightened twice after direct testing caught two real false positives BEFORE either shipped: (1) scoped to `.java` ONLY, not every source extension - an early `.py`/`.rb`-inclusive version broke an existing test fixture by wrongly relocating a legitimate root-level Python entrypoint script, since Python/Ruby have no Java-equivalent rigid single-source-root convention; (2) grouped strictly within `.java`, never across unrelated extensions sharing a raw suffix - a first version grouped by raw extension alone and wrongly "corrected" the incident's own correctly-bare `pom.xml` into `src/main/resources/pom.xml`, purely for sharing `.xml` with an unrelated resource file. Final logic: only corrects when exactly one `.java` file is bare and every other `.java` file in the same list agrees on one shared directory - any ambiguity (disagreeing siblings, multiple bare files, no siblings to infer from) is left untouched, not guessed. 7 new tests, including the real incident reproduced byte-for-byte and both caught false positives locked in as regression tests. See `docs/design.md` §7.16.

**HELD, same as the other two fixes this session - not committed, not yet live-tested.**

## 2026-08-16, same day: a second fresh run (b-9) confirmed the three fixes above working, then found a real false-positive in the diagnosis-mismatch check itself

`b-9` (launched to live-validate §7.14/§7.15/§7.16 together) gave a clean positive signal on all three: no illegal var+cast hybrid, correctly-pathed entrypoint, zero mode/format contradictions across 7 attempts on 2 models. But the goal still timed out - `DIAGNOSIS MISMATCH` fired 6 times straight on an analysis that correctly named the exact right one-line fix every time (`return (Person) cache.get(1);`).

Root-caused with zero live-model cost: `find_edits_ignoring_own_diagnosis()` is a pure function, directly testable. A textbook-correct single edit passed the check cleanly in isolation - but adding a second, UNRELATED, no-op edit elsewhere in the same response (one whose search text merely happened to mention "Person," an unavoidable identifier in a Person-caching app) caused the correct first edit to be wrongly flagged. Mechanism: the check flat-joined every edit's search/replace text into one shared pool before asking "is this quoted term new" - a multi-edit response (normal, not rare) let an unrelated edit's search text poison the pool for a completely different, already-correct edit.

**Fixed**: rewrote the addition-signal check to operate per-edit-pair instead of flat-joined pools - a quoted term now passes if it's new relative to ANY individual edit pair, not the whole batch. The removal-signal check stays global (correctly - "is the bad pattern gone anywhere" is inherently whole-file). Behaviorally identical to before for every existing single-edit test. 2 new tests: the real bug reproduced, plus a negative case confirming a genuine multi-edit no-op is still caught. See `docs/design.md` §7.17.

**Not proven to be THE cause of b-9's specific failure** (raw completions weren't persisted) but confirmed as a real, reproducible bug regardless - found by directly testing the pure function, not inferred from the log alone. HELD, not committed, worth a fresh live run of its own.

- **A pure, deterministic function is directly testable with zero live-model cost - reach for that before accepting "the model just isn't good enough" as the explanation.** The instinct after seeing the SAME correct diagnosis fail 6 times was to conclude a fallback-model capability gap. Testing the actual DETECTOR against a realistic synthetic case (not a live call) found the real mechanism in minutes, for free - worth defaulting to whenever the suspect code is pure/offline-testable, before spending a live run chasing a hypothesis about model behavior.

## 2026-08-16, same day: a fifth run (b-10) - checked whether today's fixes were the cause (they weren't), then auto-fixed an invalid-XML-comment footgun instead of just detecting it

User asked directly whether today's just-committed fixes could be causing new drift, after `b-10` burned 3 retries on a `STRUCTURAL CORRUPTION in pom.xml` failure. Checked rigorously, not assumed: attempt 1's corrupted file came from the Planner-reuse path (never touches `_fill_missing_content`/`file_sys_prompt` at all - confirmed identically in `b-10` and an earlier same-day clean-success run, `b-6`); `git diff` against the pre-fix commit confirmed the retry-path prompt wording is byte-identical for what existed before, the mode-split only changes WHICH prompt gets sent. Not implicated.

Real cause, confirmed via `xml.etree.ElementTree` directly: `<!-- Ignite --add-opens flags -->` - the `--add-opens` JVM flag text (correctly documented as plain prose in `skills/ignite-java17/rules.txt`) echoed into an XML comment body. XML forbids `--` anywhere in a comment body - a hard spec rule. The model DID eventually self-fix it (confirmed: current worktree file has the hyphen removed) but only on its third attempt.

**Fixed differently from everything else this session**: this one is 100% deterministically detectable AND 100% safely auto-fixable with zero behavior-change risk (a comment isn't executed, so collapsing its hyphens can never change program meaning) - so instead of a prompt warning relying on the model noticing, added `DeveloperAgent._fix_xml_comment_double_hyphens()`, wired into the existing `sanitize_generated_content()` (already handling 3 other real model habits) as a fourth, unconditional step. General, not Ignite-specific - harmless no-op for every non-XML stack. 4 new tests, including direct `ET.fromstring()` validation (not just string comparison) and confirming the real flag text outside the comment stays untouched. See `docs/design.md` §7.18. HELD, not committed, not yet live-tested.

- **Not every fix needs to route through the model at all - when a mistake is 100% deterministically detectable AND 100% safely correctable without touching program behavior, fix it in code and skip the retry loop entirely.** Every other fix from today's investigation works by changing what the model is told (a prompt warning, a corrected detector) and still depends on the model noticing and acting on it next attempt. A comment's hyphens carry no semantic weight, so this one didn't need that step at all - three live-model retries got shortened to zero.

## 2026-08-16, same day: second external review, Finding 5 - fenced RunVerifier's captured output as untrusted

Picked up the next item from the second (prompt-quality) external review's list while `b-10a` ran elsewhere - safe to do, a running subprocess has its own already-loaded copy of the code, unaffected by later source edits. `RunVerifierAgent.grade()`'s captured stdout/stderr fed a pass/fail decision with zero untrusted-content framing - confirmed real by direct read, not accepted at face value. `learned_rag_context` already has this exact mitigation ("Begin/End Untrusted Reference Context") for externally-ingested content; just hadn't been applied to generated-program output before. Fixed: mirrored that convention - one sentence in the system prompt, explicit `=== Begin/End Untrusted Captured Output ===` delimiters with a warning repeated right after the content in the user prompt. 1 new test. HELD, not committed. See `docs/design.md` §7.19.

## 2026-08-16, same day: a SECOND, distinct false positive in find_edits_ignoring_own_diagnosis() - deletion-shaped fixes

While §7.19 was being verified, `b-10a` (running concurrently, unaffected by the source edits) hit `DIAGNOSIS MISMATCH` again - a different shape from the multi-edit-pooling bug fixed earlier today. `"the code imports `org.apache.ignite.cache.IgniteCache`... correct import location is the top-level `org.apache.ignite` package"` - correcting the import by DELETING `.cache` from it - was wrongly flagged. Mechanism: the existing additive/wrap signals both require some text that WASN'T there before; a deletion-shaped correction introduces nothing new - every quoted fragment of the dotted identifier is a substring of both the wrong and corrected versions. The removal-phrase signal doesn't cover it either, since this diagnosis never uses an "instead of X"/"remove X" template.

**Fixed**: added a third signal to the same per-edit-pair check - a quoted term counts as evidence if it's present in a pair's search AND absent from that same pair's replace ("this named-as-wrong thing is now gone from the edit that fixed it"). Scoped per-pair, same reasoning as the earlier fix. Verified it doesn't reopen the ORIGINAL motivating incident this whole check was built for (`var` staying present when only a comment gets appended). 2 new tests; every existing test for this function re-verified directly, not assumed. See `docs/design.md` §7.20. HELD, not committed.

- **The same function can have more than one distinct false-positive shape, discovered on consecutive live runs of the SAME goal - don't assume "found and fixed one bug" means the mechanism is now sound.** Two different DIAGNOSIS MISMATCH false positives surfaced on `b-9` and `b-10a` within the same session, each requiring its own root-cause and its own targeted signal. Worth re-testing a just-fixed pure function against whatever the NEXT live run produces, rather than treating the first fix as closing the whole class of risk.

- **Testing a fix against the EXACT real incident data, immediately after writing it and before writing any formal test, caught a serious false positive (relocating a correctly-placed `pom.xml`) that a less literal test might have missed.** The first version of this fix looked correct by inspection and passed a mental walkthrough of the target incident - it was a direct `python3 -c` run against the real captured file list that surfaced the `pom.xml` collision immediately, before it ever reached a test file, let alone shipped. A second such check (scanning existing test fixtures for any `.java` list with mixed bare/pathed entries) caught the second false positive the same way. Neither would have been caught by reasoning about the fix in the abstract.

## 2026-08-16, same day: closed a small log-noise issue on the spot rather than letting it sit

While investigating run `b-8` (see above), a `fetch_url_text()` failure (`mvnrepository.com` 403ing - a known scraper-blocking site, unrelated to Kriya) logged as a full ERROR-level traceback even though it's completely routine: `kriya/workflow/live_lookup.py`'s multi-candidate loop already catches it and moves to the next URL, succeeding immediately. User's call: fix small things on the spot rather than letting them accumulate into an ever-growing backlog. Fixed: `fetch_url_text()` gained a `quiet_on_failure: bool = False` param (default unchanged - still loud ERROR+traceback for its other 3 call sites, where a single deliberate URL failing genuinely is worth surfacing); `live_lookup.py`'s own multi-candidate call site now passes `quiet_on_failure=True`, downgrading to a single DEBUG line with no traceback. 2 new tests (`tests/test_web.py`) plus 2 existing `test_workflow.py` mock signatures (`fetch_side_effect`, `fake_fetch`) updated to accept the new kwarg, since both exercise `live_lookup.py`'s real call site. Small, low-risk, pure logging change - doesn't touch generation/prompt logic at all, unlike the three HELD fixes above.

- **Not every finding needs to become a backlog line - a small, well-scoped, low-risk fix is often cheaper to just do than to write up and defer.** This one took a few minutes end-to-end (identify the two call sites, add the param, fix two test mocks) - writing it up as a deferred "someday" item would have cost more attention later than closing it now did.

## 2026-08-16, same day: pom.xml semantic pre-validation, plus Planner-reuse observability - and pushing back on a second review's over-scoped proposal

A deep-dive into WHY first-attempt compile failures keep happening (user question, "external reviewer" framing) found the real gap precisely: `find_structural_corruption()` only confirms `pom.xml` is well-formed XML, not a semantically valid Maven POM. `b-6`'s wrong-root-element `pom.xml` (`<plugin>` instead of `<project>`) sailed through that check and was only caught by the full, expensive `mvn compile`, after every other file in the batch had already been written for nothing.

A second, independently-pasted external review proposed a much bigger five-layer redesign (structured Planner contracts, full Architect interface ledger, four separate Developer system prompts, layered validation). Assessed point by point, not deferred to wholesale: several claims didn't hold up (e.g. "Planner isn't necessarily the strongest model" - checked directly, every run this session uses the identical model for all three agents); its proposed `mvn validate` placement (after the whole batch, not at write time) would have thrown away the actual benefit, since `pom.xml` has no cross-file dependency on siblings; its four-mode Developer split re-proposes exactly what was deliberately scoped down from earlier the same day (§7.14), for reasons that still hold (JSON-encoding risk for local models; no evidence the combined no-change-assessment call is broken). One genuinely new, evidence-worthy point kept: `b-6` and `b-10b` both show Planner-reused code (bypassing fresh Developer generation) correlating with that day's first-attempt failures - real enough to measure, not yet enough to act on alone.

Separately: when scoping a proactive jdtls-based Java-import check (for `b-10b`'s missing-`ObjectMapper`-import shape), the user correctly caught that jdtls is deliberately reactive-only today - by the time it runs (after the first compile failure), every file in the batch genuinely exists, so every "unresolved symbol" it flags is real. Checking a single file mid-write-loop would risk flagging a reference to a sibling application class that just hasn't been written yet this same batch. Combined with `mvn compile` needing the same dependency-resolution work jdtls would (no clear time savings), this idea was dropped, not implemented. `pom.xml` doesn't have this problem, which is exactly why only the POM side shipped.

**Shipped**: new `PolymorphicValidator.run_pom_validate()` (`kriya/tools/validate.py`) - runs `mvn validate`, the cheap first Maven lifecycle phase, catching a semantically-wrong-but-well-formed POM before paying for full dependency resolution + javac. Confirmed live (not just mocked) against a real invocation reproducing `b-6`'s exact error text, under a second. Wired into `attempt.py`'s per-file write loop right when `pom.xml` is written - stops the loop there, before any other file in the batch is generated, using the same `Failure`/`QualityGateFailure` shape (new `type="pom_semantic_validation"`) every other gate already uses, so zero new retry-loop plumbing was needed. Also shipped: `GenerationState.planner_reuse_used_attempt1`, a new observability-only field plus a symmetric log line for the "did NOT reuse Planner content" case (previously silent) - deliberately NOT threaded into the TraceLogger SQL schema (would mean touching 6 call sites + a migration for an observability nice-to-have, disproportionate risk). 4 new tests. See `docs/design.md` §7.21. HELD, not committed, not yet live-tested.

- **When a second review proposes a much bigger redesign than what's already in flight, evaluate it on its own merits - don't defer to it just because it's more comprehensive, and don't dismiss it either.** Several of its claims were checked directly and didn't hold (the "weakest model" claim); one genuinely useful piece of its schema (constructors in the interface ledger) was acknowledged as answering an earlier objection, while still declining to adopt the larger mechanism it belonged to; and its core "mvn validate" idea was kept but with a different, better-reasoned placement than the review itself proposed. Point-by-point assessment, not wholesale acceptance or rejection.
- **The user's own architectural instinct (jdtls's reactive-only timing being deliberate, not an oversight) was correct and directly prevented a real design mistake before it was built.** Worth taking a "wait, is the current design actually already correct here" question seriously rather than treating it as something to route around.

**Correction, same day, caught by the user's own mocked-suite run**: `run_pom_validate()` is a real-subprocess check, and 12 existing workflow-level tests (most writing a bare `<project></project>` pom.xml fixture, never a problem before) hit REAL `mvn validate` unmocked, genuinely failing against fake content. Exactly the "new error cases" risk flagged when this work was scoped - it happened anyway, caught by testing rather than avoided by design. Fixed by adding the matching `run_pom_validate` mock to each affected test (10 call sites), mirroring the existing `run_compile_check` mocking convention exactly; one test's own call-filter tightened instead of mocked (it assumed only one real `mvn` subprocess call existed). Also found and fixed, unrelated: two `assert_called_once_with()` checks never updated for an EARLIER same-day change (`fetch_url_text`'s `quiet_on_failure` kwarg) - a real leftover gap in already-committed work, not new. Full suite back to the exact known 6-item baseline, confirmed by the user's re-run. See `docs/design.md` §7.21 for the full accounting.
- **Reporting "4 new tests, done" before the full suite actually ran was premature - a real regression was hiding in the parts of the suite not directly exercised while writing those 4 tests.** The 4 new tests for `run_pom_validate()` itself were correct and passed; they said nothing about the ~600 OTHER tests that also happen to write a pom.xml fixture. Full-suite verification isn't optional even for a change that looks self-contained - matches the standing session discipline, worth restating after nearly skipping it in the moment.

## 2026-08-16, same day: the "6 known baseline failures" were wrongly mischaracterized all session - all 6 were real, fixable bugs

Alongside the 12 real `run_pom_validate()` regressions above, the same full-suite run surfaced 6 more failing tests that had been carried all session as "environment-dependent, unrelated baseline failures" - never investigated, just excluded from scope each time they showed up. Asked directly to fix them anyway. None of the 6 were actually environment-dependent; every one was a real bug, root-caused individually via direct reproduction (small standalone scripts invoking the test function directly, never the full `pytest` CLI, per the standing quota rule) rather than assumed:

- `test_resolve_java_home_override_resolves_when_goal_matches_java_side` - patched `kriya.workflow.workflow._resolve_jdk_home_for_version` (the re-exported name `workflow.py` keeps for backward compatibility after the 2026-08-11 modularization), but `toolchain.py`'s own functions call each other via same-module references, not through that re-export - so the patch silently never took effect. Fixed by patching `kriya.workflow.toolchain._resolve_jdk_home_for_version` instead.
- `test_get_or_start_jdtls_client_logs_not_found_at_info` / `test_get_or_start_jdtls_client_logs_success_at_info` - the exact same class of bug, one module over: both used `caplog.at_level(logging.INFO, logger="kriya.workflow.workflow")`, but `_get_or_start_jdtls_client()` actually lives in `kriya/workflow/lsp_integration.py` and logs via `logging.getLogger(__name__)`, i.e. `"kriya.workflow.lsp_integration"` - a sibling module, not a child of `"kriya.workflow.workflow"` in the logger hierarchy, so setting that logger's level to INFO left the real logger's effective level at the root default (WARNING) and its INFO records got filtered before `caplog` ever saw them. The sibling WARNING-level test in the same block (`..._logs_start_failure_at_warning_not_debug`) has the identical wrong `logger=` argument but happened to keep passing all along, purely because WARNING clears the unaffected root's default threshold anyway - it was never actually verifying the right logger either, just accidentally not failing. Fixed by pointing both INFO tests' `logger=` at `"kriya.workflow.lsp_integration"`.
- `test_workflow_full_set_prompt_includes_existing_dependencies_checklist` / `test_workflow_surfaces_toolchain_warning_even_on_success` - each had two independent real bugs stacked together: (1) missing `cfg.autonomy.mode = "guardrails"`, hitting the same fail-open-approval-gate fix from earlier the same day (§ "Findings 5 and 2") - these two were excluded from that original bulk fix pass because they were already broken for the second reason below at the time, so never got swept up; (2) a stale JSON-array-shaped Developer-response fixture (`'[{"filepath": "pom.xml", "content": "..."}]'`) left over from before `known_target_files` existed - that parameter makes `DeveloperAgent.run_generation()` skip Step 1's batch-JSON parsing entirely for a single known file and use the model's response as raw content directly, so the fixture needed to be bare content, not a JSON envelope. Fixed both parts in each test.
- `test_workflow_checks_toolchain_only_once_across_retries` - the same two bugs, plus a third wrinkle that took three iterations to pin down: making both retry attempts' fixtures bare content broke a different way, because the deliberately-invalid content used to force a compile failure (`<project><bad></project>`, unclosed tag) tripped `find_structural_corruption()`'s XML well-formedness check before the mocked compile gate ever ran, consuming an extra unbudgeted retry. Making it well-formed-but-semantically-wrong (`<project><bad/></project>`) fixed that, but then a `StopAsyncIteration` on the next attempt revealed the real reason attempt 2 needs the JSON-array shape after all: the mocked failure message here has no real filename in it, so `extract_implicated_files()` can't scope the retry to `pom.xml` alone, and it falls through to an unscoped full-set retry - which genuinely does go through Step 1's batch-JSON path. Final shape: attempt 1 (scoped, via `known_target_files`) is bare content; attempt 2 (unscoped full-set retry) stays JSON-array-shaped.

All 6 fixed and individually verified via direct script reproduction (not the pytest CLI). Not yet committed - pending the user's own full-suite confirmation run.

- **"Environment-dependent" was never verified for any of these 6 - it was an assumption made once, early, and then carried forward unquestioned for the rest of the session.** Every one turned out to be a same-module-reference or stale-fixture bug, the identical class already fixed elsewhere the same day. Worth treating "environment-dependent" as a hypothesis to check (does the failure actually reference something machine-specific - a binary path, an OS quirk, a real network call?) rather than a label that, once applied, exempts a failing test from investigation.
- **The `caplog.at_level(level, logger=name)` bug is easy to write and easy for a passing test to hide:** a WARNING-level assertion kept passing with the wrong logger name only because the root logger's own default threshold (WARNING) happened to let the record through anyway: an INFO-level assertion with the same mistake fails loudly. A test can be "accidentally right" for the wrong reason and only be caught when a sibling test at a different log level exposes the same latent bug.

## 2026-08-16/17: live-model investigation into "correct diagnosis, edit doesn't take effect" - 4 independent false positives found and fixed in `find_edits_ignoring_own_diagnosis()`, plus a real extraction bug

A live `ignite_qpid_person` run (`b-10c`) hit an inexplicable recurrence of an identical runtime failure after what looked like a real applied edit. Rather than guess from a reconstructed, buffering-ambiguous log, added DEBUG-level logging of the literal, unparsed Developer completion right before parsing touches it (`kriya/agents/agent.py`) plus a `--log-level` flag on `spikes/eval_harness/run_harness.py` (default unchanged). This one logging addition turned every subsequent question into direct ground truth for the rest of the night - the highest-leverage change of the session.

- **Finding 1 (`b-10h`) - cast-insertion false positive.** A missing-cast diagnosis quotes the operand and type as separate spans, never the fused `(Type) operand` cast expression - none of the existing signals see anything "new." Confirmed live: a correct cast fix rejected 3 retries straight in one run, 3 more in a second (6 wasted attempts total, escalating through both models). **Fixed**: 4th signal - a quoted type appearing as a literal `(q)` cast in the replace text, absent as that exact form from search, counts as genuine evidence.
- **Finding 2 (`b-10i`) - "result-describing instead of" false positive.** "Instead of" is ambiguous between an instruction ("do Y instead of X" - X discarded) and a result description ("returns X instead of Y" - Y is desired, correctly still present). The same cast fix, phrased the second way, got its own correct quote wrongly captured by the removal signal. **Fixed**: narrow exclusion - a result-describing verb (returns/produces/gives/yields/is/was) before "instead of" in the same clause means the term after it is desired, not removed.
- **Finding 3/4 (`b-10k`) - a pasted external review, verified claim-by-claim before acting on it, not trusted outright.** Both of its core claims checked out:
  - **Extraction bug, confirmed real**: `extract_planner_code_blocks()` (`kriya/workflow/file_resolution.py`) maps fences to filenames by proximity and lets a later match overwrite an earlier one; its plausibility-check table (built specifically because this SAME failure shape - an `mvn -q compile exec:exec...` snippet mis-extracted as file content - was found live once already, 2026-08-12) only ever covered `.java`/`.py`, never `.xml`. `b-10k`'s `ignite-config.xml` failed with a syntax error consistent with exactly this. **Fixed**: added `.xml`/`.json` real-parser plausibility checks to the same table (deliberately not `.properties` - no reliable syntax check exists).
  - **Self-referential filepath false positive, confirmed via raw-completion DEBUG capture**: a diagnosis for a broken file routinely quotes the FILE'S OWN PATH for self-identification, never appearing inside that file's own content. Pulled the exact raw completions for all 3 rejected retries: genuinely valid, complete XML proposed identically every time, rejected every time because the only quote was the file's own path. **Fixed**: a structural bare-path exclusion applied before evidence-checking.
  - **Pushed back on 2 of the review's own recommendations**: "disable Planner-reuse by default" (the actual root cause is the missing `.xml` check, not reuse itself - no evidence reuse is causally defective once extraction is trustworthy) and "make deterministic validation authoritative over the diagnosis heuristic" (the same broader redesign question already explicitly deferred as its own scope earlier the same day - kept deferred).
- 6 new tests, full existing suite for both touched functions re-run directly (39 total, zero regressions). See `docs/design.md` §7.23.

- **Four independent false-positive shapes in one function, across 3 live runs in one night, says something about the function's fundamental design, not just its edge-case coverage.** Verifying analysis prose against diff content via backtick-quote matching fights an inherently fuzzy correspondence - every new code shape (cast, wrap, deletion, self-reference) breaks the mapping differently. Researched how Aider/SWE-agent actually handle this before proposing anything sweeping: the answer isn't a cleverer NLP check, it's mechanical verification plus leaning on real compile/test execution as ground truth ("the model isn't where edits get reliable, the harness is" - Aider's own framing). Kept every fix tonight narrow and incident-scoped anyway, at the user's explicit direction - the bigger redesign question stays a separate, not-yet-scoped discussion.
- **DEBUG-level raw-completion logging, gated behind a harness flag defaulting to the existing behavior, was worth more than any single bug fix tonight.** It converted "guess what the model actually said from a log that interleaves streamed stdout and buffered logger output unpredictably" into direct, unambiguous ground truth - closing an investigation in minutes that had previously required hand-tracing regex logic against a reconstructed approximation of the real text.
- **An externally-pasted review is a hypothesis to verify, not a report to trust** - both of its core claims were checked directly against the actual log lines and code before any fix was written, and its recommendations were assessed point-by-point (2 adopted as-is, 2 explicitly declined with reasons) rather than accepted wholesale.

## 2026-08-17: second external review (`b-10l`), verified then acted on - self-correction's unhandled exception silently overwrote the real compile error, plus a same-line marker-separator parser bug

A follow-up run to confirm the XML/JSON extraction fix (above) arrived with a second pasted review. Confirmed the fix worked (all 6 files, including `applicationContext.xml`, written cleanly) - the run's real failure was a genuine model defect (`var cache = ...; Person cachedPerson = cache.get(1);`, untyped cache despite explicit skill guidance). Two further claims checked directly, both confirmed real:

- **Finding 1 - unhandled exception in self-correction.** `run_self_correction_loop()`'s `complete_with_tools()` call had zero exception handling, despite its own docstring promising a non-resolved outcome is "exactly the same as if the loop had never run." Traced the propagation directly: an unguarded exception passes through `attempt.py`'s own call site up to `workflow.py`'s outer handler, where `handle_attempt_failure()` does `raw_error_context = str(e)` and - a bare exception having no `.failure` attribute - falls through to a generic `Failure(type="general_error", ...)`, discarding the real Maven error sitting unused in `attempt.py`'s scope. Confirmed live: three HTTP 500s from Ollama while parsing the model's own native tool call replaced the attempt's failure text entirely, causing every retry after that to implicate `applicationContext.xml` (the only file-shaped token in the HTTP error) instead of the actually-broken `App.java`. Fixed: wrapped the call in try/except, returning `resolved=False` with the original compile error preserved - restores the documented contract exactly.
- **Finding 2 - same-line marker-separator parser bug.** `_split_fix_analysis_edit()`'s `.strip("\n")` only strips newlines (by design, to preserve meaningful leading indentation) - but a model writing `"REPLACE: <?xml...?>..."` with content on the SAME line as the marker leaves the one separator space untouched, producing a leading-space-prefixed XML declaration, invalid per spec. Matches the log's recurring `"XML or text declaration not at start of entity: line 1, column 1"` across 2 retries exactly. Fixed: new `_strip_marker_separator()` strips exactly one marker-separator character (space or newline) before the existing newline-strip runs - not a blanket `lstrip()`, verified safe against real multi-line indented content.
- 3 new tests, full existing suites re-run directly (5/5 + 29/29, zero regressions). See `docs/design.md` §7.24.

- **An unhandled exception in an OPTIONAL, best-effort code path corrupted the AUTHORITATIVE failure signal for every subsequent retry - worse than the LSP-silent-failure class from earlier this session, because it wasn't just invisible, it was actively wrong.** Any optional recovery step inside the main retry loop needs to fail closed to "as if it never ran," never fail open into replacing what the caller already had with its own error text.

## 2026-08-17: third external review (`b-10m`), verified then acted on - self-correction's own stale-context grounding bug, plus a fifth false positive in the diagnosis-mismatch check

A follow-up run to confirm the previous two fixes still timed out at 2400s. Confirmed both prior fixes worked (no false XML failure, correct retry target) - the real blocker was two NEW bugs, both checked directly before acting:

- **Finding 1 - self-correction's own `apply_patch` rejected a fix grounded through its own `read_file` tool.** `_dispatch_tool_call()` correctly re-reads the real current file for the actual edit, but separately validated the search block against `active_code_context` - a snapshot frozen at conversation start, never updated as `read_file` gets called mid-loop. A model that read the real file and copied its search block exactly could still be rejected as "elided in the skeletonized context," comparing against a stale snapshot instead of what it was actually shown. This was the EARLIEST failure point in the whole cascade - self-correction should have just fixed the cast error directly. Fixed: track `read_files: Set[str]` across the loop; `apply_patch` skips the stale-context check entirely once a file has been read or modified this conversation (the strict anchor-matching against real content still protects against ambiguous edits either way); the check is untouched for a file never read - still catches a genuine ungrounded guess.
- **Finding 2 - a FIFTH false positive in `find_edits_ignoring_own_diagnosis()`.** Kriya's own `BareVerificationMarkerCheck` correctly flagged a bare `[VERIFICATION] PASS` line; the model's repair correctly wrapped it in `println(...)` - but signal (b) (the wrap detector) requires the quote to equal the ENTIRE search block, which fails when the search also needs surrounding if/else context to match uniquely. This genuinely correct repair was rejected 3 times across 2 runs, directly causing the timeout. Fixed: a 5th signal recognizing a quoted span that was its own standalone line in search, no longer standalone in replace, now wrapped in a print/println/puts call on some replace line - verified against the live incident AND two regression-risk negative cases (marker unchanged nearby-line-changes, and a new trailing-comment-no-print-call case).
- 5 new tests, full existing suites re-run directly (7/7 + 27/27, zero regressions). See `docs/design.md` §7.25. HELD, not committed.

- **Asked directly, mid-investigation: roughly half of tonight's 10 total bugs across 3 rounds of review needed DEBUG raw-completion capture to PROVE a rejected edit was genuinely correct; the other half were found through code reading at the exact point an always-visible WARNING fired, DEBUG or not.** Neither method alone would have found everything - both were load-bearing.

## 2026-08-17: deterministic-validation-first for `find_edits_ignoring_own_diagnosis()` - breaking the signal-per-incident pattern instead of extending it

Asked directly whether 5 false-positive shapes in one function in one night is addressable generically instead of one incident at a time. Answer: yes, for the specific question this check exists to answer ("does a proposed repair actually fix the reported failure") - a cheap, deterministic, authoritative answer already exists in the check's two real contexts, no heuristic proxy needed:

- **`static_rule_violation`-triggered retries**: re-run the SAME registered static check (`kriya/workflow/static_checks.py::STATIC_CHECKS` - covers all 4 current checks, not just the verification-marker one signal (e) targets, plus any future addition) against the proposed candidate content (`run_static_checks()` extended with an `overrides` param, no disk write needed). Violation gone → bypass the prose check entirely.
- **`compile`-triggered retries**: no equally-cheap re-check exists (the real compiler runs immediately after anyway), so bypass outright and let it decide. Evidence-backed: 3 of 5 confirmed false positives were compile-triggered, each rejecting an edit that would have compiled; the check's own original 2026-08-13 motivating incident would cost at most one extra, otherwise-identical compile cycle if bypassed instead - the SAME repeat-failure signal the retry loop already tracks independently.
- Every other failure type left untouched - not a blanket disable.
- **Caught a real regression before shipping**: the original 2026-08-13 test encoded the opposite expectation for the compile case. Ran it, confirmed it now fails as predicted, updated it to assert the new intentional behavior (one extra compile cycle, not a blocked pre-flight) rather than treating the failure as a blocker.
- 2 new integration tests (one deliberately using a DIFFERENT static check than the verification-marker one, and an analysis phrasing that satisfies none of the 5 existing signals, to prove the bypass itself - not any signal - is what accepts it). Full suite re-run directly: 31/31, zero regressions. See `docs/design.md` §7.26. HELD, not committed.

- **The generalization has a real, honest boundary, worth stating precisely rather than overclaiming.** The reusable principle - replace a heuristic proxy with the real deterministic check wherever one cheaply exists - already has one precedent in the codebase (`pass_verdict_is_grounded()` for RunVerifier's own hallucination risk) and one more flagged candidate (`extract_implicated_files()`'s heuristic fallback when no real compiler locator exists - the exact gap the self-correction-exception bug exploited). But it only applies where a deterministic oracle exists for the question being asked. It does nothing for inherently subjective judgments (does this match the goal's intent, is this the right design) where no compiler or parser can ever answer - whack-a-mole risk doesn't vanish everywhere, only where it was never actually necessary to accept in the first place.

## 2026-08-17: `b-10n` ran clean (zero diagnosis-mismatch occurrences), then audited for the self-correction exception-handling bug class elsewhere - found and fixed in 4 more places

`b-10n` - the first full run with everything above in place - completed successfully, zero `DIAGNOSIS MISMATCH` failures across 5 attempts + a full fallback-model escalation. Real evidence the pattern is closed, not just reasoned about. Moved to the next priority: audit whether the self-correction unguarded-exception bug (an auxiliary component's failure getting misattributed as an authoritative Quality Gate failure) was a one-off or recurring.

Traced the exact vulnerable boundary (`workflow.py`'s retry loop wraps `run_attempt()` in ONE `try`/`except`, so any unguarded exception anywhere in that call chain gets misattributed) and checked every optional/best-effort call reachable from it:
- LSP grounding (`_get_or_start_jdtls_client`, `_build_lsp_diagnostics_context`) and live-lookup enrichment (`_resolve_via_web_lookup`) - **already safe**, both properly degrade internally, matching their own docstrings' promises.
- `RunVerifierAgent.judge()`, `RunVerifierAgent.grade()`, `SkillGapAgent.extract_skill_update()`, `SkillGapAgent.check_skill_conflicts()` - **confirmed vulnerable, identical shape**. All four call `call_with_escalation()` (which explicitly re-raises on total exhaustion per its own docstring) unguarded - only the SUBSEQUENT `json.loads()` was ever wrapped. `judge()`/`grade()` are the most consequential: they only run AFTER compile/tests already passed, so a transient failure there could fail an otherwise-fully-correct run outright.

Fixed all 4 by wrapping the call in its own try/except, degrading to the EXACT same safe fallback each function's own pre-existing unparseable-JSON handler already used - no new fallback shape invented, just extending "garbled response" handling to also cover "call itself failed." 4 new tests (one per function, each simulating total LLM exhaustion), full existing suite re-run directly: 40/40, zero regressions. See `docs/design.md` §7.27. HELD, not committed.

- **The audit paid off - a proven bug pattern found in one place is worth checking systematically elsewhere, not assumed to be a one-off.** 4 more real instances found by tracing the actual vulnerable code boundary precisely (which try/except actually catches what) rather than guessing which components "feel" risky. Also confirmed the inverse is equally valuable: 3 OTHER optional components checked and found to be ALREADY correctly guarded - worth knowing precisely what's safe, not just what isn't.

## 2026-08-17: corpus-wide failure survey (105 runs, 211 log files) - built replay tooling on `spike/incident-replay-harness`, not merged

Asked "grep every failure across every run, turn it into a smoke-test set." Built survey_runs.py (categorizes every "Quality Gates FAILED" line by type across the whole corpus) and analyze_diagnosis_mismatch_history.py (exact replay of every anchored-edit completion across the 8 DEBUG-capturable runs against CURRENT code - exact, not approximate, since that check's signals operate entirely on each edit's own search/replace pair, never the orig_text parameter). Deliberately did NOT auto-generate tests from grepped lines - a failure line proves a gate fired, not that the cause is still live or that the obvious fix is real; auto-injecting causes without verification risks exactly the "test that doesn't actually reproduce the real mechanism" trap.

Result: 153 successes / 42 timeouts / 16 unknown across 211 (run, goal) pairs. Failure-type frequency: run_verification=131, compile=119, targeted_test_failure=78, diagnosis_mismatch=52, anchored_edit=44, unaddressed_error_location=23, structural_corruption=16, general_error=14, static_rule_violation=10, incomplete_generation=3, developer_json_parse_failure=1. For diagnosis_mismatch specifically: 12 of 25 replayed completions were historically rejected and now correctly pass (confirmed fixed); every remaining rejection individually checked and confirmed legitimate (hallucinated search text, byte-identical no-ops, stale search blocks, or never actually reached live because an earlier layer caught it first).

- **NOT YET FIXED, low priority - a rare JSON-escaping failure in the Step-1 batch file-list path.** One occurrence in 211 log files (`20260806-220544/django_healthcheck_gap`): `DeveloperAgent.run_generation()`'s initial batch-JSON file-list response broke because the model's own generated Python code contained a triple-quoted docstring (`"""..."""`) with its inner `"` characters left unescaped inside the JSON string value - `json.loads` correctly saw the docstring's closing `"""` as ending the string early. Root cause matches an ALREADY-established finding from earlier this session: local/quantized models are unreliable at JSON-escaping multiline, quote-heavy source - exactly why the retry loop's own SEARCH/REPLACE format was deliberately chosen over a JSON envelope (§7.14/`b-10k`/`b-10l` review pushback). This is the one path that still uses JSON for content (the initial file list), and it broke exactly the way the established finding predicts. Given the 1/211 frequency and that a real fix (detect + repair unescaped-triple-quote patterns inside a JSON string value) is a nontrivial parsing-robustness feature, not a quick patch, this is flagged for later, not fixed now - revisit if it recurs.

## 2026-08-17: dug into the 14 `anchored_edit` "elided in the skeletonized context" failures - found and fixed a real bug in `apply_anchored_edits()`'s grounding check (design doc §7.28)

Ruled out the retry-prompt builders first (`_build_targeted_retry_prompt`/`_build_full_set_retry_prompt`/`_build_missing_files_retry_prompt` in `kriya/workflow/retry_prompts.py` all read every already-written file's full, unskeletonized content from disk unconditionally on every retry - skeletonization only ever applies to the starting graph-context portion, not what's later appended). Real root cause: `apply_anchored_edits()`'s grounding check compared each edit's search block only against `shown_context`, a parameter fixed for the whole call, never against `current_content`, which legitimately evolves as earlier edits in the *same* multi-edit response are applied via `current_content.replace(...)`. A response where edit #2 targets text edit #1 just introduced was rejected as a hallucination even though it was real, freshly-written content from the same turn - matches the historical `b-1` "edit #4" failure exactly.

Fix: grounding now accepts a match against either the original shown context or the current evolving content, so a genuinely fabricated search block (present in neither) is still rejected exactly as before. 2 new tests (fix case + a fabricated-search-text negative case to confirm the guard didn't weaken), zero regressions in the full existing `edit_safety`/`workflow` coverage.

Found and initially fixed on `spike/incident-replay-harness` (the survey tooling's branch) before catching that this is a production fix, not tooling - moved to `main` via `git stash push -u` / `checkout main` / `stash pop`, re-verified working there directly. Committed (`ab39748`) and pushed same session.

## 2026-08-17: dug into `anchored_edit`'s remaining 30 occurrences and all 23 `unaddressed_error_location` occurrences - found and fixed a second real bug (design doc §7.29)

`anchored_edit` remainder: no new fix, evidence boundary. The 24 "matched 0 times" cases have zero overlap with any DEBUG-capturable run across the WHOLE corpus (not just the original 8) - can't distinguish genuine model hallucination from a hidden Kriya bug without a fresh live run under `--log-level DEBUG`. Ruled out cross-file confusion directly (already caught separately by `find_misdirected_edit_target`, closed 2026-08-14). The 3 "Could not find formatted match" occurrences are dead - that message doesn't exist in current source, traced via `git log -S` to a matching algorithm superseded before this session. The 2 ambiguous "matched N times" cases match the already-queued symbol-editing architectural item, no new finding.

`unaddressed_error_location` (23 occurrences): both DEBUG-capturable cases (`b-10f`, `b-10n`) replayed against real raw completions - one false positive, one true positive. `b-10f`: the model's response had TWO edits - one fixed the real root cause (a bad import), a second, later edit in the SAME response redundantly echoed a now-transitively-fixed usage site unchanged (byte-identical no-op). `find_edits_ignoring_reported_line()` only checks one edit in isolation, so it flagged the no-op even though the file compiles fine overall. `b-10n`: model's own analysis said it would add a missing import and never did - genuinely unaddressed, correctly caught. Fix: since this check is compile-scoped by construction on every single call (its own reported-line extraction only ever succeeds against a real javac locator), it doesn't need signature-based dispatch like `find_edits_ignoring_own_diagnosis`'s bypass did - just defer unconditionally to the real compiler that runs right after anyway. Changed the hard `raise QualityGateFailure` to a logged INFO bypass in `kriya/workflow/attempt.py`. `b-10f`'s false positive is eliminated for free; `b-10n`'s genuine miss just costs one extra compile-and-fail cycle instead of a pre-flight block, same tradeoff already accepted for `diagnosis_mismatch`'s compile bypass (§7.26). Given the same handful of line numbers recurred 10x/9x/6x/6x across many historical runs, false positives were very plausibly the dominant driver of this whole category.

2 tests updated/added (renamed regression test now expects 3 compile calls instead of 2 and zero `unaddressed_error_location` outcomes; new test reproduces the `b-10f` companion-edit shape and confirms zero wasted retry cycles) - both confirmed failing pre-fix via `git stash`, passing post-fix. `find_edits_ignoring_reported_line()` itself is unchanged; its 3 existing unit tests pass unmodified. Found/fixed on `spike/incident-replay-harness` by habit, moved to `main` via the same stash sequence as the prior entry, 9/9 directly-relevant tests re-verified passing on `main`.

